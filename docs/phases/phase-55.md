---
era: "001"
---

# flex — Phase 55: Story-scoped file permissions via hook enforcement

← [Phase 54: sync-all wrapper command](phase-54.md)

<!-- Phase doc = planning surface only. Story-level detail (acceptance criteria,
     file paths, implementation guidance, test instructions, codebase recon)
     belongs in docs/stories/<RAIL>/<ID>.md — not here. -->
## Goal

Replace the per-story `write-permissions`/`clear-permissions` cycle — which writes allow rules into `.claude/settings.local.json` and requires user intervention in auto mode — with a permissions-file approach that is zero-interaction and auto-mode-safe. Each story's `primary_files` + `touches` frontmatter is materialised into a JSON permissions file under `docs/phases/permissions/`. That path is already covered by the existing `Edit(docs/phases/**)` / `Write(docs/phases/**)` deny rule, so the file is immutable to builder Edit/Write tool calls without any new settings.json entries. A new `scope_guard.py` module, delegated to from an extended `pre_tool_use.py`, reads the active story's permissions file on every Edit/Write tool call and blocks writes to undeclared paths at the hook level. The orchestrator's build loop replaces the `write-permissions` Bash call with `permissions create` and drops `clear-permissions` entirely — both operations now write to `docs/phases/` rather than `.claude/`, so they require no user approval in auto mode. A companion fix to `sync.py` closes the regression where syncs wiped the deny list from downstream projects. Era 001's INFRA rail continues shifting deterministic enforcement out of prose and settings files into versioned, testable code.

## Stories

| ID | Title | Status |
|----|-------|--------|
| INFRA-137 | `flex_build.py permissions create` — generate story permissions file from spec frontmatter | complete |
| INFRA-138 | `scope_guard.py` — story file-scope enforcement module | complete |
| INFRA-139 | `pre_tool_use.py` Edit/Write dispatch + CLAUDE.md carve-out update | complete |
| INFRA-140 | Fix sync regression: `sync.py` maintains deny list after sync | complete |
| BUILD-024 | Replace `write-permissions`/`clear-permissions` with `permissions create` in build loop | planned |

## Schema delivery

For each new persistent schema object (table, collection, migration) introduced in
this phase, record the management surface before the phase is checkpointed.

| Object | Management surface | Exception |
|---|---|---|
| `docs/phases/permissions/*.json` | Generated by `flex_build.py permissions create`; committed with story spec | Append-only build artifact; no CRUD UI needed |

---

### CP-55 Cold-eyes checklist

— developer fills in after phase completion —
