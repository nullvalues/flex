# flex — Cold-Eyes Review (CER) Backlog

*Last updated: 2026-07-16*

This file is the structured triage log for findings from external cold-eyes reviews.
Each finding is assigned to one quadrant. Findings are not deleted — resolved findings
remain in place with a resolution note.

---

## Do Now

Urgent and important. Blocks correctness, security, or the next phase.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|

| CER-072 | CRITICAL: the RESOLVER-008 checkpoint build-gate guard can never pass in any downstream project. `_run_build_gate_subprocess` (`skills/pairmode/scripts/next_action.py:367-395`) hardcodes `uv run pytest tests/pairmode/ -q --tb=no` in the target project directory and treats any non-zero exit as gate-red — but `tests/pairmode/` exists only in flex itself; verified absent in every fleet project checked (forqsite, coherra, meander, caddy, radar, asp), where pytest exits non-zero with "file or directory not found" and the guard returns False. The fail-open branch covers only exceptions/timeouts, not exit-code failures, so `next-action` returns `await-user / checkpoint-guard-failed:build-gate` unconditionally at every downstream checkpoint. Live-hit: forqsite 2026-07-22, first downstream checkpoint under 0.3.0 (phase PM065-main, the migration phase itself) — the project's real build gate (`pnpm build && pnpm typecheck && pnpm test`, 2827 tests) was green while the guard hard-blocked the dispatcher. Fix direction: make the guard command project-aware — read the project's build-gate command from state/config (or detect: `tests/pairmode/` present → pytest; `package.json` present → the project's own test script), or treat "no such test directory" (pytest exit 4) as advisory-pass like the other error paths; add a downstream-shaped fixture (no tests/pairmode, passing package.json gate) to the RESOLVER-008 tests. **RESOLVED Phase 97 — INFRA-230 made `_run_build_gate_subprocess` (`skills/pairmode/scripts/next_action.py`) project-aware: it now reads `test_command` from `project_dir/.companion/pairmode_context.json` (populated by `bootstrap.py` for every bootstrapped project) and runs that exact command via `shell=True` (honoring `&&`-chains like forqsite's `pnpm build && pnpm typecheck && pnpm test`), honoring its real exit code so a genuinely-failing gate still reports red. When the file is absent, unreadable, invalid JSON, or has no non-empty `test_command` string, it falls back to the historical hardcoded `uv run pytest tests/pairmode/ -q --tb=no` — so flex-harness's own gate (no `pairmode_context.json`) is unaffected. Four regression tests added to `tests/pairmode/test_next_action.py` (passing command → green; failing command → red; no context → pytest fallback; malformed/empty context → pytest fallback, no crash). Existing fail-open on exception/timeout preserved for both paths.** | forqsite migration (first downstream 0.3.0 checkpoint) + fleet spot-check | 2026-07-22 | 97 |

| CER-067 | `skills/pairmode/scripts/bootstrap.py` has exactly one hook-registration function, `_register_pretooluse_hook`, which is the only one ever called during `bootstrap` (line ~1137) or `sync`/`sync-all`. Flex's own canonical `hooks/hooks.json` registers six event types across ten blocks (`Stop`, `PermissionRequest`/`ExitPlanMode`, `PreToolUse` ×3, `PostToolUse` ×2, `SessionEnd`, `SessionStart`, `UserPromptSubmit`), but every downstream bootstrapped project's `.claude/settings.json` gets only the `PreToolUse` block — confirmed by inspecting the full fleet (coherra, meander, caddy, forqsite, forqsite.help, radar, asp, aab, lumin, halfhorse, base56, pokus, rockue all show `hooks: ['PreToolUse']` only; `cora` additionally carries a manually-added `PostToolUse`/`Stop` pair, not from bootstrap). Consequence: `user_prompt_submit.py` (INFRA-192) never fires downstream, so `context_budget_user_turn_seq` never increments — the "genuine new turn since last block" requirement INFRA-193 built specifically to close the CER-047 self-clearing bug can never be satisfied by a normal reply in any downstream project. `session_start.py`'s reset (session_reset.py) never fires, so `context_current_tokens` never resets on `/clear`. `post_tool_use.py`'s `Task`/`Agent` branch never fires, so `context_current_tokens` is never actually written from a live transcript read. The entire context-budget gate is effectively decorative in every downstream project — it reads state nothing downstream ever writes or advances. Live symptom: an agent working in `asp` reported the budget gate "can never self-clear via a normal 'Continue building' reply" and applied an undocumented workaround (`context_budget_acknowledged_at = tokens - margin`, `context_budget_acknowledged_user_turn_seq = seq - 1`, manually forged into `state.json`) to unstick it, noting "I've hit this before" — no such workaround is documented anywhere in flex's own docs, meaning this has been informally reinvented and is being applied silently/routinely across sessions to defeat a broken mechanical gate, echoing the exact failure mode CER-039 already flagged (LLM-attention-only protection once the mechanical gate is dead) — except now actively papered over rather than just silently absent. Fix direction: generalize `bootstrap.py`'s hook registration so it registers the full set of load-bearing hooks (`UserPromptSubmit`, `SessionStart`, `PostToolUse` `Task`\|`Agent` branch — the three the context-budget gate depends on; evaluate whether `Stop`, `PermissionRequest`/`ExitPlanMode`, and `PostToolUse` `Write`\|`Edit`\|`MultiEdit` are downstream-appropriate too, since those are companion/sidebar-oriented and may need to stay opt-in rather than blanket-registered) instead of the current single hand-rolled `_register_pretooluse_hook` function; re-run the fleet rollout for the newly-registered hooks once fixed. CRITICAL severity (a core methodology safety gate has never functioned in any downstream project, and its absence is now being actively worked around rather than merely unnoticed). `skills/pairmode/scripts/bootstrap.py`, `hooks/hooks.json`, `hooks/user_prompt_submit.py`, `hooks/session_start.py`, `hooks/post_tool_use.py`. **RESOLVED Phase 95 — INFRA-208 generalized `bootstrap.py`/`sync.py` registration to wire `UserPromptSubmit`, `SessionStart`, and `PostToolUse` `Task\|Agent` alongside the existing `PreToolUse` block (deferring the four companion/sidebar blocks as opt-in). INFRA-209's read-only fleet audit (2026-07-21) found the rollout already applied to 13 of 14 in-scope projects ahead of the story's build (no commits needed); `cora` was formally excluded as a known carve-out (not touched); `anchor` remains excluded as a non-pairmode-consumer sibling plugin repo. `asp`'s forged `context_budget_acknowledged_*` workaround keys are still present in its `state.json` — resetting them is an explicit follow-up, not required for this resolution.** | Live-hit in `asp` (agent workaround report) + direct verification against every fleet project's `.claude/settings.json` | 2026-07-18 | 95 |

| CER-066 | `_update_story_row_in_phase` in `skills/pairmode/scripts/story_update.py:225-235` parses a Stories-table row with a naive `stripped.split('\|')`, which does not respect markdown-escaped pipes (`\\\|`) inside a cell. Any story title containing a literal escaped pipe — a real, likely occurrence in this codebase's own INFRA rail, which frequently documents matcher strings like `Edit\|Write` or `Task\|Agent` (see INFRA-205, INFRA-206) — gets its title truncated at the first `\\\|` and the intended status value spliced into the truncated fragment instead of the real status cell, while the real last cell is left unchanged. Reproduced directly: a fixture phase file with row `\| INFRA-1 \| Register the Edit\\\|Write matcher \| planned \|`; calling `update_phase_story_status("INFRA-1", ..., "complete")` corrupted the row to `\| INFRA-1 \| Register the Edit\\\|complete \| planned \|` — title truncated, status not actually updated in the real cell. Live-hit during Phase 93's own build: `story_update.py --story-id INFRA-205 --status complete` corrupted `docs/phases/phase-93.md`'s INFRA-205 row this exact way; manually repaired by the orchestrator. INFRA-206's title contains the same escaped-pipe pattern and is expected to hit the same corruption when its status is flipped. Fix direction: split on an un-escaped-pipe boundary (e.g. `re.split(r'(?&lt;!\\\\)\|', stripped)`, treating `\\\|` as a literal pipe within a cell rather than a column separator) instead of a naive `str.split('\|')`; add regression tests with escaped-pipe titles (including the exact INFRA-205 collision shape) to `tests/pairmode/test_story_update.py`. HIGH severity (silent phase-doc title corruption on story titles containing escaped pipes — a pattern this codebase's own INFRA rail produces routinely when documenting hook matcher syntax). `skills/pairmode/scripts/story_update.py:225-235`. **RESOLVED Phase 94 (cp94) — INFRA-207 replaced the naive split with `re.split(r'(?<!\\)\|', stripped)`, treating an escaped pipe as a literal cell character rather than a column separator. Regression tests cover the exact CER-066 reproduction and the INFRA-205/INFRA-206 collision shape.** | Live-hit during Phase 93 build (INFRA-205 status update) + direct reproduction | 2026-07-16 | 94 |

| CER-065 | `hooks/pre_tool_use.py` has full dispatch logic for three tool types (`Task`/`Agent` → `context_budget.py`; `Edit`/`Write` → `scope_guard.py`, Phase 55; `Read` → `cold_read_guard.py`, INFRA-196), but `hooks/hooks.json`'s `PreToolUse` array registers only one block, `matcher: "Task\|Agent"` — no `Edit\|Write` or `Read` block, unlike the `PostToolUse` array immediately below it in the same file, which correctly carries two separate matcher blocks for `post_tool_use.py`. Result: Claude Code never invokes `pre_tool_use.py` for `Edit`, `Write`, or `Read` tool calls, so the `scope_guard.py` and `cold_read_guard.py` branches are dead code — in every project using this plugin, including flex itself (confirmed: flex's own `hooks/hooks.json` has the same single-block gap). Worse for non-plugin bootstrapped projects: `bootstrap.py`'s `_register_pretooluse_hook()` (skills/pairmode/scripts/bootstrap.py:309-355) only ever creates/finds a `matcher: "Task"` block — not even `"Task\|Agent"` (predates the CER-049 rename fix) — and never an `Edit\|Write` or `Read` block. Root cause traced via git history: `hooks.json`'s `PreToolUse` block was created Task-only in INFRA-128 (99a181f) when the hook only handled the context-budget gate; two later stories each added a new dispatch branch to `pre_tool_use.py`'s Python source without touching the registration manifest — INFRA-139 (123eae7, `Edit`/`Write` → `scope_guard.py`) and INFRA-196 (b333739, `Read` → `cold_read_guard.py`). Neither commit's diff touches `hooks/hooks.json` or `bootstrap.py`. Both stories passed their reviewer checklist (and INFRA-196 presumably a security-audit checkpoint); the CLAUDE.md HOOK PERFORMANCE checklist item verifies dispatch logic stays thin but never checks whether a new branch is actually reachable via the matcher. Reported via an external `caddy` repo session that found the same gap independently while investigating a `sync-all` deletion of the now-dead INFRA-196 CLAUDE.md paragraph. Fix direction: add `Edit\|Write` and `Read` matcher blocks to `hooks/hooks.json`'s `PreToolUse` array (mirroring the two-block `PostToolUse` pattern already in the same file); update `bootstrap.py._register_pretooluse_hook` to register all three matchers (or a single combined `Task\|Agent\|Edit\|Write\|Read` block) for non-plugin bootstrapped projects; add a test asserting `hooks.json`'s registered matchers are a superset of the `tool_name` branches `pre_tool_use.py` actually dispatches on (so a future new branch fails a test instead of shipping silently dead). CRITICAL severity (two documented enforcement mechanisms — story-file-scope enforcement and orchestrator cold-read blocking — have likely never executed on any project, ever; distinct from the separate story-scoped Layer-1/Layer-2 permissions system and the static `.claude/settings.json` deny list, CER-048, which are unaffected and still provide some protection). `hooks/hooks.json`, `skills/pairmode/scripts/bootstrap.py:309-355`, `hooks/pre_tool_use.py`. **RESOLVED Phase 93 (cp93) — INFRA-205 registered `Edit\|Write` and `Read` PreToolUse matcher blocks in `hooks/hooks.json` (mirroring the `PostToolUse` two-block pattern) with a regression test asserting the registered matchers are a superset of `pre_tool_use.py`'s dispatched tool_name literals; INFRA-206 widened `bootstrap.py`'s `_register_pretooluse_hook` to register the canonical combined matcher for downstream bootstrapped projects, migrating any stale `"Task"`-only block in place.** | caddy repo (external report) + direct verification against flex's own hooks.json/bootstrap.py/git history | 2026-07-16 | 93 |

| CER-064 | `update_phase_story_status` in `skills/pairmode/scripts/story_update.py:109-121` globs every file under `docs/phases/*.md` and updates the status column in any Stories-table row whose bare first column matches `--story-id`, with no phase or rail disambiguation. When two phases carry a row for the same bare story ID — a real, demonstrated scenario in this repo (see CER-063: `INFRA-203` collides between `main` phase 91 and the unmerged `fold-prep` branch's `HARNESS011-main`) — running `story_update.py --story-id RAIL-NNN --status complete` against one phase's story silently flips the unrelated phase's row to the same status too. Reproduced directly: two fixture phase files each with an independent `REF-106` row; a single `--status complete` call updated both. Reported externally by the `ud` migration repo (its own Phase 24 and Phase 29 both carried an unrelated `REF-106` row; a status update on one leaked into the other) — that repo has no authority to fix shared pairmode tooling directly. Fix direction: scope `update_phase_story_status` to the phase(s) named in the story file's own `phase:` frontmatter field (already read by `record_attempt.py` for the same purpose) instead of scanning every phase manifest; only fall back to the current whole-glob behavior when the frontmatter `phase:` field is absent (legacy story). HIGH severity (silent cross-phase status corruption on any story-ID collision — undermines phase Stories tables as a source of truth). `skills/pairmode/scripts/story_update.py:109-121`. **RESOLVED Phase 92 (cp92) — INFRA-204 scopes `update_phase_story_status` to the story's declared `phase:` frontmatter (suffix-aware, mirroring `story_new.py._append_to_phase`'s glob precedence), falling back to the whole-glob scan only for legacy phase-less stories. Regression tests reproduce the exact cross-phase leak.** | ud-migration repo (external report) + Phase 91 intent review (CER-063 precedent) | 2026-07-16 | 92 |

| CER-057 | `tests/pairmode/test_lesson_review.py::TestCLIOutputClarity` invokes the real `lesson_review` CLI (`--approve`/`--reject`) without `--project-dir` or `--skip-drift`, so the post-review drift-promotion step reads the live `.companion/state.json` `registered_projects` and runs real drift detection. When ≥2 registered projects share a convergence pattern (observed 2026-06-26: `registered_projects = [coherra, meander]`), the CLI hits the interactive `Promote to canonical? [y/n/skip]` prompt, gets no stdin, and aborts (exit 1) — failing `test_approved_lesson_output_contains_action_required` and `test_end_of_run_summary_contains_review_complete`. Environment-dependent (green when <2 projects share a pattern; was green at session start). A unit test must not invoke live interactive fleet drift promotion. Fix: pass `--skip-drift` (these tests assert lesson-annotation output, not drift) in the affected CLIOutputClarity invocations; scan the class for sibling tests with the same latent coupling. HIGH (blocks the build gate; flaky green/red on fleet state). `tests/pairmode/test_lesson_review.py:596-640`. **RESOLVED HARNESS001-ante1 (cp-HARNESS001-ante1) — INFRA-185 added `--skip-drift` to the two CLIOutputClarity invocations; the build gate is now green regardless of `registered_projects` contents.** | RELEASE-003 build (HARNESS001-ante1) | 2026-06-26 | HARNESS001-ante1 |

| CER-049 | The `PreToolUse` context-budget hook appears to never fire: `hooks/hooks.json` registers the matcher `"Task"`, but the agent-spawn tool in current Claude Code harnesses is named `Agent`. Observed Phase 68 build (2026-06-12): four subagent spawns proceeded with `context_current_tokens` between 151k and 694k — far past the 132k block ceiling — with no block and no `context_budget_acknowledged_at` written. If the rename hypothesis is confirmed, the belt-and-suspenders secondary gate (CER-027, resurrected in CER-039, redesigned in CER-045) is silently disabled fleet-wide and the only remaining protection is the LLM-attention primary gate — the exact failure mode CER-039 documented. Verify first (log `tool_name` from a PreToolUse catch-all matcher), then fix: matcher `"Task | Agent"` in `hooks.json` plus the same dual check in `pre_tool_use.py` dispatch. HIGH severity (silent loss of mechanical enforcement). `hooks/hooks.json:26-37`, `hooks/pre_tool_use.py:25`. **RESOLVED Phase 69 (cp69) — `hooks.json` matcher + `pre_tool_use.py` dispatch now accept `Task | Agent` (CER-049 phase).** | Phase 68 build observation |

| CER-047 | `context_current_tokens` survives `/clear` and blocks the build loop on a phantom number. Confirmed in production (coherra, 2026-06-12): operator cleared mid-build at 212,492 accumulated tokens; `state.json` retained the old session's counter; the context gate re-blocked INFRA-006 on a value describing a window that no longer exists. The CER-041 TTL (60 min) only recovers when the gap exceeds the TTL — the operator cleared within ~20 min — and even the stale path routes to the same manual `/context` + `set-context-tokens` step. CER-041 originally named a `SessionStart` hook reset as an alternative fix; only the TTL shipped (Phase 59 INFRA-151). The SessionStart hook input carries `source: "startup" \ | "resume" \ | "clear" \ | "compact"`, but `hooks/session_start.py` never reads stdin. Fix: thin-delegation reset — pure decide module in `skills/pairmode/scripts/`, single hook-owned state write resetting the counter to a fresh-session baseline on `clear`/`startup` (never `resume`). `compact` is deliberately out of scope (post-compact window size unknown; counter overstates, which fails safe by over-blocking — revisit if it bites). HIGH severity (defeats the context gate on every mid-build clear; the gate's primary recovery path is the manual step the methodology is trying to eliminate). `hooks/session_start.py`, `skills/pairmode/scripts/context_budget.py` (read side), `docs/architecture.md` writer contract, CLAUDE.md checklist item 1. **RESOLVED Phase 68 (cp68) — `session_start.py` reads the `source` field and resets the counter on `clear`/`startup` via `session_reset.py` (INFRA-175).** |

| CER-046 | `PAIRMODE_VERSION` is defined in three places with diverging values: `audit.py:28` = `"0.1.0"`, `bootstrap.py:33` = `"0.2.0"`, `sync.py` imports from `audit.py` (stale). Every `sync` run stamps `"0.1.0"` into the target project's `state.json["pairmode_version"]`, silently downgrading projects that were bootstrapped at `"0.2.0"`. Confirmed in production: coherra's state.json was downgraded and required manual restoration. Fix: single-source the constant (e.g. `skills/pairmode/scripts/_version.py`) and have `audit.py`, `bootstrap.py`, and `sync.py` import from it. HIGH severity (silent state corruption on every sync run). `audit.py:28`, `bootstrap.py:33`, `sync.py` (import site). **RESOLVED Phase 66 — `PAIRMODE_VERSION` single-sourced in `_version.py`; audit/bootstrap/sync import it.** | Upstream operator report | 2026-06-11 | 69 |

| CER-036 | `flex_build.py` `cmd_permissions_create` takes `STORY_ID` as a positional CLI argument and uses it unsanitised to construct both an input path (`docs/stories/<rail>/<story_id>.md`) and an output path (`docs/phases/permissions/<story_id>.json`). No regex validation on `story_id`, no `Path.resolve().relative_to(project_path)` containment on `out_path`. A crafted `story_id` with `..` segments can write attacker-shaped JSON to any path the invoking process can reach. The permissions file is the source of truth that `scope_guard.py` consults for hook-level write enforcement — write-side traversal weakens scope enforcement integrity. Fix: add `re.fullmatch(r"[A-Z][A-Z0-9_]*-\d{3}", story_id)` guard at function entry; add `resolve().relative_to()` containment on both `story_path` and `out_path` before any I/O. HIGH severity. `flex_build.py` cmd_permissions_create. **RESOLVED Phase 55 — `cmd_permissions_create` now applies `resolve().relative_to()` containment on both story_path and out_path (traversal blocked). Format pre-validation was later removed (cp78); the path containment is the security guard.** | Phase 55 security audit | 2026-06-03 | 55 |

| CER-037 | `docs/architecture.md` still describes `hooks/pre_tool_use.py` as "context budget enforcement (CER-027); thin delegate to skills/pairmode/scripts/context_budget.py" (line 26). Phase 55 INFRA-139 added a second dispatch branch (`Edit`/`Write` → `scope_guard.py`) but did not update architecture.md. The "Documented exception" block at line 785 also names only CER-027 and context_budget. A future reviewer reading architecture.md alone sees no authority for the scope_guard dispatch. HIGH severity (doc-currency gap for security-relevant decision-block surface). `docs/architecture.md` lines 26, 169-178, 785-792. **RESOLVED Phase 55 — `architecture.md` documents the `scope_guard` Edit/Write dispatch (7 refs).** | Phase 55 security audit | 2026-06-03 | 55 |

| CER-038 | `phase_new.py` `--phase-id` and `--suffix` CLI arguments are accepted as free-form strings and embedded into filesystem paths (`phases_dir / f"phase-{phase_key}.md"`) without sanitization or containment check. The `relative_to()` calls are display-only and provide no write protection. A crafted argument (e.g. `--suffix "../../../../../../attack"`) can write outside the project directory. Secondary: unsanitised `phase_key` is also written verbatim into `index.md` and era files (markdown injection). Missing tests for traversal vectors in new test classes. Fix: validate `phase_id` and `suffix` against `re.fullmatch(r"[A-Za-z0-9][A-Za-z0-9_-]*", value)` at CLI entry; add traversal-payload test cases. HIGH severity. `phase_new.py` lines 287–294. **RESOLVED Phase 56 — `phase_new.py` validates `--phase-id`/`--suffix` via `_SAFE_PHASE_COMPONENT.fullmatch` at CLI entry.** | Phase 56 security audit | 2026-06-03 | 56 |

| CER-039 | `context_budget.py` secondary hook gate non-functional since Phase 47. `compute_context_tokens(transcript_path)` returns `None` whenever `transcript_path` is absent or incorrect in PreToolUse hook payloads — which is every production session. Confirmed: `context_budget_acknowledged_at` is absent from ALL projects' `state.json` across the entire fleet (flex, forqsite, cora, radar, and 8 others). Root cause: Phase 47 D3 chose transcript JSONL parsing instead of a state.json contract written by the orchestrator's `/context` call. Effect: the secondary gate has never blocked a single Task spawn; the only protection was the LLM-attention primary gate, which also fails on long phases (confirmed forqsite PM023-main: 6-story phase with retries ran to 100% context with no interrupt). Fix: add `flex_build.py set-context-tokens --tokens N` CLI; have orchestrator write token count to `state.json["context_current_tokens"]` after each `/context` call; have `context_budget.py` read from there. HIGH severity. `skills/pairmode/scripts/context_budget.py`, `hooks/pre_tool_use.py`, `CLAUDE.build.md` Context gate. **RESOLVED Phase 58 (cp58) — `set-context-tokens` CLI + `state.json` context-token contract; `context_budget.py` reads from state (INFRA-148).** | Phase 57 build post-mortem | 2026-06-03 | — |

| CER-045 | Three interlocking bugs defeat the context gate on multi-story phases. (1) `story_context.py --clear` erases `context_current_tokens` after every story (Phase 59 CER-041 belt-and-suspenders), forcing a re-estimate at each Context gate. (2) The orchestrator cannot call `/context` programmatically; it estimates (e.g. 25k) and calls `set-context-tokens` with the wrong value every story. So `decide()` always sees 25k + 53k = 78k < 132k → never blocks. (3) `story-cost-estimate` filters by (rail, story_class) with ≥5 sample requirement — new phases always show "insufficient data." Fix (Phase 65): (A) remove token-count pops from `clear_current_story()` (INFRA-170); (B) add `bump-context-tokens --cost N` that accumulates actual story cost (INFRA-169); (C) redesign Context gate to READ from state.json rather than calling /context — absent/stale key emits CONTEXT CHECK REQUIRED once per session (BUILD-027); (D) cross-phase/cross-rail estimation fallback (INFRA-171). HIGH severity. `story_context.py`, `context_budget.py`, `flex_build.py`, `CLAUDE.build.md`. **SUPERSEDED by CER-054 (D2) — the Phase 65 / 70–74 rework addressed (1) and (3). Symptom (2) was tracked forward into CER-054, which investigated the "stuck at 25k" observability-surface symptom directly and RESOLVED cp-HARNESS007-main: root-caused to genuine fleet-project idle state (no Task/Agent spawns), not a writer bug — no code fix required, the SPA's staleness badge already surfaces it. No open action remains on this chain.** | forqsite operator observation 2026-06-10 | 2026-06-10 | — |



---

## Do Later

Important, not urgent. Quality improvements, architectural refinements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|

| CER-095 | HIGH: single-orchestrator parallel story builds (an original design intent, previously exercised) are broken by single-slot coordination state. (1) The resolver has no in-flight awareness — `find_next_story` (`next_story.py:169-220`) returns the first uncommitted story, never consulting `.pairmode-worktrees/`, so while story A builds, `next-action` keeps offering A and never B; the worktree/branch is already a de-facto atomic claim (`flex_build.py:2388-2403`), the resolver just doesn't read it. (2) `state.json["current_story"]` is one global slot: a second `create-story-worktree` overwrites the first's stamp, so `scope_guard` (`scope_guard.py:43,140-185`) evaluates builder A's writes against story B's allow-list (every write denied, misattributed), and merge/discard clear the slot unconditionally (`flex_build.py:2497,2548` → `story_context.py:98`), silently disabling story-scope enforcement for the still-running builder. (3) `.companion/attempt_counter.json` is a single-slot whole-file rewrite (`flex_build.py:929-932`); parallel FAILs cross-clobber and `read_attempt_count` returns 0 on story mismatch (`:970-971`), silently resetting the escalation ladder; `clear_attempt_count` on merge (`:2492`) wipes the other story's live count. (4) `checkpoint_step`/`checkpoint_phase` are one shared list/stamp (`flex_build.py:2102-2160`) — parallel phase checkpoints interleave and the terminal branch resets the other phase's sequence. Fix: resolver skips claimed stories; key `current_story`, the attempt counter, and checkpoint state by story/phase id with clears scoped to own key; INFRA-264's E9 guard must be per-key. **INFRA-280 (Phase 109) — item (1) resolved:** `flex_build.claimed_story_ids` reads `.pairmode-worktrees/` (the existing create/merge/discard claim, no new state); `next_story.find_next_story` gained an opt-in `claimed=` filter and `infer_position`/`resolve_next_action` now skip claimed stories and refuse to checkpoint when every remaining story is claimed (`await-user`/`all-stories-claimed`, evaluated ahead of Row 9). **INFRA-281 (Phase 109) — item (2) resolved:** `state.json` gained a story-keyed `current_stories` record (`story_context.set_current_story`/`clear_current_story`), with the flat `current_story` retained only as a derived mirror for the five readers outside this fix's scope. `scope_guard.resolve_call_story(project_dir, file_path)` resolves the acting story **per call** — from the call's own worktree cwd or target path first, falling back to `state.json` only from a demonstrable main-checkout call, and refusing to guess (resolving to no active story, never "most recent") when `current_stories` holds two or more entries. `flex_build._clear_active_story` now takes the story ID, and both `merge-story-worktree` and `discard-story-worktree` pass their own `--story-id` through, so landing or discarding one story's worktree no longer wipes a still-building sibling's scope enforcement. All four items are now closed. **INFRA-282 (Phase 109) — item (3) resolved:** `.companion/attempt_counter.json` gained a story-keyed record (`{"stories": {"<story_id>": <count>, ...}}` via `flex_build._read_attempt_counters`/`write_attempt_count`), with the legacy flat `{"story_id", "attempt_count"}` shape still read transparently and upgraded to the keyed shape only on the next write — no migration step. `clear_attempt_count(project_dir, story_id=None)` gained a story scope — `merge-story-worktree` now clears only its own landed story's entry, and `discard-story-worktree` clears nothing at all, so a discarded attempt's count survives to escalate the retry. `subagent_transcript._story_accepts_late_bump`'s liveness check now resolves against the story-keyed `current_stories` record first (INFRA-281's authority), falling back to the flat `current_story` mirror only for pre-INFRA-281 state files, so a second live story's first late FAIL bump is no longer refused. **INFRA-283 (Phase 109) — item (4) resolved:** `state.json` gained a phase-keyed `checkpoint_steps` record (`dict[phase_key, list[step_id]]`), the key resolved by INFRA-265's existing `--phase-key`/stamp/re-derivation precedence chain (no new resolution logic) and consumed by that same chain's A2/A4/A3 ordering, now run before a per-key idempotency check rather than a global one. `checkpoint-tag` pops only its own key (`steps.pop(effective_key, None)`), so a sibling phase's mid-sequence progress survives a terminal call for a different phase. The legacy flat `checkpoint_step`/`checkpoint_phase` pair is retained as a **derived mirror** for readers outside this fix's scope (`resolverState.ts`, `client.ts`) — written but never read to decide what to append — and a legacy-shape `state.json` is read correctly and upgraded to the keyed shape on its next successful write, with no migration step. `next_action.infer_position` reads the active phase's own keyed entry directly once the keyed record exists, so two phases resolve independently; the CER-083 stale-stamp rule and `record-checkpoint-step`'s A4 disagreement check now apply only on the legacy path, since once concurrency is genuinely in play the single flat stamp can no longer reliably name which phase is mid-sequence for more than one phase at a time. | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |
| CER-096 | MEDIUM: `effort.db` has no concurrency configuration — all 10 `sqlite3.connect` sites (`effort_db.py:217-564`) use rollback-journal mode with default busy handling; contention beyond 5s raises `OperationalError` which `record_effort`/`record_attempt_from_transcript` swallow (`effort_recorder.py:143-144`, `subagent_transcript.py:849-850`) = silent row loss. `init_db` runs five ALTERs in a write txn on every recording (`effort_recorder.py:165`), maximising contention. `next_attempt_number` is a COUNT(*) read-then-write whose own comment concedes the race is only safe because "the era's serial build loop makes the resulting race a non-issue" (`subagent_transcript.py:800-806`; same claim `docs/architecture.md:2194-2200`) — void under parallel spawns. `reconcile_pending_attempts` sweeps globally with `ORDER BY id DESC LIMIT 5` (`effort_db.py:394-431`) — no ownership filter, and >5 pending rows permanently starve the oldest. Fix: WAL + explicit busy_timeout on every connect, init_db once-per-process, atomic write-side attempt-number derivation, sweep ownership/cursor semantics; amend both stale serialism comments. **RESOLVED Phase 109 — INFRA-284 closed all four defects at the query layer: (A) every `effort_db.py` connection now routes through a single `_connect` helper (`sqlite3.connect(timeout=BUSY_TIMEOUT_SECONDS)` + `PRAGMA busy_timeout`/`synchronous=NORMAL`), and `init_db` enables `PRAGMA journal_mode = WAL` once, persistently, tolerating failure; (B) a per-process `ensure_db` cache (keyed by resolved path, re-initialising a deleted file) replaces the per-recording `init_db` call in `effort_recorder.record_effort`; (C) `insert_attempt_derived` derives `attempt_number` as `COALESCE(MAX(attempt_number), 0) + 1` inside one `BEGIN IMMEDIATE` transaction, `record_effort(attempt_number=None)` uses it, and `subagent_transcript.record_attempt_from_transcript` no longer pre-computes the ordinal via `next_attempt_number` (which survives, unchanged, as an advisory/read-only helper); (D) `pending_reconcilable` gained an `output_prefix` ownership filter (escaped `LIKE`, parameterised) and an `order` (`newest`/`oldest`) parameter, and `reconcile_pending_attempts` now fetches both ends (`RECONCILE_OLDEST_ROWS = 2`) so no pending row starves. Two items are explicitly NOT this story's: the stale serialism prose in `docs/architecture.md:2459-2468` is INFRA-286's (only the in-code comment at `subagent_transcript.py` was corrected here), and wiring a real session-scoped `output_prefix` value at any call site is INFRA-285's (CER-097) — this story built and exposed the mechanism only.** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |
| CER-097 | HIGH: any second session (even pure investigation) corrupts a live build loop's companion state via hooks. SessionStart resets `context_current_tokens` to the new session's baseline (`hooks/session_start.py:63-93`) — the loop's budget gate then measures the wrong window; both sessions' PostToolUse write the same token key from different transcripts (`hooks/post_tool_use.py:65-87`), losing ring-buffer samples and corrupting `expected_step_tokens`; every SessionStart also runs the global reconcile sweep over the other session's live rows (`session_start.py:103-107`) and can bump the shared attempt counter cross-story (`subagent_transcript.py:691-697`). Compounding: `user_turn_seq.py:125` does a NON-atomic whole-`state.json` rewrite on every prompt in every session (torn file = silent whole-state reset to defaults; readers fail open); `sync.py:650`, `flex_build.py:929`, `phase_new.py:215,231`, `story_update.py:110,213` are also non-atomic writers. No cross-process lock exists anywhere (only atomic-replace, `state_utils.py:17-46`; CER-050 note "concurrent writers are not expected" is no longer true). Fix: session-scoped context accounting (SessionStart must not clobber another live session's counter), sweep session-ownership, atomic-replace for all writers, and a cheap advisory flock around state.json RMW. **RESOLVED Phase 109 — INFRA-285 closed all six items: (A) `skills/pairmode/scripts/session_state.py` introduces the `state.json["context_sessions"][<session_id>]` keyed record (`SESSION_SCOPED_KEYS`, `session_view`/`apply_session_view`, `prune_stale_sessions` at `SESSION_LIVE_TTL_MINUTES = 180`, `other_live_session_prefixes`), with the flat keys retained as a derived, display-only mirror for `routes/context.ts` and session-less CLI readers; (B) `hooks/session_start.py` reads the whole stdin payload (`_read_payload_from_stdin`), writes `decide_reset()`'s keys into the *starting* session's own entry, seeds/registers the session on a non-reset source, and folds reset + seed + prune into ONE locked read-modify-write — `session_reset.decide_reset` is untouched and still pure (D11); (C) `context_budget.decide(..., session_id=)` resolves through `session_view` and fail-safes to the existing CONTEXT CHECK REQUIRED block (no new reason string) for an unregistered session with live siblings, and `hooks/post_tool_use.py`'s token writer and `record_step_growth` call now operate on the session view, so two sessions no longer remix ring-buffer deltas; (D) `effort_db.pending_reconcilable` gained `exclude_output_prefixes` (escaped, parameterised, with the required `output_file IS NULL OR` disjunct), `reconcile_pending_attempts` forwards it to BOTH ends of INFRA-284's cursor, `subagent_transcript.session_output_prefix` derives a session's spawn-output prefix, SessionStart excludes other live sessions' prefixes while PostToolUse includes its own — the asymmetry is deliberate, since an inclusive SessionStart filter would strand every dead-session orphan row — and the cross-story counter bump stops by construction (`_story_accepts_late_bump` unmodified); (E) `state_utils` gained `_atomic_write_text`, `state_lock` and `update_state_json`, and every writer CER-097 named (`user_turn_seq`, `sync`, `phase_new`, `story_update`, all three hooks) plus `story_context`'s `current_stories` RMW was converted. **The lock is advisory, bounded (`STATE_LOCK_TIMEOUT_SECONDS = 2.0`) and fail-open — it reduces but does not eliminate lost-update risk, and multi-orchestrator operation remains explicitly OUT OF SCOPE (`docs/phases/phase-109.md` § Scope statement); this row must not be read as a stronger guarantee than was built.** Retiring CER-050's "concurrent writers are not expected" note and the stale serialism prose in `docs/architecture.md` are CER-098/INFRA-286's, not this story's.** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |
| CER-098 | MEDIUM: `merge-story-worktree` robustness gaps under concurrency. (a) `git worktree remove --force` and `git branch -D` return codes are ignored (`flex_build.py:2487-2488`) — on failure the command still prints success and exits 0, leaving a stale worktree/branch that breaks the story's next `create-story-worktree` with a confusing "already exists". (b) If the `--ff-only` merge loses a race to another landing (`:2478-2485`), the command exits 1 with the worktree rebased-but-unremoved and the counter/current_story/permissions stamps uncleared — loop state and git state diverge with no documented recovery. (c) Parallel merges must serialize (git index.lock makes the loser fail loudly; needs retry-or-queue semantics, not manual repair). Fix: check both return codes, make the cleanup block run (or report precisely what remains) on merge failure, document/implement merge serialization; also update CER-050's "concurrent writers not expected" doctrine note (`docs/cer/backlog.md:178`). **RESOLVED Phase 109 — INFRA-286: (a) `flex_build.py` gained `_teardown_story_worktree`, shared by `merge-story-worktree` and `discard-story-worktree`, which checks both `git worktree remove` and `git branch -D` return codes, short-circuits the branch delete on a failed removal, and returns residue text rendered with exact repair commands by `_residue_lines`; on merge the three loop-state clears still run unconditionally before residue is reported (the story landed), while on discard residue short-circuits before any clear (nothing landed) — the asymmetry is deliberate. (b) A failed `--ff-only` merge (or a failed rebase) tears down nothing and clears nothing; both failure branches now print a `recovery: `-prefixed block naming what survives and the exact re-run command, and re-running is the documented, idempotent recovery. (c) `flex_build.py` gained `MERGE_LOCK_TIMEOUT_SECONDS` (120.0 s) and `_merge_lock`, wrapping both commands' critical sections in `state_utils.state_lock` on `.companion/merge`. **This is a bounded advisory lock that narrows the `index.lock` contention window, not retry-or-queue semantics and not a multi-orchestrator guarantee — non-acquisition is fail-open (a warning, never fatal), matching `state_utils.state_lock`'s own design.** Doc amendments: the `next_attempt_number` serialism paragraph and the INFRA-285 deferred-lock forward pointer in `docs/architecture.md` are corrected (D1/D2), and a new **Merge robustness** paragraph documents (a)-(c) (D3).** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |
| CER-093 | MEDIUM: `tests/pairmode/test_vendored_payload_tracked.py::test_no_vendored_payload_is_gitignored` is brittle to environmental noise — upstream npm packages can *ship* `.claude/settings.local.json` inside their published tarballs (hit live at cp-103: `nanoid@3.3.12` and `thread-stream@4.2.0` under `skills/observability/node_modules/.pnpm` carried them; a machine-local `~/.config/git/ignore` rule made them ignored-but-untracked, failing the guard's `ALLOWED_IGNORED` allow-list; removed at checkpoint). Fix: extend the allow-list (or the discovery logic) to tolerate `.claude/` artifact dirs under vendored `node_modules`, and document the caveat in the architecture vendoring note. `tests/pairmode/test_vendored_payload_tracked.py`, `docs/architecture.md`. | cp-103 intent review | 2026-07-25 | 103 |
| CER-094 | MEDIUM: INFRA-261's payload commit tracks a *second* executable native binary the spec never enumerated — `skills/observability/node_modules/.pnpm/better-sqlite3@12.10.0/node_modules/better-sqlite3/build/Release/test_extension.node` (only `better_sqlite3.node` was justified). Decide: enumerate + justify it in the architecture vendoring note, or delete it from the tracked payload (it is a test fixture, not runtime-required) and add it to the guard test's expectations. | cp-103 security audit | 2026-07-25 | 103 |
| CER-099 | MEDIUM: containment-guard parity gap — `classify_pending_reason` and the `include_quiescent` branch of `reconcile_pending_attempts` call `_stream_spawn_output(path)` on the raw `attempts.output_file` value (`skills/pairmode/scripts/subagent_transcript.py:722`, `:1156`), bypassing the `_contained_spawn_output` guard INFRA-266 added one function away (`:802`) for `read_completed_spawn`. Reachable from `pairmode_effort.py pending`. Impact bounded: read-only, returns an enum reason string, discloses no file content; the quiescent path writes only numeric usage back to the DB. INFRA-266's Ensures scoped containment to `read_completed_spawn` only, so this is a scope seam between INFRA-264 and INFRA-266, not a spec violation. Fix: route both call sites through `_contained_spawn_output` before opening. | cp-104 security audit | 2026-07-26 | — |
| CER-101 | MEDIUM: checkpoint-time effort rollup reads empty for a fully-built phase — `checkpoint-report` at cp-109 printed "no attempts recorded" for all seven INFRA-280..286 stories even though `hooks/post_tool_use.py` recorded a spawn row for every builder/reviewer/gate-worker dispatch (rows 383–398, confirmed in `.companion/effort_recording.log`), because every row still had `tokens_total`/`outcome` NULL at checkpoint time and the phase-scoped rollup counts only reconciled rows. Compounding diagnostic mismatch: `pairmode_effort.py pending` classified 14 of those rows `reconcilable`, but a direct `subagent_transcript.reconcile_pending_attempts(project_dir=..., limit=5)` loop reconciled 0 — classification and sweep disagree about the same rows (suspects: `tasks_root`/`_contained_spawn_output` containment on the session tasks dir, or `stop_reason` recognition against live-session transcripts). Fix: make the sweep and `classify_pending_reason` share one reachability predicate, and have `checkpoint-report` either run a bounded reconcile first or print pending-row counts per story instead of "no attempts recorded" (which reads as data loss when it's deferred reconciliation). | cp-109 checkpoint report | 2026-07-27 | — |
| CER-102 | MEDIUM: FAIL-outcome escalation ladder is dead in the live build loop — across two consecutive reviewer FAILs on RELEASE-062 (cp-105 build, 2026-07-27), `.companion/attempt_counter.json` was never created/bumped, so `next-action` kept emitting `spawn-builder` attempt 1 / model haiku (`auto-downgrade`) with no retry escalation, no model upgrade, and no loop-breaker/human-pause row; the orchestrator had to escalate the model manually. Hook-side recording (INFRA-236/237 contract: post_tool_use.py's Task/Agent branch bumps the counter on FAIL derived from the spawn transcript) either never fired or never classified the reviewer outcome as FAIL. Likely adjacent to CER-101's reconciliation gap (outcome NULL at read time means no FAIL is ever seen) — fix together: the counter bump must key off the reviewer verdict at reconciliation, or the resolver must treat a discarded worktree as a failed attempt. | cp-105 build loop | 2026-07-27 | — |
| CER-103 | HIGH: effort-recording attribution is session-bound, not target-bound — spawns dispatched from a flex session against another project (`--project-dir /mnt/work/meander`, worktree cwd under meander) record their attempt rows into flex's effort.db (RELEASE-063 canary: LEGAL-001 rows 419/420 in flex's db, zero in meander's). Native-session runs attribute correctly, so campaign work driven centrally from flex is invisible to the consumer project's db. Recorder should key on the spawn's target project (worktree cwd / --project-dir), falling back to session project. Also: phase-attribution parses the spawn prompt loosely — flex row 416 recorded story_id='phase:key' and meander rows 233-236 'phase:checkpoint' instead of the actual phase key. | RELEASE-063 canary | 2026-07-27 | — |
| CER-104 | HIGH: every attempt row is inserted twice — meander's native 0.3.0 session (post-RELEASE-063 migration) wrote perfect duplicate pairs with near-identical timestamps for all 31 spawns of the day (rows 211-272: LEGAL-002..005, OPS-001..004, INFRA-011, checkpoint workers), and all rows additionally show the CER-101 NULL tokens/outcome pattern natively (checkpoint-report printed 'no attempts recorded' for phase MN028-main while lifetime pre-0.3 rows kept real data). Suspect double hook invocation or a double record call in post_tool_use.py's Task/Agent branch; dedupe fix must also decide which of the pair the reconciliation sweep should own. Confirms CER-101 is a downstream regression, not a flex-session quirk; blocks RELEASE-064..070 with CER-101/CER-102. | meander native session post-canary | 2026-07-27 | — |
| CER-100 | LOW: the security-auditor procedure skill's thin-delegation exception list (check 1, `skills/pairmode/skills/security-auditor/procedure.md`) still names only `session_reset.py` for `session_start.py`, but `hooks/session_start.py:104` has imported `reconcile_pending_attempts` from `subagent_transcript` since INFRA-258 (Phase 103) — documented in `docs/architecture.md` but stale in the skill; a future auditor reading the skill literally would flag it CRITICAL. Fix: add the reconcile sweep (and INFRA-264's `log_recording_event` path if applicable) to the skill's exception list. | cp-104 security audit | 2026-07-26 | — |
| CER-073 | HIGH: `flex_build.py record-attempt` alias (RELEASE-009) forwards no options — the click command declares only `--help`, so every documented invocation (`--story-file`, `--agent-role`, ...) exits 2 with "No such option". The Era-3 orchestrator template tells downstream orchestrators to use this alias; all of them must fall back to calling `record_attempt.py` directly. Live-hit: forqsite, first Era-3 story cycle, 2026-07-22. Fix: declare the alias with `context_settings={"ignore_unknown_options": True}` + a variadic argument pass-through, and add a test that a full real record-attempt flag set round-trips through the alias. **RESOLVED Phase 104 — INFRA-263 declared `cmd_record_attempt` (`skills/pairmode/scripts/flex_build.py`) with `context_settings={"ignore_unknown_options": True}`, `add_help_option=False`, and a variadic `@click.argument("args", nargs=-1, type=click.UNPROCESSED)` in place of the empty option set, forwarding the collected argument tuple to `record_attempt.py` unchanged (including `--help`). New test file `tests/pairmode/test_flex_build_record_attempt_alias.py` covers the full-flag round trip, the `sys.argv`-truncation regression, the exact CER-073 reproducer, `--story-file` forwarding, `--help` forwarding, delegate error propagation, and group-help regression. This is a duplicate filing of CER-071; both close together in this story.** | forqsite Era-3 build | 2026-07-22 | 104 |
| CER-074 | MEDIUM: `next_action.resolve_next_action` never emits `spawn-reviewer` — the constant is declared and listed as a valid action, but no code path constructs it. DP3 infers PASS from a story commit existing, yet only the reviewer commits, so the intended builder→reviewer dispatch is structurally unreachable; a poll after the builder finishes (uncommitted worktree, counter written) infers FAIL and dispatches a wasteful attempt-2 rebuild. Downstream orchestrators must adopt an undocumented one-iteration-per-story reading (builder + reviewer + merge inside a single spawn-builder iteration) for the loop to work — forqsite did, successfully, across 6 stories on 2026-07-22, but nothing documents this contract. Fix: either emit spawn-reviewer from a builder-completed state (e.g. dirty story worktree + attempt recorded) or document the one-iteration-per-story contract in the Era-3 orchestrator template and remove the dead action constant. **RESOLVED Phase 104 — INFRA-268 documented the one-iteration-per-story contract in three places (`CLAUDE.build.md` § Build loop, `skills/pairmode/templates/CLAUDE.build.md.j2`, and `docs/architecture.md` § Pairmode build loop, plus the `next_action.py` module-structure entry), and marked the `SPAWN_REVIEWER` constant as orchestrator-dispatched-only rather than removing it — it stays because the orchestrator's ACTION_SUBAGENT_TYPE map and the model-override rule key on it, and a resolver-emitted `spawn-reviewer` was rejected because it would require non-durable dirty-worktree state in a pure-read resolver and would split one story's cycle across two polls. A parametrized regression test (`tests/pairmode/test_next_action.py::TestResolveNextActionNeverEmitsSpawnReviewer`) pins that `resolve_next_action` never emits `spawn-reviewer` across six position shapes.** | forqsite Era-3 build (HOME-001 near-miss) | 2026-07-22 | 104 |
| CER-075 | MEDIUM: `create-story-worktree` provisions no build environment — fresh worktrees lack `node_modules` and `.env.local` (gitignored), so every builder/reviewer re-discovers and re-applies symlink workarounds, and three forqsite test files fail worktree-only (see also the project-side DL entry on absolute mock paths). Cost: repeated wasted agent effort + "known failures" habituation in every review. Fix direction: worktree-create hook or config (`worktree_provision: [node_modules, .env.local]`-style symlink list in state/config), or document the symlink convention in the builder/reviewer procedure skills. | forqsite Era-3 build (every HOME story) | 2026-07-22 | — |
| CER-076 | LOW: `_STUB_DELEGATION_RE` (`see phase doc|see docs/phases/|see phase-`) false-positives on story files that *quote* deliverable text containing those phrases — e.g. forqsite's HOME-006 instructed the builder to append a backlog resolution note reading "... See docs/phases/phase-PM066-main.md" and the stub gate blocked the story until the quoted text was reworded. Fix: skip matches inside fenced code blocks/inline code spans, or add a frontmatter escape hatch (e.g. `stub_gate: exempt` with a required reason). **RESOLVED Phase 104 — INFRA-268 added a length-preserving code-region mask (`_mask_code_regions` in `skills/pairmode/scripts/flex_build.py`) that blanks fenced code blocks and inline code spans before the delegation search, so quoted text no longer reads as delegation while prose delegation still blocks and the reported line still shows the original text; the frontmatter escape hatch was rejected because a per-story bypass switch is a permanent silent hole in a gate whose whole value is that it cannot be argued with.** | forqsite Era-3 build (HOME-006) | 2026-07-22 | 104 |
| CER-077 | HIGH: `cmd_record_checkpoint_step`'s `checkpoint-tag` branch (`flex_build.py`, ~line 2036) marks a phase complete in `docs/phases/index.md` by calling `resolve_current_phase(project_dir)` and using whatever phase file that returns — there is no explicit phase-key parameter. When two phases are simultaneously flagged `active` in the index (a real, observed state: `fold-prep` had both phase-97 and phase-98 active at once), the resolved phase can be the *wrong* one. Live-hit: Phase 98's `checkpoint-tag` step (2026-07-23) marked phase-97 — the still-in-progress fold, blocked on RELEASE-058 — complete as a side effect of tagging phase-98; caught and manually reverted in the same session (commit `c6c2c6a`). Fix direction: thread an explicit phase key through `record-checkpoint-step` (e.g. resolved once at the top of the build loop and passed on every call, not re-derived per-call from ambiguous "current phase" state), or make `resolve_current_phase` deterministic/erroring when more than one phase is active rather than silently picking one. `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`, `resolve_current_phase`. **Resolved by INFRA-265 (Phase 104):** both fix directions taken, not one or the other. (1) `record-checkpoint-step` gained an optional `--phase-key`; `_record_checkpoint_step` now resolves the closing phase by a fixed precedence — explicit `--phase-key` (validated against the index; a disagreeing stamp is an error, not a choice) → the `state.json["checkpoint_phase"]` stamp (INFRA-260) → `resolve_current_phase`'s candidate set only when unambiguous — with every validation and ambiguity check happening before any write, so an exit-2 path leaves `state.json` and `docs/phases/index.md` byte-identical; a non-terminal step degrades an ambiguous re-derivation to a warning rather than an error. `CLAUDE.build.md` and `skills/pairmode/templates/CLAUDE.build.md.j2` now pass `--phase-key <phase-key>` on every call, so the mandated loop path never depends on re-derivation. (2) `resolve_current_phase` and `next_action._resolve_active_phase` (composing one shared `_active_phase_candidates` helper, no second row walk) now raise `AmbiguousActivePhaseError` when more than one index row is flagged `active` with an existing phase file — the exact condition this finding observed — while multiple `planned` rows (the normal steady state of a multi-phase index, this repo's own 105–108 included) remain unaffected. `current-phase`, `checkpoint-report`, `next-action`, `resolver-state`, and `record-checkpoint-step` each catch the exception at the CLI boundary and exit 2 with the message on stderr, never a traceback. Regression tests: `tests/pairmode/test_record_checkpoint_step.py::TestPhaseKeyPrecedence` (the A5 five-`planned`-row fixture mirroring this repo's live index, both its exit-2 and `--phase-key 104` halves), `tests/pairmode/test_flex_build_current_phase.py` (double-`active` raise), `tests/pairmode/test_next_action.py::TestNextActionCLI::test_infra265_two_active_rows_exits_2_no_traceback`, `tests/pairmode/test_templates.py`. | Phase 98 checkpoint-tag (2026-07-23) | 2026-07-23 | 104 |
| CER-078 | LOW: `skills/pairmode/skills/security-auditor/procedure.md`'s "Documented thin-delegation exceptions" list and PIPE CONTRACT check text describe the pipe path as read from `.companion/state.json["pipe_path"]` — no hook has done this since INFRA-238 (Phase 98) removed the dead `pipe_path` state-key branches in favor of the single hardcoded `tempfile.gettempdir()/companion.pipe` convention. The parallel checklist in `skills/pairmode/skills/reviewer/procedure.md` was updated this same phase; `security-auditor/procedure.md` was not. Doesn't change today's audit verdicts (code is compliant with the *current*, correct behavior) but the next security-auditor cold-start will be checking against a stale description. Fix: sync `security-auditor/procedure.md`'s pipe-path description to match `reviewer/procedure.md`'s current text. `skills/pairmode/skills/security-auditor/procedure.md`. | Phase 98 checkpoint-security audit | 2026-07-23 | 98 |
| CER-079 | LOW: `docs/architecture.md` § Data flow still describes the companion pipe as project-scoped by an md5 hash of the project path. This has not been accurate since before Phase 98 (confirmed: `post_tool_use.py` already used the flat hardcoded `/tmp/companion.pipe`-style path pre-phase-98; INFRA-238 only removed dead code in the *other* hooks that still referenced the old state-key). Pre-existing drift, not introduced this phase. Fix: correct § Data flow to describe the actual non-project-scoped hardcoded pipe path. `docs/architecture.md` § Data flow. | Phase 98 checkpoint-security audit | 2026-07-23 | 98 |
| CER-080 | MEDIUM, cross-repo: `/mnt/work/flex` (the main checkout that `/mnt/work/flex-harness` is a linked git worktree of) carries a stale `current_story: INFRA-209` stamp in its own `.companion/state.json`, dated 2026-07-20, never cleared (INFRA-209 itself still shows `status: planned` there despite having commits). Because `scope_guard.py`'s `_resolve_main_project_root` (INFRA-238) correctly resolves file operations from a linked worktree back to the main checkout's state, this stale stamp silently blocks the `Edit`/`Write` tools for *any* legitimate doc/code edit made from `/mnt/work/flex-harness` (or any other worktree of the same repo) whenever the touched path falls outside `INFRA-209`'s declared scope — surfaced during Phase 98's `checkpoint-docs` step, worked around via direct file writes outside the hook path. This is a different project's state (`/mnt/work/flex`), not flex-harness's own — flagging rather than fixing directly. Fix direction (in `/mnt/work/flex`, not here): clear or correctly resolve the stale `current_story` stamp; consider whether `scope_guard`'s cross-worktree resolution should also account for a genuinely-idle main checkout (no active story) rather than trusting a possibly-stale stamp indefinitely. **RESOLVED Phase 105 — INFRA-271: the enforcement fix is a staleness cutoff (`scope_guard.STATE_STORY_MAX_AGE_HOURS = 24.0h`, via the public `entry_is_fresh()` predicate) applied to the `state.json` fallback only — a `current_stories`/`current_story` entry with a missing, unparseable, or too-old `set_at` resolves to a new `"stale"` source (fail-open for ordinary paths, naming the cutoff and the remedy; still fail-closed for `PROTECTED_GLOBS`), while a worktree claim (`worktree-cwd`/`worktree-path`) never ages out. The operator fix is `flex_build.py clear-stale-stories [--project-dir DIR] [--max-age-hours N] [--apply]`, which reports (default) or clears (`--apply`) exactly these stale stamps ahead of a fleet campaign, importing the staleness rule from `scope_guard` rather than re-deriving it. flex's own `/mnt/work/flex` `.companion/state.json` was already verified clean of the `INFRA-209` stamp at spec time (neither `current_story` nor `current_stories` present) — this story delivers and proves the mechanism against fixtures; there was no live stamp left in this repo to clear.** | Phase 98 checkpoint-docs step (2026-07-23) | 2026-07-23 | 105 |
| CER-081 | HIGH: `bootstrap.py`'s hook-registration functions (`_register_pretooluse_hook`, `_register_context_budget_hooks`) only migrate a stale hook block \"in place\" (basename-match fallback, INFRA-228) when at most one stale entry exists for that event — if a project already has BOTH a stale `/mnt/work/flex`-pointing block and a correct `/mnt/work/flex-harness`-pointing block (the exact state INFRA-228 was fixing going forward, but does not clean up retroactively), a subsequent `sync-all` run finds the harness block already registered via exact-command match and never reaches the basename fallback — the stale block is never removed. Live-hit: confirmed on all four migrated fleet projects surveyed (aab, asp, coherra, forqsite) — every one had two hook blocks per event (`PreToolUse`, `UserPromptSubmit`, `SessionStart`, `PostToolUse`), both firing on every tool call. Consequence: the stale `/mnt/work/flex` copy still carries every pre-migration bug (e.g. INFRA-246's reviewer-spawn context-budget-gate fix, INFRA-236's effort recording) since hooks are not deduplicated or overridden by Claude Code — both registered commands execute independently on every event, so a downstream project could still exhibit a bug that was already fixed on `flex-harness`, plus duplicate/racing writes to `.companion/state.json`/`effort.db` from two different code versions. Hand-fixed on all four surveyed projects this session (stale blocks removed, single correct block remains per event) — no code fix applied. Fix direction: extend the registration logic to detect and remove/replace ALL stale same-basename blocks for an event, not just migrate when the harness block is entirely absent; add a dedicated `pairmode_sync.py` or `bootstrap.py` subcommand to audit/clean a project's `settings.json` for duplicate hook registrations (useful both for already-migrated projects and as a periodic drift check); consider a pre-fold discovery check (DP8, `fleet_discovery.py`) that flags any bound project with >1 hook block for the same event. `skills/pairmode/scripts/bootstrap.py` `_register_pretooluse_hook`/`_register_context_budget_hooks`. **RESOLVED phase 105 (INFRA-269) — all three fix directions built.** (1) Registrar dedupe: `bootstrap.py` gained `_find_all_entries_by_command_basename` and `_prune_stale_hook_entries`, called from both `_register_pretooluse_hook` and `_register_context_budget_hooks` after the correct entry is resolved, removing every stale same-basename sibling entry (and any block left with empty `hooks`) while leaving the pre-existing in-place migration path (used only when no correct entry yet exists) unchanged. (2) `pairmode_sync.py audit-hooks` (dry-run by default, exits 1 on a finding / 0 clean; `--apply` cleans, reusing `_prune_stale_hook_entries`) audits and cleans an already-migrated project's `.claude/settings.json` and serves as a periodic drift check. (3) `fleet_discovery.py` gained `_check_duplicate_hooks` (read-only) and a `duplicate_hooks` field on every `discover()` result, surfaced in `--json` output, the human-readable summary, and the `## Duplicate hook registrations (CER-081)` snapshot section — reporting only; discovery's exit status is unchanged, `audit-hooks` is the enforcing instrument. | Phase 98 fold-readiness check (2026-07-23) | 2026-07-23 | 105 |
| CER-082 | [Renumbered from main-branch CER-068 at the RELEASE-059 fold merge — the ID collided with harness-branch CER-068.] Era-doc phase ledger drifts uncommitted and its status column is never updated. `phase_new.py` appends a `| <phase> | <title> | planned |` row to the active era doc's phase ledger (`docs/eras/*.md`) on every scaffold, but no commit path ever stages `docs/eras/`: the spec-mode commit list in `CLAUDE.build.md` step 5c stages only `docs/phases/` + `docs/stories/` + `docs/phases/permissions/`, and the pre-reviewer methodology commit (Step 1.5) stages only `docs/phases/` + `docs/cer/`. Result: ledger rows silently accumulate as working-tree drift until an operator notices an "unexplained" modified era doc (observed in forqsite 2026-07-22: three uncommitted rows spanning PM064-main, PM064-post1, PM065-main — two of them from prior sessions). Secondary gap: the ledger's status column is written once as `planned` and never updated — `flex_build.py mark-phase-complete` updates `docs/phases/index.md` only, so checkpointed phases remain `planned` in the era ledger indefinitely (both PM064 rows were complete-and-tagged while still reading `planned`). Fix direction: (a) stage the active era doc in the spec-mode commit (phase_new.py writes it, so the spec commit should carry it) and/or add `docs/eras/` to the Step 1.5 blanket stage; (b) have `mark-phase-complete` update the era ledger row status alongside `index.md`, or drop the status column from the ledger template if it is not meant to be load-bearing. MEDIUM severity (bookkeeping drift + a status column that misleads readers; no enforcement impact). `skills/pairmode/scripts/phase_new.py` (era append), `skills/pairmode/scripts/flex_build.py` (mark-phase-complete), `CLAUDE.build.md` (spec-mode step 5c, build-loop Step 1.5). **RESOLVED phase 104 (INFRA-267) — three fixes.** (1) Both `CLAUDE.build.md` and `skills/pairmode/templates/CLAUDE.build.md.j2` now state that commits of scaffolded planning docs stage `docs/eras/` alongside `docs/phases/`/`docs/stories/`, and that the checkpoint-tag step commits `docs/phases/index.md` + `docs/eras/` before `git tag` — note that the anchors this finding cites (`CLAUDE.build.md` step 5c and build-loop Step 1.5) no longer exist: HARNESS006-main flipped both files to the ~20-line dispatch loop, which retains no staging-path list at all, so the clause was added to the build-loop preamble and the checkpoint order instead. (2) `flex_build.py::_mark_phase_complete_in_era_ledger` flips the active era doc's ledger row to `complete` from both `mark-phase-complete` and `record-checkpoint-step checkpoint-tag`, re-using the same resolved phase key (fix direction (b); direction (b)'s alternative — dropping the status column — was rejected because `check-index` check 2c already enforces it). (3) Half 3, found while speccing: on flex the appender had never fired at all — `phase_new._update_era_phases_table` matched its section with `stripped == "## Phases"`, and era 003's heading was `## Phases (proposed — …)`, so it returned silently having written nothing for 44 phases; the match is now `== "## Phases"` or `startswith("## Phases ")`, era 003's narrative section was renamed to `## Planned phase design`, and a real `| Phase | Title | Status |` ledger for phases 96–108 was backfilled (zero era-003 `check-index` violations after). | forqsite session (operator noticed unexplained modified era doc) | 2026-07-22 | 104 |

| CER-070 | Per-story git worktrees (INFRA-224) cannot reliably pass `tests/pairmode/test_observability_ui.py::test_ui_build_emits_dist_index_html`. The root `.gitignore`'s Python-oriented `build/` and `dist/` patterns (lines 6-7) also match literally-named `build`/`dist` output directories nested inside the intentionally-vendored `skills/observability/ui/node_modules` tree — so those directories were never committed to git at all, not merely stale. Confirmed during RELEASE-042's build: a fresh worktree cut from a clean commit was missing `node_modules/.pnpm/.../@tanstack+react-query@.../node_modules/@tanstack/react-query/build`, `.../vite@.../node_modules/vite/dist`, `.../tailwindcss@.../node_modules/tailwindcss/dist`, and `.../\@vitejs+plugin-react@.../node_modules/\@vitejs/plugin-react/dist` — present on the main worktree's filesystem (locally pnpm-installed once, never removed) but absent from every git-tracked snapshot. Copying the four missing directories in by hand cleared the "Cannot find module" errors but surfaced a second, unresolved layer of TS7006 implicit-`any` errors in worktree-only `tsc -b` runs that do not reproduce on the main worktree even after a from-scratch rebuild there — root cause not yet found; likely another gitignored `build`/`dist` path providing type declarations consumed transitively. Fix direction: scope the root `.gitignore`'s `build/`/`dist/` rules to the actual Python source tree (e.g. `/build/`, `/dist/` anchored at repo root, or an explicit `!skills/observability/**/node_modules/**` negation) so vendored npm package output is genuinely fully committed; then audit for any other silently-stripped vendored paths before trusting per-story worktrees' UI-build gate again. Until fixed, treat `test_ui_build_emits_dist_index_html` FAILs inside a per-story worktree as a suspect environmental false-negative — verify by running the same test against the main worktree before accepting a story's content as broken. HIGH severity (the build gate that INFRA-224's whole worktree-isolation design depends on cannot pass inside the isolation it created, for any story whose diff happens to touch the observability UI's dependency surface — and silently misleads reviewers into FAILing correct doc-only diffs). `.gitignore:6-7`, `skills/observability/ui/node_modules/` (vendoring completeness). **Addendum (INFRA-225 build, 2026-07-22):** every worktree pytest run that touches the UI-build gate also dirties the tracked `skills/observability/ui/tsconfig.tsbuildinfo` (the failed `tsc -b` still writes its incremental cache before erroring), which then blocks `merge-story-worktree`'s rebase with "You have unstaged changes" until manually `git checkout`'d — a second-order symptom of the same root cause, adding a recurring manual step to every affected story's merge. | RELEASE-042 build/review (2026-07-21/22) | 2026-07-22 | 97 |
| CER-071 | `flex_build.py record-attempt`'s CLI-forwarding wrapper rejects every option `record_attempt.py` itself accepts — e.g. `flex_build.py record-attempt --story-id RELEASE-042 --agent-role builder ...` fails with `Error: No such option: --story-id`, even though `record_attempt.py --help` lists `--story-id` directly. The direct script (`uv run skills/pairmode/scripts/record_attempt.py ...`) works fine standalone. Root cause not yet diagnosed — likely the Click forwarding/alias definition in `flex_build.py`'s `record-attempt` command declares its own narrower option set instead of passing `*args` through, unlike other alias commands in the same file (e.g. `merge-story-worktree`, `discard-story-worktree`, which work as documented). CLAUDE.build.md's own build-loop pseudocode calls `flex_build.py record-attempt ...` directly, so any orchestrator following it literally hits this same failure. Fix direction: compare the `record-attempt` Click command definition in `flex_build.py` against a working alias (e.g. `create-story-worktree`) and correct the option forwarding/passthrough. MEDIUM severity (effort tracking happens to be disabled in this project, which is the only reason this hasn't blocked a build loop yet — a project with effort tracking enabled would hit a hard CLI error on every recorded attempt). `skills/pairmode/scripts/flex_build.py` (record-attempt command). **RESOLVED Phase 104 — INFRA-263: the root cause is now diagnosed — the alias's Click declaration was empty (only the auto-added `--help`), not merely narrower than `record_attempt.py`'s option set. Fixed by declaring `cmd_record_attempt` (`skills/pairmode/scripts/flex_build.py`) with `context_settings={"ignore_unknown_options": True}`, `add_help_option=False`, and a variadic `@click.argument("args", nargs=-1, type=click.UNPROCESSED)`, forwarding the collected tuple to `record_attempt.py`. New test file `tests/pairmode/test_flex_build_record_attempt_alias.py` round-trips the full documented flag set. This is a duplicate filing of CER-073; both close together in this story.** | RELEASE-042 build (2026-07-22) | 2026-07-22 | 104 |

| CER-066 | `state.json["checkpoint_step"]` (RESOLVER-008/RESOLVER-012) is never cleared between phases. `resolve_next_action`'s Row 9 branch computes `_remaining = [s for s in _CHECKPOINT_SEQUENCE if s not in _checkpoint_step]` against the same fixed 4-step sequence for every phase — there is no per-phase key or reset on `checkpoint-tag` completion. Confirmed in production: HARNESS013-main's checkpoint left all four step names (`checkpoint-security`, `checkpoint-intent`, `checkpoint-docs`, `checkpoint-tag`) in `state.json`; when HARNESS014-main reached its checkpoint row, `flex_build.py next-action` returned `{"action": "done"}` immediately — `checkpoint-security`/`checkpoint-intent`/`checkpoint-docs` were never dispatched and no `cp-HARNESS014-main` tag was created. HARNESS014-main's checkpoint was completed manually against the reviewer procedure checklist to unblock. Fix direction: reset `checkpoint_step` to `[]` when `checkpoint-tag` is recorded (natural end-of-sequence trigger), or key `checkpoint_step` by the active phase so a stale list from a prior phase can't satisfy `_remaining`. HIGH severity (silently skips the entire checkpoint sequence — security audit, intent review, docs review, and tagging — for every phase after the first one a given `state.json` has seen). `skills/pairmode/scripts/next_action.py` `resolve_next_action` Row 9, `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`. **RESOLVED HARNESS015-main (RESOLVER-017) — `cmd_record_checkpoint_step` now resets `state["checkpoint_step"]` to `[]` when `checkpoint-tag` is recorded.** | HARNESS014-main checkpoint (cold-eyes review) | 2026-07-13 | HARNESS014-main |

| CER-069 | CER-066's naive-`split("|")`-on-a-Stories-table-row bug (fixed once in `story_update.py`, Phase 94/INFRA-207) recurred independently in `next_action.py`'s `_check_phase_completion` (fixed in INFRA-222, Phase 95) — the same code shape (`stripped.split("|")` then a positional index into `cols`) also appears, unaudited, in: `next_story.py:92`, `index_integrity.py:124`, `flex_build.py:415`, `flex_build.py:634`, `flex_build.py:1248`, `story_resolver.py:166`. Whether each site is actually vulnerable (reads a fixed/positional index from `cols`, vs. only using `cols` to detect a header row or count columns — the latter pattern, e.g. `next_action.py`'s own `_check_cer_do_now`, is safe) has not been verified per-site. Fix direction: audit each site; for any that index positionally into `cols`, apply the same `re.split(r'(?<!\\)\|', stripped)` fix (matching `story_update.py`'s and INFRA-222's pattern); consider factoring the split into one shared helper (e.g. in a new `table_utils.py`) once ≥2 more sites are confirmed vulnerable, so this bug class stops recurring site-by-site. MEDIUM severity (recurring bug class, but each individual site is unconfirmed — not a live-hit until audited). `skills/pairmode/scripts/next_story.py:92`, `index_integrity.py:124`, `flex_build.py:415,634,1248`, `story_resolver.py:166`. | INFRA-222 spec discussion (2026-07-21 build session) | 2026-07-21 | — |

| CER-063 | The `fold-prep` branch (`HARNESS011-main`, not yet merged to `main`) already owns a different `INFRA-203` ("Context gate edge cases and session_id safety") — itself the product of a prior renumbering to avoid colliding with `main`'s then-existing `INFRA-192`. Phase 91 (`main`) has now allocated a second, unrelated `INFRA-203` ("Make empty/missing-variable template renders in the sync-agents body-merge path fail loudly"). When `fold-prep`/`harness` eventually merges into `main`, this rail+number will collide again — the second time this exact pattern has occurred. Not urgent while `fold-prep` remains unmerged. Fix at merge time: renumber the incoming `fold-prep` `INFRA-203` (recommended, since it was already renumbered once and its content is less coupled to the flex-self-hosting-tool identity than the `main` story). MEDIUM severity (forward-looking; no current-branch defect). No specific files — applies to whichever phase performs the `fold-prep`/`harness` merge into `main`. | Phase 91 intent review | 2026-07-16 | HARNESS (fold-prep merge) |

| CER-062 | `story_new.py`'s `_append_to_phase` phase-manifest lookup (`skills/pairmode/scripts/story_new.py:127-137`) only matches two filename shapes: `{phase}-*.md` (phase arg as filename prefix) and exact `phase-{phase}.md`. Suffixed phase manifests — filenames of the form `phase-<phase_id>-<suffix>.md`, the naming convention `phase_new.py --phase-id --suffix` produces (CER-038) — match neither glob, so `story_new.py --phase MU020` silently returns `False` from `_append_to_phase` and the new story is never added to the phase's Stories table, with no error surfaced to the caller. Confirmed on the radar project (fable-orchestrated build): the operator had to add the Stories table rows by hand. Fix: add a third glob `phase-{phase}-*.md` (or generalize to `*{phase}*.md`) alongside the existing two in `_append_to_phase`, and consider surfacing a warning when auto-registration falls through to `False` rather than failing silently. MEDIUM severity (silent auto-registration failure on suffixed phases — story files are still created correctly; only the phase manifest's Stories table drifts until manually reconciled). `skills/pairmode/scripts/story_new.py:127-137`. **Pulled forward into Phase 87 as INFRA-197.** | radar repo orchestrator (fable, external report) | 2026-07-14 | 87 |

| CER-061 | `cmd_record_checkpoint_step` in `flex_build.py` (RESOLVER-012) does not call `_depth_guard` before writing `state.json`, unlike every other write-capable `--project-dir` command. The write target is always the fixed relative path `.companion/state.json` and `step_id` is whitelist-validated — no arbitrary path construction — so there is no traversal risk. But the missing guard is an inconsistency with the project convention. Fix: add `_depth_guard(project_dir)` at the entry of `cmd_record_checkpoint_step`. MEDIUM. `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-015) — `_depth_guard(project_dir)` added at entry of `cmd_record_checkpoint_step`, consistent with all other write-capable commands.** | HARNESS009-main security audit | 2026-07-04 | HARNESS009-main |

| CER-068 | [Renumbered from harness-branch CER-062 during the HARNESS016-main fold merge (RELEASE-014) — the ID collided with a new main-branch CER-062.] `_CHECKPOINT_SEQUENCE` is defined twice: once in `next_action.py` (the authoritative location) and once inlined as literal strings in `flex_build.py`'s `record-checkpoint-step` command (RESOLVER-012). Current values match, but a future change to `next_action.py`'s tuple must also be applied manually to `flex_build.py`. Fix: import `_CHECKPOINT_SEQUENCE` from `next_action` in `flex_build.py` (or extract to a shared constants module). MEDIUM (silent divergence risk if sequence ever changes). `skills/pairmode/scripts/flex_build.py` record-checkpoint-step, `skills/pairmode/scripts/next_action.py`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-015) — `_CHECKPOINT_SEQUENCE` imported from `next_action` in `flex_build.py`; inlined tuple removed. Single authoritative definition.** | HARNESS009-main intent review | 2026-07-04 | HARNESS009-main |

| CER-063 | `parse_worker_verdict_text` was marked deprecated in RESOLVER-013 but not removed. It has no live call sites in production code; tests in `test_next_action.py` still exercise it. Preferred action per story spec was removal. Remove the function and update the test file to remove or replace the tests that exercise it. LOW-MEDIUM. `skills/pairmode/scripts/next_action.py:1077`, `tests/pairmode/test_next_action.py`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-016) — `parse_worker_verdict_text` removed from `next_action.py`; `TestParseWorkerVerdictText` class removed from `test_next_action.py`; any uncovered cases ported to `test_parse_worker_verdict_json.py`.** | HARNESS009-main intent review | 2026-07-04 | HARNESS009-main |

| CER-064 | [Renumbered from main-branch CER-061 at the RELEASE-008 fold merge — the ID collided with harness's CER-061.] `spec_preflight.py` and `flex_build.py` `spec-preflight` subcommand construct `story_path` by splitting `--story-id` on `-` and building `docs/stories/<rail>/<story_id>.md` without a `Path.resolve().relative_to(project_dir)` containment check. The path is read-only (`read_text` / `.exists()`) over trusted orchestrator input — not exploitable in practice — but the sibling guard pattern from `permission_scope.py` is not applied here. Fix: add `path.resolve().relative_to(project_path)` guard before any `story_path` I/O in both `spec_preflight.py:_story_path_for` and `flex_build.py cmd_spec_preflight`. LOW severity (defense-in-depth; read-only, trusted input). `skills/pairmode/scripts/spec_preflight.py:128-129`, `skills/pairmode/scripts/flex_build.py:65-71,138-139`. | Phase 84 checkpoint security audit | 2026-07-06 | 84 |

| CER-065 | [Renumbered from main-branch CER-060 at the RELEASE-008 fold merge — the ID collided with harness's CER-060. Fold note: the fold deleted `reviewer.md.j2` and `.claude/agents/reviewer.md`; the residual `git clean -fd` surface is now `skills/pairmode/skills/reviewer/procedure.md` (FAIL-revert block).] BUILD-038 dropped `git clean -fd` from the FAIL-revert block in `.claude/agents/reviewer.md`, but the canonical template `skills/pairmode/templates/agents/reviewer.md.j2:237` still carries `git clean -fd`. The story spec deliberately scoped the template out (template propagation gets its own spec). **Re-introduction vector is bootstrap, not sync-agents:** `bootstrap.py` (`:63,:1014`) renders the full `reviewer.md.j2` into every *fresh* project, so any newly-bootstrapped project gets `git clean -fd` back. `sync-agents` does **not** re-add it — `_merge_body_sections` (`pairmode_sync.py:216`) is additive and leaves existing target sections (the `## Decision` H2 that holds this line) untouched. **Fix must also update the guardrail test:** `tests/pairmode/test_templates.py:677-679` (`test_revert_on_fail`) currently asserts `"git clean -fd" in self.output` — dropping the line from the template makes this assertion fail. The follow-up `doc`-class story must: (a) drop `git clean -fd` from `reviewer.md.j2` FAIL-revert + update surrounding prose to mirror BUILD-038; (b) replace the line-679 positive assertion with a negative assertion (`git clean -fd` absent) so the build lands green. primary_files: `skills/pairmode/templates/agents/reviewer.md.j2`, `tests/pairmode/test_templates.py`. MEDIUM (fresh bootstraps re-introduce the deleted blast radius; the live reviewer.md fix is not durable across bootstraps until propagated). Carried forward into HARNESS002-main as CF-1 (harness branch). `skills/pairmode/templates/agents/reviewer.md.j2:237`, `tests/pairmode/test_templates.py:677-679`. | Phase 79 build summary + intent + cold-eyes review | 2026-06-29 | 79 |

| CER-059 | Intent review of HARNESS001-ante1 surfaced three forward (HARNESS006-era) items from `fleet_discovery.py` (RELEASE-005): **(a)** the current `docs/fleet-snapshot.md` shows all 9 discovered projects bound via Signal 2 (`pairmode_version`) only — **zero Signal 1 (`pairmode_scripts_dir`) hits** — even though the fleet is expected to be scripts-bound to this checkout. This may be a false-negative in Signal-1 detection (`_check_signal1` `relative_to` comparison vs `_THIS_SCRIPTS_DIR`/`_FLEX_ROOT`) or it may be accurate (projects baked an absolute path that no longer resolves under this checkout). Must be diagnosed **before the pre-fold discovery gate runs**, because that gate relies on accurate Signal-1 binding to know which projects break at the fold; a false negative would under-report blast radius. **(b)** Add an explicit Signal-1 verification step to the pre-fold gate run in `docs/harness-cutover-runbook.md` ("after syncing each project, re-run discovery and confirm `binding: scripts` appears"). **(c)** When HARNESS006 is specced, its story for executing the fold must carry an explicit AC: reconcile `docs/stories/RELEASE/RELEASE-002.md` status `deferred → complete` on main after the fold merge (the runbook notes this in prose; it should be a checkable AC). MEDIUM (cutover-safety accuracy; no impact on HARNESS001-ante1 correctness — the tool is built and tested). `skills/pairmode/scripts/fleet_discovery.py` `_check_signal1`, `docs/fleet-snapshot.md`, `docs/harness-cutover-runbook.md`. **RESOLVED Phase 105 — INFRA-270: (a) diagnosed and instrumented — the zero-hit result is correct for a 0.2.x fleet (the key-value `pairmode_scripts_dir` form is only written by `sync-all --apply` at 0.3.0 migration); Signal-1 absence is now classified via `signal1_absence_reason` into four reason codes (`no-build-md`, `no-declaration`, `inline-only`, `foreign-checkout`), surfaced in the CLI, `--json`, and the snapshot, and `discover()` now reports inline- and foreign-bound projects that previously fired neither signal and were invisible to the pre-fold gate. (b) and (c) verified as already satisfied, not rebuilt: `docs/harness-cutover-runbook.md` carries the CER-059b Signal-1 verification step, and `docs/stories/RELEASE/RELEASE-002.md` is `status: complete`.** | HARNESS001-ante1 intent review | 2026-06-26 | 105 |

| CER-058 | A brand-new project (`meander`, bootstrapped 2026-06-26, still in inception) appeared in flex's `.companion/state.json` `registered_projects` without the operator ever running the observability/registration flow (`pairmode_sync.py register`). Suspected cause: the project's bootstrap path (or a coalescing agent session) wrote `registered_projects` directly rather than going through the registration command/hook. Needs investigation: enumerate every writer of `registered_projects` and confirm none bypasses the intended registration entry point; if a bootstrap path writes it directly, either route it through `register` or document the provenance. Relevant to DP8 (fleet discovery) — the discovered fleet must reflect deliberate registrations, not bootstrap artifacts. Severity MEDIUM (no incident, but unexplained fleet-state writes undermine cutover blast-radius accuracy). Investigate `bootstrap.py`, `pairmode_sync.py register`, and any agent-side registration. **RESOLVED Phase 105 — INFRA-270: audit found no in-repo bypass path — `pairmode_register.py`'s `register`/`unregister` are the only in-repo assignments to `registered_projects` (`bootstrap.py` only prints the register command as operator guidance; it never writes the key). The `meander` entry's provenance is genuinely unrecoverable — it arrived out of band (a manual or agent-session edit of `state.json`), not through any traceable in-repo writer, and now audits as `source: unknown` via the new `audit-projects` command. The single-writer invariant is now test-enforced (`test_registered_projects_has_a_single_writer` against `REGISTERED_PROJECTS_WRITERS`) rather than resting on a comment, and a provenance sidecar (`registered_projects_provenance`) records `source`/`registered_at` for every registration going forward. The invariant covers in-repo code paths only — it does not and cannot prevent an out-of-band edit of `state.json` itself.** | RELEASE-003 build / operator observation | 2026-06-26 | 105 |

| CER-048 | The static deny list (`.claude/settings.json` `Edit/Write(hooks/**)` etc.) and the story-scoped permissions system (Phase 55 `scope_guard.py` + `permissions-create`) do not talk to each other. `scope_guard` fails open in every non-build context (no active story → allow; no permissions file → allow; malformed → allow), so the static denies are the only always-on protection for `hooks/`, `.claude-plugin/`, `skills/seed/`, `skills/companion/`, `lessons/`. Consequence: a story that legitimately declares a protected file in `primary_files` (with stated reason, per CLAUDE.md item 7) is still hard-blocked at the permission layer — observed Phase 68 INFRA-175, where the builder was denied `hooks/session_start.py` despite an authorized spec, requiring a manual operator lift-and-restore of the deny entries. Fix direction: teach `scope_guard` a fail-closed protected-path list (the same globs as the static denies) that is satisfiable only by an active story whose permissions file explicitly names the protected path; once shipped and verified, retire the static `Edit/Write` denies in favor of the story-aware gate. Must preserve: always-on protection outside build mode, and protection against the orchestrator itself. MEDIUM severity (methodology friction + duplicate protection surfaces that can drift). `skills/pairmode/scripts/scope_guard.py`, `.claude/settings.json`, `hooks/pre_tool_use.py` (read-only branch), CLAUDE.md item 7. **RESOLVED Phase 100 (cp100) — the fix direction largely shipped in v0.3 (`PROTECTED_GLOBS` story-aware fail-closed gate, satisfiable only via an active story whose permissions artifact names the path; `hooks/**` already retired from the static deny list). A post-cp99 audit (2026-07-24) found three residues, all closed by INFRA-253: (1) a fail-open hole where `scope_guard.check_path()`'s active-story branch returned allow when the permissions artifact was missing/empty/malformed without checking `_is_protected()` — only the no-story branch checked it, leaving `hooks/**` etc. wide open for the duration of a build whose `permissions-create` never ran; (2) the remaining four `PROTECTED_GLOBS`-duplicate static denies in flex's own `.claude/settings.json` (`skills/companion/**`, `lessons/**`, `.claude-plugin/**`, `skills/seed/**`) retired, leaving only tooling (PostToolUse pytest hook, Bash allow rules, `Edit(.claude/agents/**)` allow) plus the deliberately-outside-the-gate `Edit(docs/phases/permissions/**)` deny (anti-self-scope-modification); (3) this stale row. Explicit remainder, not curable by scope_guard: writes to `.claude/settings.json` itself are blocked by the Claude Code auto-mode classifier — harness-level, above project hooks — so operator-applied edits remain the sanctioned path for changes to the settings file. Two live hits in phase 99: INFRA-247 (classifier block, operator-applied) and INFRA-249 (worktree-isolation, not classifier, but same operator-fallback pattern — a worktree's `.claude/settings.json` is a separate filesystem copy from the main checkout's, so an in-worktree edit cannot land in the main checkout regardless of classifier state).** | Phase 68 build observation; residues found by post-cp99 audit 2026-07-24 | 2026-06-12 | 68, 100 |

| CER-040 | `context_budget.decide()` fails open silently when `state.json` is absent or contains malformed JSON — same failure shape as the old transcript-path bug, just one layer out. `_read_state()` returns `None` on any I/O or parse error; `decide()` returns `None` (pass-through) immediately. A pairmode project with a corrupted or missing state.json gets an invisible dead gate with no operator signal. Fix options: (a) return `_CONTEXT_CHECK_REQUIRED_MSG` block when `_read_state()` returns `None` (strictest — treats missing state as uninitialized); (b) add a `state_missing` sentinel path that emits a softer warning but still passes through (preserves non-pairmode compat). Non-pairmode projects intentionally have no state.json — fix must not break that case. MEDIUM severity. `skills/pairmode/scripts/context_budget.py` `_read_state()` and `decide()`. **RESOLVED Phase 105 — INFRA-272 took option (b) (soft, observable pass-through) for the four fail-open branches option (a) did not already cover: an unanchored/unparseable staleness comparison in `_is_stale()`, a failed `session_state` import, and the hook's own blanket `except` around `decide()` now each emit one `context_budget: gate not enforced — ` line to stderr before continuing, on top of the missing-state.json and malformed-state.json blocks INFRA-150/INFRA-182 already implemented for option (a)'s two cases. Non-pairmode projects (no `.companion/` directory) remain silent by design — that case is unchanged.** | Phase 58 back-check (opus) | 2026-06-03 | 58 |

| CER-041 | `context_current_tokens` has no session invalidation. After `/clear`, `state.json` retains the previous session's token value until the orchestrator overwrites it at the first Context gate. If anything spawns a Task tool before the Context gate runs (e.g. an early orchestrator Bash call that happens to be a Task), the hook reads a stale — potentially too-high — value and may block incorrectly. In the happy path this is benign (Context gate runs before any builder spawn per CLAUDE.build.md). Fix: add `context_current_tokens_recorded_at` (ISO timestamp) alongside the value; `read_context_tokens_from_state` treats a value older than N minutes as absent. Alternatively: reset the key in `story_context.py --clear` or a `SessionStart` hook. LOW-MEDIUM severity (mitigated by protocol; only dangerous if Context gate is skipped or reordered). `skills/pairmode/scripts/context_budget.py` `read_context_tokens_from_state`, `skills/pairmode/scripts/flex_build.py` `set-context-tokens`. **RESOLVED Phase 105 — INFRA-272.** The 60-minute TTL that shipped here (Phase 59, INFRA-151) was superseded by the `context_session_reset_at` comparison (CER-047, Phase 68/74, INFRA-175/182) — `decide()` has read `context_current_tokens` directly and bypassed the TTL ever since. This story did not build a new invalidation mechanism (none was needed); it removed the now-unreachable TTL from `read_context_tokens_from_state`, deleted the dead `_CONTEXT_TOKEN_STALE_MINUTES` constant, removed `context_current_tokens_ttl_minutes`'s last reader (`skills/observability/api/src/routes/context.ts`'s threshold list), and corrected the three prose claims (`story_context.py`, `session_state.py`, `docs/architecture.md`) that still described the retired TTL as live. | Phase 58 back-check (opus) | 2026-06-03 | 58 |

| CER-001 | reconstruct.py `parse_ideology()` compat wrapper uses NamedTemporaryFile round-trip; on SIGKILL leaves ideology.md copy in /tmp. Fix: add `parse_ideology_text(text: str)` to ideology_parser.py to eliminate temp file. reconstruct.py:33-50 | Security audit cp12 | 2026-04-24 | 12 |

| CER-002 | bootstrap.py has no `--yes`/`--no-input` flag; non-interactive callers must use `input="y\n" * N` workaround. Limits CI/scripted use. Add `--yes` flag that auto-confirms all prompts. | Intent review cp13 | 2026-04-25 | 13 |

| CER-003 | cer.py and phase_new.py call Path(project_dir).resolve() but do not apply the len(parts) < 3 suspicious-path depth check that all other pairmode scripts apply. Inconsistent guard discipline. | Security audit cp14 | 2026-04-25 | 14 |

| CER-004 | lesson_review.py uses str.startswith() for path containment instead of Path.relative_to(); vulnerable to prefix collision on unusual paths. lesson_review.py:149 | Security audit cp14 | 2026-04-25 | 14 |

| CER-005 | phase_new.py missing len(parts) < 3 depth guard; resolve() is called but shallow-path check absent. All other pairmode entry points apply this guard consistently (bootstrap, audit, sync, score, reconstruct, story_new, era_new). phase_new.py:286 | Security audit cp15 | 2026-04-27 | 15 |

| CER-006 | validate_story_file rejects empty primary_files list, but story_new.py writes primary_files: [] by default. Every new story file fails its own validator until primary_files is filled in. Either allow empty list for draft stories, or omit the field entirely when empty. schema_validator.py, story_new.py | Intent review cp15 | 2026-04-27 | 15 |

| CER-007 | era_new.py writes unquoted id field (id: 001 — YAML integer) while bootstrap.py writes quoted id (id: "001" — YAML string). Current regex-based parsers tolerate both, but a proper YAML parser (e.g., python-frontmatter, PyYAML) would return integer 1 from era_new output. Fix era_new.py to write id: "{era_id}" (quoted string). era_new.py | Intent review cp15 | 2026-04-27 | 15 |

| CER-008 | permission_scope.py: if settings.local.json contains valid non-object JSON (e.g., a bare array), _read_json returns it as-is and the subsequent .setdefault() call raises AttributeError. Add a guard: if the parsed value is not a dict, treat it as {}. permission_scope.py:75 | Security audit cp16 | 2026-04-28 | 16 |

| CER-009 | hooks/stop.py, post_tool_use.py, session_end.py: PIPE_PATH defaults to hardcoded "/tmp/companion.pipe" and is then conditionally overridden by reading pipe_path from .companion/state.json (relative path). A crafted state.json could redirect pipe writes to an arbitrary path. No secrets in payloads; write silently drops on ENXIO if no FIFO reader. LOW severity. exit_plan_mode.py correctly uses tempfile.gettempdir(). | Security audit cp17 | 2026-04-30 | 17 |

| CER-010 | story_new.py --rail input is .upper()'d but not validated against a regex before being used in path construction. A caller passing --rail "../../../etc" constructs a path that escapes project_dir. No containment check present. MEDIUM severity. story_new.py:183-185 | Security audit cp18 | 2026-04-30 | 18 |

| CER-011 | era_new.py _slugify() neutralizes "/" and "." in --name before path construction, providing effective (but informal) traversal prevention. No formal resolve().relative_to() containment check present. LOW severity — _slugify() is correct but not a formal guard. era_new.py:25-31, 114-116 | Security audit cp18 | 2026-04-30 | 18 |

| CER-012 | pairmode_status.py: FLEX_ROOT computed as `Path(__file__).resolve().parent.parent.parent` resolves to `<repo>/skills/`, not the flex repo root. The constructed `start_sidebar.sh` path becomes `<repo>/skills/skills/companion/scripts/start_sidebar.sh` — a file that does not exist. Must be `parent.parent.parent.parent` (four levels up). HIGH severity — surfaced to user as broken instructions. pairmode_status.py:33 | Story review cp20 INFRA-019 | 2026-05-01 | 20 |

| CER-013 | INFRA-033 propagation gap: fallback-policy pointer was added to flex's own CLAUDE.build.md but NOT to the canonical `skills/pairmode/templates/CLAUDE.build.md.j2` template. Future bootstraps will not inherit the orchestrator-level pointer to the fallback policy (the inline `# fallback:` template comments and architecture.md subsection still propagate via separate paths). LOW severity — flex's own dogfood is correct; downstream effect contained. **RESOLVED cp-HARNESS001-main / HARNESS006-main — CLAUDE.build.md.j2 was rewritten from scratch as the thin dispatch loop; the fallback-policy pointer is not relevant to the thin-loop template and was intentionally omitted.** | Phase 21 intent review | 2026-05-04 | 21 |

| CER-014 | docs/architecture.md "Reviewer-class agent tool restriction (build-loop safety)" subsection asserts the existence of a "pre-reviewer commit discipline (committing story files and running `git checkout -- lessons/` before the reviewer fires)" as one of two layers protecting the working tree, but neither CLAUDE.build.md (project) nor CLAUDE.build.md.j2 (template) encodes this discipline. Either add the discipline to CLAUDE.build.md.j2 (preserving the architecture claim) or trim the claim (preserving truth). MEDIUM severity — defense-in-depth claim rests on aspirational orchestrator behaviour; the load-bearing tool-restriction layer remains in force regardless. | Phase 21 security audit + intent review | 2026-05-04 | 21 |

| CER-015 | INFRA-030's CLAUDE.build.md examples hardcode `--phase N --rail RAIL` and `--attempt-number 1` placeholder literals; the orchestrator has no plumbed source for phase/rail at record time and no per-story retry counter state. The cp22 cleanup commit clarified that phase and rail are read from the current story file's frontmatter, but a small helper (e.g. `record_attempt.py --story-file <path>` that auto-extracts phase/rail from frontmatter) would close the typo surface. Without it, retry attempts may be miscounted as fresh attempts and rollup reports may see NULL phase/rail rows. MEDIUM severity. CLAUDE.build.md:82-94. **RESOLVED cp-HARNESS006-main — The thin dispatch loop template has no record_attempt.py example; the hardcoded placeholder literals are absent from the rewritten template.** | Phase 22 intent review | 2026-05-05 | 22 |

| CER-016 | effort_db.py `resolve_effort_db_path` accepts an absolute or relative `effort_db_path` from .companion/state.json and applies only `_depth_guard` (rejects paths with fewer than 3 parts after resolution) — it does not assert containment under project_dir. Same shape mirrored in record_attempt.py and pairmode_effort.py. Strictly weaker guard than permission_scope.py's resolve().relative_to() containment check. State.json is treated as project-owned (consistent with existing trust boundary), so this is informational, not exploitable. LOW severity. effort_db.py:115-139. **RESOLVED Phase 104 — INFRA-266. The `resolve_effort_db_path` half (state.json's `effort_db_path`) was already closed by INFRA-058 (commit `ee584df`), which added the `resolve().relative_to(project_dir.resolve())` containment check — that part of this finding was stale. This story closed the remaining mirrors: `record_attempt.py` and `pairmode_effort.py` both bypassed `resolve_effort_db_path` for an explicit `--db-path` and did their own unguarded resolution. Both now delegate to a new single-sourced `effort_db.resolve_db_path_arg`, which applies `_depth_guard` and containment and raises on an escaping explicit `--db-path` (deliberately stricter than the silent state.json fallback, since an operator-named path escaping must not write somewhere else unnoticed).** | Phase 22 security audit | 2026-05-05 | 104 |

| CER-017 | bootstrap.py `_record_state` auto-enables `effort_tracking: true` on every pairmode-bootstrapped project unless the user explicitly sets the key first. No data leaves the host (sqlite is local), but the behaviour is documented in architecture.md only — not surfaced to the user during bootstrap. Consider an interactive prompt or a one-line bootstrap-summary note. LOW severity (transparency, not security). bootstrap.py:267-268. | Phase 22 security audit | 2026-05-05 | 22 |

| CER-018 | `lesson.py` CLI (`capture_lesson()` and its argparse entry point) does not accept the `value_framing` or `validation_phase` fields introduced by L012 (Phase 24). These fields exist in `lessons/lessons.json` and are documented in `architecture.md`, but can only be written by direct JSON append — not through the canonical CLI writer. Schema drift: data model ahead of the writer. LOW severity (usable via direct edit; append-only policy and lessons integrity check are unaffected). lesson.py: CLI definition. | Phase 24 intent review | 2026-05-07 | 24 |

| CER-019 | `pairmode_sync.py` `_get_project_name` returns `state["project_name"]` with only `.strip()` applied before rendering into agent-file frontmatter. A `project_name` containing embedded newlines or YAML control characters could inject extra frontmatter keys (e.g. additional `tools:` or `model:` lines) when `sync-agents` writes agent files. A malicious or malformed `.companion/state.json` could silently elevate reviewer-class tool permissions or override model assignments. Realistic threat is low (state.json is local and user-owned, same trust boundary as bootstrap.py:582), but this is the first script to write project_name into security-sensitive frontmatter fields. Fix: strip `\n`/`\r` from project_name or YAML-quote the value before template substitution. LOW severity. pairmode_sync.py:55-63. | Phase 25 security audit | 2026-05-07 | 25 |

| CER-031 | NP-6 ("Source of Truth over Recall") pattern doc (`docs/patterns/agentic-architecture/source-of-truth-over-recall.md`) was drafted in Phase 48 and is catalog-ready, but was held from PR #4 submission because open PR #3 in cloudnirvana/open-patterns ("Canonical Source Over Recall," nsmedia-io, 2026-05-29) covers the same principle in a RAG/vector-retrieval context. Our NP-6 targets agentic development workflows (LLM recall vs. canonical files) and is a genuinely distinct scope, but submitting both while PR #3 is open risks editorial conflict for the catalog maintainer. Trigger: submit NP-6 as a follow-on PR after PR #3 merges or closes. Check PR #3 status: `gh pr view 3 --repo cloudnirvana/open-patterns`. MEDIUM severity (PR #4 ships 5/6 intended patterns; NP-6 is complete and tested, just blocked by external PR timing). `docs/patterns/agentic-architecture/source-of-truth-over-recall.md` | Phase 48 intent review | 2026-06-01 | 48 |

| CER-032 | `CLAUDE.build.md.j2` template omits the four extended `<usage>` block fields (`input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_write_tokens`) and their corresponding `--tokens-in`/`--tokens-out`/`--cache-read-tokens`/`--cache-write-tokens` flags in the `record_attempt.py` example, while the live `CLAUDE.build.md` includes them. Any project bootstrapped or `sync-build`'d from the template will silently miss recording these columns in `effort.db`. LOW severity. `skills/pairmode/templates/CLAUDE.build.md.j2` Step 1 / Step 2. **RESOLVED cp-HARNESS006-main — The thin dispatch loop template has no record_attempt.py or extended usage block; the missing columns finding does not apply to the rewritten template.** | Phase 52 intent review | 2026-06-02 | 52 |

| CER-033 | `builder.md`, `reviewer.md` (and their `.j2` templates) retain the old verbose output blocks (`BUILT: Story [N.X]`, `REVIEW PASS/FAIL — Story [N.X]`) alongside the new `## Final output to orchestrator` `BUILD-RESULT`/`REVIEW-RESULT` blocks. The orchestrator correctly ignores non-structured output now, but a future template reader sees two competing exit conventions with no label marking which is authoritative. Cleanup: remove the legacy verbose blocks from both live agent files and both `.j2` templates. LOW severity (no runtime breakage). `.claude/agents/builder.md`, `.claude/agents/reviewer.md`, and their `.j2` counterparts. **RESOLVED cp-HARNESS002-main — builder.md.j2, reviewer.md.j2, .claude/agents/builder.md, and .claude/agents/reviewer.md were deleted by the dogfood flip (HARNESS-002); the legacy verbose output blocks no longer exist.** | Phase 52 intent review | 2026-06-02 | 52 |

| CER-035 | `docs/architecture.md` line 248 states "`security-auditor` has no `Bash` since it never runs commands" — but BUILD-020 (Phase 53) explicitly added `tools: [Read, Bash, Glob, Grep]` to all four reviewer-class agents including security-auditor (needed for running tests and grepping). The claim is now stale and contradicts the live agent definition. Fix: update architecture.md § Agent definitions to reflect that all reviewer-class agents share `[Read, Bash, Glob, Grep]`. LOW severity (doc accuracy). `docs/architecture.md` line 248. | Phase 53 BUILD-020 reviewer | 2026-06-02 | 53 |

| CER-034 | `flex_build.py` `_story_path()` constructs `docs/stories/<rail>/<story_id>.md` from the `--story-id` CLI argument with no `Path.resolve().relative_to(stories_root)` containment check. A crafted story ID could escape `docs/stories/`. Blast radius is low (read-only, operator-supplied input from the orchestrator), but inconsistent with the containment discipline applied elsewhere. Fix: add `relative_to` guard after resolving the constructed path. LOW severity. `flex_build.py` `_story_path()`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-015) — `_story_path()` now applies `Path.resolve().relative_to(stories_root)` containment check; raises `ValueError` on traversal attempt.** | Phase 52 security audit | 2026-06-02 | 52 |

| CER-042 | `skills/observability/api/src/server.ts` registers `@fastify/cors` with `origin: '*'` (line 35). When the server is bound to the default `127.0.0.1` this is acceptable (browser same-origin restrictions do not apply to loopback, and a local dev tool has no secret to protect). Risk activates if an operator sets `FLEX_OBS_HOST=0.0.0.0` to expose the server on a LAN or remote interface — in that case the wildcard origin allows any website to read all registered repos' context data, effort metrics, and file paths via cross-origin requests. Fix: narrow `origin` to explicit allowed-origins list when `FLEX_OBS_HOST != "127.0.0.1"`, or document the loopback-only constraint in the SKILL.md serve notes and warn to stderr when host override is non-loopback. LOW severity (default config is safe; requires explicit operator override to activate). `skills/observability/api/src/server.ts:35`. | Phase 63 security audit | 2026-06-10 | 63 |

| CER-043 | `GET /api/user/memories` and `GET /api/user/policies` return an `abs_path` field containing the absolute filesystem path of each memory/policy file (e.g. `/home/username/.claude/projects/-mnt-work-flex/memory/user_role.md`). This discloses the operator's home directory path, username, and directory structure to any client that can reach the API. In the default loopback-only configuration this is low-risk (only the local user can reach the endpoint). Risk activates when the server is exposed beyond loopback. Fix: either omit `abs_path` from the response (the UI can reconstruct relative paths) or gate it behind a separate `?include_path=true` query parameter. LOW severity (loopback default; field is primarily a dev-convenience field). `skills/observability/api/src/routes/user.ts:15,37,126,160`. | Phase 63 security audit | 2026-06-10 | 63 |

| CER-044 | `skills/observability/api/src/parsers/phaseIndex.ts:44–46` extracts the `href` from a Markdown link in `index.md` and constructs a filesystem path as `docs/phases/${href}` without any `Path.resolve().relative_to()` containment check (TypeScript equivalent: `path.resolve()` + check that the resolved path starts with the project root). A crafted `index.md` with a link like `[Phase](../../../../etc/passwd)` would cause the system endpoint to attempt to read an arbitrary file outside the project directory. In production the operator controls `index.md` so practical exploitability is low, but the pattern is inconsistent with the containment discipline applied in the Python scripts. Fix: after constructing the candidate path, verify it resolves under `projectDir` before reading. LOW severity (attacker must control `index.md` which is project-owned). `skills/observability/api/src/parsers/phaseIndex.ts:44-46`. | Phase 63 security audit | 2026-06-10 | 63 |

| CER-053 | D1 — expected_step_tokens is mis-sourced and uniform fleet-wide. Seeded from the effort baseline BUILDER MEDIAN (bootstrap.py:417 _load_seed_expected_step_tokens -> by_role.builder.median; _DEFAULT_EXPECTED_STEP_TOKENS=53000), stamped to every project (sync.py:594) and read by context_budget.py:526. So the context-budget WINDOW-GROWTH constant is sourced from per-story EFFORT COST — the effort.db != context-control comingling (= harness agreements DP7) — and never re-estimated per project (radar & forqsite both show exactly 53000). Effect: live budget gate over-reserves ~53k and fires far too early everywhere. RESOLVE in Era 003: re-derive expected_step_tokens from thin-harness return-block growth, not effort; gate rewrite at HARNESS006 + Phase G. Cross-ref: docs/agreements/HARNESS001-ante1.md DP7, docs/agreements/era-002-closeout.md D1. **RESOLVED cp-HARNESS007-main (OBS-003) — THIN_HARNESS_STEP_TOKENS = 5000 replaces the effort.db-derived 53k; context route reports provenance label "thin-harness return-block growth"; source = "default" (not "state.json").** **Follow-up (INFRA-254, Phase 100):** the HARNESS-003/CER-053 fix above severed the *live* estimate entirely rather than re-sourcing it — `decide()` called `estimate_next_step_tokens(None, None, seed)`, leaving a static `state.json` value that a hand-edited number (e.g. `111`) could silently occupy indefinitely. INFRA-254 restores liveness from a DP7-clean source: `context_current_tokens` deltas observed by PostToolUse are appended to a bounded ring buffer (`context_step_growth_samples`, capped at 20), and `expected_step_tokens` is derived as the ring-buffer median once >= 5 samples exist (falling back to the stored seed, then `THIN_HARNESS_STEP_TOKENS`) — `derive_expected_step_tokens()`/`record_step_growth()` in `context_budget.py`, never reading effort.db. The DP7 boundary from this row's original resolution remains intact. | observability-surface | 2026-06-25 | — |

| CER-054 | D2 — context_current_tokens stuck at session-reset seed (25.0k) and STALE on fleet projects (radar recorded ~45h ago, forqsite ~15h ago) in the observability SPA. The live PostToolUse token writer appears not to be updating these projects. Needs root-cause: writer not firing vs. projects simply idle since the Phase 74 fix. RESOLVE in Era 003 Phase G (observability refactor) alongside the harness context-control rework. Cross-ref: docs/agreements/era-002-closeout.md D2. **RESOLVED cp-HARNESS007-main (OBS-004) — Root-cause diagnosed: writer (post_tool_use.py PostToolUse branch) is correct; 25k stale values are genuine idle (no Task/Agent spawns on those projects). No code fix required; the SPA's staleness badge (age > TTL) already surfaces this. Context-control rework remains for future phase.** | observability-surface | 2026-06-25 | — |

| CER-055 | D3 — waypoint outcomes render uniformly FAIL across all recent waypoints on both radar (224 attempts) and forqsite (460 attempts) — implausible. Either outcome recording (record_attempt.py -> effort.db outcome column; builder rows may carry null outcome) or the SPA render (effortDb.ts queryWaypoints / context.ts) is wrong. Also corrupts the pairmode_effort.py 'models' PASS-rate report. RESOLVE in Era 003 Phase G. Cross-ref: docs/agreements/era-002-closeout.md D3. **RESOLVED cp-HARNESS007-main (OBS-005) — Root cause: queryWaypoints had `AND outcome = 'FAIL' AND agent_role = 'reviewer'` filters; all 224 displayed waypoints were genuine reviewer-FAIL rows but builder/PASS/NULL rows were silently hidden. Fix: removed both filters. NULL outcome preserved as null (not mapped to FAIL). record_attempt.py confirmed correct: builder rows store NULL (pending outcome), reviewer rows store PASS/FAIL.** | observability-surface | 2026-06-25 | — |

| CER-056 | current-phase / _parse_index_phases treat only 'complete' as inactive, so a 'deferred' (or 'backlog') phase reads as the active phase. Surfaced at Era 002 close-out: after deferring Phase 64, 'flex_build.py current-phase' returned docs/phases/phase-64.md (deferred) as active. Latent in practice — once a later non-complete phase exists (e.g. HARNESS001-ante1 post-transition) the last-non-complete walk selects it correctly. RESOLVE in Era 003: the next-action resolver must treat deferred/backlog as inactive by design (supersedes current-phase). Cross-ref: docs/agreements/era-002-closeout.md (deferred-recognition gap). **RESOLVED cp-HARNESS009-main (RESOLVER-014) — `_resolve_active_phase` now uses first-non-inactive-wins (break after first hit); `is_phase_inactive` covers complete/deferred/backlog. Phase 23 index status corrected to 'deferred'.** | era-002-closeout | 2026-06-25 | — |

| CER-060 | [HARNESS001-main] resolve_next_action Row 5 (retry-upgrade) hardcodes model='opus' instead of delegating to select_builder_model(attempt_number=2). Behaviour is correct today (matches the selector table) but the retry tier is encoded in two places; the DP5 composition guard (test_next_action_compose.py) only asserts import-presence, not call-site coverage, so a future model_selector retry-tier change would silently diverge. Fix in HARNESS002: either compute the Position's builder_model at the next attempt number on FAIL so Row 5 uses position.builder_model, or add a test asserting Row 5 model == select_builder_model(...,attempt_number=2)[0]. | intent-reviewer | 2026-06-27 | RESOLVED cp-HARNESS002-main — RESOLVER-006 implemented the fix: infer_position now computes builder_model at attempt_count+1 on FAIL; Row 5 emits position.builder_model/builder_model_reason. Selector is now the single source of the retry tier. |



---

## Do Much Later

Not urgent, marginal value. Style, cosmetics, speculative improvements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|



| CER-050 | `.companion/state.json` writes are non-atomic across all writers — `hooks/session_start.py:73-75` (INFRA-175 reset write), `hooks/pre_tool_use.py` (`acknowledged_at`), and the flex_build.py CLI paths all use direct `write_text()` with no temp-file + `os.replace()`. A process kill mid-write could truncate state.json (recovery: re-bootstrap or manual `set-context-tokens`). Concurrent writers are not expected in normal operation. **AMENDED Phase 109 — INFRA-286: concurrent writers ARE now expected — single-orchestrator parallel story builds (Phase 109's target capability) run more than one builder/reviewer cycle at once. INFRA-285 (CER-097) added the bounded advisory `state_utils.state_lock` and converted every writer CER-097 named to route through it; the lock narrows the read-modify-write window, it does not close it — this doctrine note must not be read as "concurrent writers are now safe," only as "expected and partially mitigated."** Fix: shared `_atomic_write_state()` helper (tempfile in same dir + `os.replace`) adopted by all writers. LOW severity (informational; pre-existing pattern, called out by Phase 68 security audit for parity). **RESOLVED HARNESS015-main (INFRA-202) — remaining writers (`hooks/post_tool_use.py`, `story_context.py`, `bootstrap.py`, `sidebar.py`) now use the shared `state_utils._atomic_write_json` helper.** | Phase 68 security audit | 2026-06-12 | 68 |

| CER-051 | `_derive_transcript_path` constructs `home/".claude"/"projects"/cwd_key/f"{session_id}.jsonl"` without normalising or rejecting traversal sequences in the `session_id` basename. If `session_id` ever contained `../`, the constructed path could resolve outside `~/.claude/projects/`. Impact is low in practice: `session_id` is supplied by Claude Code in the PreToolUse stdin payload (not user input); the function only tail-reads the file; all exceptions are caught and `None` returned (fail-open). Worst case: read of an arbitrary `.jsonl` file, sum of three integer fields written to `state.json["context_current_tokens"]`. Fix: reject any `session_id` containing `/` or `..` before path construction, or replace the existence check with a `Path.resolve()` containment check against `home / ".claude" / "projects" / cwd_key`. LOW severity. `skills/pairmode/scripts/context_budget.py:_derive_transcript_path`. **RESOLVED INFRA-203 — `_derive_transcript_path` rejects `session_id` containing `/` or `..` and applies a `.resolve().is_relative_to()` containment check against `home / ".claude"`; covered by `test_context_budget.py:1384-1413`. Confirmed already fixed in code during HARNESS015-main phase planning — this backlog entry's status was stale.** | Phase 72 security audit | 2026-06-16 | 72 |

| CER-052 | `_derive_transcript_path` in `context_budget.py` constructs a JSONL path from `session_id` (hook event input) without a `Path.resolve().is_relative_to()` containment check. A crafted `session_id` with `..` segments could escape `~/.claude/`. Fix: add containment guard after `candidate.resolve()`. HIGH severity. `skills/pairmode/scripts/context_budget.py` `_derive_transcript_path`. **RESOLVED cp-HARNESS009-main (Phase 75) — _derive_transcript_path() applies is_relative_to((home / ".claude").resolve()) at context_budget.py:116; the containment check is live. Resolved note was not written at checkpoint time.** | Phase 74 security audit | 2026-06-22 | 75 |
| CER-083 | [Renumbered from a duplicate CER-066 at the cp-100 checkpoint-docs review — that ID is already used twice (story_update pipe bug, resolved Phase 94; original checkpoint_step finding, resolved HARNESS015-main).] Stale `checkpoint_step` state recurred at cp99→phase-100 **despite** the RESOLVER-017 fix being live: `cmd_record_checkpoint_step` does reset the list to `[]` when `checkpoint-tag` is *recorded*, but `CLAUDE.build.md`'s checkpoint-tag instruction is a raw `git tag && git push` — the terminal step is never recorded via the CLI, so the reset (and the INFRA-239 index mark-complete) never fires. Evidence: cp99's leftover list held only the three gate steps, no `checkpoint-tag` entry. Result: phase-100's `next-action` resolved straight to `checkpoint-tag`, silently skipping all three gate workers; caught live 2026-07-24 via resolver-state inspection, state manually cleared, gates run explicitly. Fix direction: make `record-checkpoint-step checkpoint-tag` the mandated tagging path (it already marks the phase complete per INFRA-239 — fold the `git tag`/push into it, or update `CLAUDE.build.md`/template to record the step before/with the git tag); add a regression test that a state.json holding a full prior-phase gate list still dispatches checkpoint-security first after a raw-tag-style checkpoint. HIGH severity (checkpoint gates are the phase-quality backstop; skipping them is silent). `CLAUDE.build.md` § Checkpoint, `skills/pairmode/templates/CLAUDE.build.md.j2`, `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`. **Resolved by INFRA-260 (Phase 102):** both halves fixed — (1) `CLAUDE.build.md` § Checkpoint and `skills/pairmode/templates/CLAUDE.build.md.j2` § Checkpoint now mandate `record-checkpoint-step checkpoint-tag` before any raw `git tag`, with the ordering rationale stated inline (a raw tag alone is forbidden); (2) `_record_checkpoint_step` now stamps `state.json["checkpoint_phase"]` with the resolved phase key on every call (reset to `""` alongside `checkpoint_step` on the terminal step), and `next_action.infer_position` clears the exposed `checkpoint_step` to `[]` whenever that stamp names a phase other than the one currently active — a stale gate-step list recorded for a superseded phase can no longer be silently mistaken for the active phase's own progress. Regression tests in `tests/pairmode/test_next_action.py::TestResolveNextActionCheckpoint`. | phase-100 build observation (session 2026-07-24); renumbered + root-caused at cp-100 checkpoint-docs | 2026-07-24 | 102 |
| CER-084 | LOW: `skills/pairmode/skills/security-auditor/procedure.md`'s documented thin-delegation exception list for `hooks/post_tool_use.py` (check 1) enumerates only `context_current_tokens` + timestamp as authorized state.json writes; INFRA-254 (Phase 100) added a second delegated write path (`record_step_growth()` → `context_step_growth_samples`, `expected_step_tokens`), documented in `docs/architecture.md` but not in the procedure's own exception list. The cp-100 security auditor had to re-derive the authorization from architecture.md. Same doc-lag class as CER-078. Fix: name the `record_step_growth` write explicitly in the procedure's exception list. `skills/pairmode/skills/security-auditor/procedure.md`. | Phase 100 checkpoint-security audit (informational note) | 2026-07-24 | — |
| CER-085 | LOW: `docs/architecture.md:583,605` and `README.md:162` still present the spec-derived settings.json deny list as an active protection surface for downstream projects; this sits in unreconciled tension with INFRA-253's "settings.json carries tooling only; scope_guard + permissions artifacts are the single protection surface" doctrine (architecture.md § 9.5). The passages describe the downstream mechanism, not flex's own retired denies, but a reconciliation pass should state which doctrine downstream projects are expected to converge on. | Phase 100 checkpoint-docs review | 2026-07-24 | — |
| CER-087 | MEDIUM: INFRA-255's fail-closed containment blocks *all* legitimate out-of-repo Edit/Write tool calls from a session anchored in this project, not just traversal attacks — observed live minutes after cp100: a Write to the harness memory directory (`~/.claude/projects/-mnt-work-flex/memory/…`) was denied with `path escapes project root` (no active story; previously this fell through as `no active story — allowing`). The same applies to scratchpad writes (`/tmp/claude-*/…`) and any other user-level path. The spec intended fail-closed on escapes in every guard state, but the blast radius includes benign harness-infrastructure writes that have nothing to do with story scope. Fix direction: an explicit allow-list of harness-owned out-of-root prefixes (e.g. `~/.claude/projects/<cwd-key>/memory/`, the session scratchpad root from env) checked *after* resolution (so traversal into them still can't be forged from a relative path), or scoping the escape-deny to the active-story guard states only while keeping no-story out-of-root writes allowed for non-protected targets. `skills/pairmode/scripts/scope_guard.py` `check_path`/`_normalise`, `hooks/pre_tool_use.py`. **RESOLVED Phase 105 — INFRA-271: `scope_guard.harness_owned_prefixes(project, raw_project_dir, home)` derives a narrow allow-list — `<home>/.claude/projects/<key>/memory/` (allow-listed only at this subdirectory; the sibling `<session>.jsonl` transcripts are excluded because `subagent_transcript.py` derives the effort ledger from them, so an agent that could write those could forge its own effort record), `<tmp>/claude-<uid>/<key>/` (the session scratchpad root), and `<home>/.claude/plans` — keyed on both the resolved main-checkout root and, when different, the resolved raw `project_dir`. `_out_of_root_decision()` consults this allow-list, on the *resolved* path via `Path.is_relative_to` (never a string prefix check), only after `_normalise` has already determined the path escapes the project root, and both `"path escapes project root"` deny sites in `check_path()` now route through it. `_normalise`'s containment itself is explicitly unchanged — this is a decision layered on top of it, not a hole punched in it — and everything else under `~/.claude/` (`settings.json`, `policies/`, `plugins/`, `skills/`) stays denied, since those are harness configuration, not harness scratch state.** | Live-hit post-cp100 (orchestrator memory write denied, session 2026-07-24) | 2026-07-24 | 105 |
| CER-088 | MEDIUM: `effort_db.pending_reconcilable()`'s query (`WHERE tokens_total IS NULL AND output_file IS NOT NULL ORDER BY id DESC LIMIT ?`) has no supporting index (existing indices cover story_id/phase/rail only) — a full table scan on every invocation, reachable from the SessionStart hook path via `reconcile_pending_attempts`. LIMIT bounds result size, not scan cost; small per-project tables make this low-likelihood today. Fix: add an index (or partial index) on `output_file`/`tokens_total` if attempts tables grow. `skills/pairmode/scripts/effort_db.py` `pending_reconcilable`. **RESOLVED Phase 104 — INFRA-266. Added a partial index `idx_attempts_pending` on `attempts(id DESC) WHERE (tokens_total IS NULL OR outcome IS NULL) AND output_file IS NOT NULL` — created after `_MIGRATIONS` so a legacy (pre-INFRA-258) table upgrades cleanly. Also added an opt-in `max_age_days` cutoff (`effort_db.PENDING_MAX_AGE_DAYS = 14`) applied only at the `reconcile_pending_attempts` hook-sweep call site — the shared `pending_reconcilable` query itself still defaults to no cutoff so a diagnostic that needs to see permanently-pending rows is unaffected.** | Phase 101 checkpoint-security delta audit (non-blocking note) | 2026-07-24 | 104 |
| CER-089 | LOW: `subagent_transcript.read_completed_spawn()` opens the persisted `output_file` path directly with no `resolve()`/containment check against the session tasks directory. The value is harness-generated launch metadata (same trust category as hook-payload `cwd`, which the audit procedure excludes from traversal flagging) and no adversarial injection path was found — observational only. Fix if desired: contain resolved path under the session tasks root before opening. `skills/pairmode/scripts/subagent_transcript.py` `read_completed_spawn`/`_extract_spawn_ref`. **RESOLVED Phase 104 — INFRA-266. `read_completed_spawn` now routes `output_file` through a new pure helper, `_contained_spawn_output`, before opening anything: by default the path must resolve under a temp root (`tempfile.gettempdir()`/`$TMPDIR`) and contain a `tasks` path component; an explicit `tasks_root` (used by `reconcile_pending_attempts` and tests) requires strict containment instead. A rejected path returns `None` (row stays pending) rather than being opened.** | Phase 101 checkpoint-security delta audit (observational note) | 2026-07-24 | 104 |
| CER-086 | LOW: `CHANGELOG.md` has no `[Unreleased]` entry for Phase 99 (post-fold self-sync remediation, INFRA-247..252) — the per-phase-entry convention (Phases 95/96/98, Phase 100) was skipped at cp99's checkpoint-docs step. Backfill from `docs/phases/phase-99.md`'s story list. `CHANGELOG.md`. | Phase 100 checkpoint-docs review (pre-existing gap) | 2026-07-24 | — |
| CER-090 | HIGH: the vendored `skills/observability/node_modules` tree is only partially tracked — `.gitignore`'s global `build/` and `dist/` patterns (`.gitignore:7`) exclude payload directories inside vendored packages (e.g. `@tanstack/react-query`'s `build/` with all its compiled JS and `.d.ts` files; ~untracked `dist/` dirs across many `.pnpm` packages, visible via `git status --ignored`). The main worktree works only because the files exist untracked on disk; every fresh pairmode story worktree gets just the tracked subset, so `tsc -b` fails with TS2307 module-not-found and `test_observability_ui.py::test_ui_build_emits_dist_index_html` FAILs the build gate for *every* story. Hit live during INFRA-259's review (reviewer FAIL, environmental); orchestrator worked around it by rsyncing the payload from the main worktree into the story worktree. Fix: negate the ignore patterns under the vendored roots (e.g. `!skills/observability/node_modules/**`) and commit the payload, or scope `build/`/`dist/` ignores away from vendored trees; add a guard that the vendored tree builds from a clean checkout. `.gitignore`, `skills/observability/node_modules`, `skills/observability/ui/node_modules`. **RESOLVED cp-103 (INFRA-261) — `.gitignore` scoped to un-ignore vendored payload, 1613-file payload committed, guard test `tests/pairmode/test_vendored_payload_tracked.py` added; fresh-worktree build + full suite proven with no repair.** | INFRA-259 build cycle (reviewer build-gate FAIL, session 2026-07-24) | 2026-07-24 | 102 |
| CER-091 | HIGH: INFRA-259's live smoke test caught three async effort-recording defects (evidence in `docs/stories/INFRA/INFRA-259.md` § Smoke results C–E): (1) a reviewer Agent spawn produced **no attempts row at all** — the PostToolUse Task/Agent recording branch did not fire for the second (re-review) spawn of the same story in the same session, so no attempt_number=2 row exists and no sweep ran; (2) row 344 (`phase:101`, intent-reviewer) sits in a partially-backfilled state — `tokens_total = 6597` set but `outcome` still NULL — and the lifetime rollup happily counts it, meaning reconcile can commit tokens without outcome; (3) row 343 (`phase:101`, security-auditor) never reconciles despite multiple sweep opportunities and an extant transcript — likely its last entry lacks `stop_reason: end_turn`, leaving a permanent-pending leak with no diagnostic surface. (4, added same session: row 352's FAIL reconciled at a later spawn's PostToolUse *after* `merge-story-worktree` had already cleared the attempt counter, resurrecting `.companion/attempt_counter.json` to `{INFRA-259: 1}` for a story already merged — reconciliation-time counter bumps have no story-lifecycle guard, so a stale counter can outlive its story.) Fix directions: instrument/verify the PostToolUse recording branch for repeat spawns of the same story+role (dedup key? stamp state?); make reconcile atomic over tokens+outcome; add a pending-rows age diagnostic (read-only view on `pairmode_effort.py` per INFRA-259's out-of-scope note). `hooks/post_tool_use.py`, `skills/pairmode/scripts/subagent_transcript.py`, `skills/pairmode/scripts/effort_db.py`. **Resolved by INFRA-264 (Phase 104):** all four defects closed. (1) No code-level dedup ever suppressed a repeat spawn's row (proven by regression test); the real gap was diagnostic — `subagent_transcript.log_recording_event` now appends an append-only, size-capped trace (`.companion/effort_recording.log`) on every `record_attempt_from_transcript` return path, and `hooks/post_tool_use.py`'s spawn branch now also covers `SendMessage` (a continuation, logged as `observed:non-spawn-tool`, never recorded as an attempts row) — the next occurrence is attributable from one `tail`. (2) `parse_worker_outcome` now recognises `ALIGNED` (`RECOGNISED_REVIEW_VERDICTS`, mirroring `worker_result.py`'s `REVIEW_RESULT` enum) and `effort_db.reconcile_attempt` is atomic over `tokens_total`+`outcome` (`_ATOMIC_RECONCILE_FIELDS`) — writing tokens without a resolvable outcome no longer commits. (3) `pending_reconcilable`'s guard widened to `(tokens_total IS NULL OR outcome IS NULL)` so a partial row is reachable again; `classify_pending_reason` names one of nine enumerated reasons (`no-output-file`/`file-missing`/`file-empty`/`in-flight`/`not-terminated`/`no-usage`/`line-cap`/`no-outcome`/`reconcilable`), surfaced read-only via `pairmode_effort.py pending` (age + reason per row, zero writes) and retirable via an explicit `include_quiescent` sweep (age-gated on both the row's `ts` and the output file's mtime, `outcome="UNKNOWN"`, never bumps the counter). (4) `subagent_transcript._story_accepts_late_bump` gates the reconciliation-time FAIL bump — skipped when the story's own frontmatter `status` is complete/merged/deferred/backlog, or when the story is neither counter-recorded nor the loop's `current_story`; the synchronous PostToolUse-time bump remains deliberately ungated. A third reconciliation trigger — `uv run python skills/pairmode/scripts/subagent_transcript.py reconcile` — lets an operator sweep on demand instead of waiting for the next spawn or session start. Regression tests: `tests/pairmode/test_subagent_transcript.py`, `tests/pairmode/test_effort_db.py`, `tests/pairmode/test_pairmode_effort.py`, `tests/pairmode/test_hooks_json.py`, `tests/pairmode/test_post_tool_use_hook.py`. Repairing the live rows 343/344 in `.companion/effort.db` is an explicit post-merge operator action, not part of this fix (INFRA-264 § Instructions 14). | INFRA-259 live smoke test (review-close observations, session 2026-07-25) | 2026-07-25 | 104 |
| CER-092 | MEDIUM: `skills/pairmode/scripts/story_new.py:67` emits story-stub frontmatter with a trailing comment on the `touches:` line (`touches:  # If this story changes any documented architecture, ...`). The minimal frontmatter parser (`schema_validator._parse_frontmatter`) reads that as a *string* value, and `flex_build.generate_permissions_artifact` then crashes with `TypeError: can only concatenate list (not "str") to list` inside `create-story-worktree` — blocking the build loop for every freshly-stubbed story until the frontmatter is hand-fixed (hit live on INFRA-259, fixed by two spec commits). Worse, `tests/pairmode/test_story_new.py::test_story_frontmatter_touches_has_architecture_comment` pins the buggy output. Fix: emit `touches: []` and move the architecture guidance to an adjacent HTML/MD comment line (or a `<!-- -->` outside the frontmatter); invert the pinning test; consider hardening `_parse_frontmatter` to strip trailing `#` comments on scalar lines. `skills/pairmode/scripts/story_new.py`, `tests/pairmode/test_story_new.py`, `skills/pairmode/scripts/schema_validator.py`. **RESOLVED cp-103 (INFRA-262) — `story_new.py` emits `touches: []` (guidance moved to story body), pinning test inverted, `_parse_frontmatter` strips scalar trailing comments via shared `_strip_inline_comment`; the 20 on-disk trailing-comment stubs now parse.** | INFRA-260 spec-writer recon (session 2026-07-25); root-caused live during INFRA-259 worktree creation | 2026-07-25 | 102 |



---

## Do Never

Rejected findings. Record the rejection reason so it is not re-raised.

| ID | Finding | Source | Date | Phase | Resolution |
|----|---------|--------|------|-------|------------|


| — | *(none)* | — | — | — | — |

