# flex — Cold-Eyes Review (CER) Backlog

*Last updated: 2026-08-05*

This file is the structured triage log for findings from external cold-eyes reviews.
Each finding is assigned to one quadrant. Findings are not deleted — a resolved
finding keeps its row and gains a marker annotation of the form
`**RESOLVED <phase or checkpoint> — <what landed>**` (or `**SUPERSEDED by
CER-NNN**`). The marker must begin its annotation segment — at the start of the
finding text, right after a sentence-ending `.`/`;`/`:`/em-dash plus a space, or
inside an emphasis/bracket opener (`**`, `(`, `[`) — and its case does not
matter (`RESOLVED`, `Resolved` and `resolved` are equivalent). The `cer-do-now`
checkpoint guard reads exactly this form (`cer.is_resolution_marked`,
INFRA-322/CER-130): a keyword appearing mid-clause, such as `UNRESOLVED` or
`should be resolved`, is never read as a marker, so a genuinely open finding
cannot be waved through a checkpoint by accident.

<!-- A Do Later / Do Much Later row may carry an inline `gate: <condition text>`
     token inside its Finding cell — free text running to the next `**` bold
     opener or the cell's end, NOT a sixth table column. `cer.py groom`
     re-reads both quadrants for these rows and surfaces the condition text
     for operator triage; it never edits this file and never promotes a row
     automatically (INFRA-313). -->

---

## Do Now

Urgent and important. Blocks correctness, security, or the next phase.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|



| CER-168 | Three PreToolUse-registration tests (`test_pairmode_migrate.py::test_to030_relocates_stale_hook_command`, `test_pairmode_migrate.py::test_to030_relocates_stale_hook_command_when_plugin_root_named_flex_harness`, `test_sync.py::TestSyncRegistersPreToolUseHook::test_sync_registers_pretooluse_hook`) called through to `bootstrap.py`'s `_register_pretooluse_hook` (via `to-030` and `sync_project`) without any test-fixture isolation from the real `Path.home()`/`~/.claude/plugins/` tree. INFRA-383's marketplace-install dogfooding requirement made `~/.claude/plugins/cache/nullvalues-flex/flex/0.3.1/hooks/hooks.json` exist on this machine, so the INFRA-288/CER-104/CER-127 dedup guard correctly skipped settings-level PreToolUse registration and the three tests went red — the dedup logic was right, the test isolation was the defect. MEDIUM severity (test-only host-state coupling, not a production bug). `skills/pairmode/scripts/hook_view.py` `plugin_hook_files`, `skills/pairmode/scripts/bootstrap.py` `_plugin_registered_hook_pairs`/`_register_pretooluse_hook`/`_register_context_budget_hooks`, `tests/pairmode/test_pairmode_migrate.py`, `tests/pairmode/test_sync.py`. **RESOLVED Phase 120 — INFRA-385:** `hook_view.plugin_hook_files` already accepted `home=`; `bootstrap._plugin_registered_hook_pairs`, `_register_pretooluse_hook`, and `_register_context_budget_hooks` now also accept an optional `home=` (defaulting to `None` — unchanged production behaviour) and thread it through to `hook_view.merged_hook_view`. The three named tests are isolated by monkeypatching `Path.home()` to an empty fixture directory for the duration of each test (their call paths run through Click/`sync_project` entry points with no `home=` plumbing reaching that far); a new opposite-case test (`test_sync.py::TestSyncRegistersPreToolUseHook::test_sync_skips_pretooluse_registration_when_plugin_already_provides_it`) asserts the dedup guard still skips settings-level registration when the fixture home's `~/.claude/plugins/` tree provides the hook. `test_hook_view.py` and `test_bootstrap.py` were audited and found already isolated (per-test explicit `home=` args and a file-level autouse `HOME`-patching fixture, respectively) — no changes needed there. Full `tests/pairmode/` suite (4930 passed) verified green both on this host (real flex plugin installed) and with `HOME` pointed at an empty temporary directory. | builder | 2026-08-04 | 120 |

| CER-170 | HIGH: `audit.py`'s `.pairmode-overrides` suppression mechanism never actually suppresses a MISSING/INCONSISTENT finding for a canonical section, for any project using it. `_normalise(text)` (`skills/pairmode/scripts/audit.py:398-400`) lowercases and collapses whitespace but does not strip a leading `##` header marker; `_split_sections`'s `key = _normalise(header)` (around line 381) therefore produces section keys of the form `'## inputs'` for a markdown header `## Inputs`. `_load_overrides_with_diagnostics` (around line 477) parses `.pairmode-overrides` lines of the form `file_path: section_key` and stores `section_key` exactly as written after stripping whitespace — no `##` prefix, no further normalisation — which matches the format SKILL.md documents (`## Review checklist` → `review checklist`, i.e. drop the `##` and lowercase; SKILL.md line 220). The five `(dest_rel, key) in overrides` membership checks (`skills/pairmode/scripts/audit.py:651, 672, 707, 723, 827`) compare project/canonical section keys in `'## foo'` form against override entries in `'foo'` form, so they can never match — an operator's `.pairmode-overrides` entry written in the SKILL.md-documented style silently never suppresses anything. Existing regression coverage in `tests/pairmode/test_audit.py` (`TestAuditOverridesSuppress`) did not catch this because its own `_get_first_canonical_section_key` helper builds its expected override key via `_normalise(headers[0])` where `headers[0]` is the raw `## Foo` header string — i.e. the test writes overrides in the same buggy `'## foo'` form the internal keys use, not the SKILL.md-documented `'foo'` form a real operator would write, so both sides of the test's comparison share the same bug and it passes despite the mechanism being broken for real usage. Confirmed by reading current `audit.py` and SKILL.md directly (not from a prior description). Fix direction: strip a leading `#+\s*` from the header text before normalising when `_split_sections` builds a section key (not inside `_normalise()` itself, which is also used for general body-text comparison and must not change that behaviour), so the resulting canonical/project keys match the SKILL.md-documented, no-`##` `.pairmode-overrides` entry format operators already write. `skills/pairmode/scripts/audit.py` `_normalise`, `_split_sections`, `_load_overrides_with_diagnostics`, the five `in overrides` call sites, `tests/pairmode/test_audit.py`. Filed alongside a spec (INFRA-391, Phase 123) elaborating the fix. **RESOLVED Phase 123 — INFRA-391:** `_split_sections` now strips the leading markdown header marker before normalising, so internal section keys match the `.pairmode-overrides` format SKILL.md already documents; the same fix rippled into `sync.py`'s own header/`RETIRED_SECTIONS` matching, which independently assumed the pre-fix key shape. | Investigation while diagnosing an unrelated issue in the `Repo-G` project (out of scope there; confirmed as a flex-repo-native bug) | 2026-08-04 | 123 |

| CER-171 | MEDIUM: `docs/exemplars/EXEMPLAR-000.md` (INFRA-363, Phase 119) — the spec-writer procedure's frozen bounded input 4 (`skills/pairmode/skills/spec-writer/procedure.md` § Input contract item 4, lines 52/98/103) — is not scaffolded, synced, or audited for any flex-bootstrapped project other than flex itself. Confirmed by grepping `skills/pairmode/scripts/bootstrap.py`, `skills/pairmode/scripts/sync.py`, and `skills/pairmode/scripts/audit.py` for `EXEMPLAR`: zero hits in all three. `bootstrap.py`'s `SCAFFOLD_FILES` (Phase 7 project-owned prose docs), `AGENT_FILES` (the ten harness-owned agent shells, skip-if-exists unless `--force-agents`), and `NARRATIVE_FILES` (the nine harness-owned role narratives) registries do not include it; `audit.py`'s `CANONICAL_FILES` (mirrors `AGENT_FILES`, strict section-level comparison, EXTRA sections flagged ERROR/WARN) and `SCAFFOLD_FILES` (mirrors `bootstrap.py`'s, lenient STALE-PLACEHOLDER comparison for inherently project-specific prose) do not include it either — so `sync.py`'s `_dest_to_template` (which only resolves a template for a `CANONICAL_FILES`/`SCAFFOLD_FILES`/`EXISTENCE_CHECK_FILES` destination) returns `None` for `docs/exemplars/EXEMPLAR-000.md` and `sync_project`'s missing-file remediation loop silently skips it — the same "structurally excluded from scaffolding" shape CER-165 found for `.pairmode-overrides`. This file exists in flex's own repo only because INFRA-363 hand-authored it directly; it was never added to any of the three registries that make a canonical file reproducible fleet-wide. Confirmed independently the gap is live: it was first reported from a different flex-bootstrapped downstream project's own spec-writer run (referenced there as INFRA-004), which found the file absent and degraded gracefully to a built-in section-list fallback rather than failing outright — but every subsequent stub-elaboration in that project, and in every other project bootstrapped from this harness without a hand-authored copy, hits the identical gap. Fix direction: treat `EXEMPLAR-000.md` as a `CANONICAL_FILES` entry (not `SCAFFOLD_FILES`) — its content is harness-owned and meant to stay identical fleet-wide (it is a frozen structural exemplar, not project-specific narrative prose a project should freely diverge on, per INFRA-363's own framing) — mirroring the existing `AGENT_FILES`→`CANONICAL_FILES` mirroring convention and its `TestCanonicalFilesAgentFilesConsistency`-style regression test (`tests/pairmode/test_audit.py`); add a `docs/exemplars/EXEMPLAR-000.md.j2` template (a literal, non-parameterised copy of the current frozen content — no per-project Jinja variables, matching the file's own "frozen" contract) under `skills/pairmode/templates/`; scaffold it in `bootstrap.py` (skip-if-exists like the agent shells, since a project may have already hand-diverged the format); register it in `audit.py`'s `CANONICAL_FILES` so a missing/inconsistent copy is flagged and MEDIUM severity (silent-degrade gap, not data corruption — the spec-writer procedure's own graceful-fallback behaviour already prevents an outright failure; downgraded from a would-be HIGH because of that existing fallback). `skills/pairmode/scripts/bootstrap.py`, `skills/pairmode/scripts/sync.py`, `skills/pairmode/scripts/audit.py`, `docs/exemplars/EXEMPLAR-000.md`, `skills/pairmode/templates/docs/exemplars/EXEMPLAR-000.md.j2` (new), `tests/pairmode/test_bootstrap.py`, `tests/pairmode/test_sync.py`, `tests/pairmode/test_audit.py`. Filed alongside a spec (INFRA-392, Phase 124) elaborating the fix. **RESOLVED Phase 124 — INFRA-392:** `docs/exemplars/EXEMPLAR-000.md` registered as a `CANONICAL_FILES` entry in `audit.py` and a new `EXEMPLAR_FILES` scaffold entry in `bootstrap.py`, with a literal-copy template under `skills/pairmode/templates/`; `sync.py` needed no changes since its generic backfill path already covers any `CANONICAL_FILES` entry. | Dogfooding the spec-writer role in a flex-bootstrapped downstream project (referenced there as INFRA-004); confirmed independently against flex's own `bootstrap.py`/`sync.py`/`audit.py` | 2026-08-04 | 124 |

| CER-172 | HIGH: this repo is public, but real directory names for private sibling ("fleet") repos outside this project's own scope have been committed into it in two ways. (1) `skills/pairmode/scripts/fleet_discovery.py`'s `DOCUMENTED_DIRS`-style constant hardcodes a literal list of real sibling-repo directory names directly in committed source, used as a fallback candidate list for fleet-wide operations when `.companion/state.json`'s `registered_projects` (already gitignored) doesn't cover them. (2) Roughly 150 already-committed doc files across this project's whole history — phase docs, story specs, `docs/architecture.md`'s own design-rationale prose, and the CER backlog itself — reference these same real repo names in prose, including passages that compare/contrast several specific repos by name within the same passage. Found while an operator was reviewing recently-added fleet-remediation stories (INFRA-386/387) and noticed real sibling-repo names in the diff. Fix direction (specced as INFRA-393/INFRA-394, Phase 125): externalize the hardcoded list in `fleet_discovery.py` into a local, gitignored config mapping a stable anonymized label (e.g. "Repo-A".."Repo-O") to each real path, with a committed template using only fake placeholder names; then mechanically scrub the ~150 existing doc files to reference repos only via that same anonymized-label mapping, sourced from the local config at build time — never re-listing real names in any committed spec, test, or CER row (including this one). `skills/pairmode/scripts/fleet_discovery.py`, and the full `docs/` tree wherever a real sibling-repo name currently appears. **RESOLVED Phase 125 — INFRA-393/INFRA-394:** `fleet_discovery.py`'s hardcoded repo-name list externalized into a local gitignored `.pairmode-fleet.local.json` (committed `.example` template uses only fake placeholders), then a scripted scrub (`skills/pairmode/scripts/scrub_fleet_names.py`) replaced every real sibling-repo name across ~200 already-committed files with a stable anonymized `Repo-A`..`Repo-O` label sourced from that same local config at runtime. The `lessons/lessons.json`/`LESSONS.md` residual this scrub deliberately excluded (protected append-only path) was tracked separately as CER-173. Caveat: the Phase 125 checkpoint's security-auditor found the scrub was incomplete — a real sibling-repo name was dropped from `.pairmode-fleet.local.json` during externalization and never scrubbed, and at least two more real sibling-repo names were never added to the map at all — and that the mechanism can regress, since `fleet_discovery.py` still writes real absolute paths into the tracked `docs/fleet-snapshot.md`; this completeness gap is tracked and being fixed under CER-188 (filed as a new Do Now item, Phase 130). | Operator review of INFRA-386/387 diff noticing real sibling-repo names committed to a public repo | 2026-08-05 | 125 |

| CER-174 | CRITICAL: INFRA-388's shadow-reviewer write-capability restoration (Phase 122, live — `shadow_review=concurrent` is currently enabled in `CLAUDE.build.md`) shipped with two compensating-control gaps found by the Phase 122 checkpoint's security-auditor run. **(1) CRITICAL — Bash allowlist bypass.** `skills/pairmode/scripts/reviewer_bash_guard.py`'s new `agent_type="shadow-reviewer"` allowlist (`_check_shadow_reviewer_command`, ~lines 155-187) validates only the FIRST git invocation found anywhere in the command's token list via `_find_git_invocation` (~lines 190-215), which stops collecting at `&&`/` | `/`;`/` | ` — but the guard still returns allow for the whole command string. Verified live: `git status && rm -rf docs` → ALLOW, `git log ; touch /tmp/x` → ALLOW, `git status`+newline+`rm -rf docs` → ALLOW, `rm -rf docs && git log` → ALLOW, `git diff --stat $(rm -rf docs)` → ALLOW, a backtick command-substitution form → ALLOW, `git status && git reset --hard` → ALLOW, `git log && git push origin main` → ALLOW. The `>`/`<`/` | ` substring pre-check only catches redirection; `&&`, `;`, newline, `$( )`, and backticks are unhandled — this is the exact reviewer-improvisation bypass class INFRA-324 was built to block, reopened for this new agent type. The four new shadow-reviewer test cases in `tests/pairmode/test_reviewer_bash_guard.py` cover no chaining or substitution form, so this shipped without regression coverage catching it. **(2) HIGH — unscoped Write grant.** The `Write` grant added to `.claude/agents/shadow-reviewer.md`/`skills/pairmode/templates/agents/shadow-reviewer.md.j2` is not role-scoped anywhere: `skills/pairmode/scripts/scope_guard.py`'s `check_path` (~lines 101-116) has no `agent_type` parameter and resolves the caller to the active story exactly as it does for the builder, so the shadow-reviewer inherits the builder's entire write scope (`primary_files`/`touches` plus every `STANDING_SURFACES` entry) instead of being confined to `.pairmode-suggestions.md`, while running concurrently in the same worktree with no lock or ordering against the builder's real writes. The shadow-reviewer procedure's own "Never write to any file other than `.pairmode-suggestions.md`" is currently prose-only, unenforced by the hook layer. Fix direction (specced as INFRA-396, Phase 126): make the shadow-reviewer Bash branch genuinely default-deny (reject any shell control/substitution token before subcommand matching; require the git token be first, not merely present); thread an agent-type-aware branch through the Write-path check that denies any shadow-reviewer write outside `.pairmode-suggestions.md` regardless of what the concurrently-running builder's own scope allows. `skills/pairmode/scripts/reviewer_bash_guard.py`, `skills/pairmode/scripts/scope_guard.py`, `tests/pairmode/test_reviewer_bash_guard.py`. **RESOLVED Phase 126 — INFRA-396:** `reviewer_bash_guard.py`'s shadow-reviewer branch reworked to default-deny — rejects any shell control/substitution token (`&&`, ` |

| CER-072 | CRITICAL: the RESOLVER-008 checkpoint build-gate guard can never pass in any downstream project. `_run_build_gate_subprocess` (`skills/pairmode/scripts/next_action.py:367-395`) hardcodes `uv run pytest tests/pairmode/ -q --tb=no` in the target project directory and treats any non-zero exit as gate-red — but `tests/pairmode/` exists only in flex itself; verified absent in every fleet project checked (Repo-E, Repo-A, Repo-B, Repo-C, Repo-L, Repo-I), where pytest exits non-zero with "file or directory not found" and the guard returns False. The fail-open branch covers only exceptions/timeouts, not exit-code failures, so `next-action` returns `await-user / checkpoint-guard-failed:build-gate` unconditionally at every downstream checkpoint. Live-hit: Repo-E 2026-07-22, first downstream checkpoint under 0.3.0 (phase PM065-main, the migration phase itself) — the project's real build gate (`pnpm build && pnpm typecheck && pnpm test`, 2827 tests) was green while the guard hard-blocked the dispatcher. Fix direction: make the guard command project-aware — read the project's build-gate command from state/config (or detect: `tests/pairmode/` present → pytest; `package.json` present → the project's own test script), or treat "no such test directory" (pytest exit 4) as advisory-pass like the other error paths; add a downstream-shaped fixture (no tests/pairmode, passing package.json gate) to the RESOLVER-008 tests. **RESOLVED Phase 97 — INFRA-230 made `_run_build_gate_subprocess` (`skills/pairmode/scripts/next_action.py`) project-aware: it now reads `test_command` from `project_dir/.companion/pairmode_context.json` (populated by `bootstrap.py` for every bootstrapped project) and runs that exact command via `shell=True` (honoring `&&`-chains like Repo-E's `pnpm build && pnpm typecheck && pnpm test`), honoring its real exit code so a genuinely-failing gate still reports red. When the file is absent, unreadable, invalid JSON, or has no non-empty `test_command` string, it falls back to the historical hardcoded `uv run pytest tests/pairmode/ -q --tb=no` — so flex-harness's own gate (no `pairmode_context.json`) is unaffected. Four regression tests added to `tests/pairmode/test_next_action.py` (passing command → green; failing command → red; no context → pytest fallback; malformed/empty context → pytest fallback, no crash). Existing fail-open on exception/timeout preserved for both paths.** | Repo-E migration (first downstream 0.3.0 checkpoint) + fleet spot-check | 2026-07-22 | 97 |

| CER-067 | `skills/pairmode/scripts/bootstrap.py` has exactly one hook-registration function, `_register_pretooluse_hook`, which is the only one ever called during `bootstrap` (line ~1137) or `sync`/`sync-all`. Flex's own canonical `hooks/hooks.json` registers six event types across ten blocks (`Stop`, `PermissionRequest`/`ExitPlanMode`, `PreToolUse` ×3, `PostToolUse` ×2, `SessionEnd`, `SessionStart`, `UserPromptSubmit`), but every downstream bootstrapped project's `.claude/settings.json` gets only the `PreToolUse` block — confirmed by inspecting the full fleet (Repo-A, Repo-B, Repo-C, Repo-E, Repo-D, Repo-L, Repo-I, Repo-H, Repo-J, Repo-F, base56, Repo-K, Repo-M all show `hooks: ['PreToolUse']` only; `Repo-G` additionally carries a manually-added `PostToolUse`/`Stop` pair, not from bootstrap). Consequence: `user_prompt_submit.py` (INFRA-192) never fires downstream, so `context_budget_user_turn_seq` never increments — the "genuine new turn since last block" requirement INFRA-193 built specifically to close the CER-047 self-clearing bug can never be satisfied by a normal reply in any downstream project. `session_start.py`'s reset (session_reset.py) never fires, so `context_current_tokens` never resets on `/clear`. `post_tool_use.py`'s `Task`/`Agent` branch never fires, so `context_current_tokens` is never actually written from a live transcript read. The entire context-budget gate is effectively decorative in every downstream project — it reads state nothing downstream ever writes or advances. Live symptom: an agent working in `Repo-I` reported the budget gate "can never self-clear via a normal 'Continue building' reply" and applied an undocumented workaround (`context_budget_acknowledged_at = tokens - margin`, `context_budget_acknowledged_user_turn_seq = seq - 1`, manually forged into `state.json`) to unstick it, noting "I've hit this before" — no such workaround is documented anywhere in flex's own docs, meaning this has been informally reinvented and is being applied silently/routinely across sessions to defeat a broken mechanical gate, echoing the exact failure mode CER-039 already flagged (LLM-attention-only protection once the mechanical gate is dead) — except now actively papered over rather than just silently absent. Fix direction: generalize `bootstrap.py`'s hook registration so it registers the full set of load-bearing hooks (`UserPromptSubmit`, `SessionStart`, `PostToolUse` `Task`\|`Agent` branch — the three the context-budget gate depends on; evaluate whether `Stop`, `PermissionRequest`/`ExitPlanMode`, and `PostToolUse` `Write`\|`Edit`\|`MultiEdit` are downstream-appropriate too, since those are companion/sidebar-oriented and may need to stay opt-in rather than blanket-registered) instead of the current single hand-rolled `_register_pretooluse_hook` function; re-run the fleet rollout for the newly-registered hooks once fixed. CRITICAL severity (a core methodology safety gate has never functioned in any downstream project, and its absence is now being actively worked around rather than merely unnoticed). `skills/pairmode/scripts/bootstrap.py`, `hooks/hooks.json`, `hooks/user_prompt_submit.py`, `hooks/session_start.py`, `hooks/post_tool_use.py`. **RESOLVED Phase 95 — INFRA-208 generalized `bootstrap.py`/`sync.py` registration to wire `UserPromptSubmit`, `SessionStart`, and `PostToolUse` `Task\|Agent` alongside the existing `PreToolUse` block (deferring the four companion/sidebar blocks as opt-in). INFRA-209's read-only fleet audit (2026-07-21) found the rollout already applied to 13 of 14 in-scope projects ahead of the story's build (no commits needed); `Repo-G` was formally excluded as a known carve-out (not touched); `anchor` remains excluded as a non-pairmode-consumer sibling plugin repo. `Repo-I`'s forged `context_budget_acknowledged_*` workaround keys are still present in its `state.json` — resetting them is an explicit follow-up, not required for this resolution.** | Live-hit in `Repo-I` (agent workaround report) + direct verification against every fleet project's `.claude/settings.json` | 2026-07-18 | 95 |

| CER-066 | `_update_story_row_in_phase` in `skills/pairmode/scripts/story_update.py:225-235` parses a Stories-table row with a naive `stripped.split('\|')`, which does not respect markdown-escaped pipes (`\\\|`) inside a cell. Any story title containing a literal escaped pipe — a real, likely occurrence in this codebase's own INFRA rail, which frequently documents matcher strings like `Edit\|Write` or `Task\|Agent` (see INFRA-205, INFRA-206) — gets its title truncated at the first `\\\|` and the intended status value spliced into the truncated fragment instead of the real status cell, while the real last cell is left unchanged. Reproduced directly: a fixture phase file with row `\| INFRA-1 \| Register the Edit\\\|Write matcher \| planned \|`; calling `update_phase_story_status("INFRA-1", ..., "complete")` corrupted the row to `\| INFRA-1 \| Register the Edit\\\|complete \| planned \|` — title truncated, status not actually updated in the real cell. Live-hit during Phase 93's own build: `story_update.py --story-id INFRA-205 --status complete` corrupted `docs/phases/phase-93.md`'s INFRA-205 row this exact way; manually repaired by the orchestrator. INFRA-206's title contains the same escaped-pipe pattern and is expected to hit the same corruption when its status is flipped. Fix direction: split on an un-escaped-pipe boundary (e.g. `re.split(r'(?&lt;!\\\\)\|', stripped)`, treating `\\\|` as a literal pipe within a cell rather than a column separator) instead of a naive `str.split('\|')`; add regression tests with escaped-pipe titles (including the exact INFRA-205 collision shape) to `tests/pairmode/test_story_update.py`. HIGH severity (silent phase-doc title corruption on story titles containing escaped pipes — a pattern this codebase's own INFRA rail produces routinely when documenting hook matcher syntax). `skills/pairmode/scripts/story_update.py:225-235`. **RESOLVED Phase 94 (cp94) — INFRA-207 replaced the naive split with `re.split(r'(?<!\\)\|', stripped)`, treating an escaped pipe as a literal cell character rather than a column separator. Regression tests cover the exact CER-066 reproduction and the INFRA-205/INFRA-206 collision shape.** | Live-hit during Phase 93 build (INFRA-205 status update) + direct reproduction | 2026-07-16 | 94 |

| CER-065 | `hooks/pre_tool_use.py` has full dispatch logic for three tool types (`Task`/`Agent` → `context_budget.py`; `Edit`/`Write` → `scope_guard.py`, Phase 55; `Read` → `cold_read_guard.py`, INFRA-196), but `hooks/hooks.json`'s `PreToolUse` array registers only one block, `matcher: "Task\|Agent"` — no `Edit\|Write` or `Read` block, unlike the `PostToolUse` array immediately below it in the same file, which correctly carries two separate matcher blocks for `post_tool_use.py`. Result: Claude Code never invokes `pre_tool_use.py` for `Edit`, `Write`, or `Read` tool calls, so the `scope_guard.py` and `cold_read_guard.py` branches are dead code — in every project using this plugin, including flex itself (confirmed: flex's own `hooks/hooks.json` has the same single-block gap). Worse for non-plugin bootstrapped projects: `bootstrap.py`'s `_register_pretooluse_hook()` (skills/pairmode/scripts/bootstrap.py:309-355) only ever creates/finds a `matcher: "Task"` block — not even `"Task\|Agent"` (predates the CER-049 rename fix) — and never an `Edit\|Write` or `Read` block. Root cause traced via git history: `hooks.json`'s `PreToolUse` block was created Task-only in INFRA-128 (99a181f) when the hook only handled the context-budget gate; two later stories each added a new dispatch branch to `pre_tool_use.py`'s Python source without touching the registration manifest — INFRA-139 (123eae7, `Edit`/`Write` → `scope_guard.py`) and INFRA-196 (b333739, `Read` → `cold_read_guard.py`). Neither commit's diff touches `hooks/hooks.json` or `bootstrap.py`. Both stories passed their reviewer checklist (and INFRA-196 presumably a security-audit checkpoint); the CLAUDE.md HOOK PERFORMANCE checklist item verifies dispatch logic stays thin but never checks whether a new branch is actually reachable via the matcher. Reported via an external `Repo-C` repo session that found the same gap independently while investigating a `sync-all` deletion of the now-dead INFRA-196 CLAUDE.md paragraph. Fix direction: add `Edit\|Write` and `Read` matcher blocks to `hooks/hooks.json`'s `PreToolUse` array (mirroring the two-block `PostToolUse` pattern already in the same file); update `bootstrap.py._register_pretooluse_hook` to register all three matchers (or a single combined `Task\|Agent\|Edit\|Write\|Read` block) for non-plugin bootstrapped projects; add a test asserting `hooks.json`'s registered matchers are a superset of the `tool_name` branches `pre_tool_use.py` actually dispatches on (so a future new branch fails a test instead of shipping silently dead). CRITICAL severity (two documented enforcement mechanisms — story-file-scope enforcement and orchestrator cold-read blocking — have likely never executed on any project, ever; distinct from the separate story-scoped Layer-1/Layer-2 permissions system and the static `.claude/settings.json` deny list, CER-048, which are unaffected and still provide some protection). `hooks/hooks.json`, `skills/pairmode/scripts/bootstrap.py:309-355`, `hooks/pre_tool_use.py`. **RESOLVED Phase 93 (cp93) — INFRA-205 registered `Edit\|Write` and `Read` PreToolUse matcher blocks in `hooks/hooks.json` (mirroring the `PostToolUse` two-block pattern) with a regression test asserting the registered matchers are a superset of `pre_tool_use.py`'s dispatched tool_name literals; INFRA-206 widened `bootstrap.py`'s `_register_pretooluse_hook` to register the canonical combined matcher for downstream bootstrapped projects, migrating any stale `"Task"`-only block in place.** | Repo-C repo (external report) + direct verification against flex's own hooks.json/bootstrap.py/git history | 2026-07-16 | 93 |

| CER-064 | `update_phase_story_status` in `skills/pairmode/scripts/story_update.py:109-121` globs every file under `docs/phases/*.md` and updates the status column in any Stories-table row whose bare first column matches `--story-id`, with no phase or rail disambiguation. When two phases carry a row for the same bare story ID — a real, demonstrated scenario in this repo (see CER-063: `INFRA-203` collides between `main` phase 91 and the unmerged `fold-prep` branch's `HARNESS011-main`) — running `story_update.py --story-id RAIL-NNN --status complete` against one phase's story silently flips the unrelated phase's row to the same status too. Reproduced directly: two fixture phase files each with an independent `REF-106` row; a single `--status complete` call updated both. Reported externally by the `Repo-O` migration repo (its own Phase 24 and Phase 29 both carried an unrelated `REF-106` row; a status update on one leaked into the other) — that repo has no authority to fix shared pairmode tooling directly. Fix direction: scope `update_phase_story_status` to the phase(s) named in the story file's own `phase:` frontmatter field (already read by `record_attempt.py` for the same purpose) instead of scanning every phase manifest; only fall back to the current whole-glob behavior when the frontmatter `phase:` field is absent (legacy story). HIGH severity (silent cross-phase status corruption on any story-ID collision — undermines phase Stories tables as a source of truth). `skills/pairmode/scripts/story_update.py:109-121`. **RESOLVED Phase 92 (cp92) — INFRA-204 scopes `update_phase_story_status` to the story's declared `phase:` frontmatter (suffix-aware, mirroring `story_new.py._append_to_phase`'s glob precedence), falling back to the whole-glob scan only for legacy phase-less stories. Regression tests reproduce the exact cross-phase leak.** | Repo-O-migration repo (external report) + Phase 91 intent review (CER-063 precedent) | 2026-07-16 | 92 |

| CER-057 | `tests/pairmode/test_lesson_review.py::TestCLIOutputClarity` invokes the real `lesson_review` CLI (`--approve`/`--reject`) without `--project-dir` or `--skip-drift`, so the post-review drift-promotion step reads the live `.companion/state.json` `registered_projects` and runs real drift detection. When ≥2 registered projects share a convergence pattern (observed 2026-06-26: `registered_projects = [Repo-A, Repo-B]`), the CLI hits the interactive `Promote to canonical? [y/n/skip]` prompt, gets no stdin, and aborts (exit 1) — failing `test_approved_lesson_output_contains_action_required` and `test_end_of_run_summary_contains_review_complete`. Environment-dependent (green when <2 projects share a pattern; was green at session start). A unit test must not invoke live interactive fleet drift promotion. Fix: pass `--skip-drift` (these tests assert lesson-annotation output, not drift) in the affected CLIOutputClarity invocations; scan the class for sibling tests with the same latent coupling. HIGH (blocks the build gate; flaky green/red on fleet state). `tests/pairmode/test_lesson_review.py:596-640`. **RESOLVED HARNESS001-ante1 (cp-HARNESS001-ante1) — INFRA-185 added `--skip-drift` to the two CLIOutputClarity invocations; the build gate is now green regardless of `registered_projects` contents.** | RELEASE-003 build (HARNESS001-ante1) | 2026-06-26 | HARNESS001-ante1 |

| CER-049 | The `PreToolUse` context-budget hook appears to never fire: `hooks/hooks.json` registers the matcher `"Task"`, but the agent-spawn tool in current Claude Code harnesses is named `Agent`. Observed Phase 68 build (2026-06-12): four subagent spawns proceeded with `context_current_tokens` between 151k and 694k — far past the 132k block ceiling — with no block and no `context_budget_acknowledged_at` written. If the rename hypothesis is confirmed, the belt-and-suspenders secondary gate (CER-027, resurrected in CER-039, redesigned in CER-045) is silently disabled fleet-wide and the only remaining protection is the LLM-attention primary gate — the exact failure mode CER-039 documented. Verify first (log `tool_name` from a PreToolUse catch-all matcher), then fix: matcher `"Task | Agent"` in `hooks.json` plus the same dual check in `pre_tool_use.py` dispatch. HIGH severity (silent loss of mechanical enforcement). `hooks/hooks.json:26-37`, `hooks/pre_tool_use.py:25`. **RESOLVED Phase 69 (cp69) — `hooks.json` matcher + `pre_tool_use.py` dispatch now accept `Task | Agent` (CER-049 phase).** | Phase 68 build observation |

| CER-047 | `context_current_tokens` survives `/clear` and blocks the build loop on a phantom number. Confirmed in production (Repo-A, 2026-06-12): operator cleared mid-build at 212,492 accumulated tokens; `state.json` retained the old session's counter; the context gate re-blocked INFRA-006 on a value describing a window that no longer exists. The CER-041 TTL (60 min) only recovers when the gap exceeds the TTL — the operator cleared within ~20 min — and even the stale path routes to the same manual `/context` + `set-context-tokens` step. CER-041 originally named a `SessionStart` hook reset as an alternative fix; only the TTL shipped (Phase 59 INFRA-151). The SessionStart hook input carries `source: "startup" \ | "resume" \ | "clear" \ | "compact"`, but `hooks/session_start.py` never reads stdin. Fix: thin-delegation reset — pure decide module in `skills/pairmode/scripts/`, single hook-owned state write resetting the counter to a fresh-session baseline on `clear`/`startup` (never `resume`). `compact` is deliberately out of scope (post-compact window size unknown; counter overstates, which fails safe by over-blocking — revisit if it bites). HIGH severity (defeats the context gate on every mid-build clear; the gate's primary recovery path is the manual step the methodology is trying to eliminate). `hooks/session_start.py`, `skills/pairmode/scripts/context_budget.py` (read side), `docs/architecture.md` writer contract, CLAUDE.md checklist item 1. **RESOLVED Phase 68 (cp68) — `session_start.py` reads the `source` field and resets the counter on `clear`/`startup` via `session_reset.py` (INFRA-175).** |

| CER-046 | `PAIRMODE_VERSION` is defined in three places with diverging values: `audit.py:28` = `"0.1.0"`, `bootstrap.py:33` = `"0.2.0"`, `sync.py` imports from `audit.py` (stale). Every `sync` run stamps `"0.1.0"` into the target project's `state.json["pairmode_version"]`, silently downgrading projects that were bootstrapped at `"0.2.0"`. Confirmed in production: Repo-A's state.json was downgraded and required manual restoration. Fix: single-source the constant (e.g. `skills/pairmode/scripts/_version.py`) and have `audit.py`, `bootstrap.py`, and `sync.py` import from it. HIGH severity (silent state corruption on every sync run). `audit.py:28`, `bootstrap.py:33`, `sync.py` (import site). **RESOLVED Phase 66 — `PAIRMODE_VERSION` single-sourced in `_version.py`; audit/bootstrap/sync import it.** | Upstream operator report | 2026-06-11 | 69 |

| CER-036 | `flex_build.py` `cmd_permissions_create` takes `STORY_ID` as a positional CLI argument and uses it unsanitised to construct both an input path (`docs/stories/<rail>/<story_id>.md`) and an output path (`docs/phases/permissions/<story_id>.json`). No regex validation on `story_id`, no `Path.resolve().relative_to(project_path)` containment on `out_path`. A crafted `story_id` with `..` segments can write attacker-shaped JSON to any path the invoking process can reach. The permissions file is the source of truth that `scope_guard.py` consults for hook-level write enforcement — write-side traversal weakens scope enforcement integrity. Fix: add `re.fullmatch(r"[A-Z][A-Z0-9_]*-\d{3}", story_id)` guard at function entry; add `resolve().relative_to()` containment on both `story_path` and `out_path` before any I/O. HIGH severity. `flex_build.py` cmd_permissions_create. **RESOLVED Phase 55 — `cmd_permissions_create` now applies `resolve().relative_to()` containment on both story_path and out_path (traversal blocked). Format pre-validation was later removed (cp78); the path containment is the security guard.** | Phase 55 security audit | 2026-06-03 | 55 |

| CER-037 | `docs/architecture.md` still describes `hooks/pre_tool_use.py` as "context budget enforcement (CER-027); thin delegate to skills/pairmode/scripts/context_budget.py" (line 26). Phase 55 INFRA-139 added a second dispatch branch (`Edit`/`Write` → `scope_guard.py`) but did not update architecture.md. The "Documented exception" block at line 785 also names only CER-027 and context_budget. A future reviewer reading architecture.md alone sees no authority for the scope_guard dispatch. HIGH severity (doc-currency gap for security-relevant decision-block surface). `docs/architecture.md` lines 26, 169-178, 785-792. **RESOLVED Phase 55 — `architecture.md` documents the `scope_guard` Edit/Write dispatch (7 refs).** | Phase 55 security audit | 2026-06-03 | 55 |

| CER-038 | `phase_new.py` `--phase-id` and `--suffix` CLI arguments are accepted as free-form strings and embedded into filesystem paths (`phases_dir / f"phase-{phase_key}.md"`) without sanitization or containment check. The `relative_to()` calls are display-only and provide no write protection. A crafted argument (e.g. `--suffix "../../../../../../attack"`) can write outside the project directory. Secondary: unsanitised `phase_key` is also written verbatim into `index.md` and era files (markdown injection). Missing tests for traversal vectors in new test classes. Fix: validate `phase_id` and `suffix` against `re.fullmatch(r"[A-Za-z0-9][A-Za-z0-9_-]*", value)` at CLI entry; add traversal-payload test cases. HIGH severity. `phase_new.py` lines 287–294. **RESOLVED Phase 56 — `phase_new.py` validates `--phase-id`/`--suffix` via `_SAFE_PHASE_COMPONENT.fullmatch` at CLI entry.** | Phase 56 security audit | 2026-06-03 | 56 |

| CER-039 | `context_budget.py` secondary hook gate non-functional since Phase 47. `compute_context_tokens(transcript_path)` returns `None` whenever `transcript_path` is absent or incorrect in PreToolUse hook payloads — which is every production session. Confirmed: `context_budget_acknowledged_at` is absent from ALL projects' `state.json` across the entire fleet (flex, Repo-E, Repo-G, Repo-L, and 8 others). Root cause: Phase 47 D3 chose transcript JSONL parsing instead of a state.json contract written by the orchestrator's `/context` call. Effect: the secondary gate has never blocked a single Task spawn; the only protection was the LLM-attention primary gate, which also fails on long phases (confirmed Repo-E PM023-main: 6-story phase with retries ran to 100% context with no interrupt). Fix: add `flex_build.py set-context-tokens --tokens N` CLI; have orchestrator write token count to `state.json["context_current_tokens"]` after each `/context` call; have `context_budget.py` read from there. HIGH severity. `skills/pairmode/scripts/context_budget.py`, `hooks/pre_tool_use.py`, `CLAUDE.build.md` Context gate. **RESOLVED Phase 58 (cp58) — `set-context-tokens` CLI + `state.json` context-token contract; `context_budget.py` reads from state (INFRA-148).** | Phase 57 build post-mortem | 2026-06-03 | — |

| CER-045 | Three interlocking bugs defeat the context gate on multi-story phases. (1) `story_context.py --clear` erases `context_current_tokens` after every story (Phase 59 CER-041 belt-and-suspenders), forcing a re-estimate at each Context gate. (2) The orchestrator cannot call `/context` programmatically; it estimates (e.g. 25k) and calls `set-context-tokens` with the wrong value every story. So `decide()` always sees 25k + 53k = 78k < 132k → never blocks. (3) `story-cost-estimate` filters by (rail, story_class) with ≥5 sample requirement — new phases always show "insufficient data." Fix (Phase 65): (A) remove token-count pops from `clear_current_story()` (INFRA-170); (B) add `bump-context-tokens --cost N` that accumulates actual story cost (INFRA-169); (C) redesign Context gate to READ from state.json rather than calling /context — absent/stale key emits CONTEXT CHECK REQUIRED once per session (BUILD-027); (D) cross-phase/cross-rail estimation fallback (INFRA-171). HIGH severity. `story_context.py`, `context_budget.py`, `flex_build.py`, `CLAUDE.build.md`. **SUPERSEDED by CER-054 (D2) — the Phase 65 / 70–74 rework addressed (1) and (3). Symptom (2) was tracked forward into CER-054, which investigated the "stuck at 25k" observability-surface symptom directly and RESOLVED cp-HARNESS007-main: root-caused to genuine fleet-project idle state (no Task/Agent spawns), not a writer bug — no code fix required, the SPA's staleness badge already surfaces it. No open action remains on this chain.** | Repo-E operator observation 2026-06-10 | 2026-06-10 | — |

| CER-175 | HIGH x2 (Phase 122 checkpoint re-run, post-INFRA-396): (1) skills/pairmode/scripts/reviewer_bash_guard.py's shadow-reviewer allowlist still admits an arbitrary-file-write vector via git's --output=<path>/--output <path> flag on git diff (verified live: 'git diff --output=/tmp/pwn.txt HEAD' -> ALLOW, and creates/truncates the target file) and via --exec-path=<dir> (verified live: 'git --exec-path=/tmp/evil log' -> ALLOW), directly contradicting the guard's own stated invariant that the shadow-reviewer has no legitimate write path via Bash. This is the same arbitrary-write class INFRA-396/CER-174 closed for shell chaining/substitution, now found via an unscreened git flag instead. (2) skills/pairmode/scripts/scope_guard.py's new agent_type=='shadow-reviewer' short-circuit (added by INFRA-396) compares the normalised path against the literal string '.pairmode-suggestions.md' but never strips the '.pairmode-worktrees/<story-id>/' prefix the way the builder path does (_strip_worktree_prefix) -- so an absolute-path write to <worktree>/.pairmode-suggestions.md (the shape the real Write tool call actually takes, since the shadow-reviewer is dispatched into the per-story worktree) is DENIED, while a relative-path write to the same logical file is allowed. INFRA-396's own end-to-end tests (tests/pairmode/test_pre_tool_use_scope_guard.py) all use relative file_path and did not catch this -- the shadow-reviewer's only sanctioned write channel is unreachable in production, silently voiding shadow_review=concurrent a second time. Found during the Phase 122 checkpoint security-auditor re-run immediately following INFRA-396's merge; CER-174's own two named defects were independently reverified as genuinely fixed by the same audit pass. **RESOLVED Phase 127 — INFRA-397:** `reviewer_bash_guard.py`'s shadow-reviewer branch now screens for git flags with write/redirect side effects (`--output`/`-o`, `--exec-path`, `-c`/`--config-env`, `-C`, `--git-dir`, `--work-tree`, `--namespace`, both attached and separated forms) anywhere in the token list, closing the arbitrary-file-write bypass via `git diff --output=<path>` and the exec-path redirect bypass, with the `reviewer` agent_type's own behavior for the same flags unchanged; `scope_guard.py`'s shadow-reviewer write-confinement now strips the `.pairmode-worktrees/<story-id>/` prefix via the same `resolve_call_story`/`_strip_worktree_prefix` path the builder branch already uses, so the shadow-reviewer's real absolute-path write to `<worktree>/.pairmode-suggestions.md` is correctly allowed while every other absolute path (including a different story's worktree) remains denied — both fixes have real end-to-end test coverage through `hooks/pre_tool_use.py`'s actual dispatch, reviewed PASS with no findings. | security-auditor | 2026-08-05 | 127 |

| CER-180 | HIGH x2 (Phase 123 checkpoint, first security-auditor pass): INFRA-391's audit.py _split_sections fix (CER-170) changed the .pairmode-overrides section-key format to match SKILL.md's documented no-## shape, but (1) skills/pairmode/templates/.pairmode-overrides.j2:9-13 -- the operator-facing bootstrap template every project actually receives -- still documents the OLD, now-broken ##-included format ('The section key is the full header line -- ## markers included -- lowercased and whitespace-collapsed', with examples 'CLAUDE.md:## review checklist' and '.claude/agents/reviewer.md:### checklist'), and no migration/dual-shape acceptance window was shipped, so every fleet project that followed the in-file instructions silently loses override suppression with no diagnostic (_check_overrides_health at audit.py:566-582 only reports parse errors, never a leading-# section key). (2) The same stranded-key-shape break defeats sync.py's destructive-write protection: the four call sites at sync.py:645,702,773,826 gate section replace/append/remove on (dest_rel, section_key) in overrides, so a project's previously-working ##-prefixed override entry no longer matches, and 'pairmode sync --apply' can now overwrite or prune a section the operator explicitly declared project-owned -- contradicting SKILL.md:216-217's promise that sync will never touch a declared override section. Also flagged, non-blocking: lesson_review.py's .pairmode-drift-rejected keys are built from the same now-changed shape and orphan previously-persisted rejections (MEDIUM); the '^#+\s*' strip is duplicated across audit.py and sync.py's _normalise_section_boundary with no shared helper (MEDIUM); a latent cross-depth key collision is possible for ## Foo vs ### Foo in the same file (MEDIUM, no live collision found in managed templates); sync.py's _header_from_key docstring is stale and the function is dead code (LOW). Fix direction: update .pairmode-overrides.j2 to the current no-## key format (with a worked example matching audit.py's actual comparison), and add either a dual-shape acceptance window in the override-matching code (accept both ##-prefixed and bare keys for one release) or a startup/audit-time diagnostic that flags a project's on-disk .pairmode-overrides entries using the stale ##-prefixed shape so operators can migrate deliberately rather than silently losing protection. **RESOLVED Phase 128 — INFRA-398:** `.pairmode-overrides.j2` rewritten to document the current marker-free section-key format `audit.py` actually compares against, verified by a template round-trip test asserting the worked example against real `_split_sections`/`_normalise` output; `_load_overrides_with_diagnostics` now accepts both the current and legacy `##`-prefixed key shapes (dual-shape acceptance) so existing fleet projects' override files keep working without an immediate edit; `_check_overrides_health` now emits an explicit diagnostic when it detects a legacy-shape entry, pointing operators at the fix. Reviewed PASS with no findings by both a formal reviewer and a shadow-reviewer; full suite green (5063 passed). | security-auditor | 2026-08-05 | 128 |

| CER-181 | HIGH (Phase 123 checkpoint, second security-auditor pass): skills/pairmode/scripts/pairmode_drift_report.py carries an independent, duplicated copy of audit.py's section-key derivation and override loading -- pairmode_drift_report.py:97 has its own _split_sections that still keys sections as '## review checklist' (no leading #+ strip), and :145 has its own _load_overrides with no strip either. CER-170 (INFRA-391) and CER-180 (INFRA-398) both fixed audit.py's copy and the operator-facing .pairmode-overrides.j2 template, but neither touched this second implementation, so an override written in the now-documented, correct format is honoured by 'pairmode audit'/'pairmode sync' but silently IGNORED by the live 'pairmode drift-report' command (SKILL.md:822) -- it reports the operator-declared section as DRIFT and offers it as a fleet-wide convergence candidate, i.e. recommends a canonical template change on the basis of divergence the operator already explicitly declared intentional. Proven by an executable repro through the real drift_report() entrypoint (auditor's own words): 'currently-documented ##-free override key was IGNORED by drift-report; section still reported as DRIFT'. Not CRITICAL: pairmode_drift_report.py performs no writes, so this is silently-wrong advisory output, not data corruption. tests/pairmode/test_drift_report.py:686,736 pin the legacy ##-prefixed shape exclusively, which is why the suite stayed green through both prior phases. Fix direction: pairmode_drift_report.py should import and reuse audit.py's _split_sections/_load_overrides (or their current successors) rather than maintaining an independent, now-diverged copy -- the same de-duplication direction CER-177 already flagged for the audit.py/sync.py '^#+\s*' strip duplication, but here the divergence is already live-wrong rather than latent. Update tests/pairmode/test_drift_report.py's pinned legacy shape to the current documented format as part of the fix. **RESOLVED Phase 129 — INFRA-399:** pairmode_drift_report.py's independent, stale copy of audit.py's section-key derivation and .pairmode-overrides loading was removed; it now imports and reuses audit.py's current _split_sections/_normalise/_load_overrides (with a thin re-normalising wrapper) instead of maintaining a diverged copy. Verified end-to-end through the real drift_report() entrypoint that a correctly-formatted override now suppresses the DRIFT finding it previously silently ignored, with no regression to other convergence-detection behavior and no circular import introduced. Reviewed PASS with no findings; full suite green (5071 passed). | security-auditor | 2026-08-05 | 129 |

| CER-188 | CRITICAL x2 + HIGH + MEDIUM (Phase 125 checkpoint, first security-auditor pass): CER-172's scrub (INFRA-393/394) is incomplete and can regress. (1) CRITICAL: a real private sibling-repo directory name (including its full /mnt/work/<name> absolute path) survives in 24+ committed files (e.g. docs/fleet-snapshot.md:42, docs/cer/backlog.md:48) because it was dropped from .pairmode-fleet.local.json during externalization (the pre-externalization _DOCUMENTED_CANDIDATES literal carried 16 entries; the local map carries only 15), so scrub_fleet_names.py never saw it -- docs/stories/INFRA/INFRA-394.md:163-167 incorrectly rationalizes the leftover as a 'decommissioned project' when the directory exists on disk. (2) CRITICAL: at least two further real sibling-repo names that exist on disk under /mnt/work were never added to the local map at all and were never scrubbed (one referenced 47 times, described in backlog prose as 'a non-pairmode-consumer sibling plugin repo'; another in docs/stories/RELEASE/RELEASE-069.md:25) -- nothing reconciles the map against the actual sibling-directory set, so a name absent from the map is silently declared clean. 'scrub_fleet_names --verify' derives its name universe from the same incomplete map, so it reports 'zero remaining real-name hits' while real names remain -- the acceptance gate itself is blind to what it's missing. (3) HIGH: fleet_discovery.py:508,586 writes the real absolute path straight into the git-tracked docs/fleet-snapshot.md on every default run (f"### " then snapshot_path.write_text(...)) -- INFRA-393 externalized the input list but left this output path untouched, so the next default fleet_discovery run re-introduces all real names into a committed file with nothing anonymizing at write time. (4) MEDIUM: scrub_fleet_names.py:349,352-354,531,534-536 prints the raw matched real name (not just file:line) to its own stdout when reporting hits, and :170-173 raises a ValueError embedding two real names in its message -- exactly the kind of output that gets pasted into a commit message, story doc, or CI log under this phase's own threat model. (5) MEDIUM: no mechanical gate exists anywhere (no hook, checkpoint step, pre-commit, or CI wiring) requiring 'scrub_fleet_names --verify' to pass before a commit lands -- regression prevention is entirely manual/operator-remembered. Fix direction: reconcile .pairmode-fleet.local.json against the actual /mnt/work sibling-directory set (or the prior _DOCUMENTED_CANDIDATES literal) and add a test asserting no candidate was silently dropped during externalization; anonymize fleet_discovery.py's snapshot write path (map real path to its label before writing, not just at read time); change scrub_fleet_names.py's hit-reporting and error paths to report the mapped label + file:line, never the matched literal; add a checkpoint-security-adjacent or pre-commit gate that runs 'scrub_fleet_names --verify' and blocks on a nonzero exit. Do NOT mark CER-172 resolved-and-closed until this is actually fixed -- it is currently marked RESOLVED while real names remain in tracked files. **RESOLVED Phase 130 — INFRA-400:** added a reconciliation check (`fleet_map.py`'s `unmapped_sibling_repos`, consumed by `scrub_fleet_names.py`'s `verify()`) so `--verify` now fails when the local fleet map under-covers the actual on-disk sibling-repo set, instead of silently passing; moved `fleet_discovery.py`'s snapshot-writing path to anonymize real paths to their mapped labels at write time, before composing the document, rather than relying only on a post-hoc scrub pass; removed the raw matched literal from `scrub_fleet_names.py`'s own hit-reporting and error-message output, which now reports the mapped label plus file:line only; and added a git pre-commit gate (a new `install-hook` subcommand) that runs `--verify` and blocks the commit on a nonzero exit. All new logic is covered by tests using only synthetic fixture names; both the reviewer and shadow-reviewer independently confirmed no real repo name appears anywhere in the diff. Git-history remediation of real names in pre-scrub commits remains separately tracked and deferred as CER-192; one-time reconciliation of the actual local fleet map against the real on-disk sibling set is a manual operator action outside this story's automated scope. | security-auditor | 2026-08-05 | 130 |

| CER-194 | CRITICAL + HIGH x2 (Phase 125 checkpoint, second security-auditor pass): INFRA-400's CER-188 fix shipped a reconciliation/anonymization mechanism that is either broken or incompletely wired. (1) CRITICAL: skills/pairmode/scripts/fleet_map.py:103's sibling_repo_dirs() raises an unhandled PermissionError from its (p / '.git').exists() probe on an ordinary unreadable directory under the default fleet root, contradicting its own docstring ('Returns [] when fleet_root doesn't exist or isn't a directory -- never raises'). The exception propagates through unmapped_sibling_repos -> _reconcile_fleet_root (scrub_fleet_names.py:170) -> verify (scrub_fleet_names.py:346), so 'scrub_fleet_names.py --verify' currently aborts before scanning anything at all on this machine -- the gate is not merely blind, it's inoperable. Fix: catch PermissionError (and other OSError subclasses) per-candidate-directory inside sibling_repo_dirs and skip that entry rather than propagating, matching the documented never-raises contract. (2) HIGH: fleet_discovery.py's _anonymize_results_for_snapshot (~line 488) maps only each result's 'path' key -- the snapshot writer then emits two further unanonymized raw path strings into the tracked docs/fleet-snapshot.md: signal1_value (~line 569) and signal1_absent_detail (~line 577, an arbitrary absolute path read out of a third-party CLAUDE.build.md that can carry a real sibling-repo directory name straight into the tracked snapshot). The CLI print path (~lines 734,756,760,770) likewise still echoes raw real absolute paths to stdout, capturable into an agent transcript and re-committed. Fix: anonymize signal1_value/signal1_absent_detail using the same path-to-label mapping before they're written or printed anywhere. (3) HIGH: the new git pre-commit gate (scrub_fleet_names.py's install-hook subcommand, from INFRA-400) is documented as operator-invoked-once-per-checkout but nothing (no checkpoint step, runbook, bootstrap path, or tracked doc) actually invokes or requires it -- .git/hooks/pre-commit does not exist in this checkout, so the regression gate CER-188 asked for currently protects nothing. Compounding: if it were installed now it would inherit finding (1)'s PermissionError and abort every commit. Fix: wire install-hook into an existing operational touchpoint (bootstrap.py, a checkpoint-security step, or equivalent) so it's actually applied, not merely available, and document the expectation. Note: the underlying map-vs-disk data gap (real names still present in tracked files because the local map was never reconciled against the real sibling set) is being handled directly by the operator in-session, NOT via this CER -- this CER covers only the code-level bugs (crash, incomplete anonymization coverage, unwired gate). **RESOLVED Phase 131 — INFRA-401:** `fleet_map.py`'s `sibling_repo_dirs()` now catches `OSError`/`PermissionError` per-candidate directory and skips it instead of raising, restoring the documented never-raises contract and fixing `scrub_fleet_names.py --verify`'s previous crash. `fleet_discovery.py`'s write-time anonymization now also covers the `signal1_value`/`signal1_absent_detail` free-text fields (substituting any mapped or on-disk sibling-repo path found within them) at both the snapshot-file and CLI/`--json` output boundaries, not just the primary result path. `scrub_fleet_names.py`'s `install_hook()` now returns a five-outcome status (installed/already-installed/not-a-git-repo/worktree/foreign-hook) and `bootstrap.py` calls it on every normal bootstrap run as a new step, so the pre-commit regression gate is now actually wired into an operational touchpoint instead of sitting unused. `docs/architecture.md` documents the new wiring. Reviewed PASS with no findings by both a formal reviewer and a shadow-reviewer, both confirming no real repo name appears anywhere in the diff; full suite green (5109 passed, 0 failed). | security-auditor | 2026-08-05 | 131 |

| CER-195 | MEDIUM (Phase 125 checkpoint follow-up, found during operator-directed real-name reconciliation): scrub_fleet_names.py's --verify reconciliation (added by INFRA-400/401, CER-188/194) treats every on-disk sibling directory under the fleet root that is absent from .pairmode-fleet.local.json as an unmapped-and-therefore-failing entry, with no way to mark a sibling as intentionally excluded from anonymization. In practice several sibling directories are NOT private third-party fleet clients and should never be added to the anonymized map: some are this project's own documented architecture/history (already legitimately named in committed docs and CLI subcommand names), and others are the operator's own already-otherwise-published projects. Without an exclusion mechanism, --verify can never report a clean pass -- it will permanently list these directories as 'unmapped', indistinguishable from a genuine new leak, defeating the gate's purpose as something that can be trusted to flag only real problems. Fix direction: add a small, local-config-only exclusion list (e.g. a reserved key or a sibling gitignored file alongside .pairmode-fleet.local.json, following the same 'real names never in committed source' contract CER-172/188/194 already established) that scrub_fleet_names.py's reconciliation check consults -- a directory present in the exclusion list is treated as deliberately out of scope, not as an unmapped failure. The committed .pairmode-fleet.local.json.example template should document the exclusion mechanism's shape using only synthetic names. Populating the actual exclusion list with real directory names is a manual, local, non-automated operator action (same category as populating the map itself) -- not something a builder subagent should do. **RESOLVED Phase 132 — INFRA-402:** added a reserved `_excluded` config key (`fleet_map.py`: `EXCLUDED_REPOS_CONFIG_KEY`, `RESERVED_CONFIG_KEYS`, `excluded_repo_names`) subtracted from `unmapped_sibling_repos`, a mapped/excluded conflict check, and expanded mapped/excluded/unmapped counts in `scrub_fleet_names.py`'s `verify()` success line; `.pairmode-fleet.local.json.example` documents the mechanism with a synthetic name only. | operator | 2026-08-05 | 132 |

| CER-196 | HIGH: .pairmode-fleet.local.json.example (added/changed by INFRA-402, Phase 132) leads with six `//` comment lines, making the committed template invalid JSON. fleet_map.load_local_fleet_map (skills/pairmode/scripts/fleet_map.py:49-58) swallows json.JSONDecodeError and returns {}, so scrub_fleet_names.verify() (skills/pairmode/scripts/scrub_fleet_names.py:341-343) prints 'no local fleet config found, skipping verification' and returns 0 for anyone who copies the template verbatim -- the anonymization/leak-prevention gate silently fails open, and no test parses the committed template so nothing catches it. Found by the Phase 125 checkpoint security-auditor run. Fix direction: make the .example template either valid JSON (move guidance into a sibling README/doc comment or JSON5-safe key names) or make load_local_fleet_map fail loudly (non-empty stderr / non-zero verify exit) on a JSON parse error instead of silently treating it as absent, plus a test that parses the committed template as JSON. skills/pairmode/scripts/fleet_map.py, skills/pairmode/scripts/scrub_fleet_names.py, .pairmode-fleet.local.json.example, tests/pairmode/test_scrub_fleet_names.py. **RESOLVED Phase 133 — INFRA-403:** `.pairmode-fleet.local.json.example` is now valid JSON (guidance moved to a `_comment` reserved key, filtered out of `repo_entries`); `fleet_map.load_local_fleet_map` now raises `FleetMapConfigError` on malformed JSON (absent-file behavior unchanged, still `{}`), and `scrub_fleet_names.py`'s `apply`/`verify`/`apply_lessons`/`verify_lessons` all propagate the failure so `main()` reports a non-zero exit instead of a silent pass; `fleet_discovery.py` gained a narrowly-scoped local catch to preserve its own documented never-raises contract. New test asserts the committed template parses as JSON. | security-auditor | 2026-08-05 | 132 |



---

## Do Later

Important, not urgent. Quality improvements, architectural refinements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|



| CER-160 | MEDIUM: a worker resolving the hardcoded absolute path to `flex-harness`'s copy of `skills/pairmode/skills/spec-writer/procedure.md` (per INFRA-304's E13 rationale for using that path) can silently run a stale, pre-checkpoint-promotion version of the procedure mid-phase. Reproduced live during INFRA-362's dogfood exercise (Phase 118): a spec-writer instructed to use that absolute path found a 298-line, five-bounded-input procedure with no narrative step at all, while the correct in-repo copy (already updated by INFRA-355/357, same phase) was 381 lines with six inputs and Steps 4c/4d. The release-channel design (harness only updated at checkpoint-tag) means any worker resolving a hardcoded harness-absolute path is running last-checkpoint's tooling by construction — worth auditing every other hardcoded `flex-harness` path reference (agent shells, skill docs) for the same staleness risk, and considering whether procedure/skill docs specifically should resolve from the project's own tree instead of the pinned harness copy. `skills/pairmode/skills/spec-writer/procedure.md`, any other absolute-`flex-harness`-path reference. | INFRA-362 dogfood exercise (real spec-writer run against INFRA-363), Phase 118 | 2026-08-03 | 118 |

| CER-161 | MEDIUM: INFRA-355 shipped a spec-writer contract inconsistency between code comment and procedure text. `story_new.py`'s stub-template comment says "a human **or spec-writer** decides which narrative role(s) apply, never auto-inferred" — implying the spec-writer may set `narrative_roles:`. But `skills/pairmode/skills/spec-writer/procedure.md`'s Step 4 drafting rules forbid changing any frontmatter field except `model:`/`reviewer_model:`, with no carve-out for `narrative_roles:`; Step 4c only *reads* the field, nothing in the procedure authorizes *writing* it. A cold spec-writer following the procedure literally would refuse the exact judgment call `story_new.py`'s own comment says it should make. Separately, the same story left `docs/narratives/SPEC-WRITER/SPEC-WRITER-000-ideology.md` stale relative to its own landed mechanism: `## Always true` still says "exactly five bounded inputs" (procedure now says six); `## Never` still says "Never edits any file except the single story file it was given" — which the procedure's own Step 4c `stories:` backfill (a compliant, intended action) now violates as literally written, meaning an intent-reviewer applying `## Never` mechanically would flag correct behavior as a violation; `## Open gaps` items on missing narrative input and missing size ceiling/brevity counterweight are closed by INFRA-355/357 but not marked resolved. Found via INFRA-362's real dogfood spec-writer run against INFRA-363 (Phase 118). `skills/pairmode/skills/spec-writer/procedure.md`, `skills/pairmode/scripts/story_new.py`, `docs/narratives/SPEC-WRITER/SPEC-WRITER-000-ideology.md`. **RESOLVED Phase 118 — INFRA-362:** fixed inline as directly caused by this phase's own work — `procedure.md` Step 4 now names the `narrative_roles:` exception explicitly; `SPEC-WRITER-000-ideology.md`'s `## Always true` (five → six inputs), `## Never` (backfill carve-out), and `## Open gaps` (two items marked resolved with INFRA-355/357 pointers) are corrected. | INFRA-362 dogfood exercise (real spec-writer run against INFRA-363), Phase 118 | 2026-08-03 | 118 |

| CER-162 | LOW: `docs/narratives/SPEC-WRITER/SPEC-WRITER-000-ideology.md`'s `## Narrative` section states "Builder attempts on the largest specs run roughly 50% higher than on the earliest ones" as load-bearing evidence for its `## Open gaps` section. INFRA-363 (Phase 119, drafted concurrently with this phase) independently re-measured this exact statistic (n=90 stories, quartile analysis) and found it wrong by roughly 5x: the real attempt-count delta is ~10% (1.32 vs 1.45), with the actual cost driver being duration-per-attempt (7.4x), not attempt count. INFRA-363's own Ensures only corrects this identical debunked claim in `docs/stories/INFRA/INFRA-357.md`'s Context — it does not touch the SPEC-WRITER narrative that states the same false number, even though that narrative is INFRA-363's own cited `narrative_roles:` bounded input (added during INFRA-362's dogfood exercise). Found via a real intent-reviewer narrative-alignment run against INFRA-363, which returned FAIL on this exact gap. Separately, the same procedure's Step 2 item 4 prefers an exemplar "roughly 14-36 lines," but the shortest complete INFRA story as of Phase 118 is 84 lines (the ten most recent range 84-129) — the stated baseline is a historical measurement, not a currently selectable target, so the "unless no shorter complete story exists" escape hatch fires on every run. `docs/narratives/SPEC-WRITER/SPEC-WRITER-000-ideology.md`, `docs/stories/INFRA/INFRA-363.md`, `skills/pairmode/skills/spec-writer/procedure.md`. **RESOLVED Phase 119 — INFRA-363**: `SPEC-WRITER-000-ideology.md`'s `## Narrative` and `## Open gaps` sections now state the corrected duration-per-attempt effect (7.4x time for 2.3x file-surface, ~10% attempt-count delta) in place of the debunked ~50% figure; `docs/stories/INFRA/INFRA-357.md`'s Context carries the same correction as an appended note; and the Step 2 item 4 "roughly 14-36 lines" range-based exemplar selection was replaced entirely by a frozen exemplar file (`docs/exemplars/EXEMPLAR-000.md`), so the stale-range escape-hatch problem no longer applies (there is no range left to go stale). | INFRA-362 dogfood exercise (real intent-reviewer run against INFRA-363), Phase 118 | 2026-08-03 | 118 |

| CER-164 | MEDIUM: INFRA-376 (Phase 119) removed `Bash` from the shadow-reviewer agent-shell's `tools:` grant (correctly, closing CER-163's bypass), but `skills/pairmode/skills/shadow-reviewer/procedure.md` was left unchanged — it still instructs the role to poll `git log`/`git status --porcelain`/`git diff` (needs Bash) and append entries to `.pairmode-suggestions.md` (needs Write/Edit/Bash). `CLAUDE.build.md:24` dispatches the role live when `shadow_review == "concurrent"`; flex itself is opted out so there is no live impact here, but a downstream project that opts in gets a shadow-reviewer that cannot read git state or write a single suggestion, silently returning `suggestions_written: 0` indistinguishable from "nothing worth flagging." INFRA-376's own premise ("the suggestion write is performed by the orchestrator/harness, not by the agent") has no corresponding implementation anywhere. Also leaves INFRA-365's `.pairmode-suggestions.md` `STANDING_SURFACES` allowlist entry (`scope_guard.py:67`) permanently unreachable. Fix direction: either restore a narrowly-scoped write capability (e.g. `Edit` limited to the suggestions file, if the harness supports per-tool path scoping) or rewrite the procedure to describe an orchestrator-mediated read→relay flow that never needs the agent itself to write. `skills/pairmode/skills/shadow-reviewer/procedure.md`, `CLAUDE.build.md`, `skills/pairmode/scripts/scope_guard.py`. **RESOLVED Phase 122 — INFRA-388:** restored the shadow-reviewer agent shell's `tools:` grant to `[Read, Write, Bash]`, added a strict default-deny `shadow-reviewer` branch to `reviewer_bash_guard.py` (git log/status/diff only, no redirection/pipes, no non-git commands), and rewrote `skills/pairmode/skills/shadow-reviewer/procedure.md` to match the real write mechanism — then flipped `CLAUDE.build.md`'s `shadow_review` to `concurrent`. | Phase-119 checkpoint-security audit (post-INFRA-376) | 2026-08-03 | 119 |

| CER-165 | MEDIUM: INFRA-372 (Phase 119) added a dedicated `.pairmode-overrides` existence/parse-health check to `audit.py`, but that file is deliberately absent from `sync.py`'s `CANONICAL_FILES`/`SCAFFOLD_FILES` (`_dest_to_template` returns `None` for it by design), so `sync_project`'s missing-file remediation loop silently skips it (`if template_rel is None: continue`). Reproduced live on this checkout: `audit.py --project-dir .` reports `.pairmode-overrides: File missing entirely` and then recommends `/flex:pairmode sync` — a remediation that structurally cannot resolve the finding, since sync never scaffolds this file. Every project without a hand-created `.pairmode-overrides` now carries a permanent, unresolvable MISSING finding. Fix direction: either scaffold an empty/commented `.pairmode-overrides` template via `bootstrap.py` (so the file exists from project creation and the audit finding becomes a real signal) or have `audit.py`'s MISSING finding for this specific file point at the correct manual-creation step instead of the generic sync recommendation. `skills/pairmode/scripts/audit.py`, `skills/pairmode/scripts/sync.py`, `skills/pairmode/scripts/bootstrap.py`. | Phase-119 checkpoint-security audit (post-INFRA-372) | 2026-08-03 | 119 |

| CER-166 | LOW: `hooks/post_tool_use.py`'s new `context_current_tokens_source` write (INFRA-374) is not named in `skills/pairmode/skills/security-auditor/procedure.md` § check 1's authorized-write enumeration for that hook. The write is legitimate (documented in `docs/architecture.md:3046`, a provenance stamp on a field the hook is already authorized to write) but a future security-auditor has to independently re-derive that it's in-scope rather than checking it against an up-to-date list. Fix: add the key to check 1's enumeration. `skills/pairmode/skills/security-auditor/procedure.md`. | Phase-119 checkpoint-security audit (post-INFRA-374) | 2026-08-03 | 119 |

| CER-167 | LOW: `skills/pairmode/scripts/story_new.py`'s `_story_frontmatter` interpolates `--primary-file` CLI values into YAML frontmatter unquoted (`lines.append(f"  - {pf}")`), the same defect class CER-163 fixed for `bootstrap.py`'s `--operator-note`. A path argument containing `: `, a leading quote, or an embedded newline produces malformed frontmatter. Low severity since the value is operator-supplied and non-privileged (a typo, not an attack surface), but the same YAML-serialisation fix pattern applies. `skills/pairmode/scripts/story_new.py` (`_story_frontmatter`, INFRA-370's `--primary-file` option). | Phase-119 checkpoint-security audit (post-INFRA-370) | 2026-08-03 | 119 |

| CER-163 | LOW: three sub-HIGH observations from the phase-118 checkpoint-security re-audit (after INFRA-365/366 closed the two HIGH findings), not individually blocking but worth tracking: (1) flex's own `.claude/agents/shadow-reviewer.md` was never synced via `sync-agents`, so the shadow-reviewer role INFRA-358/359 built is currently unreachable in this repo's own checkout — dogfood gap, not a downstream-project issue; (2) the shadow-reviewer's agent-shell template (`skills/pairmode/templates/agents/shadow-reviewer.md.j2`) grants `tools: [Read, Bash]`, meaning a real dispatched shadow-reviewer could append to `.pairmode-suggestions.md` via a `Bash` heredoc/append rather than `Edit`/`Write`, bypassing `scope_guard.py`'s `pre_tool_use.py` enforcement path entirely (INFRA-365 only closed the `Edit`/`Write` route); (3) `bootstrap.py` interpolates the free-text `--operator-note` CLI value into a frontmatter-bearing markdown file (`OPERATOR-010-project.md`) unescaped — a note containing `---` or YAML-special characters could corrupt that file's frontmatter block. `.claude/agents/shadow-reviewer.md` (missing), `skills/pairmode/templates/agents/shadow-reviewer.md.j2`, `skills/pairmode/scripts/bootstrap.py`. | Phase-118 checkpoint-security re-audit (post-INFRA-365/366) | 2026-08-03 | 118 |

| CER-159 | HIGH (unconfirmed root cause — needs live-session investigation before a fix can be specced): the `PostToolUse` hook that is supposed to write `context_current_tokens` after every `Task`/`Agent` spawn (`hooks/hooks.json`'s `PostToolUse` block, matcher `Task\|Agent\|SendMessage` → `hooks/post_tool_use.py`) has not fired once during a live orchestrator session that ran ~40+ `Agent` spawns plus dozens of `Edit`/`Bash` calls across roughly two days (2026-08-01 to 2026-08-02, the Phase 117 INFRA-336–347 build). Evidence: `.companion/state.json`'s `context_current_tokens_recorded_at` is stamped `2026-07-31T05:56:18` — over a day *before* this session started — while the file's own mtime is fresh (`2026-08-02T13:19:28`, same day as the session), because unrelated fields (`current_stories`, `attempt_counter`, `gate_verdict`, `recently_discarded_stories`) were being written directly by the orchestrator's own `flex_build.py` CLI calls throughout, with zero hook-driven `context_current_tokens` writes interleaved. Ruled out as a simple non-install: `~/.claude.json`'s `pluginUsage.flex@inline` shows heavy, recent use (52,183 total invocations, very recent `lastUsedAt`), and `hooks/hooks.json` (the correct plugin-level registration point, not `.claude/settings.json`) correctly declares `PreToolUse` on `Task\|Agent\|Edit\|Write\|Read\|Bash`, `PostToolUse` on `Task\|Agent\|SendMessage` and `Write\|Edit\|MultiEdit`, plus `SubagentStop`/`SessionStart`/`SessionEnd`/`UserPromptSubmit` — a complete, correct manifest on paper. Comparative control check across the fleet's `.companion/state.json` files found mixed results, not a universal break: `Repo-E` and `Repo-L` show `context_current_tokens_recorded_at` matching their state.json's last-write timestamp to the second (hook actively firing there, at least as of each project's last session); `Repo-B` and `Repo-G` show smaller staleness gaps (hours to days); flex's own repo shows the largest gap (32+ hours) and, uniquely, zero interleaved hook writes across an entire long-running multi-hour session — suggesting this may be specific to this session's shape (a single very long-running orchestrator conversation issuing many `Agent` spawns) or to flex's own dogfooding install, rather than a fleet-wide regression. This directly explains why no context-budget alarm fired despite the operator's own estimate that the session was running ~2x over its expected budget — the `should_block()`/`decide()` predicate downstream of `context_current_tokens` never received fresh data to act on. Distinct from (but the same recurring failure family as) CER-067/CER-049/CER-039/CER-045/CER-047, all previously-resolved instances of this same gate silently not functioning — this is the first occurrence traced specifically to flex's own plugin-hook install rather than a downstream bootstrapped project. Investigation still needed before a fix can be specced: confirm whether `hooks/post_tool_use.py` is actually invoked at all in this session shape (no host-level hook-invocation log was available from inside the sandbox to check directly); if it is invoked, determine why its `Task\|Agent` branch's write is not landing (silent exception being swallowed, `${CLAUDE_PLUGIN_ROOT}` resolution issue, a session-type this hook doesn't handle); if it is not invoked at all, determine what differs about this session's hook-engagement compared to `Repo-E`/`Repo-L`'s working sessions. `hooks/hooks.json`, `hooks/post_tool_use.py`, `hooks/pre_tool_use.py`, `skills/pairmode/scripts/context_budget.py`, `.companion/state.json`. **CONFIRMED, still unresolved (2026-08-04):** upgraded from "unconfirmed root cause" — `.companion/effort_recording.log` (written exclusively by `hooks/post_tool_use.py`'s Task/Agent branch, the same dispatcher that writes `context_current_tokens`) has its last entry at `2026-07-31T05:52:22Z` and zero entries since, despite phases 115–119 (2026-07-31 through 2026-08-03, per git log) running dozens of builder/reviewer/security-auditor/intent-reviewer Agent spawns in this exact repo. `.companion/state.json`'s `context_sessions` map corroborates: no session entries newer than 2026-07-31, though the file's own mtime is current (state is being written by other paths, just not this hook's branch). This is no longer a single-session anomaly — it is total, sustained loss of this hook's execution for ~4 days and 5 full phases, specifically on this repo's `flex@inline` plugin registration (confirmed not marketplace-installed: `~/.claude/plugins/known_marketplaces.json` lists only `claude-plugins-official`). Root cause for *why* the hook stopped firing around 2026-07-31 05:52 is still open — the last `hooks/hooks.json` edit (`e2389fb6`, 2026-07-30 23:36) predates the last successful fire by ~6 hours, so that edit did not immediately break it; something changed later, either at the Claude Code CLI/session level or in how this specific plugin install resolves hook registration. Not yet investigated: whether this reproduces live in a *fresh* session (this evidence is all from log/state artifacts, not a direct real-time hook trace), and whether it affects `PreToolUse` too or is scoped to `PostToolUse` Task/Agent specifically. Era-004 was closed (2026-08-04, tag `cp-119`) with this item still open in `## Do Later` and undisclosed in the closeout doc's qualifier — see `docs/eras/004-flex-operational-closeout-and-0-3-1.md` reopening note. **Root cause identified (2026-08-04, high confidence, not fully proven):** direct standalone invocation of `hooks/post_tool_use.py` (synthetic Agent-spawn payload, real subprocess call) works correctly — writes both `effort_recording.log` and `context_sessions` cleanly. The script is not the bug. `hooks/hooks.json`'s actual command is `python3 ${CLAUDE_PLUGIN_ROOT}/hooks/post_tool_use.py`; with `CLAUDE_PLUGIN_ROOT` unset (confirmed empty in this session's own shell env — `echo $CLAUDE_PLUGIN_ROOT` → empty), that expands to `python3 /hooks/post_tool_use.py`, which fails instantly with `FileNotFoundError` before any Python code runs — no exception for `post_tool_use.py`'s own try/except to swallow, because the process never starts. This matches the last live session's own recorded `pre_tool_hook_duration_ms` distribution (p50 = 1ms, p99 ≈ 87ms — bimodal, consistent with most invocations failing near-instantly on exec rather than actually starting Python, which takes tens of ms for this hook's imports). Working hypothesis: flex is registered as `flex@inline` — a self-referential plugin (this repo IS the plugin root), not a marketplace install (`~/.claude/plugins/known_marketplaces.json` lists only `claude-plugins-official`) — and `CLAUDE_PLUGIN_ROOT` may never get populated for that registration path the way it does for a marketplace-installed plugin. Not confirmed at the harness level (no way to directly instrument the CLI's actual hook-subprocess environment from inside this sandboxed session); the standalone repro and statistical fit are strong but circumstantial. Fix direction once confirmed: make `hooks.json`'s commands resolve the script path without depending on `${CLAUDE_PLUGIN_ROOT}` for inline/self-referential installs (e.g. a wrapper that falls back to a path relative to `hooks.json` itself when the env var is empty), or file upstream against the CLI if inline-plugin env-var population is the actual gap. **Cross-machine corroboration (2026-08-04):** a second machine's install was audited and found in a *different* broken state — flex there IS registered as a proper directory-source marketplace plugin (`~/.claude/settings.json` `extraKnownMarketplaces`/`enabledPlugins`, mirrored in `known_marketplaces.json`, installed to `~/.claude/plugins/cache/nullvalues-flex/flex/0.3.0`), and its hooks **do** fire (`CLAUDE_PLUGIN_ROOT` correctly resolves to the cache dir) — but they run a stale snapshot frozen at install time (2026-07-28, commit `0bab2ee`), 41 files behind the source tree's `961a6e1`, missing `hooks/subagent_stop.py` entirely (didn't exist yet at install). This machine (`/mnt/work/flex`) was directly re-verified and confirmed to have *no* marketplace registration at all for flex — no `extraKnownMarketplaces`, no `enabledPlugins` entry, no cache dir; purely the implicit inline auto-load. The two machines corroborate rather than conflict: a directory-source marketplace install does populate `CLAUDE_PLUGIN_ROOT` and get hooks running (supporting the fix direction), but installs are version-keyed snapshot copies, not live links — a reinstall against an unchanged version string can silently no-op onto stale cached code. Any fix for CER-159 that moves flex to a marketplace install must pair with a version-bump discipline (bump `plugin.json`/`marketplace.json` before every reinstall) or risk trading "hooks don't fire" for "hooks fire but run silently outdated code" — arguably worse, since it's not observable the same way (log entries keep appearing, just from stale logic). **CONFIRMED via direct test (2026-08-04):** `flex@nullvalues-flex` installed from a `cp-119`/`0.3.1` snapshot via `claude plugin marketplace add`/`install` (directory source), then exercised from an isolated scratch directory with no `.claude-plugin/plugin.json` of its own (so `flex@inline` could not have fired). `effort_recording.log` shows real, fresh entries at the correct timestamp for the Agent spawn, including what looks like a working `SubagentStop` relay (`skip:no-state` entries carrying `agent_id`) — a hook that was entirely absent from the other machine's stale cache. This directly confirms the root cause: `hooks/post_tool_use.py` and the hook dispatch mechanism are not broken; the `@inline` self-referential plugin registration this repo has been running under does not populate `CLAUDE_PLUGIN_ROOT`, and every hook invocation has been failing on exec before any Python code runs. **Not yet re-verified inside `/mnt/work/flex` itself** (the test above used a separate scratch dir, deliberately, to isolate the variable) — next step is confirming the fix holds when actually building in the real repo, and specifying the operational fix (migrate this repo's own working session off `@inline` onto the marketplace-installed + version-bump workflow) as a phase story per the spec-before-build policy. This is a distribution/registration fix, not a code defect in `hooks.json`/`post_tool_use.py` — those were exonerated by this test. **RESOLVED 120 — @inline auto-load does not populate ${CLAUDE_PLUGIN_ROOT}, causing all hook commands to fail silently on exec before any Python code runs; fix is marketplace-installed plugin from cloned tag snapshot, procedure documented in architecture.md § Self-hosted plugin installation.** **Accepted limitation, not further pursued (Phase 120 — INFRA-384):** the marketplace-install fix above does not stop the `@inline` registration from also loading in the same session — `~/.claude.json`'s `pluginUsage` map confirms both `flex@inline` and `flex@nullvalues-flex` incrementing together across a restart, and `claude plugin disable flex@inline -s project` (exit 0, writes `"enabledPlugins": {"flex@inline": false}` to `.claude/settings.json`) has no functional effect on the inline auto-load. This is an upstream Claude Code CLI gap (no supported way to suppress an implicit inline auto-load once a marketplace install of the same plugin also exists) and is not fixable in this project; the accepted cost is wasted hook exec cycles and misleading `pluginUsage` telemetry, with no correctness regression. Documented in full in `docs/architecture.md` § Accepted limitation: `@inline` / marketplace dual-registration. Not re-opening this row — recorded here as an annotation rather than a new CER row since it is a direct corollary of this row's own investigation. | Live orchestrator session, Phase 117 build (INFRA-336–347), investigated 2026-08-02; reconfirmed via `.companion/effort_recording.log` + `context_sessions` audit and root-caused via direct hook reproduction, 2026-08-04 | 2026-08-04 | — |

| CER-146 | LOW: `tests/pairmode/test_pairmode_migrate.py::test_to030_relocates_stale_flex_harness_hook_command` fails whenever the pairmode test suite is physically run from inside a checkout literally named `flex-harness` (e.g. `/mnt/work/flex-harness`) — confirmed pre-existing (reproduces at cp-115/`b373d7e1`, not introduced by any Phase 116 story). The test asserts the migrated hook command does not contain the substring `"flex-harness"`, but the migration code (or the fixture project's derived path) picks up the literal directory name of wherever `pairmode_migrate.py`'s own module lives, which legitimately contains `flex-harness` when running from that checkout — a test-environment coupling bug, not a real migration defect (the flex-harness checkout's own hook commands correctly reference its own path). Surfaced during Phase 116's checkpoint-tag promotion (`cp-116` merge into flex-harness's `fold-prep`, session 2026-08-01) when the full suite was run post-merge inside `/mnt/work/flex-harness` for the first time. Fix direction: parameterize the test's fixture project path/name so the assertion is independent of the literal directory name the suite happens to run from, or assert on a synthetic marker rather than the substring `"flex-harness"`. `tests/pairmode/test_pairmode_migrate.py`. **RESOLVED Phase 119 — INFRA-369:** the test's fixture project path is now parameterized rather than keyed on the literal `flex-harness` substring, so the assertion no longer depends on the checkout directory name it happens to run from. | Phase 116 checkpoint-tag promotion, full suite run inside /mnt/work/flex-harness (2026-08-01) | 2026-08-01 | — |

| CER-147 | MEDIUM: `attempt_counter.json`'s writers (`write_attempt_count`, `bump_attempt_count`, `clear`) do a lock-free read-modify-write of the whole counter map, unlike every `state.json` writer migrated under INFRA-285/CER-097's `state_lock`. Under the declared parallel-build model (Phase 109's target capability), two near-simultaneous FAIL bumps for different in-flight stories, or a merge-clear racing a sibling story's bump, can silently lose an update — an understated `attempt_count` that delays or skips escalation. `effort.db` got WAL + `BEGIN IMMEDIATE` for the analogous concurrency risk (CER-096); the control-state counter got nothing. Fix direction: route through the existing `state_lock` (or an equivalent file lock scoped to `attempt_counter.json`), same pattern already used for `state.json`. `skills/pairmode/scripts/flex_build.py` (`write_attempt_count`/`bump_attempt_count`/counter `clear`). **Absorbed at spec time by INFRA-336 (Phase 117)** — folded into the escalation-ladder fix rather than deferred, since it's the same file/subsystem; resolution annotation lands when that story completes. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, corroborated by both reviewers as fable finding H4 / opus finding M3) | 2026-08-01 | 117 |

| CER-148 | MEDIUM: a double-FAIL-in-one-cycle (builder self-reports FAIL, then the reviewer also FAILs the same worktree) can double-bump the attempt counter for what is semantically one failed cycle, collapsing the 3-strike escalation ladder to roughly 1.5 real cycles before loop-breaker fires. The bump paths key only on `story_id` + `outcome=="FAIL"`, blind to which role produced the FAIL. Medium confidence — depends on whether the orchestrator's documented pseudocode (`CLAUDE.build.md`) always spawns the reviewer unconditionally even after a builder self-reported FAIL (it does, per the current loop text), so this is a real risk unless that dispatch behavior changes. `skills/pairmode/scripts/subagent_transcript.py` (`RECORDABLE_SUBAGENT_ROLES`, bump call sites). **Absorbed at spec time by INFRA-336 (Phase 117)** — folded into the escalation-ladder fix rather than deferred, since it's the same file/subsystem; resolution annotation lands when that story completes. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, fable finding H3) | 2026-08-01 | 117 |

| CER-145 | LOW: three consecutive Phase 116 stories against `skills/pairmode/scripts/model_selector.py` (INFRA-333, INFRA-334, and their common test companion) all omitted `tests/pairmode/test_model_selector.py` from `touches:`, even though it's the direct unit-test file for the story's own `primary_files:` module — each time, the builder correctly wrote/updated the tests, the reviewer correctly flagged the file as undeclared scope per rail-scope discipline and left it uncommitted, and the orchestrator had to widen `touches:` after the fact to avoid losing the coverage (INFRA-333: commit cf94af3c; INFRA-334: commit 8cbf3abf). The spec-writer procedure has no rule that a `primary_files:` entry's own conventional test path (`tests/pairmode/test_<module>.py`) should be auto-included in `touches:` by default. Fix direction: either the spec-writer procedure auto-derives and includes the primary module's test file, or `story_new.py`'s scaffolding does it mechanically before spec-writer elaboration ever runs. `skills/pairmode/skills/spec-writer/procedure.md`, `skills/pairmode/scripts/story_new.py`. **RESOLVED Phase 119 — INFRA-370:** `story_new.py` gained a `derive_test_paths()` helper and repeatable `--primary-file` CLI option that merges each primary file's conventional test path into scaffolded `touches:`; the spec-writer procedure's Step 4 documents the same auto-derivation rule. | Recurring pattern across INFRA-333/334 reviews (session 2026-07-31) | 2026-07-31 | — |

| CER-144 | LOW: `docs/eras/003-flex-orchestrator-as-harness.md` still carries `status: active` alongside `docs/eras/004-flex-operational-closeout-and-0-3-1.md` (also `active`) — `record-checkpoint-step checkpoint-tag` warns on this dual-active state (INFRA-326's tie-break exists to pick the right one when it happens, but the underlying data drift was never cleaned up). Era 003's own summary states formal close was "gated on phase 108"; phase 108 was subsequently deferred/superseded by phases 113-116/INFRA-310 (2026-07-30), so era 003's close condition was silently invalidated with no corresponding status update. Fix: either formally close era 003 (write its final `## Era summary`, flip `status: closed`) now that its gating phase is a known-superseded dead end, or explicitly restate what condition still gates its close. **RESOLVED Phase 116 — INFRA-310: era 003 formally closed by ID (`status: complete`, `closed_at: 2026-08-01`) via INFRA-314's gated by-ID transition path, after phases 106/107/108 were each dispositioned and the era's `## Exit criterion` section was written; exactly one era (004) reads `status: active` after the close.** `docs/eras/003-flex-orchestrator-as-harness.md`. | cp-115 checkpoint-tag warning (record-checkpoint-step) | 2026-07-31 | — |

| CER-108 | `pairmode_migrate.py` rules 9/10 lack a `name:` rewrite: a migrated anchor project keeps `name: anchor:pairmode`/`anchor:companion` in SKILL.md frontmatter while rule 8 normalizes seed to bare `seed` (pre-existing gap, made visible by INFRA-292's rule-8 fix) **RESOLVED INFRA-303 (Phase 114)**: rules 9 and 10 gained bare `name:` rewrites (`name: pairmode`, `name: companion`) mirroring rule 8's existing frontmatter pattern. | phase-111 security audit (LOW) | 2026-07-28 | 111 |

| CER-109 | `tests/pairmode/test_plugin_manifest.py::_EXPECTED_SKILL_NAMES` hardcodes the four top-level skills instead of globbing `skills/*/SKILL.md`; a fifth skill would ship unguarded against the `flex:` prefix regression. **RESOLVED (confirmed Phase 119, INFRA-379):** the glob-derivation fix already existed from INFRA-308; INFRA-379 added the missing tmp_path unit test proving a `skills/<name>/` directory with no `SKILL.md` is excluded from the derived set. | phase-111 security audit (LOW) | 2026-07-28 | 111 |

| CER-110 | Fleet-wide plugin-sourced duplicate-hook signal: merged hook view reports 16/16 projects with duplicates (`session-start.sh` ×2, `security_reminder_hook.py` ×6), all plugin-sourced/non-pairmode, unprunable by `audit-hooks --apply`; diagnose hook_view plugin discovery (genuine duplication vs. double-count) and decide the RELEASE-064 E3 assertion's future form. **RESOLVED (INFRA-300)** — both shapes are false positives, not double-counts: (a) `security-guidance`'s `security_reminder_hook.py` ×6 is one script registered under two `PostToolUse` blocks (one bare, one `Bash`-matched with five entries discriminated only by an `"if"` predicate `hook_view.py` didn't record); (b) `session-start.sh` ×2 is two *different* plugins (`explanatory-output-style`, `learning-output-style`) shipping byte-identical commands under a shared basename. `hook_view.py` gained an `predicate` key and `duplicate_hook_groups` now classifies each group `actionable` (>= 1 settings-level member) vs. plugin-internal, refining only all-plugin groups by `(path, matcher, predicate)` — a settings-touching group is always emitted unrefined, so no existing detection weakens. The settled E3 assertion is **`Projects with actionable duplicate hooks: 0`**, recorded in `docs/harness-cutover-runbook.md`. | RELEASE-064 E12 new-1 | 2026-07-28 | 106 |

| CER-111 | `pairmode_migrate.py to-030` silently rewrote Repo-J's custom `expected_step_tokens` 53000 → 5000; canary run kept the custom value with a WARN — restore keep+WARN (or prompt) for custom values **CORRECTED / RESOLVED INFRA-303 (Phase 114):** the row's premise was incorrect — (a) keep+WARN was never removed; the three-way branch at `pairmode_migrate.py:956/:969` was live the whole time and is pinned by `test_to030_keeps_custom_expected_step_tokens`, so there was nothing to "restore"; (b) Repo-J's value was exactly `ERA2_STAMP` (53000), which is definitionally indistinguishable from a deliberately-chosen custom 53000 — `state.json` records a number, not its provenance — so Repo-J's rewrite and the canary's keep+WARN are the two arms of a correct branch, not a contradiction; (c) "silently" was inaccurate — the rewrite arm always echoed an `[apply] rewrote expected_step_tokens: ...` line. The delivered remedy is a `--keep-expected-step-tokens` opt-out flag on `to-030` that suppresses the B6 rewrite entirely, plus a rewritten rewrite-arm message that names the ambiguity and the flag. Recording provenance for the value in `state.json` was considered and rejected — it would add a permanent fleet-wide state key to serve a one-time migration command, and cannot help projects already migrated. | RELEASE-064 E12 new-2 | 2026-07-28 | 106 |

| CER-112 | "three skills" count stale in `docs/brief.md:10` and `docs/reconstruction.md:12` (four skills since observability); out of INFRA-291's scope by design **RESOLVED Phase 114 — INFRA-305: both documents now read "four skills", each gained a `/flex:observability` bullet, and `tests/pairmode/test_docs.py::test_skill_count_prose_matches_skill_dirs` pins the count against the live `skills/*/SKILL.md` glob so it cannot drift again.** | INFRA-291 spec recon | 2026-07-28 | 111 |

| CER-095 | HIGH: single-orchestrator parallel story builds (an original design intent, previously exercised) are broken by single-slot coordination state. (1) The resolver has no in-flight awareness — `find_next_story` (`next_story.py:169-220`) returns the first uncommitted story, never consulting `.pairmode-worktrees/`, so while story A builds, `next-action` keeps offering A and never B; the worktree/branch is already a de-facto atomic claim (`flex_build.py:2388-2403`), the resolver just doesn't read it. (2) `state.json["current_story"]` is one global slot: a second `create-story-worktree` overwrites the first's stamp, so `scope_guard` (`scope_guard.py:43,140-185`) evaluates builder A's writes against story B's allow-list (every write denied, misattributed), and merge/discard clear the slot unconditionally (`flex_build.py:2497,2548` → `story_context.py:98`), silently disabling story-scope enforcement for the still-running builder. (3) `.companion/attempt_counter.json` is a single-slot whole-file rewrite (`flex_build.py:929-932`); parallel FAILs cross-clobber and `read_attempt_count` returns 0 on story mismatch (`:970-971`), silently resetting the escalation ladder; `clear_attempt_count` on merge (`:2492`) wipes the other story's live count. (4) `checkpoint_step`/`checkpoint_phase` are one shared list/stamp (`flex_build.py:2102-2160`) — parallel phase checkpoints interleave and the terminal branch resets the other phase's sequence. Fix: resolver skips claimed stories; key `current_story`, the attempt counter, and checkpoint state by story/phase id with clears scoped to own key; INFRA-264's E9 guard must be per-key. **INFRA-280 (Phase 109) — item (1) resolved:** `flex_build.claimed_story_ids` reads `.pairmode-worktrees/` (the existing create/merge/discard claim, no new state); `next_story.find_next_story` gained an opt-in `claimed=` filter and `infer_position`/`resolve_next_action` now skip claimed stories and refuse to checkpoint when every remaining story is claimed (`await-user`/`all-stories-claimed`, evaluated ahead of Row 9). **INFRA-281 (Phase 109) — item (2) resolved:** `state.json` gained a story-keyed `current_stories` record (`story_context.set_current_story`/`clear_current_story`), with the flat `current_story` retained only as a derived mirror for the five readers outside this fix's scope. `scope_guard.resolve_call_story(project_dir, file_path)` resolves the acting story **per call** — from the call's own worktree cwd or target path first, falling back to `state.json` only from a demonstrable main-checkout call, and refusing to guess (resolving to no active story, never "most recent") when `current_stories` holds two or more entries. `flex_build._clear_active_story` now takes the story ID, and both `merge-story-worktree` and `discard-story-worktree` pass their own `--story-id` through, so landing or discarding one story's worktree no longer wipes a still-building sibling's scope enforcement. All four items are now closed. **INFRA-282 (Phase 109) — item (3) resolved:** `.companion/attempt_counter.json` gained a story-keyed record (`{"stories": {"<story_id>": <count>, ...}}` via `flex_build._read_attempt_counters`/`write_attempt_count`), with the legacy flat `{"story_id", "attempt_count"}` shape still read transparently and upgraded to the keyed shape only on the next write — no migration step. `clear_attempt_count(project_dir, story_id=None)` gained a story scope — `merge-story-worktree` now clears only its own landed story's entry, and `discard-story-worktree` clears nothing at all, so a discarded attempt's count survives to escalate the retry. `subagent_transcript._story_accepts_late_bump`'s liveness check now resolves against the story-keyed `current_stories` record first (INFRA-281's authority), falling back to the flat `current_story` mirror only for pre-INFRA-281 state files, so a second live story's first late FAIL bump is no longer refused. **INFRA-283 (Phase 109) — item (4) resolved:** `state.json` gained a phase-keyed `checkpoint_steps` record (`dict[phase_key, list[step_id]]`), the key resolved by INFRA-265's existing `--phase-key`/stamp/re-derivation precedence chain (no new resolution logic) and consumed by that same chain's A2/A4/A3 ordering, now run before a per-key idempotency check rather than a global one. `checkpoint-tag` pops only its own key (`steps.pop(effective_key, None)`), so a sibling phase's mid-sequence progress survives a terminal call for a different phase. The legacy flat `checkpoint_step`/`checkpoint_phase` pair is retained as a **derived mirror** for readers outside this fix's scope (`resolverState.ts`, `client.ts`) — written but never read to decide what to append — and a legacy-shape `state.json` is read correctly and upgraded to the keyed shape on its next successful write, with no migration step. `next_action.infer_position` reads the active phase's own keyed entry directly once the keyed record exists, so two phases resolve independently; the CER-083 stale-stamp rule and `record-checkpoint-step`'s A4 disagreement check now apply only on the legacy path, since once concurrency is genuinely in play the single flat stamp can no longer reliably name which phase is mid-sequence for more than one phase at a time. **RESOLVED Phase 109 — INFRA-280..286 (see CHANGELOG Phase 109 entry): resolver in-flight claim via `.pairmode-worktrees/` (CER-095.1), story-keyed `current_stories` with per-call `scope_guard.resolve_call_story` (CER-095.2), story-keyed attempt counter (CER-095.3), phase-keyed checkpoint step state (CER-095.4). Row was never annotated at the cp-109 checkpoint; annotated during 2026-07-29 backlog grooming.** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |

| CER-096 | MEDIUM: `effort.db` has no concurrency configuration — all 10 `sqlite3.connect` sites (`effort_db.py:217-564`) use rollback-journal mode with default busy handling; contention beyond 5s raises `OperationalError` which `record_effort`/`record_attempt_from_transcript` swallow (`effort_recorder.py:143-144`, `subagent_transcript.py:849-850`) = silent row loss. `init_db` runs five ALTERs in a write txn on every recording (`effort_recorder.py:165`), maximising contention. `next_attempt_number` is a COUNT(*) read-then-write whose own comment concedes the race is only safe because "the era's serial build loop makes the resulting race a non-issue" (`subagent_transcript.py:800-806`; same claim `docs/architecture.md:2194-2200`) — void under parallel spawns. `reconcile_pending_attempts` sweeps globally with `ORDER BY id DESC LIMIT 5` (`effort_db.py:394-431`) — no ownership filter, and >5 pending rows permanently starve the oldest. Fix: WAL + explicit busy_timeout on every connect, init_db once-per-process, atomic write-side attempt-number derivation, sweep ownership/cursor semantics; amend both stale serialism comments. **RESOLVED Phase 109 — INFRA-284 closed all four defects at the query layer: (A) every `effort_db.py` connection now routes through a single `_connect` helper (`sqlite3.connect(timeout=BUSY_TIMEOUT_SECONDS)` + `PRAGMA busy_timeout`/`synchronous=NORMAL`), and `init_db` enables `PRAGMA journal_mode = WAL` once, persistently, tolerating failure; (B) a per-process `ensure_db` cache (keyed by resolved path, re-initialising a deleted file) replaces the per-recording `init_db` call in `effort_recorder.record_effort`; (C) `insert_attempt_derived` derives `attempt_number` as `COALESCE(MAX(attempt_number), 0) + 1` inside one `BEGIN IMMEDIATE` transaction, `record_effort(attempt_number=None)` uses it, and `subagent_transcript.record_attempt_from_transcript` no longer pre-computes the ordinal via `next_attempt_number` (which survives, unchanged, as an advisory/read-only helper); (D) `pending_reconcilable` gained an `output_prefix` ownership filter (escaped `LIKE`, parameterised) and an `order` (`newest`/`oldest`) parameter, and `reconcile_pending_attempts` now fetches both ends (`RECONCILE_OLDEST_ROWS = 2`) so no pending row starves. Two items are explicitly NOT this story's: the stale serialism prose in `docs/architecture.md:2459-2468` is INFRA-286's (only the in-code comment at `subagent_transcript.py` was corrected here), and wiring a real session-scoped `output_prefix` value at any call site is INFRA-285's (CER-097) — this story built and exposed the mechanism only.** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |

| CER-097 | HIGH: any second session (even pure investigation) corrupts a live build loop's companion state via hooks. SessionStart resets `context_current_tokens` to the new session's baseline (`hooks/session_start.py:63-93`) — the loop's budget gate then measures the wrong window; both sessions' PostToolUse write the same token key from different transcripts (`hooks/post_tool_use.py:65-87`), losing ring-buffer samples and corrupting `expected_step_tokens`; every SessionStart also runs the global reconcile sweep over the other session's live rows (`session_start.py:103-107`) and can bump the shared attempt counter cross-story (`subagent_transcript.py:691-697`). Compounding: `user_turn_seq.py:125` does a NON-atomic whole-`state.json` rewrite on every prompt in every session (torn file = silent whole-state reset to defaults; readers fail open); `sync.py:650`, `flex_build.py:929`, `phase_new.py:215,231`, `story_update.py:110,213` are also non-atomic writers. No cross-process lock exists anywhere (only atomic-replace, `state_utils.py:17-46`; CER-050 note "concurrent writers are not expected" is no longer true). Fix: session-scoped context accounting (SessionStart must not clobber another live session's counter), sweep session-ownership, atomic-replace for all writers, and a cheap advisory flock around state.json RMW. **RESOLVED Phase 109 — INFRA-285 closed all six items: (A) `skills/pairmode/scripts/session_state.py` introduces the `state.json["context_sessions"][<session_id>]` keyed record (`SESSION_SCOPED_KEYS`, `session_view`/`apply_session_view`, `prune_stale_sessions` at `SESSION_LIVE_TTL_MINUTES = 180`, `other_live_session_prefixes`), with the flat keys retained as a derived, display-only mirror for `routes/context.ts` and session-less CLI readers; (B) `hooks/session_start.py` reads the whole stdin payload (`_read_payload_from_stdin`), writes `decide_reset()`'s keys into the *starting* session's own entry, seeds/registers the session on a non-reset source, and folds reset + seed + prune into ONE locked read-modify-write — `session_reset.decide_reset` is untouched and still pure (D11); (C) `context_budget.decide(..., session_id=)` resolves through `session_view` and fail-safes to the existing CONTEXT CHECK REQUIRED block (no new reason string) for an unregistered session with live siblings, and `hooks/post_tool_use.py`'s token writer and `record_step_growth` call now operate on the session view, so two sessions no longer remix ring-buffer deltas; (D) `effort_db.pending_reconcilable` gained `exclude_output_prefixes` (escaped, parameterised, with the required `output_file IS NULL OR` disjunct), `reconcile_pending_attempts` forwards it to BOTH ends of INFRA-284's cursor, `subagent_transcript.session_output_prefix` derives a session's spawn-output prefix, SessionStart excludes other live sessions' prefixes while PostToolUse includes its own — the asymmetry is deliberate, since an inclusive SessionStart filter would strand every dead-session orphan row — and the cross-story counter bump stops by construction (`_story_accepts_late_bump` unmodified); (E) `state_utils` gained `_atomic_write_text`, `state_lock` and `update_state_json`, and every writer CER-097 named (`user_turn_seq`, `sync`, `phase_new`, `story_update`, all three hooks) plus `story_context`'s `current_stories` RMW was converted. **The lock is advisory, bounded (`STATE_LOCK_TIMEOUT_SECONDS = 2.0`) and fail-open — it reduces but does not eliminate lost-update risk, and multi-orchestrator operation remains explicitly OUT OF SCOPE (`docs/phases/phase-109.md` § Scope statement); this row must not be read as a stronger guarantee than was built.** Retiring CER-050's "concurrent writers are not expected" note and the stale serialism prose in `docs/architecture.md` are CER-098/INFRA-286's, not this story's.** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |

| CER-098 | MEDIUM: `merge-story-worktree` robustness gaps under concurrency. (a) `git worktree remove --force` and `git branch -D` return codes are ignored (`flex_build.py:2487-2488`) — on failure the command still prints success and exits 0, leaving a stale worktree/branch that breaks the story's next `create-story-worktree` with a confusing "already exists". (b) If the `--ff-only` merge loses a race to another landing (`:2478-2485`), the command exits 1 with the worktree rebased-but-unremoved and the counter/current_story/permissions stamps uncleared — loop state and git state diverge with no documented recovery. (c) Parallel merges must serialize (git index.lock makes the loser fail loudly; needs retry-or-queue semantics, not manual repair). Fix: check both return codes, make the cleanup block run (or report precisely what remains) on merge failure, document/implement merge serialization; also update CER-050's "concurrent writers not expected" doctrine note (`docs/cer/backlog.md:178`). **RESOLVED Phase 109 — INFRA-286: (a) `flex_build.py` gained `_teardown_story_worktree`, shared by `merge-story-worktree` and `discard-story-worktree`, which checks both `git worktree remove` and `git branch -D` return codes, short-circuits the branch delete on a failed removal, and returns residue text rendered with exact repair commands by `_residue_lines`; on merge the three loop-state clears still run unconditionally before residue is reported (the story landed), while on discard residue short-circuits before any clear (nothing landed) — the asymmetry is deliberate. (b) A failed `--ff-only` merge (or a failed rebase) tears down nothing and clears nothing; both failure branches now print a `recovery: `-prefixed block naming what survives and the exact re-run command, and re-running is the documented, idempotent recovery. (c) `flex_build.py` gained `MERGE_LOCK_TIMEOUT_SECONDS` (120.0 s) and `_merge_lock`, wrapping both commands' critical sections in `state_utils.state_lock` on `.companion/merge`. **This is a bounded advisory lock that narrows the `index.lock` contention window, not retry-or-queue semantics and not a multi-orchestrator guarantee — non-acquisition is fail-open (a warning, never fatal), matching `state_utils.state_lock`'s own design.** Doc amendments: the `next_attempt_number` serialism paragraph and the INFRA-285 deferred-lock forward pointer in `docs/architecture.md` are corrected (D1/D2), and a new **Merge robustness** paragraph documents (a)-(c) (D3).** | parallel-build concurrency audit (session 2026-07-25) | 2026-07-25 | 109 |

| CER-093 | MEDIUM: `tests/pairmode/test_vendored_payload_tracked.py::test_no_vendored_payload_is_gitignored` is brittle to environmental noise — upstream npm packages can *ship* `.claude/settings.local.json` inside their published tarballs (hit live at cp-103: `nanoid@3.3.12` and `thread-stream@4.2.0` under `skills/observability/node_modules/.pnpm` carried them; a machine-local `~/.config/git/ignore` rule made them ignored-but-untracked, failing the guard's `ALLOWED_IGNORED` allow-list; removed at checkpoint). Fix: extend the allow-list (or the discovery logic) to tolerate `.claude/` artifact dirs under vendored `node_modules`, and document the caveat in the architecture vendoring note. `tests/pairmode/test_vendored_payload_tracked.py`, `docs/architecture.md`. **RESOLVED Phase 115 — INFRA-307** landed a compiled pattern, `ALLOWED_IGNORED_PATTERNS = (re.compile(r"(?:^ | /)node_modules/(?:.*/)?\.claude/"),)`, with no package name, version, or `.pnpm` store path encoded — the tolerance is structural (anchored under any `node_modules` root), not a growing list of literals, and a `.claude/` directory outside `node_modules` is still reported as offending. The architecture vendoring note carries the caveat. This resolves *this class* of environmental noise (a `.claude/` artifact directory inside a vendored package); it does not claim the guard is now immune to environmental noise in general. | cp-103 intent review | 2026-07-25 |

| CER-094 | MEDIUM: INFRA-261's payload commit tracks a *second* executable native binary the spec never enumerated — `skills/observability/node_modules/.pnpm/better-sqlite3@12.10.0/node_modules/better-sqlite3/build/Release/test_extension.node` (only `better_sqlite3.node` was justified). Decide: enumerate + justify it in the architecture vendoring note, or delete it from the tracked payload (it is a test fixture, not runtime-required) and add it to the guard test's expectations. **RESOLVED Phase 115 — INFRA-307** took the row's second option: deletion, not justify-and-keep. `test_extension.node` is a distinct node-gyp target (`binding.gyp` `targets[1]`, `test_extension`, sources `deps/test_extension.c`) used only by better-sqlite3's own `loadExtension` test suite; the runtime loader (`better-sqlite3/lib/database.js:48`) requires only `better_sqlite3.node`. The binary was `git rm -f`'d, `.gitignore` gained a re-exclusion so a `pnpm rebuild better-sqlite3` cannot resurrect it as a guard failure, and the remaining seven tracked native binaries are now enumerated with justification in `docs/architecture.md` and asserted by `tests/pairmode/test_vendored_payload_tracked.py::test_tracked_native_binaries_match_enumerated_set`. | cp-103 security audit | 2026-07-25 | 103 |

| CER-099 | MEDIUM: containment-guard parity gap — `classify_pending_reason` and the `include_quiescent` branch of `reconcile_pending_attempts` call `_stream_spawn_output(path)` on the raw `attempts.output_file` value (`skills/pairmode/scripts/subagent_transcript.py:722`, `:1156`), bypassing the `_contained_spawn_output` guard INFRA-266 added one function away (`:802`) for `read_completed_spawn`. Reachable from `pairmode_effort.py pending`. Impact bounded: read-only, returns an enum reason string, discloses no file content; the quiescent path writes only numeric usage back to the DB. INFRA-266's Ensures scoped containment to `read_completed_spawn` only, so this is a scope seam between INFRA-264 and INFRA-266, not a spec violation. Fix: route both call sites through `_contained_spawn_output` before opening. **RESOLVED Phase 112 — INFRA-293.** The `classify_pending_reason` half closed incidentally in INFRA-287 when it adopted `is_reconcilable_spawn_output` (routing containment through the same predicate `read_completed_spawn` uses). The `include_quiescent` half — a raw `path.stat()`/`_stream_spawn_output(path)` on the unvalidated `attempts.output_file` value, plus a skip-list that predated the `uncontained` pending reason INFRA-287 introduced — closed here: `"uncontained"` was added to the skip tuple and both raw-path uses were replaced with a `_contained_spawn_output(...)`-resolved path. | cp-104 security audit | 2026-07-26 | — |

| CER-113 | MEDIUM: JSON-path `BUILD-RESULT` outcome asymmetry — `parse_worker_outcome`'s JSON scan writes a `BUILD-RESULT` object's `outcome` field through unvalidated (`subagent_transcript.py:339`), while a `REVIEW-RESULT` verdict is enum-checked against `RECOGNISED_REVIEW_VERDICTS` before assignment. INFRA-293 (§ Out of scope) validated only the *new* plain-text BUILD path it introduced (the `_LEGACY_BUILD_VERDICTS` map), deliberately leaving the pre-existing JSON path unvalidated: retro-tightening it could newly strand rows that today record a non-enum outcome string in `effort.db` — a behaviour change with its own blast radius, unrelated to closing E6b. Fix direction: enum-check the JSON `BUILD-RESULT` outcome against `worker_result.py`'s `_SCHEMAS[BUILD_RESULT]["enums"]["outcome"]` (`{"PASS", "FAIL"}`) the same way the REVIEW path already does, after auditing live `effort.db` rows fleet-wide for any non-enum outcome value the tightening would newly strand. **RESOLVED Phase 113 — INFRA-299:** the JSON `BUILD-RESULT` branch now gates on `subagent_transcript.RECOGNISED_BUILD_OUTCOMES` (`{"PASS", "FAIL"}`, a mirror of `worker_result.py`'s `_SCHEMAS[BUILD_RESULT]["enums"]["outcome"]` pinned by an import-identity test, not a runtime import — this module is on the hook path), structurally mirroring the REVIEW branch; `fail_cause` extraction stays ungated. A refused verdict leaves the row pending and emits the new `RECORDING_DECISIONS` entry `skip:non-enum-outcome` to `.companion/effort_recording.log`, naming the rejected value. The prerequisite fleet audit (7 projects, 1715 rows — see INFRA-299 § Evidence) found **3 non-enum values**: `PARTIAL` ×1 (Repo-C id 31) and lowercase `pass` ×2 (Repo-G ids 8, 12). Forward-only: no existing row is rewritten. | INFRA-293 spec recon | 2026-07-28 | 113 |

| CER-101 | MEDIUM: checkpoint-time effort rollup reads empty for a fully-built phase — `checkpoint-report` at cp-109 printed "no attempts recorded" for all seven INFRA-280..286 stories even though `hooks/post_tool_use.py` recorded a spawn row for every builder/reviewer/gate-worker dispatch (rows 383–398, confirmed in `.companion/effort_recording.log`), because every row still had `tokens_total`/`outcome` NULL at checkpoint time and the phase-scoped rollup counts only reconciled rows. Compounding diagnostic mismatch: `pairmode_effort.py pending` classified 14 of those rows `reconcilable`, but a direct `subagent_transcript.reconcile_pending_attempts(project_dir=..., limit=5)` loop reconciled 0 — classification and sweep disagree about the same rows (suspects: `tasks_root`/`_contained_spawn_output` containment on the session tasks dir, or `stop_reason` recognition against live-session transcripts). Fix: make the sweep and `classify_pending_reason` share one reachability predicate, and have `checkpoint-report` either run a bounded reconcile first or print pending-row counts per story instead of "no attempts recorded" (which reads as data loss when it's deferred reconciliation). **RESOLVED Phase 110 — INFRA-287, at every named layer. Root cause: Claude Code writes the spawn output at `<tmp>/.../tasks/<hash>.output` as a *symlink* into `~/.claude/projects/<slug>/<session>/subagents/`, and `_contained_spawn_output` called `Path.resolve()` before judging containment — so the rule was applied to the link target and rejected 100% of real output files (294/294 measured live, 2026-07-28): the sweep reconciled nothing, ever. Containment is now judged lexically on the link path while the link target is separately allowlisted (`_permitted_output_target`: `~/.claude/` or a temp root) — CER-089's protection moved, not weakened. The terminator was also widened: measured last-entry distribution across the same 294 files was 215 `assistant`/`end_turn`, 51 `assistant` with absent stop_reason (50 of 51 quiescent >1h — finished agents), 26 with no parseable entry, 1 `assistant`/`tool_use`, 1 `user`; a spawn now terminates on `end_turn` *or* file quiescence past `QUIESCENT_AGE_SECONDS` on an assistant last entry. The classify-vs-sweep disagreement (14 reconcilable vs 0 reconciled) is ended structurally: one shared predicate, `is_reconcilable_spawn_output`, is consumed by both `read_completed_spawn` and `classify_pending_reason`, and a containment-rejected path is the new first-class pending reason `uncontained` (distinct from `file-missing`). Collateral fixed in the same story: `session_output_prefix` had the same `resolve()` bug, so CER-097's ownership filter had never been armed in production — it is now. `checkpoint-report` gains `_query_pending_by_story_ids` and prints `<sid>: no reconciled attempts (N pending)` plus a phase-level pending-count line instead of reporting deferred reconciliation as absence; the filed reconcile-first option was declined — the sweep already runs on every PostToolUse and SessionStart, and a reporting command that mutates the database it reports on is a boundary this project does not cross for convenience. Historical NULL rows are not backfilled (forward-only).** | cp-109 checkpoint report | 2026-07-27 | 110 |

| CER-102 | MEDIUM: FAIL-outcome escalation ladder is dead in the live build loop — across two consecutive reviewer FAILs on RELEASE-062 (cp-105 build, 2026-07-27), `.companion/attempt_counter.json` was never created/bumped, so `next-action` kept emitting `spawn-builder` attempt 1 / model haiku (`auto-downgrade`) with no retry escalation, no model upgrade, and no loop-breaker/human-pause row; the orchestrator had to escalate the model manually. Hook-side recording (INFRA-236/237 contract: post_tool_use.py's Task/Agent branch bumps the counter on FAIL derived from the spawn transcript) either never fired or never classified the reviewer outcome as FAIL. Likely adjacent to CER-101's reconciliation gap (outcome NULL at read time means no FAIL is ever seen) — fix together: the counter bump must key off the reviewer verdict at reconciliation, or the resolver must treat a discarded worktree as a failed attempt. **RESOLVED Phase 110 — INFRA-287 + INFRA-289.** The root cause was CER-101's unreachable reconciliation predicate (every async row stayed `NULL`-outcome forever, so the reconcile-time FAIL branch — the ladder's only live path, since the insert-time bump requires a synchronous `tool_response` an async-launched Agent spawn never has — could never fire), fixed by INFRA-287. The ladder was revived by *removing an obstruction*, not by making the insert-time bump work on async spawns — it still cannot, and is not claimed to. INFRA-289 added the missing test coverage and observability for the path INFRA-287 unblocked: `reconcile_pending_attempts`'s FAIL branch now logs `bump:late-fail` (with `story_id`/`row_id`) or `skip:late-bump-blocked` on every reconciled FAIL, so the *next* silent-ladder-stall is a `tail` away instead of an archaeology session, and a comment above the insert-time bump names why it is structurally dead on the async path (CER-102) so a future reader does not "fix" it by loosening its gate. Collateral (dossier item D, "fix if cheap, else note"): `attempts.model` is `NULL` on reviewer rows because (a) the orchestrator omits `model=` on reviewer spawns — a `next_action.py`/`CLAUDE.build.md` change, out of INFRA-289's file set — and (b) pre-reconcile async usage extraction has no model to report; (b) is already covered by the reconcile-time backfill at `subagent_transcript.py`'s `if row.get("model") is None and result.get("model")` branch. (a) remains open, noted rather than fixed here. | cp-105 build loop | 2026-07-27 | 110 |

| CER-103 | HIGH: effort-recording attribution is session-bound, not target-bound — spawns dispatched from a flex session against another project (`--project-dir /mnt/work/Repo-B`, worktree cwd under Repo-B) record their attempt rows into flex's effort.db (RELEASE-063 canary: LEGAL-001 rows 419/420 in flex's db, zero in Repo-B's). Native-session runs attribute correctly, so campaign work driven centrally from flex is invisible to the consumer project's db. Recorder should key on the spawn's target project (worktree cwd / --project-dir), falling back to session project. Also: phase-attribution parses the spawn prompt loosely — flex row 416 recorded story_id='phase:key' and Repo-B rows 233-236 'phase:checkpoint' instead of the actual phase key. **RESOLVED Phase 110 — INFRA-289.** `subagent_transcript.resolve_recording_project` now resolves the spawn's actual target — an explicit `--project-dir` flag, an explicit `Project dir:`/`project_dir:` label, or a `.pairmode-worktrees/<ID>/` segment collapsed to its project root — and every downstream use (`_read_state`, the `effort_tracking` gate, the db path, `reconcile_pending_attempts`, `bump_attempt_count`, phase-doc lookup) follows it. A prompt-derived candidate is admitted only through the operator-controlled `state.json["registered_projects"]` allowlist (never created, never trusted blindly); a rejected candidate falls back to the session project (unchanged pre-existing behaviour) with a `skip:target-unregistered` log line as the alarm rather than a silent drop. The loose bare-phase-key parse is fixed separately: `_PHASE_KEY_STRICT_RE` now rejects any candidate that does not end in a digit (optionally with a `-main`/`-ante<N>`/`-post<N>` suffix), so `phase:key` and `phase:checkpoint` are no longer producible — an underivable key resolves to the honest `unattributed:<role>` sentinel instead. Historical rows (`LEGAL-001` 419/420, flex row 416, Repo-B rows 233-236) are not backfilled — see this story's Out of scope. | RELEASE-063 canary | 2026-07-27 | 110 |

| CER-104 | HIGH: every attempt row is inserted twice — Repo-B's native 0.3.0 session (post-RELEASE-063 migration) wrote perfect duplicate pairs with near-identical timestamps for all 31 spawns of the day (rows 211-272: LEGAL-002..005, OPS-001..004, INFRA-011, checkpoint workers), and all rows additionally show the CER-101 NULL tokens/outcome pattern natively (checkpoint-report printed 'no attempts recorded' for phase MN028-main while lifetime pre-0.3 rows kept real data). Suspect double hook invocation or a double record call in post_tool_use.py's Task/Agent branch; dedupe fix must also decide which of the pair the reconciliation sweep should own. Confirms CER-101 is a downstream regression, not a flex-session quirk; blocks RELEASE-064..070 with CER-101/CER-102. **RESOLVED Phase 110 — INFRA-288, at both ends: (a) the recorder is idempotent — `effort_db.insert_or_update_attempt` uses `attempts.agent_id` (previously written-never-read) as an idempotency key, so a second insert for a live pending `(agent_id, agent_role)` row within `AGENT_DEDUPE_WINDOW_SECONDS` updates that row (coalescing, inside the same `BEGIN IMMEDIATE` transaction) instead of inserting, logged as `recorded:deduped`; by construction the deduped pair leaves exactly one row and it is the row `pending_reconcilable`'s sweep already matches. This end is best-effort only — it requires a recoverable `agent_id` from the spawn's `tool_response`. (b) The primary cure is merged-view duplicate-hook detection: `hook_view.py` unions settings.json + settings.local.json + plugin hooks.json for `fleet_discovery._check_duplicate_hooks`, `pairmode_sync` `audit-hooks` (which now keeps the plugin entry and prunes settings-level ones, never writing a plugin's files), and `bootstrap._register_context_budget_hooks` (which skips a spec already plugin-registered) — the double registration (bootstrap settings entry + installed plugin hooks.json) that caused the pairs, invisible to the old settings-only checks. Forward-only: rows 211-272 are not backfilled. CER-101 (reconciliation) and CER-102 (FAIL escalation) remain open — INFRA-287/INFRA-289's, not fixed here.** | Repo-B native session post-canary | 2026-07-27 | 110 |

| CER-105 | LOW: `attempts.phase` column has split semantics — NULL on 100% of story rows (`_derive_attribution` returns None for non-checkpoint roles, intentional per INFRA-256) but populated on checkpoint-worker rows; two attribution schemes share one column and checkpoint-report scopes by story-ID list instead. Either write phase on story rows too or document the column as checkpoint-only. **RESOLVED Phase 113 — INFRA-299 as a documented decision, not a code fix:** the disposition is *documented, not changed*. `docs/architecture.md` § Effort tracking now records the column's split semantics — checkpoint-only population, INFRA-258's reason for it, per-story rollups scoping by story-ID list, `effort_db.query_by_phase` returning checkpoint rows only, and the SPA's `SELECT DISTINCT phase ... WHERE phase IS NOT NULL` breakdown being checkpoint-only by design — and the comment above `subagent_transcript.CHECKPOINT_ROLES` cross-references it. No schema change, no back-fill; writing `phase` on story rows is explicitly not built. | phase-110 data-flow audit | 2026-07-28 | 113 |

| CER-106 | LOW: `state.json` key `context_budget_acknowledged_at` stores a token count (e.g. 222252), not a timestamp, despite the `_at` suffix (written pre_tool_use.py ~:173, read context_budget.py ~:891). Works, but the name/semantics mismatch invites misreads; rename or document. **RESOLVED Phase 113 — INFRA-299 as a documented decision, not a code fix:** the disposition is *documented, not renamed*. Both ends of the key carry a comment stating it holds a token count, not a timestamp — `hooks/pre_tool_use.py` (the `_mutate` closure that writes it) and `skills/pairmode/scripts/context_budget.py` (the `decide()` read) — and `docs/architecture.md` § Companion data files gains the previously-missing `context_budget_acknowledged_at` state-key inventory entry, which names the misnomer explicitly. The **rename half stays deferred on this backlog**: it is a fleet-wide `state.json` key migration across every registered project, needing a read-both/write-one window and a `pairmode_migrate` rule, which 0.3.1 does not need. No alias, no dual-write, no deprecation shim. | phase-110 data-flow audit | 2026-07-28 | 113 |

| CER-107 | LOW: sidebar-extractor rows (e.g. flex effort.db 421-423, story_id `sidebar:no-story`) carry outcomes with no token data or spawn refs and are counted by lifetime/role rollups, polluting build-role medians. Exclude from rollups or stop recording them. **RESOLVED Phase 115 (INFRA-309) — and the original diagnosis was wrong, not just resolved.** The "polluting build-role medians" claim does not hold: every per-role statistic (`flex_build._query_effort_by_role:2659-2698`, `refresh_effort_baseline._aggregate`, `pairmode_effort._query_models:388-400`) groups *on* `agent_role`, so a non-build role forms its own bucket by construction and cannot enter a builder's or reviewer's distribution — this was checked against the live database before the fix (`sidebar-extractor` 297 rows, 100% NULL tokens/phase/rail, isolated in its own group) and confirmed unchanged by the fix. The real harm was never medians: it was the *unattributed* `(phase, rail, model)` bucket in `pairmode_effort._query_rollup` — sidebar's `phase=NULL, rail=NULL` rows landed in one anonymous group that read as hundreds of zero-cost build attempts (297/566 = ~52% of this repo's live `attempts` table before the fix; measured live at build time as 312/566 = 55%) — and `skills/observability/api/src/readers/effortDb.ts`'s `queryEffortSummary.total_attempts`, a bare `COUNT(*)` that made the SPA's headline counter majority-non-build. The row's second offered remedy, "stop recording them", was **rejected**: sidebar extraction is a real LLM call against a real model with real token cost, and deleting the writer would trade a permanent loss of cost data for a cosmetic fix to a read path. The delivered fix changed **no writer** — `effort_db.NON_BUILD_ROLES` is a new read-side-only constant consumed by every cross-role reader (`pairmode_effort._query_rollup`/`_attach_rollup_dollars`, `refresh_effort_baseline._collect_rows`, `effortDb.ts`'s `queryWaypoints`/`queryEffortSummary`/`querySpendOutliers`); `pairmode_effort._query_models` deliberately keeps and labels non-build rows instead of excluding them, since "how much did sidebar extraction cost" is a question that report should still answer. See `docs/architecture.md` § Effort tracking for the full boundary. | phase-110 data-flow audit | 2026-07-28 | 115 |

| CER-100 | LOW: the security-auditor procedure skill's thin-delegation exception list (check 1, `skills/pairmode/skills/security-auditor/procedure.md`) still names only `session_reset.py` for `session_start.py`, but `hooks/session_start.py:104` has imported `reconcile_pending_attempts` from `subagent_transcript` since INFRA-258 (Phase 103) — documented in `docs/architecture.md` but stale in the skill; a future auditor reading the skill literally would flag it CRITICAL. Fix: add the reconcile sweep (and INFRA-264's `log_recording_event` path if applicable) to the skill's exception list. **RESOLVED Phase 114 — INFRA-305: `hooks/session_start.py`'s exception bullet now names `subagent_transcript.reconcile_pending_attempts` (INFRA-258) and `session_state` (INFRA-285) verbatim, alongside its `session_reset.py` dispatch; a new durable parity test (`test_hook_delegations_are_documented_exceptions`) mechanically checks every hook's imports against this block so this cannot silently drift again.** | cp-104 security audit | 2026-07-26 | — |

| CER-073 | HIGH: `flex_build.py record-attempt` alias (RELEASE-009) forwards no options — the click command declares only `--help`, so every documented invocation (`--story-file`, `--agent-role`, ...) exits 2 with "No such option". The Era-3 orchestrator template tells downstream orchestrators to use this alias; all of them must fall back to calling `record_attempt.py` directly. Live-hit: Repo-E, first Era-3 story cycle, 2026-07-22. Fix: declare the alias with `context_settings={"ignore_unknown_options": True}` + a variadic argument pass-through, and add a test that a full real record-attempt flag set round-trips through the alias. **RESOLVED Phase 104 — INFRA-263 declared `cmd_record_attempt` (`skills/pairmode/scripts/flex_build.py`) with `context_settings={"ignore_unknown_options": True}`, `add_help_option=False`, and a variadic `@click.argument("args", nargs=-1, type=click.UNPROCESSED)` in place of the empty option set, forwarding the collected argument tuple to `record_attempt.py` unchanged (including `--help`). New test file `tests/pairmode/test_flex_build_record_attempt_alias.py` covers the full-flag round trip, the `sys.argv`-truncation regression, the exact CER-073 reproducer, `--story-file` forwarding, `--help` forwarding, delegate error propagation, and group-help regression. This is a duplicate filing of CER-071; both close together in this story.** | Repo-E Era-3 build | 2026-07-22 | 104 |

| CER-074 | MEDIUM: `next_action.resolve_next_action` never emits `spawn-reviewer` — the constant is declared and listed as a valid action, but no code path constructs it. DP3 infers PASS from a story commit existing, yet only the reviewer commits, so the intended builder→reviewer dispatch is structurally unreachable; a poll after the builder finishes (uncommitted worktree, counter written) infers FAIL and dispatches a wasteful attempt-2 rebuild. Downstream orchestrators must adopt an undocumented one-iteration-per-story reading (builder + reviewer + merge inside a single spawn-builder iteration) for the loop to work — Repo-E did, successfully, across 6 stories on 2026-07-22, but nothing documents this contract. Fix: either emit spawn-reviewer from a builder-completed state (e.g. dirty story worktree + attempt recorded) or document the one-iteration-per-story contract in the Era-3 orchestrator template and remove the dead action constant. **RESOLVED Phase 104 — INFRA-268 documented the one-iteration-per-story contract in three places (`CLAUDE.build.md` § Build loop, `skills/pairmode/templates/CLAUDE.build.md.j2`, and `docs/architecture.md` § Pairmode build loop, plus the `next_action.py` module-structure entry), and marked the `SPAWN_REVIEWER` constant as orchestrator-dispatched-only rather than removing it — it stays because the orchestrator's ACTION_SUBAGENT_TYPE map and the model-override rule key on it, and a resolver-emitted `spawn-reviewer` was rejected because it would require non-durable dirty-worktree state in a pure-read resolver and would split one story's cycle across two polls. A parametrized regression test (`tests/pairmode/test_next_action.py::TestResolveNextActionNeverEmitsSpawnReviewer`) pins that `resolve_next_action` never emits `spawn-reviewer` across six position shapes.** | Repo-E Era-3 build (HOME-001 near-miss) | 2026-07-22 | 104 |

| CER-075 | MEDIUM: `create-story-worktree` provisions no build environment — fresh worktrees lack `node_modules` and `.env.local` (gitignored), so every builder/reviewer re-discovers and re-applies symlink workarounds, and three Repo-E test files fail worktree-only (see also the project-side DL entry on absolute mock paths). Cost: repeated wasted agent effort + "known failures" habituation in every review. Fix direction: worktree-create hook or config (`worktree_provision: [node_modules, .env.local]`-style symlink list in state/config), or document the symlink convention in the builder/reviewer procedure skills. **RESOLVED Phase 114 — INFRA-302:** the fix direction shipped as a config key, not a hook — an optional `worktree_provision` list (project-relative paths) in `.companion/pairmode_context.json`, read by `_read_worktree_provision` and applied by `_provision_story_worktree` in `flex_build.py`'s `create-story-worktree`. Every entry is opt-in and symlinked absolute-target into the fresh worktree; every unsatisfiable entry (missing, escaping, already-tracked, etc.) skips with a stderr warning and never fails the command. This does **not** make the worktree build environment automatic for every project — it makes the convention expressible in config instead of re-invented per story. | Repo-E Era-3 build (every HOME story) | 2026-07-22 | — |

| CER-076 | LOW: `_STUB_DELEGATION_RE` (`see phase doc | see docs/phases/ | see phase-`) false-positives on story files that *quote* deliverable text containing those phrases — e.g. Repo-E's HOME-006 instructed the builder to append a backlog resolution note reading "... See docs/phases/phase-PM066-main.md" and the stub gate blocked the story until the quoted text was reworded. Fix: skip matches inside fenced code blocks/inline code spans, or add a frontmatter escape hatch (e.g. `stub_gate: exempt` with a required reason). **RESOLVED Phase 104 — INFRA-268 added a length-preserving code-region mask (`_mask_code_regions` in `skills/pairmode/scripts/flex_build.py`) that blanks fenced code blocks and inline code spans before the delegation search, so quoted text no longer reads as delegation while prose delegation still blocks and the reported line still shows the original text; the frontmatter escape hatch was rejected because a per-story bypass switch is a permanent silent hole in a gate whose whole value is that it cannot be argued with.** | Repo-E Era-3 build (HOME-006) |

| CER-077 | HIGH: `cmd_record_checkpoint_step`'s `checkpoint-tag` branch (`flex_build.py`, ~line 2036) marks a phase complete in `docs/phases/index.md` by calling `resolve_current_phase(project_dir)` and using whatever phase file that returns — there is no explicit phase-key parameter. When two phases are simultaneously flagged `active` in the index (a real, observed state: `fold-prep` had both phase-97 and phase-98 active at once), the resolved phase can be the *wrong* one. Live-hit: Phase 98's `checkpoint-tag` step (2026-07-23) marked phase-97 — the still-in-progress fold, blocked on RELEASE-058 — complete as a side effect of tagging phase-98; caught and manually reverted in the same session (commit `c6c2c6a`). Fix direction: thread an explicit phase key through `record-checkpoint-step` (e.g. resolved once at the top of the build loop and passed on every call, not re-derived per-call from ambiguous "current phase" state), or make `resolve_current_phase` deterministic/erroring when more than one phase is active rather than silently picking one. `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`, `resolve_current_phase`. **RESOLVED Phase 104 — INFRA-265:** both fix directions taken, not one or the other. (1) `record-checkpoint-step` gained an optional `--phase-key`; `_record_checkpoint_step` now resolves the closing phase by a fixed precedence — explicit `--phase-key` (validated against the index; a disagreeing stamp is an error, not a choice) → the `state.json["checkpoint_phase"]` stamp (INFRA-260) → `resolve_current_phase`'s candidate set only when unambiguous — with every validation and ambiguity check happening before any write, so an exit-2 path leaves `state.json` and `docs/phases/index.md` byte-identical; a non-terminal step degrades an ambiguous re-derivation to a warning rather than an error. `CLAUDE.build.md` and `skills/pairmode/templates/CLAUDE.build.md.j2` now pass `--phase-key <phase-key>` on every call, so the mandated loop path never depends on re-derivation. (2) `resolve_current_phase` and `next_action._resolve_active_phase` (composing one shared `_active_phase_candidates` helper, no second row walk) now raise `AmbiguousActivePhaseError` when more than one index row is flagged `active` with an existing phase file — the exact condition this finding observed — while multiple `planned` rows (the normal steady state of a multi-phase index, this repo's own 105–108 included) remain unaffected. `current-phase`, `checkpoint-report`, `next-action`, `resolver-state`, and `record-checkpoint-step` each catch the exception at the CLI boundary and exit 2 with the message on stderr, never a traceback. Regression tests: `tests/pairmode/test_record_checkpoint_step.py::TestPhaseKeyPrecedence` (the A5 five-`planned`-row fixture mirroring this repo's live index, both its exit-2 and `--phase-key 104` halves), `tests/pairmode/test_flex_build_current_phase.py` (double-`active` raise), `tests/pairmode/test_next_action.py::TestNextActionCLI::test_infra265_two_active_rows_exits_2_no_traceback`, `tests/pairmode/test_templates.py`. **RESOLVED — INFRA-265 threaded an explicit `--phase-key` through `record-checkpoint-step` (resolved once at loop top, checked first in the precedence chain ahead of any re-derivation), exactly the row's fix direction; enforced in CLAUDE.build.md's checkpoint sequence. Field-proven 2026-07-28: a wrong key (`phase-112`) was rejected with a CER-077 message and no write performed. Row was never annotated; annotated during 2026-07-29 backlog grooming.** | Phase 98 checkpoint-tag (2026-07-23) | 2026-07-23 | 104 |

| CER-078 | LOW: `skills/pairmode/skills/security-auditor/procedure.md`'s "Documented thin-delegation exceptions" list and PIPE CONTRACT check text describe the pipe path as read from `.companion/state.json["pipe_path"]` — no hook has done this since INFRA-238 (Phase 98) removed the dead `pipe_path` state-key branches in favor of the single hardcoded `tempfile.gettempdir()/companion.pipe` convention. The parallel checklist in `skills/pairmode/skills/reviewer/procedure.md` was updated this same phase; `security-auditor/procedure.md` was not. Doesn't change today's audit verdicts (code is compliant with the *current*, correct behavior) but the next security-auditor cold-start will be checking against a stale description. Fix: sync `security-auditor/procedure.md`'s pipe-path description to match `reviewer/procedure.md`'s current text. `skills/pairmode/skills/security-auditor/procedure.md`. **RESOLVED Phase 114 — INFRA-305: PIPE CONTRACT text in the security-auditor procedure now names `tempfile.gettempdir()/companion.pipe`, INFRA-238, and the `pipe_path` key's `to-030` retirement, copied from `reviewer/procedure.md` § 2; `test_procedure_skills.py::TestPipePathAbsence` pins both procedures against re-drift.** | Phase 98 checkpoint-security audit | 2026-07-23 | 98 |

| CER-079 | LOW: `docs/architecture.md` § Data flow still describes the companion pipe as project-scoped by an md5 hash of the project path. This has not been accurate since before Phase 98 (confirmed: `post_tool_use.py` already used the flat hardcoded `/tmp/companion.pipe`-style path pre-phase-98; INFRA-238 only removed dead code in the *other* hooks that still referenced the old state-key). Pre-existing drift, not introduced this phase. Fix: correct § Data flow to describe the actual non-project-scoped hardcoded pipe path. `docs/architecture.md` § Data flow. **RESOLVED Phase 114 — INFRA-305: § Data flow now describes the single flat `tempfile.gettempdir()/companion.pipe` path with no md5/project-scoping claim; the two downstream architecture.md mentions were reconciled too. Re-verification found `sidebar.py` genuinely still reads the retired md5-hashed path — that residual code-side divergence is filed as CER-118, not fixed here.** | Phase 98 checkpoint-security audit | 2026-07-23 | 98 |

| CER-080 | MEDIUM, cross-repo: `/mnt/work/flex` (the main checkout that `/mnt/work/flex-harness` is a linked git worktree of) carries a stale `current_story: INFRA-209` stamp in its own `.companion/state.json`, dated 2026-07-20, never cleared (INFRA-209 itself still shows `status: planned` there despite having commits). Because `scope_guard.py`'s `_resolve_main_project_root` (INFRA-238) correctly resolves file operations from a linked worktree back to the main checkout's state, this stale stamp silently blocks the `Edit`/`Write` tools for *any* legitimate doc/code edit made from `/mnt/work/flex-harness` (or any other worktree of the same repo) whenever the touched path falls outside `INFRA-209`'s declared scope — surfaced during Phase 98's `checkpoint-docs` step, worked around via direct file writes outside the hook path. This is a different project's state (`/mnt/work/flex`), not flex-harness's own — flagging rather than fixing directly. Fix direction (in `/mnt/work/flex`, not here): clear or correctly resolve the stale `current_story` stamp; consider whether `scope_guard`'s cross-worktree resolution should also account for a genuinely-idle main checkout (no active story) rather than trusting a possibly-stale stamp indefinitely. **RESOLVED Phase 105 — INFRA-271: the enforcement fix is a staleness cutoff (`scope_guard.STATE_STORY_MAX_AGE_HOURS = 24.0h`, via the public `entry_is_fresh()` predicate) applied to the `state.json` fallback only — a `current_stories`/`current_story` entry with a missing, unparseable, or too-old `set_at` resolves to a new `"stale"` source (fail-open for ordinary paths, naming the cutoff and the remedy; still fail-closed for `PROTECTED_GLOBS`), while a worktree claim (`worktree-cwd`/`worktree-path`) never ages out. The operator fix is `flex_build.py clear-stale-stories [--project-dir DIR] [--max-age-hours N] [--apply]`, which reports (default) or clears (`--apply`) exactly these stale stamps ahead of a fleet campaign, importing the staleness rule from `scope_guard` rather than re-deriving it. flex's own `/mnt/work/flex` `.companion/state.json` was already verified clean of the `INFRA-209` stamp at spec time (neither `current_story` nor `current_stories` present) — this story delivers and proves the mechanism against fixtures; there was no live stamp left in this repo to clear.** | Phase 98 checkpoint-docs step (2026-07-23) | 2026-07-23 | 105 |

| CER-081 | HIGH: `bootstrap.py`'s hook-registration functions (`_register_pretooluse_hook`, `_register_context_budget_hooks`) only migrate a stale hook block \"in place\" (basename-match fallback, INFRA-228) when at most one stale entry exists for that event — if a project already has BOTH a stale `/mnt/work/flex`-pointing block and a correct `/mnt/work/flex-harness`-pointing block (the exact state INFRA-228 was fixing going forward, but does not clean up retroactively), a subsequent `sync-all` run finds the harness block already registered via exact-command match and never reaches the basename fallback — the stale block is never removed. Live-hit: confirmed on all four migrated fleet projects surveyed (Repo-H, Repo-I, Repo-A, Repo-E) — every one had two hook blocks per event (`PreToolUse`, `UserPromptSubmit`, `SessionStart`, `PostToolUse`), both firing on every tool call. Consequence: the stale `/mnt/work/flex` copy still carries every pre-migration bug (e.g. INFRA-246's reviewer-spawn context-budget-gate fix, INFRA-236's effort recording) since hooks are not deduplicated or overridden by Claude Code — both registered commands execute independently on every event, so a downstream project could still exhibit a bug that was already fixed on `flex-harness`, plus duplicate/racing writes to `.companion/state.json`/`effort.db` from two different code versions. Hand-fixed on all four surveyed projects this session (stale blocks removed, single correct block remains per event) — no code fix applied. Fix direction: extend the registration logic to detect and remove/replace ALL stale same-basename blocks for an event, not just migrate when the harness block is entirely absent; add a dedicated `pairmode_sync.py` or `bootstrap.py` subcommand to audit/clean a project's `settings.json` for duplicate hook registrations (useful both for already-migrated projects and as a periodic drift check); consider a pre-fold discovery check (DP8, `fleet_discovery.py`) that flags any bound project with >1 hook block for the same event. `skills/pairmode/scripts/bootstrap.py` `_register_pretooluse_hook`/`_register_context_budget_hooks`. **RESOLVED phase 105 (INFRA-269) — all three fix directions built.** (1) Registrar dedupe: `bootstrap.py` gained `_find_all_entries_by_command_basename` and `_prune_stale_hook_entries`, called from both `_register_pretooluse_hook` and `_register_context_budget_hooks` after the correct entry is resolved, removing every stale same-basename sibling entry (and any block left with empty `hooks`) while leaving the pre-existing in-place migration path (used only when no correct entry yet exists) unchanged. (2) `pairmode_sync.py audit-hooks` (dry-run by default, exits 1 on a finding / 0 clean; `--apply` cleans, reusing `_prune_stale_hook_entries`) audits and cleans an already-migrated project's `.claude/settings.json` and serves as a periodic drift check. (3) `fleet_discovery.py` gained `_check_duplicate_hooks` (read-only) and a `duplicate_hooks` field on every `discover()` result, surfaced in `--json` output, the human-readable summary, and the `## Duplicate hook registrations (CER-081)` snapshot section — reporting only; discovery's exit status is unchanged, `audit-hooks` is the enforcing instrument. | Phase 98 fold-readiness check (2026-07-23) | 2026-07-23 | 105 |

| CER-082 | [Renumbered from main-branch CER-068 at the RELEASE-059 fold merge — the ID collided with harness-branch CER-068.] Era-doc phase ledger drifts uncommitted and its status column is never updated. `phase_new.py` appends a ` | <phase> | <title> | planned |

| CER-070 | Per-story git worktrees (INFRA-224) cannot reliably pass `tests/pairmode/test_observability_ui.py::test_ui_build_emits_dist_index_html`. The root `.gitignore`'s Python-oriented `build/` and `dist/` patterns (lines 6-7) also match literally-named `build`/`dist` output directories nested inside the intentionally-vendored `skills/observability/ui/node_modules` tree — so those directories were never committed to git at all, not merely stale. Confirmed during RELEASE-042's build: a fresh worktree cut from a clean commit was missing `node_modules/.pnpm/.../@tanstack+react-query@.../node_modules/@tanstack/react-query/build`, `.../vite@.../node_modules/vite/dist`, `.../tailwindcss@.../node_modules/tailwindcss/dist`, and `.../\@vitejs+plugin-react@.../node_modules/\@vitejs/plugin-react/dist` — present on the main worktree's filesystem (locally pnpm-installed once, never removed) but absent from every git-tracked snapshot. Copying the four missing directories in by hand cleared the "Cannot find module" errors but surfaced a second, unresolved layer of TS7006 implicit-`any` errors in worktree-only `tsc -b` runs that do not reproduce on the main worktree even after a from-scratch rebuild there — root cause not yet found; likely another gitignored `build`/`dist` path providing type declarations consumed transitively. Fix direction: scope the root `.gitignore`'s `build/`/`dist/` rules to the actual Python source tree (e.g. `/build/`, `/dist/` anchored at repo root, or an explicit `!skills/observability/**/node_modules/**` negation) so vendored npm package output is genuinely fully committed; then audit for any other silently-stripped vendored paths before trusting per-story worktrees' UI-build gate again. Until fixed, treat `test_ui_build_emits_dist_index_html` FAILs inside a per-story worktree as a suspect environmental false-negative — verify by running the same test against the main worktree before accepting a story's content as broken. HIGH severity (the build gate that INFRA-224's whole worktree-isolation design depends on cannot pass inside the isolation it created, for any story whose diff happens to touch the observability UI's dependency surface — and silently misleads reviewers into FAILing correct doc-only diffs). `.gitignore:6-7`, `skills/observability/ui/node_modules/` (vendoring completeness). **Addendum (INFRA-225 build, 2026-07-22):** every worktree pytest run that touches the UI-build gate also dirties the tracked `skills/observability/ui/tsconfig.tsbuildinfo` (the failed `tsc -b` still writes its incremental cache before erroring), which then blocks `merge-story-worktree`'s rebase with "You have unstaged changes" until manually `git checkout`'d — a second-order symptom of the same root cause, adding a recurring manual step to every affected story's merge. **RESOLVED Phase 114 — INFRA-302:** the root cause (the vendored `node_modules` payload silently swallowed by the root `.gitignore`'s `dist/`/`build/` patterns) was fixed by INFRA-261 — the payload was committed in full and the `.gitignore` negations added. This story closes the addendum: `skills/observability/ui/tsconfig.tsbuildinfo` is now `git rm --cached` and anchor-ignored, so a worktree UI-build-gate run can no longer dirty the tree and block `merge-story-worktree`'s rebase. CER-090's remaining payload-completeness question (whether a fresh worktree can build the UI without a manual rsync) is **not** closed by this story — that is INFRA-307's. | RELEASE-042 build/review (2026-07-21/22) | 2026-07-22 | 97 |

| CER-071 | `flex_build.py record-attempt`'s CLI-forwarding wrapper rejects every option `record_attempt.py` itself accepts — e.g. `flex_build.py record-attempt --story-id RELEASE-042 --agent-role builder ...` fails with `Error: No such option: --story-id`, even though `record_attempt.py --help` lists `--story-id` directly. The direct script (`uv run skills/pairmode/scripts/record_attempt.py ...`) works fine standalone. Root cause not yet diagnosed — likely the Click forwarding/alias definition in `flex_build.py`'s `record-attempt` command declares its own narrower option set instead of passing `*args` through, unlike other alias commands in the same file (e.g. `merge-story-worktree`, `discard-story-worktree`, which work as documented). CLAUDE.build.md's own build-loop pseudocode calls `flex_build.py record-attempt ...` directly, so any orchestrator following it literally hits this same failure. Fix direction: compare the `record-attempt` Click command definition in `flex_build.py` against a working alias (e.g. `create-story-worktree`) and correct the option forwarding/passthrough. MEDIUM severity (effort tracking happens to be disabled in this project, which is the only reason this hasn't blocked a build loop yet — a project with effort tracking enabled would hit a hard CLI error on every recorded attempt). `skills/pairmode/scripts/flex_build.py` (record-attempt command). **RESOLVED Phase 104 — INFRA-263: the root cause is now diagnosed — the alias's Click declaration was empty (only the auto-added `--help`), not merely narrower than `record_attempt.py`'s option set. Fixed by declaring `cmd_record_attempt` (`skills/pairmode/scripts/flex_build.py`) with `context_settings={"ignore_unknown_options": True}`, `add_help_option=False`, and a variadic `@click.argument("args", nargs=-1, type=click.UNPROCESSED)`, forwarding the collected tuple to `record_attempt.py`. New test file `tests/pairmode/test_flex_build_record_attempt_alias.py` round-trips the full documented flag set. This is a duplicate filing of CER-073; both close together in this story.** | RELEASE-042 build (2026-07-22) | 2026-07-22 | 104 |

| CER-066 | `state.json["checkpoint_step"]` (RESOLVER-008/RESOLVER-012) is never cleared between phases. `resolve_next_action`'s Row 9 branch computes `_remaining = [s for s in _CHECKPOINT_SEQUENCE if s not in _checkpoint_step]` against the same fixed 4-step sequence for every phase — there is no per-phase key or reset on `checkpoint-tag` completion. Confirmed in production: HARNESS013-main's checkpoint left all four step names (`checkpoint-security`, `checkpoint-intent`, `checkpoint-docs`, `checkpoint-tag`) in `state.json`; when HARNESS014-main reached its checkpoint row, `flex_build.py next-action` returned `{"action": "done"}` immediately — `checkpoint-security`/`checkpoint-intent`/`checkpoint-docs` were never dispatched and no `cp-HARNESS014-main` tag was created. HARNESS014-main's checkpoint was completed manually against the reviewer procedure checklist to unblock. Fix direction: reset `checkpoint_step` to `[]` when `checkpoint-tag` is recorded (natural end-of-sequence trigger), or key `checkpoint_step` by the active phase so a stale list from a prior phase can't satisfy `_remaining`. HIGH severity (silently skips the entire checkpoint sequence — security audit, intent review, docs review, and tagging — for every phase after the first one a given `state.json` has seen). `skills/pairmode/scripts/next_action.py` `resolve_next_action` Row 9, `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`. **RESOLVED HARNESS015-main (RESOLVER-017) — `cmd_record_checkpoint_step` now resets `state["checkpoint_step"]` to `[]` when `checkpoint-tag` is recorded.** | HARNESS014-main checkpoint (cold-eyes review) | 2026-07-13 | HARNESS014-main |

| CER-069 | CER-066's naive-`split(" | ")`-on-a-Stories-table-row bug (fixed once in `story_update.py`, Phase 94/INFRA-207) recurred independently in `next_action.py`'s `_check_phase_completion` (fixed in INFRA-222, Phase 95) — the same code shape (`stripped.split(" | ")` then a positional index into `cols`) also appears, unaudited, in: `next_story.py:92`, `index_integrity.py:124`, `flex_build.py:415`, `flex_build.py:634`, `flex_build.py:1248`, `story_resolver.py:166`. Whether each site is actually vulnerable (reads a fixed/positional index from `cols`, vs. only using `cols` to detect a header row or count columns — the latter pattern, e.g. `next_action.py`'s own `_check_cer_do_now`, is safe) has not been verified per-site. Fix direction: audit each site; for any that index positionally into `cols`, apply the same `re.split(r'(?<!\\)\|', stripped)` fix (matching `story_update.py`'s and INFRA-222's pattern); consider factoring the split into one shared helper (e.g. in a new `table_utils.py`) once ≥2 more sites are confirmed vulnerable, so this bug class stops recurring site-by-site. MEDIUM severity (recurring bug class, but each individual site is unconfirmed — not a live-hit until audited). `skills/pairmode/scripts/next_story.py:92`, `index_integrity.py:124`, `flex_build.py:415,634,1248`, `story_resolver.py:166`. **RESOLVED (INFRA-297), Phase 113 — the split now has one owner.** The audit found **seven** positional sites, not six: the row's own list omitted one of the two `flex_build` `mark-phase-complete` rewrite paths, both of which split, edit the status cell and rejoin the row before writing it back. All seven now call the new `split_table_row` helper (`skills/pairmode/scripts/table_utils.py:21`, stdlib-only, no sibling imports so it cannot join an import cycle): `next_story.py:108`, `story_resolver.py:168`, `index_integrity.py:126`, `flex_build.py:858`, `flex_build.py:1161` (phase-index rewrite), `flex_build.py:1282` (era-ledger rewrite), `flex_build.py:2283`. The two already-correct sites were rewired to the same helper (`story_update.py:267`, `next_action.py:378`), as was the filtered-but-naive eighth site `next_action.py:432` (`_check_cer_do_now`, whose index-shifting `if c.strip()` filter is kept verbatim). The `(?<!\\)\|` regex literal now exists in exactly one module, pinned by a grep-form assertion in `tests/pairmode/test_table_utils.py`; the rewrite paths are pinned by escaped-pipe round-trip tests in `tests/pairmode/test_flex_build_mark_phase_complete.py`. | INFRA-222 spec discussion (2026-07-21 build session) |

| CER-063 | The `fold-prep` branch (`HARNESS011-main`, not yet merged to `main`) already owns a different `INFRA-203` ("Context gate edge cases and session_id safety") — itself the product of a prior renumbering to avoid colliding with `main`'s then-existing `INFRA-192`. Phase 91 (`main`) has now allocated a second, unrelated `INFRA-203` ("Make empty/missing-variable template renders in the sync-agents body-merge path fail loudly"). When `fold-prep`/`harness` eventually merges into `main`, this rail+number will collide again — the second time this exact pattern has occurred. Not urgent while `fold-prep` remains unmerged. Fix at merge time: renumber the incoming `fold-prep` `INFRA-203` (recommended, since it was already renumbered once and its content is less coupled to the flex-self-hosting-tool identity than the `main` story). MEDIUM severity (forward-looking; no current-branch defect). No specific files — applies to whichever phase performs the `fold-prep`/`harness` merge into `main`. **OBSOLETE — verified Phase 115 / INFRA-310. The fold-prep/harness merge into main already happened and the colliding INFRA-203 was renumbered at merge time. Evidence: commit `3367750e` — "merge(fold-prep): … renumber colliding fold-prep INFRA-192..199 to INFRA-203..210 (RELEASE-014)"; the harness story is now INFRA-215, recorded at `docs/phases/phase-HARNESS011-main.md:18`.** | Phase 91 intent review | 2026-07-16 | HARNESS (fold-prep merge) |

| CER-062 | `story_new.py`'s `_append_to_phase` phase-manifest lookup (`skills/pairmode/scripts/story_new.py:127-137`) only matches two filename shapes: `{phase}-*.md` (phase arg as filename prefix) and exact `phase-{phase}.md`. Suffixed phase manifests — filenames of the form `phase-<phase_id>-<suffix>.md`, the naming convention `phase_new.py --phase-id --suffix` produces (CER-038) — match neither glob, so `story_new.py --phase MU020` silently returns `False` from `_append_to_phase` and the new story is never added to the phase's Stories table, with no error surfaced to the caller. Confirmed on the Repo-L project (fable-orchestrated build): the operator had to add the Stories table rows by hand. Fix: add a third glob `phase-{phase}-*.md` (or generalize to `*{phase}*.md`) alongside the existing two in `_append_to_phase`, and consider surfacing a warning when auto-registration falls through to `False` rather than failing silently. MEDIUM severity (silent auto-registration failure on suffixed phases — story files are still created correctly; only the phase manifest's Stories table drifts until manually reconciled). `skills/pairmode/scripts/story_new.py:127-137`. **Pulled forward into Phase 87 as INFRA-197.** **RESOLVED (confirmed Phase 119, INFRA-380):** the anchored `phase-{phase}-*.md` glob (INFRA-197) and not-found warning (INFRA-301) were already correctly shipped; INFRA-380 added the missing negative regression test proving prefix non-match against a longer phase id's suffixed manifest. | Repo-L repo orchestrator (fable, external report) | 2026-07-14 | 87 |

| CER-061 | `cmd_record_checkpoint_step` in `flex_build.py` (RESOLVER-012) does not call `_depth_guard` before writing `state.json`, unlike every other write-capable `--project-dir` command. The write target is always the fixed relative path `.companion/state.json` and `step_id` is whitelist-validated — no arbitrary path construction — so there is no traversal risk. But the missing guard is an inconsistency with the project convention. Fix: add `_depth_guard(project_dir)` at the entry of `cmd_record_checkpoint_step`. MEDIUM. `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-015) — `_depth_guard(project_dir)` added at entry of `cmd_record_checkpoint_step`, consistent with all other write-capable commands.** | HARNESS009-main security audit | 2026-07-04 | HARNESS009-main |

| CER-068 | [Renumbered from harness-branch CER-062 during the HARNESS016-main fold merge (RELEASE-014) — the ID collided with a new main-branch CER-062.] `_CHECKPOINT_SEQUENCE` is defined twice: once in `next_action.py` (the authoritative location) and once inlined as literal strings in `flex_build.py`'s `record-checkpoint-step` command (RESOLVER-012). Current values match, but a future change to `next_action.py`'s tuple must also be applied manually to `flex_build.py`. Fix: import `_CHECKPOINT_SEQUENCE` from `next_action` in `flex_build.py` (or extract to a shared constants module). MEDIUM (silent divergence risk if sequence ever changes). `skills/pairmode/scripts/flex_build.py` record-checkpoint-step, `skills/pairmode/scripts/next_action.py`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-015) — `_CHECKPOINT_SEQUENCE` imported from `next_action` in `flex_build.py`; inlined tuple removed. Single authoritative definition.** | HARNESS009-main intent review | 2026-07-04 | HARNESS009-main |

| CER-063 | `parse_worker_verdict_text` was marked deprecated in RESOLVER-013 but not removed. It has no live call sites in production code; tests in `test_next_action.py` still exercise it. Preferred action per story spec was removal. Remove the function and update the test file to remove or replace the tests that exercise it. LOW-MEDIUM. `skills/pairmode/scripts/next_action.py:1077`, `tests/pairmode/test_next_action.py`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-016) — `parse_worker_verdict_text` removed from `next_action.py`; `TestParseWorkerVerdictText` class removed from `test_next_action.py`; any uncovered cases ported to `test_parse_worker_verdict_json.py`.** | HARNESS009-main intent review | 2026-07-04 | HARNESS009-main |

| CER-064 | [Renumbered from main-branch CER-061 at the RELEASE-008 fold merge — the ID collided with harness's CER-061.] `spec_preflight.py` and `flex_build.py` `spec-preflight` subcommand construct `story_path` by splitting `--story-id` on `-` and building `docs/stories/<rail>/<story_id>.md` without a `Path.resolve().relative_to(project_dir)` containment check. The path is read-only (`read_text` / `.exists()`) over trusted orchestrator input — not exploitable in practice — but the sibling guard pattern from `permission_scope.py` is not applied here. Fix: add `path.resolve().relative_to(project_path)` guard before any `story_path` I/O in both `spec_preflight.py:_story_path_for` and `flex_build.py cmd_spec_preflight`. LOW severity (defense-in-depth; read-only, trusted input). `skills/pairmode/scripts/spec_preflight.py:128-129`, `skills/pairmode/scripts/flex_build.py:65-71,138-139`. **INFRA-304 (Phase 114) — RESOLVED:** both entry points (`spec_preflight.spec_preflight` and `flex_build`'s `spec-preflight` subcommand) now route through one new helper, `flex_build.story_path_checked`, which rejects a `--story-id` that `_STORY_ID_RE` does not match and then delegates to the existing `_story_path` containment guard (added by RESOLVER-015 — CER-064's fix was already half-applied on the `flex_build` side before this story; only `spec_preflight.py`'s inline construction was still unguarded). A rejected ID now exits 2 with `spec-preflight: {message}` on stderr from both entry points, instead of an uncaught `ValueError` traceback from `flex_build` and an unguarded read attempt from `spec_preflight.py`. The row's own file:line anchors were stale: `spec_preflight.py:_story_path_for` never existed (the inline construction lived directly in the `spec_preflight` command body, `spec_preflight.py:139-140` pre-fix), and `flex_build.py:65-71,138-139` is `_stamp_active_story`/`_read_story_frontmatter` today, not the story-path logic (which is `flex_build.py:129-144`, `_story_path`). Corrected anchors: `skills/pairmode/scripts/spec_preflight.py` (command body, pre-fix), `skills/pairmode/scripts/flex_build.py:129-144` (`_story_path`) and `:2734-2757` (`cmd_spec_preflight`). Parity pinned by a new parametrised test in `tests/pairmode/test_spec_preflight.py`. | Phase 84 checkpoint security audit | 2026-07-06 | 84 |

| CER-065 | [Renumbered from main-branch CER-060 at the RELEASE-008 fold merge — the ID collided with harness's CER-060. Fold note: the fold deleted `reviewer.md.j2` and `.claude/agents/reviewer.md`; the residual `git clean -fd` surface is now `skills/pairmode/skills/reviewer/procedure.md` (FAIL-revert block).] BUILD-038 dropped `git clean -fd` from the FAIL-revert block in `.claude/agents/reviewer.md`, but the canonical template `skills/pairmode/templates/agents/reviewer.md.j2:237` still carries `git clean -fd`. The story spec deliberately scoped the template out (template propagation gets its own spec). **Re-introduction vector is bootstrap, not sync-agents:** `bootstrap.py` (`:63,:1014`) renders the full `reviewer.md.j2` into every *fresh* project, so any newly-bootstrapped project gets `git clean -fd` back. `sync-agents` does **not** re-add it — `_merge_body_sections` (`pairmode_sync.py:216`) is additive and leaves existing target sections (the `## Decision` H2 that holds this line) untouched. **Fix must also update the guardrail test:** `tests/pairmode/test_templates.py:677-679` (`test_revert_on_fail`) currently asserts `"git clean -fd" in self.output` — dropping the line from the template makes this assertion fail. The follow-up `doc`-class story must: (a) drop `git clean -fd` from `reviewer.md.j2` FAIL-revert + update surrounding prose to mirror BUILD-038; (b) replace the line-679 positive assertion with a negative assertion (`git clean -fd` absent) so the build lands green. primary_files: `skills/pairmode/templates/agents/reviewer.md.j2`, `tests/pairmode/test_templates.py`. MEDIUM (fresh bootstraps re-introduce the deleted blast radius; the live reviewer.md fix is not durable across bootstraps until propagated). Carried forward into HARNESS002-main as CF-1 (harness branch). `skills/pairmode/templates/agents/reviewer.md.j2:237`, `tests/pairmode/test_templates.py:677-679`. **INFRA-304 (Phase 114) — RESOLVED (premise corrected):** (i) `reviewer.md.j2` carries no revert block — no `git` command of any kind — since HARNESS-002 rewrote it into a 47-line thin shell whose whole body loads `skills/pairmode/skills/reviewer/procedure.md` (`grep -c 'git ' skills/pairmode/templates/agents/reviewer.md.j2` is `0`); the bootstrap re-introduction vector this row names (`bootstrap.py` rendering the fat template into every fresh project) is therefore inert — there is nothing left in the template to re-introduce. (ii) The row's prescribed fix (b) — inverting `test_templates.py`'s assertion to require `git clean -fd`'s absence — is **rejected**: the whole-tree `git checkout . && git clean -fd` form is the intentional, still-live fallback for a legacy story with no declared `primary_files`/`touches`, documented at `skills/pairmode/skills/reviewer/procedure.md:511-523` and `docs/architecture.md`'s commit/revert-scoping paragraph; asserting its absence would pin a false statement about the system. (iii) The real residue was a class-level `@pytest.mark.skip` on `TestReviewerAgentTemplate` whose reason claimed `agents/reviewer.md.j2` was deleted (it was retired to the thin shell, not deleted, and is still rendered by `bootstrap.py`), and a revert contract — declared-scope form + gated legacy fallback — with no executing test anywhere. (iv) Fixed by: correcting the stale skip reason in place (class stays skipped — it documents the retired fat-template shape); a new, unskipped `TestReviewerRevertContract` class in `tests/pairmode/test_procedure_skills.py` asserting the declared-scope form, the gated whole-tree fallback, and the `FAIL-CAUSE:` ordering directly from the live procedure text; and a new `TestReviewerTemplateThinShell` test rendering `reviewer.md.j2` and asserting no `git ` command and the presence of the procedure pointer. The row's stale line references (`reviewer.md.j2:237` — pre-HARNESS-002 fat template, no longer exists; `test_templates.py:677-679` — the file has since grown, the actual skip/assertions are at `tests/pairmode/test_templates.py:554-620`) are corrected here. `skills/pairmode/templates/agents/reviewer.md.j2:237`, `tests/pairmode/test_templates.py:677-679`. | Phase 79 build summary + intent + cold-eyes review | 2026-06-29 | 79 |

| CER-059 | Intent review of HARNESS001-ante1 surfaced three forward (HARNESS006-era) items from `fleet_discovery.py` (RELEASE-005): **(a)** the current `docs/fleet-snapshot.md` shows all 9 discovered projects bound via Signal 2 (`pairmode_version`) only — **zero Signal 1 (`pairmode_scripts_dir`) hits** — even though the fleet is expected to be scripts-bound to this checkout. This may be a false-negative in Signal-1 detection (`_check_signal1` `relative_to` comparison vs `_THIS_SCRIPTS_DIR`/`_FLEX_ROOT`) or it may be accurate (projects baked an absolute path that no longer resolves under this checkout). Must be diagnosed **before the pre-fold discovery gate runs**, because that gate relies on accurate Signal-1 binding to know which projects break at the fold; a false negative would under-report blast radius. **(b)** Add an explicit Signal-1 verification step to the pre-fold gate run in `docs/harness-cutover-runbook.md` ("after syncing each project, re-run discovery and confirm `binding: scripts` appears"). **(c)** When HARNESS006 is specced, its story for executing the fold must carry an explicit AC: reconcile `docs/stories/RELEASE/RELEASE-002.md` status `deferred → complete` on main after the fold merge (the runbook notes this in prose; it should be a checkable AC). MEDIUM (cutover-safety accuracy; no impact on HARNESS001-ante1 correctness — the tool is built and tested). `skills/pairmode/scripts/fleet_discovery.py` `_check_signal1`, `docs/fleet-snapshot.md`, `docs/harness-cutover-runbook.md`. **RESOLVED Phase 105 — INFRA-270: (a) diagnosed and instrumented — the zero-hit result is correct for a 0.2.x fleet (the key-value `pairmode_scripts_dir` form is only written by `sync-all --apply` at 0.3.0 migration); Signal-1 absence is now classified via `signal1_absence_reason` into four reason codes (`no-build-md`, `no-declaration`, `inline-only`, `foreign-checkout`), surfaced in the CLI, `--json`, and the snapshot, and `discover()` now reports inline- and foreign-bound projects that previously fired neither signal and were invisible to the pre-fold gate. (b) and (c) verified as already satisfied, not rebuilt: `docs/harness-cutover-runbook.md` carries the CER-059b Signal-1 verification step, and `docs/stories/RELEASE/RELEASE-002.md` is `status: complete`.** | HARNESS001-ante1 intent review | 2026-06-26 | 105 |

| CER-058 | A brand-new project (`Repo-B`, bootstrapped 2026-06-26, still in inception) appeared in flex's `.companion/state.json` `registered_projects` without the operator ever running the observability/registration flow (`pairmode_sync.py register`). Suspected cause: the project's bootstrap path (or a coalescing agent session) wrote `registered_projects` directly rather than going through the registration command/hook. Needs investigation: enumerate every writer of `registered_projects` and confirm none bypasses the intended registration entry point; if a bootstrap path writes it directly, either route it through `register` or document the provenance. Relevant to DP8 (fleet discovery) — the discovered fleet must reflect deliberate registrations, not bootstrap artifacts. Severity MEDIUM (no incident, but unexplained fleet-state writes undermine cutover blast-radius accuracy). Investigate `bootstrap.py`, `pairmode_sync.py register`, and any agent-side registration. **RESOLVED Phase 105 — INFRA-270: audit found no in-repo bypass path — `pairmode_register.py`'s `register`/`unregister` are the only in-repo assignments to `registered_projects` (`bootstrap.py` only prints the register command as operator guidance; it never writes the key). The `Repo-B` entry's provenance is genuinely unrecoverable — it arrived out of band (a manual or agent-session edit of `state.json`), not through any traceable in-repo writer, and now audits as `source: unknown` via the new `audit-projects` command. The single-writer invariant is now test-enforced (`test_registered_projects_has_a_single_writer` against `REGISTERED_PROJECTS_WRITERS`) rather than resting on a comment, and a provenance sidecar (`registered_projects_provenance`) records `source`/`registered_at` for every registration going forward. The invariant covers in-repo code paths only — it does not and cannot prevent an out-of-band edit of `state.json` itself.** | RELEASE-003 build / operator observation | 2026-06-26 | 105 |

| CER-048 | The static deny list (`.claude/settings.json` `Edit/Write(hooks/**)` etc.) and the story-scoped permissions system (Phase 55 `scope_guard.py` + `permissions-create`) do not talk to each other. `scope_guard` fails open in every non-build context (no active story → allow; no permissions file → allow; malformed → allow), so the static denies are the only always-on protection for `hooks/`, `.claude-plugin/`, `skills/seed/`, `skills/companion/`, `lessons/`. Consequence: a story that legitimately declares a protected file in `primary_files` (with stated reason, per CLAUDE.md item 7) is still hard-blocked at the permission layer — observed Phase 68 INFRA-175, where the builder was denied `hooks/session_start.py` despite an authorized spec, requiring a manual operator lift-and-restore of the deny entries. Fix direction: teach `scope_guard` a fail-closed protected-path list (the same globs as the static denies) that is satisfiable only by an active story whose permissions file explicitly names the protected path; once shipped and verified, retire the static `Edit/Write` denies in favor of the story-aware gate. Must preserve: always-on protection outside build mode, and protection against the orchestrator itself. MEDIUM severity (methodology friction + duplicate protection surfaces that can drift). `skills/pairmode/scripts/scope_guard.py`, `.claude/settings.json`, `hooks/pre_tool_use.py` (read-only branch), CLAUDE.md item 7. **RESOLVED Phase 100 (cp100) — the fix direction largely shipped in v0.3 (`PROTECTED_GLOBS` story-aware fail-closed gate, satisfiable only via an active story whose permissions artifact names the path; `hooks/**` already retired from the static deny list). A post-cp99 audit (2026-07-24) found three residues, all closed by INFRA-253: (1) a fail-open hole where `scope_guard.check_path()`'s active-story branch returned allow when the permissions artifact was missing/empty/malformed without checking `_is_protected()` — only the no-story branch checked it, leaving `hooks/**` etc. wide open for the duration of a build whose `permissions-create` never ran; (2) the remaining four `PROTECTED_GLOBS`-duplicate static denies in flex's own `.claude/settings.json` (`skills/companion/**`, `lessons/**`, `.claude-plugin/**`, `skills/seed/**`) retired, leaving only tooling (PostToolUse pytest hook, Bash allow rules, `Edit(.claude/agents/**)` allow) plus the deliberately-outside-the-gate `Edit(docs/phases/permissions/**)` deny (anti-self-scope-modification); (3) this stale row. Explicit remainder, not curable by scope_guard: writes to `.claude/settings.json` itself are blocked by the Claude Code auto-mode classifier — harness-level, above project hooks — so operator-applied edits remain the sanctioned path for changes to the settings file. Two live hits in phase 99: INFRA-247 (classifier block, operator-applied) and INFRA-249 (worktree-isolation, not classifier, but same operator-fallback pattern — a worktree's `.claude/settings.json` is a separate filesystem copy from the main checkout's, so an in-worktree edit cannot land in the main checkout regardless of classifier state).** | Phase 68 build observation; residues found by post-cp99 audit 2026-07-24 | 2026-06-12 | 68, 100 |

| CER-040 | `context_budget.decide()` fails open silently when `state.json` is absent or contains malformed JSON — same failure shape as the old transcript-path bug, just one layer out. `_read_state()` returns `None` on any I/O or parse error; `decide()` returns `None` (pass-through) immediately. A pairmode project with a corrupted or missing state.json gets an invisible dead gate with no operator signal. Fix options: (a) return `_CONTEXT_CHECK_REQUIRED_MSG` block when `_read_state()` returns `None` (strictest — treats missing state as uninitialized); (b) add a `state_missing` sentinel path that emits a softer warning but still passes through (preserves non-pairmode compat). Non-pairmode projects intentionally have no state.json — fix must not break that case. MEDIUM severity. `skills/pairmode/scripts/context_budget.py` `_read_state()` and `decide()`. **RESOLVED Phase 105 — INFRA-272 took option (b) (soft, observable pass-through) for the four fail-open branches option (a) did not already cover: an unanchored/unparseable staleness comparison in `_is_stale()`, a failed `session_state` import, and the hook's own blanket `except` around `decide()` now each emit one `context_budget: gate not enforced — ` line to stderr before continuing, on top of the missing-state.json and malformed-state.json blocks INFRA-150/INFRA-182 already implemented for option (a)'s two cases. Non-pairmode projects (no `.companion/` directory) remain silent by design — that case is unchanged.** | Phase 58 back-check (opus) | 2026-06-03 | 58 |

| CER-041 | `context_current_tokens` has no session invalidation. After `/clear`, `state.json` retains the previous session's token value until the orchestrator overwrites it at the first Context gate. If anything spawns a Task tool before the Context gate runs (e.g. an early orchestrator Bash call that happens to be a Task), the hook reads a stale — potentially too-high — value and may block incorrectly. In the happy path this is benign (Context gate runs before any builder spawn per CLAUDE.build.md). Fix: add `context_current_tokens_recorded_at` (ISO timestamp) alongside the value; `read_context_tokens_from_state` treats a value older than N minutes as absent. Alternatively: reset the key in `story_context.py --clear` or a `SessionStart` hook. LOW-MEDIUM severity (mitigated by protocol; only dangerous if Context gate is skipped or reordered). `skills/pairmode/scripts/context_budget.py` `read_context_tokens_from_state`, `skills/pairmode/scripts/flex_build.py` `set-context-tokens`. **RESOLVED Phase 105 — INFRA-272.** The 60-minute TTL that shipped here (Phase 59, INFRA-151) was superseded by the `context_session_reset_at` comparison (CER-047, Phase 68/74, INFRA-175/182) — `decide()` has read `context_current_tokens` directly and bypassed the TTL ever since. This story did not build a new invalidation mechanism (none was needed); it removed the now-unreachable TTL from `read_context_tokens_from_state`, deleted the dead `_CONTEXT_TOKEN_STALE_MINUTES` constant, removed `context_current_tokens_ttl_minutes`'s last reader (`skills/observability/api/src/routes/context.ts`'s threshold list), and corrected the three prose claims (`story_context.py`, `session_state.py`, `docs/architecture.md`) that still described the retired TTL as live. | Phase 58 back-check (opus) | 2026-06-03 | 58 |

| CER-001 | reconstruct.py `parse_ideology()` compat wrapper uses NamedTemporaryFile round-trip; on SIGKILL leaves ideology.md copy in /tmp. Fix: add `parse_ideology_text(text: str)` to ideology_parser.py to eliminate temp file. reconstruct.py:33-50 **OBSOLETE — verified Phase 115 / INFRA-310. `reconstruct.py`'s `parse_ideology()` compat wrapper now delegates to `ideology_parser.parse_ideology_text(text)`; no temp file remains. Evidence: `skills/pairmode/scripts/ideology_parser.py:106` — `def parse_ideology_text(text: str) -> dict:`; `skills/pairmode/scripts/reconstruct.py:39` — `return _ideology_parser.parse_ideology_text(text)`.** | Security audit cp12 | 2026-04-24 | 12 |

| CER-002 | bootstrap.py has no `--yes`/`--no-input` flag; non-interactive callers must use `input="y\n" * N` workaround. Limits CI/scripted use. Add `--yes` flag that auto-confirms all prompts. **OBSOLETE — verified Phase 115 / INFRA-310. `bootstrap.py` has a `--yes`/`-y` non-interactive flag. Evidence: `skills/pairmode/scripts/bootstrap.py:1261` — `"--yes", "-y",`.** | Intent review cp13 | 2026-04-25 | 13 |

| CER-003 | cer.py and phase_new.py call Path(project_dir).resolve() but do not apply the len(parts) < 3 suspicious-path depth check that all other pairmode scripts apply. Inconsistent guard discipline. **OBSOLETE — verified Phase 115 / INFRA-310. Both `cer.py` and `phase_new.py` now apply the `len(parts) < 3` depth guard. Evidence: `skills/pairmode/scripts/cer.py:569` — `if not proj.is_dir() or len(proj.parts) < 3:`; `skills/pairmode/scripts/phase_new.py:449` — `if not project_path.is_dir() or len(project_path.parts) < 3:`.** | Security audit cp14 | 2026-04-25 | 14 |

| CER-004 | lesson_review.py uses str.startswith() for path containment instead of Path.relative_to(); vulnerable to prefix collision on unusual paths. lesson_review.py:149 **OBSOLETE — verified Phase 115 / INFRA-310. `lesson_review.py` uses `Path.relative_to()` for containment, not `str.startswith()`. Evidence: `skills/pairmode/scripts/lesson_review.py:180` — `template_path.relative_to(boundary)`.** | Security audit cp14 | 2026-04-25 | 14 |

| CER-005 | phase_new.py missing len(parts) < 3 depth guard; resolve() is called but shallow-path check absent. All other pairmode entry points apply this guard consistently (bootstrap, audit, sync, score, reconstruct, story_new, era_new). phase_new.py:286 **OBSOLETE — verified Phase 115 / INFRA-310. `phase_new.py` applies the `len(parts) < 3` depth guard. Evidence: `skills/pairmode/scripts/phase_new.py:449` — `if not project_path.is_dir() or len(project_path.parts) < 3:`.** | Security audit cp15 | 2026-04-27 | 15 |

| CER-006 | validate_story_file rejects empty primary_files list, but story_new.py writes primary_files: [] by default. Every new story file fails its own validator until primary_files is filled in. Either allow empty list for draft stories, or omit the field entirely when empty. schema_validator.py, story_new.py **OBSOLETE — verified Phase 115 / INFRA-310. `story_new.py` deliberately omits `primary_files` for new draft stories, and `schema_validator.py`'s status-aware block accepts an absent/empty `primary_files` for `draft`/`backlog` stories. Evidence: `skills/pairmode/scripts/story_new.py:71` — `# primary_files is deliberately omitted for new (draft) stories (CER-006);`; `skills/pairmode/scripts/schema_validator.py:316` — `if status in ("draft", "backlog"):`. **Drift:** the closeout plan cited `schema_validator.py:204-211`; the live anchor is `:316`.** | Intent review cp15 | 2026-04-27 | 15 |

| CER-007 | era_new.py writes unquoted id field (id: 001 — YAML integer) while bootstrap.py writes quoted id (id: "001" — YAML string). Current regex-based parsers tolerate both, but a proper YAML parser (e.g., python-frontmatter, PyYAML) would return integer 1 from era_new output. Fix era_new.py to write id: "{era_id}" (quoted string). era_new.py **OBSOLETE — verified Phase 115 / INFRA-310. `era_new.py` writes a quoted `id` field. Evidence: `skills/pairmode/scripts/era_new.py:49` — `f'id: "{era_id}"\n'`.** | Intent review cp15 | 2026-04-27 | 15 |

| CER-008 | permission_scope.py: if settings.local.json contains valid non-object JSON (e.g., a bare array), _read_json returns it as-is and the subsequent .setdefault() call raises AttributeError. Add a guard: if the parsed value is not a dict, treat it as {}. permission_scope.py:75 **OBSOLETE — verified Phase 115 / INFRA-310. `permission_scope.py`'s `_read_json` returns non-dict JSON as-is behind an explicit `isinstance` guard at the call site. Evidence: `skills/pairmode/scripts/permission_scope.py:163` — `if not isinstance(data, dict):`.** | Security audit cp16 | 2026-04-28 | 16 |

| CER-009 | hooks/stop.py, post_tool_use.py, session_end.py: PIPE_PATH defaults to hardcoded "/tmp/companion.pipe" and is then conditionally overridden by reading pipe_path from .companion/state.json (relative path). A crafted state.json could redirect pipe writes to an arbitrary path. No secrets in payloads; write silently drops on ENXIO if no FIFO reader. LOW severity. exit_plan_mode.py correctly uses tempfile.gettempdir(). **OBSOLETE — verified Phase 115 / INFRA-310. All five pipe-writing hooks use the flat `tempfile.gettempdir()/companion.pipe` path unconditionally; none reads `state.json["pipe_path"]` any more. Evidence: `hooks/post_tool_use.py:50` — `PIPE_PATH = os.path.join(tempfile.gettempdir(), "companion.pipe")`; `hooks/stop.py:16`, `hooks/session_end.py:17`, `hooks/exit_plan_mode.py:14`, `hooks/session_start.py:33` each carry an INFRA-238 comment stating the `pipe_path` state key was deleted. `grep -n 'pipe_path' hooks/*.py` returns only those comment lines.** | Security audit cp17 | 2026-04-30 | 17 |

| CER-010 | story_new.py --rail input is .upper()'d but not validated against a regex before being used in path construction. A caller passing --rail "../../../etc" constructs a path that escapes project_dir. No containment check present. MEDIUM severity. story_new.py:183-185 **OBSOLETE — verified Phase 115 / INFRA-310. `story_new.py --rail` is validated against a rail-name regex and the resulting directory is containment-checked. Evidence: `skills/pairmode/scripts/story_new.py:344` — `_RAIL_RE = re.compile(r"[A-Z][A-Z0-9_]*")`; `:248`/`:357` — `rail_dir.resolve().relative_to(stories_root)`. **Drift:** the closeout plan cited `:305-306`/`:318`/`:228`; live anchors are `:344`/`:248`/`:357`.** | Security audit cp18 | 2026-04-30 | 18 |

| CER-011 | era_new.py _slugify() neutralizes "/" and "." in --name before path construction, providing effective (but informal) traversal prevention. No formal resolve().relative_to() containment check present. LOW severity — _slugify() is correct but not a formal guard. era_new.py:25-31, 114-116 **OBSOLETE — verified Phase 115 / INFRA-310. `era_new.py` now applies a formal `resolve().relative_to()` containment check in addition to `_slugify()`. Evidence: `skills/pairmode/scripts/era_new.py:119` — `eras_root = eras_dir.resolve()`; `:121` — `era_path.resolve().relative_to(eras_root)`.** | Security audit cp18 | 2026-04-30 | 18 |

| CER-012 | pairmode_status.py: FLEX_ROOT computed as `Path(__file__).resolve().parent.parent.parent` resolves to `<repo>/skills/`, not the flex repo root. The constructed `start_sidebar.sh` path becomes `<repo>/skills/skills/companion/scripts/start_sidebar.sh` — a file that does not exist. Must be `parent.parent.parent.parent` (four levels up). HIGH severity — surfaced to user as broken instructions. pairmode_status.py:33 **OBSOLETE — verified Phase 115 / INFRA-310. `pairmode_status.py`'s `_REPO_ROOT` is computed at the correct depth and the `start_sidebar.sh` path it builds exists on disk. Evidence: `skills/pairmode/scripts/pairmode_status.py:39` — `_REPO_ROOT = Path(__file__).resolve().parents[3]`; `:129` builds `_REPO_ROOT / "skills" / "companion" / "scripts" / "start_sidebar.sh"`, which exists (`skills/companion/scripts/start_sidebar.sh`).** | Story review cp20 INFRA-019 | 2026-05-01 | 20 |

| CER-013 | INFRA-033 propagation gap: fallback-policy pointer was added to flex's own CLAUDE.build.md but NOT to the canonical `skills/pairmode/templates/CLAUDE.build.md.j2` template. Future bootstraps will not inherit the orchestrator-level pointer to the fallback policy (the inline `# fallback:` template comments and architecture.md subsection still propagate via separate paths). LOW severity — flex's own dogfood is correct; downstream effect contained. **RESOLVED cp-HARNESS001-main / HARNESS006-main — CLAUDE.build.md.j2 was rewritten from scratch as the thin dispatch loop; the fallback-policy pointer is not relevant to the thin-loop template and was intentionally omitted.** | Phase 21 intent review | 2026-05-04 | 21 |

| CER-014 | docs/architecture.md "Reviewer-class agent tool restriction (build-loop safety)" subsection asserts the existence of a "pre-reviewer commit discipline (committing story files and running `git checkout -- lessons/` before the reviewer fires)" as one of two layers protecting the working tree, but neither CLAUDE.build.md (project) nor CLAUDE.build.md.j2 (template) encodes this discipline. Either add the discipline to CLAUDE.build.md.j2 (preserving the architecture claim) or trim the claim (preserving truth). MEDIUM severity — defense-in-depth claim rests on aspirational orchestrator behaviour; the load-bearing tool-restriction layer remains in force regardless. **OBSOLETE — verified Phase 115 / INFRA-310. The aspirational "pre-reviewer commit discipline" claim was removed from `docs/architecture.md`; the subsection is now "Reviewer-class agent tool restriction (build-loop safety)", which describes only the load-bearing tool-restriction layer. Evidence: `docs/architecture.md:1342` — "Reviewer-class agent tool restriction (build-loop safety)."; `grep -rn 'pre-reviewer commit discipline\|git checkout -- lessons' docs/architecture.md CLAUDE.build.md skills/pairmode/templates/CLAUDE.build.md.j2` returns zero hits.** | Phase 21 security audit + intent review | 2026-05-04 | 21 |

| CER-015 | INFRA-030's CLAUDE.build.md examples hardcode `--phase N --rail RAIL` and `--attempt-number 1` placeholder literals; the orchestrator has no plumbed source for phase/rail at record time and no per-story retry counter state. The cp22 cleanup commit clarified that phase and rail are read from the current story file's frontmatter, but a small helper (e.g. `record_attempt.py --story-file <path>` that auto-extracts phase/rail from frontmatter) would close the typo surface. Without it, retry attempts may be miscounted as fresh attempts and rollup reports may see NULL phase/rail rows. MEDIUM severity. CLAUDE.build.md:82-94. **RESOLVED cp-HARNESS006-main — The thin dispatch loop template has no record_attempt.py example; the hardcoded placeholder literals are absent from the rewritten template.** | Phase 22 intent review | 2026-05-05 | 22 |

| CER-016 | effort_db.py `resolve_effort_db_path` accepts an absolute or relative `effort_db_path` from .companion/state.json and applies only `_depth_guard` (rejects paths with fewer than 3 parts after resolution) — it does not assert containment under project_dir. Same shape mirrored in record_attempt.py and pairmode_effort.py. Strictly weaker guard than permission_scope.py's resolve().relative_to() containment check. State.json is treated as project-owned (consistent with existing trust boundary), so this is informational, not exploitable. LOW severity. effort_db.py:115-139. **RESOLVED Phase 104 — INFRA-266. The `resolve_effort_db_path` half (state.json's `effort_db_path`) was already closed by INFRA-058 (commit `ee584df`), which added the `resolve().relative_to(project_dir.resolve())` containment check — that part of this finding was stale. This story closed the remaining mirrors: `record_attempt.py` and `pairmode_effort.py` both bypassed `resolve_effort_db_path` for an explicit `--db-path` and did their own unguarded resolution. Both now delegate to a new single-sourced `effort_db.resolve_db_path_arg`, which applies `_depth_guard` and containment and raises on an escaping explicit `--db-path` (deliberately stricter than the silent state.json fallback, since an operator-named path escaping must not write somewhere else unnoticed).** | Phase 22 security audit | 2026-05-05 | 104 |

| CER-017 | bootstrap.py `_record_state` auto-enables `effort_tracking: true` on every pairmode-bootstrapped project unless the user explicitly sets the key first. No data leaves the host (sqlite is local), but the behaviour is documented in architecture.md only — not surfaced to the user during bootstrap. Consider an interactive prompt or a one-line bootstrap-summary note. LOW severity (transparency, not security). bootstrap.py:267-268. **OBSOLETE — verified Phase 115 / INFRA-310. `bootstrap.py` surfaces `effort_tracking`'s auto-enable to the user at bootstrap time. Evidence: `skills/pairmode/scripts/bootstrap.py:1713` — `effort_newly_enabled = _record_state(...)`; `:1719` — `if effort_newly_enabled:` surfaces it.** | Phase 22 security audit | 2026-05-05 | 22 |

| CER-018 | `lesson.py` CLI (`capture_lesson()` and its argparse entry point) does not accept the `value_framing` or `validation_phase` fields introduced by L012 (Phase 24). These fields exist in `lessons/lessons.json` and are documented in `architecture.md`, but can only be written by direct JSON append — not through the canonical CLI writer. Schema drift: data model ahead of the writer. LOW severity (usable via direct edit; append-only policy and lessons integrity check are unaffected). lesson.py: CLI definition. **OBSOLETE — verified Phase 115 / INFRA-310. `lesson.py`'s CLI can write both `value_framing` and `validation_phase`. Evidence: `skills/pairmode/scripts/lesson.py:41-42` (signature), `:79-82` (writes), `:130-131` (`--value-framing` option; `--validation-phase` present in the same option block).** | Phase 24 intent review | 2026-05-07 | 24 |

| CER-019 | `pairmode_sync.py` `_get_project_name` returns `state["project_name"]` with only `.strip()` applied before rendering into agent-file frontmatter. A `project_name` containing embedded newlines or YAML control characters could inject extra frontmatter keys (e.g. additional `tools:` or `model:` lines) when `sync-agents` writes agent files. A malicious or malformed `.companion/state.json` could silently elevate reviewer-class tool permissions or override model assignments. Realistic threat is low (state.json is local and user-owned, same trust boundary as bootstrap.py:582), but this is the first script to write project_name into security-sensitive frontmatter fields. Fix: strip `\n`/`\r` from project_name or YAML-quote the value before template substitution. LOW severity. pairmode_sync.py:55-63. **OBSOLETE — verified Phase 115 / INFRA-310. `pairmode_sync.py`'s `_get_project_name` strips `\n`/`\r` from both return paths. Evidence: `skills/pairmode/scripts/pairmode_sync.py:147` — `return name.strip().replace("\\n", "").replace("\\r", "")`; `:148` — the fallback path applies the same `.replace()` pair. **Drift:** the closeout plan cited `:106-113`/`:108`; live anchors are `:137`, `:147-148`.** | Phase 25 security audit | 2026-05-07 | 25 |

| CER-031 | NP-6 ("Source of Truth over Recall") pattern doc (`docs/patterns/agentic-architecture/source-of-truth-over-recall.md`) was drafted in Phase 48 and is catalog-ready, but was held from PR #4 submission because open PR #3 in cloudnirvana/open-patterns ("Canonical Source Over Recall," nsmedia-io, 2026-05-29) covers the same principle in a RAG/vector-retrieval context. Our NP-6 targets agentic development workflows (LLM recall vs. canonical files) and is a genuinely distinct scope, but submitting both while PR #3 is open risks editorial conflict for the catalog maintainer. Trigger: submit NP-6 as a follow-on PR after PR #3 merges or closes. Check PR #3 status: `gh pr view 3 --repo cloudnirvana/open-patterns`. MEDIUM severity (PR #4 ships 5/6 intended patterns; NP-6 is complete and tested, just blocked by external PR timing). `docs/patterns/agentic-architecture/source-of-truth-over-recall.md` **BACKLOG-RETAIN — Phase 115 / INFRA-310. Not a defect and not deferrable work: NP-6 is drafted and catalog-ready with zero flex code surface. The only blocker is editorial timing in a repository this project does not own — cloudnirvana/open-patterns PR #3. Trigger: re-check `gh pr view 3 --repo cloudnirvana/open-patterns`; submit NP-6 as a follow-on PR once PR #3 merges or closes. Re-check date: 2026-08-01, status: OPEN (title "Add pattern: Canonical Source Over Recall (RAG & Knowledge)", author nsmedia-io, unchanged since Phase 48).** | Phase 48 intent review | 2026-06-01 | 48 |

| CER-032 | `CLAUDE.build.md.j2` template omits the four extended `<usage>` block fields (`input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_write_tokens`) and their corresponding `--tokens-in`/`--tokens-out`/`--cache-read-tokens`/`--cache-write-tokens` flags in the `record_attempt.py` example, while the live `CLAUDE.build.md` includes them. Any project bootstrapped or `sync-build`'d from the template will silently miss recording these columns in `effort.db`. LOW severity. `skills/pairmode/templates/CLAUDE.build.md.j2` Step 1 / Step 2. **RESOLVED cp-HARNESS006-main — The thin dispatch loop template has no record_attempt.py or extended usage block; the missing columns finding does not apply to the rewritten template.** | Phase 52 intent review | 2026-06-02 | 52 |

| CER-033 | `builder.md`, `reviewer.md` (and their `.j2` templates) retain the old verbose output blocks (`BUILT: Story [N.X]`, `REVIEW PASS/FAIL — Story [N.X]`) alongside the new `## Final output to orchestrator` `BUILD-RESULT`/`REVIEW-RESULT` blocks. The orchestrator correctly ignores non-structured output now, but a future template reader sees two competing exit conventions with no label marking which is authoritative. Cleanup: remove the legacy verbose blocks from both live agent files and both `.j2` templates. LOW severity (no runtime breakage). `.claude/agents/builder.md`, `.claude/agents/reviewer.md`, and their `.j2` counterparts. **RESOLVED cp-HARNESS002-main — builder.md.j2, reviewer.md.j2, .claude/agents/builder.md, and .claude/agents/reviewer.md were deleted by the dogfood flip (HARNESS-002); the legacy verbose output blocks no longer exist.** **INFRA-293 (Phase 112):** the equivalent debt in **consumer** 0.2-era agent bodies — `## Final output to orchestrator` carrying `BUILD-RESULT: DONE` alongside an additively-appended `## Return` — is what stranded Repo-C's `effort.db` rows 33/34 (E6b), and is what § Ensures B of INFRA-293 ports via the `sync-agents` `_LEGACY_HEADING_ALIASES` in-place-replacement mechanic; this is the runtime defect the flex-side LOW cleanup above did not cover. | Phase 52 intent review | 2026-06-02 | 52 |

| CER-035 | `docs/architecture.md` line 248 states "`security-auditor` has no `Bash` since it never runs commands" — but BUILD-020 (Phase 53) explicitly added `tools: [Read, Bash, Glob, Grep]` to all four reviewer-class agents including security-auditor (needed for running tests and grepping). The claim is now stale and contradicts the live agent definition. Fix: update architecture.md § Agent definitions to reflect that all reviewer-class agents share `[Read, Bash, Glob, Grep]`. LOW severity (doc accuracy). `docs/architecture.md` line 248. **OBSOLETE — verified Phase 115 / INFRA-310. `docs/architecture.md` states all four reviewer-class agents share `[Read, Bash, Glob, Grep]`. Evidence: `docs/architecture.md:1344` — "all four reviewer-class agents declare" `[Read, Bash, Glob, Grep]`. **Drift:** the closeout plan cited `:1052`; the live anchor is `:1344`.** | Phase 53 BUILD-020 reviewer | 2026-06-02 | 53 |

| CER-034 | `flex_build.py` `_story_path()` constructs `docs/stories/<rail>/<story_id>.md` from the `--story-id` CLI argument with no `Path.resolve().relative_to(stories_root)` containment check. A crafted story ID could escape `docs/stories/`. Blast radius is low (read-only, operator-supplied input from the orchestrator), but inconsistent with the containment discipline applied elsewhere. Fix: add `relative_to` guard after resolving the constructed path. LOW severity. `flex_build.py` `_story_path()`. **RESOLVED cp-HARNESS009-post1 (RESOLVER-015) — `_story_path()` now applies `Path.resolve().relative_to(stories_root)` containment check; raises `ValueError` on traversal attempt.** | Phase 52 security audit | 2026-06-02 | 52 |

| CER-042 | `skills/observability/api/src/server.ts` registers `@fastify/cors` with `origin: '*'` (line 35). When the server is bound to the default `127.0.0.1` this is acceptable (browser same-origin restrictions do not apply to loopback, and a local dev tool has no secret to protect). Risk activates if an operator sets `FLEX_OBS_HOST=0.0.0.0` to expose the server on a LAN or remote interface — in that case the wildcard origin allows any website to read all registered repos' context data, effort metrics, and file paths via cross-origin requests. Fix: narrow `origin` to explicit allowed-origins list when `FLEX_OBS_HOST != "127.0.0.1"`, or document the loopback-only constraint in the SKILL.md serve notes and warn to stderr when host override is non-loopback. LOW severity (default config is safe; requires explicit operator override to activate). `skills/observability/api/src/server.ts:35`. **RESOLVED Phase 119 — INFRA-378:** `origin` is now computed via `resolveCorsOrigin(host, allowedOrigins)` — loopback resolves to the wildcard (unchanged default), non-loopback with no allowlist denies all cross-origin requests and warns to stderr naming both env vars, non-loopback with an allowlist uses exactly that parsed list. | Phase 63 security audit | 2026-06-10 | 63 |

| CER-043 | `GET /api/user/memories` and `GET /api/user/policies` return an `abs_path` field containing the absolute filesystem path of each memory/policy file (e.g. `/home/username/.claude/projects/-mnt-work-flex/memory/user_role.md`). This discloses the operator's home directory path, username, and directory structure to any client that can reach the API. In the default loopback-only configuration this is low-risk (only the local user can reach the endpoint). Risk activates when the server is exposed beyond loopback. Fix: either omit `abs_path` from the response (the UI can reconstruct relative paths) or gate it behind a separate `?include_path=true` query parameter. LOW severity (loopback default; field is primarily a dev-convenience field). `skills/observability/api/src/routes/user.ts:15,37,126,160`. **RESOLVED Phase 119 — INFRA-377.** The opt-in gate had already shipped in Phase 118's INFRA-306 (`abs_path?: string`, optional and omitted by default, populated only when `?include_path=true` is passed with the exact string `true`); this backlog row was left unmarked. INFRA-377 verified the gate against all four Ensures (default-omit for both endpoints, `include_path=true` opt-in restores the field, any non-`"true"` value stays closed, no in-repo consumer reads `abs_path` without opting in — `test_ui_has_no_abs_path_or_user_api_consumer` already covered this) and closed the one gap found: `skills/observability/api/tests/user.test.ts` had default-omit and `include_path=true` coverage for `/api/user/memories` but neither for `/api/user/policies`, and no coverage for a non-`"true"` `include_path` value on either endpoint — added both. | Phase 63 security audit | 2026-06-10 | 63, 119 |

| CER-044 | `skills/observability/api/src/parsers/phaseIndex.ts:44–46` extracts the `href` from a Markdown link in `index.md` and constructs a filesystem path as `docs/phases/${href}` without any `Path.resolve().relative_to()` containment check (TypeScript equivalent: `path.resolve()` + check that the resolved path starts with the project root). A crafted `index.md` with a link like `[Phase](../../../../etc/passwd)` would cause the system endpoint to attempt to read an arbitrary file outside the project directory. In production the operator controls `index.md` so practical exploitability is low, but the pattern is inconsistent with the containment discipline applied in the Python scripts. Fix: after constructing the candidate path, verify it resolves under `projectDir` before reading. LOW severity (attacker must control `index.md` which is project-owned). `skills/observability/api/src/parsers/phaseIndex.ts:44-46`. **OBSOLETE — verified Phase 115 / INFRA-310. `phaseIndex.ts`'s `resolveFileFromHref` applies a `path.resolve()` + `startsWith(safeRoot)` containment check before the caller reads the file (shipped by OBS-006). Evidence: `skills/observability/api/src/parsers/phaseIndex.ts:54` — `function resolveFileFromHref(...)`; `:57` — `const safeRoot = path.resolve(projectDir);`; `:59` — `if (!candidatePath.startsWith(safeRoot + path.sep) && candidatePath !== safeRoot)`. **Drift:** the row cites `:44-46`; live anchor is `:54-59`.** | Phase 63 security audit | 2026-06-10 | 63 |

| CER-053 | D1 — expected_step_tokens is mis-sourced and uniform fleet-wide. Seeded from the effort baseline BUILDER MEDIAN (bootstrap.py:417 _load_seed_expected_step_tokens -> by_role.builder.median; _DEFAULT_EXPECTED_STEP_TOKENS=53000), stamped to every project (sync.py:594) and read by context_budget.py:526. So the context-budget WINDOW-GROWTH constant is sourced from per-story EFFORT COST — the effort.db != context-control comingling (= harness agreements DP7) — and never re-estimated per project (Repo-L & Repo-E both show exactly 53000). Effect: live budget gate over-reserves ~53k and fires far too early everywhere. RESOLVE in Era 003: re-derive expected_step_tokens from thin-harness return-block growth, not effort; gate rewrite at HARNESS006 + Phase G. Cross-ref: docs/agreements/HARNESS001-ante1.md DP7, docs/agreements/era-002-closeout.md D1. **RESOLVED cp-HARNESS007-main (OBS-003) — THIN_HARNESS_STEP_TOKENS = 5000 replaces the effort.db-derived 53k; context route reports provenance label "thin-harness return-block growth"; source = "default" (not "state.json").** **Follow-up (INFRA-254, Phase 100):** the HARNESS-003/CER-053 fix above severed the *live* estimate entirely rather than re-sourcing it — `decide()` called `estimate_next_step_tokens(None, None, seed)`, leaving a static `state.json` value that a hand-edited number (e.g. `111`) could silently occupy indefinitely. INFRA-254 restores liveness from a DP7-clean source: `context_current_tokens` deltas observed by PostToolUse are appended to a bounded ring buffer (`context_step_growth_samples`, capped at 20), and `expected_step_tokens` is derived as the ring-buffer median once >= 5 samples exist (falling back to the stored seed, then `THIN_HARNESS_STEP_TOKENS`) — `derive_expected_step_tokens()`/`record_step_growth()` in `context_budget.py`, never reading effort.db. The DP7 boundary from this row's original resolution remains intact. | observability-surface | 2026-06-25 | — |

| CER-054 | D2 — context_current_tokens stuck at session-reset seed (25.0k) and STALE on fleet projects (Repo-L recorded ~45h ago, Repo-E ~15h ago) in the observability SPA. The live PostToolUse token writer appears not to be updating these projects. Needs root-cause: writer not firing vs. projects simply idle since the Phase 74 fix. RESOLVE in Era 003 Phase G (observability refactor) alongside the harness context-control rework. Cross-ref: docs/agreements/era-002-closeout.md D2. **RESOLVED cp-HARNESS007-main (OBS-004) — Root-cause diagnosed: writer (post_tool_use.py PostToolUse branch) is correct; 25k stale values are genuine idle (no Task/Agent spawns on those projects). No code fix required; the SPA's staleness badge (age > TTL) already surfaces this. Context-control rework remains for future phase.** | observability-surface | 2026-06-25 | — |

| CER-055 | D3 — waypoint outcomes render uniformly FAIL across all recent waypoints on both Repo-L (224 attempts) and Repo-E (460 attempts) — implausible. Either outcome recording (record_attempt.py -> effort.db outcome column; builder rows may carry null outcome) or the SPA render (effortDb.ts queryWaypoints / context.ts) is wrong. Also corrupts the pairmode_effort.py 'models' PASS-rate report. RESOLVE in Era 003 Phase G. Cross-ref: docs/agreements/era-002-closeout.md D3. **RESOLVED cp-HARNESS007-main (OBS-005) — Root cause: queryWaypoints had `AND outcome = 'FAIL' AND agent_role = 'reviewer'` filters; all 224 displayed waypoints were genuine reviewer-FAIL rows but builder/PASS/NULL rows were silently hidden. Fix: removed both filters. NULL outcome preserved as null (not mapped to FAIL). record_attempt.py confirmed correct: builder rows store NULL (pending outcome), reviewer rows store PASS/FAIL.** | observability-surface | 2026-06-25 | — |

| CER-056 | current-phase / _parse_index_phases treat only 'complete' as inactive, so a 'deferred' (or 'backlog') phase reads as the active phase. Surfaced at Era 002 close-out: after deferring Phase 64, 'flex_build.py current-phase' returned docs/phases/phase-64.md (deferred) as active. Latent in practice — once a later non-complete phase exists (e.g. HARNESS001-ante1 post-transition) the last-non-complete walk selects it correctly. RESOLVE in Era 003: the next-action resolver must treat deferred/backlog as inactive by design (supersedes current-phase). Cross-ref: docs/agreements/era-002-closeout.md (deferred-recognition gap). **RESOLVED cp-HARNESS009-main (RESOLVER-014) — `_resolve_active_phase` now uses first-non-inactive-wins (break after first hit); `is_phase_inactive` covers complete/deferred/backlog. Phase 23 index status corrected to 'deferred'.** | era-002-closeout | 2026-06-25 | — |

| CER-060 | [HARNESS001-main] resolve_next_action Row 5 (retry-upgrade) hardcodes model='opus' instead of delegating to select_builder_model(attempt_number=2). Behaviour is correct today (matches the selector table) but the retry tier is encoded in two places; the DP5 composition guard (test_next_action_compose.py) only asserts import-presence, not call-site coverage, so a future model_selector retry-tier change would silently diverge. Fix in HARNESS002: either compute the Position's builder_model at the next attempt number on FAIL so Row 5 uses position.builder_model, or add a test asserting Row 5 model == select_builder_model(...,attempt_number=2)[0]. **RESOLVED cp-HARNESS002-main — RESOLVER-006; see the resolution column.** | intent-reviewer | 2026-06-27 | RESOLVED cp-HARNESS002-main — RESOLVER-006 implemented the fix: infer_position now computes builder_model at attempt_count+1 on FAIL; Row 5 emits position.builder_model/builder_model_reason. Selector is now the single source of the retry tier. |

| CER-114 | MEDIUM: no deterministic spawn-completion recording — pairmode registers no `SubagentStop` hook, and `post_tool_use.py`'s Task/Agent branch fires at *launch* for async spawns (tool_response = "launched"), so a spawn's final outcome/tokens are only ever recovered by timer heuristics. RELEASE-066 new-1 field-proved the gap is structural in-session: the harness re-serializes live task output files (mtime refreshed continuously), so `QUIESCENT_AGE_SECONDS=900` promotion can never fire for the driving session's own spawns, and ~18% of transcripts end with `stop_reason: None` — a live orchestrator session cannot fully reconcile its own just-finished workers (Repo-D row 13: content parse-proven `PASS`, row stuck pending until a later session sweeps). Fix direction: thin `SubagentStop` relay (verify it fires for async/background agents on the current Claude Code version) runs the single-row reconcile for its `agent_id`, stamping outcome from the event payload rather than re-parsing a possibly-mid-flush file tail; demote the quiescence sweep to backstop for crashed hooks/evicted tmp. Root-cause sibling: the upstream missing `stop_reason` stamp. **RESOLVED by INFRA-298 (Phase 113):** the harness-verification gate (Ensures A1-A3) confirmed empirically that `SubagentStop` fires for both a synchronous and an async/background spawn on the Claude Code version in use (see `docs/stories/INFRA/INFRA-298.md` § Evidence for the captured payloads and the rig that produced them), so the abort seam (A4) did not fire and the fix shipped: `hooks/subagent_stop.py` (thin relay) -> `subagent_transcript.reconcile_one` (single-row reconcile keyed on `agent_id`, payload-preferred over the row's `output_file`, file fallback when the payload carries no usable outcome). `QUIESCENT_AGE_SECONDS` and `reconcile_pending_attempts` are demoted to backstop in prose only — value and age-gating unchanged. The G1 field-run acceptance (ff-merge promotion to `/mnt/work/flex-harness`, one async spawn observed reconciling within the same session with non-NULL `tokens_total`/`outcome`) is an operator action recorded at phase-113 checkpoint (G2), not run from this build worktree. The upstream missing `stop_reason` stamp remains open as a harness defect this story routes around rather than fixes. | RELEASE-066 E12 new-1 / operator design review 2026-07-29 | 2026-07-29 | 113 |

| CER-115 | LOW: story-frontmatter flow-style YAML lists parse as strings — `primary_files: [a.html, b.html]` is read as one string by `schema_validator._parse_frontmatter`, and `generate_permissions_artifact` then crashes `create-story-worktree` with `TypeError: can only concatenate str (not "list") to str` (flex_build.py:561), leaving a half-created worktree that needs a manual `discard-story-worktree` before retry. Same parser family as CER-092. Fix direction: either parse single-line flow sequences or reject them loudly at parse time; and make `create-story-worktree` clean up after a mid-flight failure. **RESOLVED Phase 113 — INFRA-296 fixed both halves. (1) `schema_validator._parse_frontmatter` now understands single-line flow sequences (`key: [a, b]` → `['a', 'b']`) via a new `_parse_flow_sequence` helper, and refuses what it cannot parse: a value that opens with `[` but is not a well-formed *flat* flow sequence (nested `[…]`, flow mapping `{…}`, unterminated, or trailing content after the close) raises the new `FrontmatterError` (a `ValueError` subclass) instead of degrading to a string. This widens the minimal YAML subset by exactly one flat form plus a loud refusal — it is not general YAML support, and nesting, flow mappings, multi-line flow sequences, block scalars and anchors remain unsupported. `generate_permissions_artifact` additionally type-checks `primary_files`/`touches` before concatenating them and names the story spec path on any parse failure, so a bare `TypeError` can no longer escape it. (2) `create-story-worktree`'s `except PermissionsCreateError` handler is now fatal: it echoes the error, calls `_teardown_story_worktree()`, prints any residue and exits 1 without printing a worktree path — so a permissions failure leaves no worktree, no `pairmode/<ID>` branch and no claim residue, and a retry after repairing the spec succeeds with no manual `discard-story-worktree`. The `current_story` stamp failure deliberately remains a warning.** | RELEASE-066 E12 new-2 | 2026-07-29 | 113 |

| CER-116 | MEDIUM: `next_story._has_story_commit` false positive on cross-references in non-`spec(` commits — a `story(X):` commit whose subject *mentions* another story ID (`story(RELEASE-066): ... RELEASE-067+ held for operator ruling`, e83ce900) marks that other story (RELEASE-067) as built, and the resolver silently skips it (observed live: next-action offered RELEASE-068 with RELEASE-067 draft and unbuilt). RELEASE-041 exempted `spec(` prefixes for exactly this list-several-IDs-in-prose reason; the same reasoning applies to any commit whose *scope* names a different story than the mentioned ID. Fix direction: only count a mention as build evidence when the ID appears in the commit's own scope/`story-<ID>` token, not anywhere in the subject; interim discipline: never name sibling story IDs in non-`spec(` commit subjects. **RESOLVED (INFRA-297), Phase 113.** `_has_story_commit` (`skills/pairmode/scripts/next_story.py:164`) now applies a scope rule: when a commit subject's conventional-commit scope contains an uppercase story-ID token (`_SCOPE_RE`/`_SCOPE_STORY_ID_RE`, `next_story.py:157,161`), only those scope tokens count as build evidence from that commit; otherwise the whole-subject search runs as the fallback, applied to the message rather than the raw `--oneline` line so an ID cannot be matched out of the SHA field. The detector is uppercase-only on purpose — a missed restriction re-offers a built story (loud, recoverable), a wrong restriction skips one silently — so lowercase scopes (`phase-112`, `fold-prep`, `story-infra-100`) still fall back and all three documented legitimate shapes keep counting. The e83ce900 regression is pinned on a synthetic log (the real log no longer reproduces it, RELEASE-067 having since been built by 0978447b) in `tests/pairmode/test_next_story.py::test_e83ce900_does_not_mark_the_mentioned_sibling_built`. The interim operator discipline is no longer load-bearing. | RELEASE-066/067 resolver skip, 2026-07-29 | 2026-07-29 | — |

| CER-117 | LOW: `story_new.py` prompts interactively (`Rail X does not exist. Create it? [Y/n]`) when the rail is new — aborts under a non-interactive orchestrator unless piped `yes`; add a `--create-rail` (or `--yes`) flag for scripted use. **RESOLVED prior to filing (confirmed Phase 119, INFRA-367):** already fixed by `feat(story-INFRA-301)` (commit `e27232d6`, 2026-07-30), one day after this CER was surfaced — a stale/duplicate re-filing, not a live gap. | RELEASE-067 E12 new-2 | 2026-07-29 | — |

| CER-119 | CRITICAL: `sync` structurally cannot deliver canon *shrinkage* — canon-removed sections are reclassified downstream as EXTRA and preserved forever (`sync.py:344` "Never modifies EXTRA items"), which is how the INFRA-241 thin-agent reduction stranded 6×–13× stale canon (two contradictory reviewer contracts, incl. `git clean -fd` residue) across all six fleet projects. Every future canon simplification fails the same way until fixed. Filed in Do Later rather than Do Now solely because an open Do Now row blocks `next_action._check_cer_do_now` and would deadlock the very build that fixes it; severity stands as CRITICAL. Absorbed at spec time by INFRA-311 (Phase 113, AG-1) — resolution annotation lands when that story completes. **RESOLVED Phase 113 — INFRA-311: sync now prunes EXTRA sections whose key is in the canon-side `RETIRED_SECTIONS` registry (seeded with the 46 INFRA-241 reductions), each behind a per-section confirmation, with `.pairmode-overrides` winning over retirement.** | Cold-eyes review 2026-07-29 (F1) / closeout agreements AG-1 | 2026-07-29 | 113 |

| CER-120 | HIGH: `audit.py` renders EXTRA-classified sections inside `CANONICAL_FILES` with a `✓ project-specific (keep as-is)` blessing (`audit.py:555-563`, `:753`) — stale canon reads as a project prerogative, the cheap check that would have surfaced CER-119 months earlier. EXTRA-on-canonical should be a finding (WARN unregistered / ERROR canon-retired), with overrides honored visibly. Absorbed at spec time by INFRA-311 (Phase 113, AG-1) — resolution annotation lands when that story completes. **RESOLVED Phase 113 — INFRA-311: audit now classifies EXTRA-on-canonical as WARN (unregistered) / ERROR (canon-retired) via `classify_extra`, with overrides reported visibly as OVERRIDDEN / OVERRIDE-KEPT; scaffold files keep the keep-as-is contract.** | Cold-eyes review 2026-07-29 (F2) | 2026-07-29 | 113 |

| CER-121 | HIGH: `docs/architecture.md` (item #2 of the mandatory cold-start triad) and `docs/checkpoints.md` are seeded by bootstrap, deny-listed from writes, and tracked by **nothing** — no body compare, no staleness check, no drift report; observed downstream spread 99–1568 lines. INFRA-311 lands only the `bootstrap.SCAFFOLD_FILES ⊆ audit-tracked` parity test; the body-tracking mechanism itself is this row. gate: next canon change to a seeded-doc template (`architecture.md`/`checkpoints.md` families) or the first post-0.3.1 fleet sync campaign — whichever comes first. **RESOLVED Phase 119 — INFRA-381: `audit.py` gains `SEEDED_COLD_START_DOCS` (`docs/architecture.md`/`docs/checkpoints.md` paired with their seeding templates) and a dedicated `_check_seeded_doc_drift` checker, mirroring the `_check_ideology_staleness`/`_check_reconstruction_staleness` pattern — heading-set comparison against the rendered template (missing/STALE/DRIFTED/OK), with `.pairmode-overrides` suppression wired at the `audit_project` finding site and each DRIFTED finding naming the specific missing template heading.** | Cold-eyes review 2026-07-29 (F3) | 2026-07-29 | 119 |

| CER-122 | MEDIUM: all agent-shell templates emit a bare relative procedure path (`skills/pairmode/skills/<role>/procedure.md` — `builder.md.j2:34`, `reviewer.md.j2:34`, `intent-reviewer.md.j2:33`, `loop-breaker.md.j2:38`, `security-auditor.md.j2:33`) that resolves in no consuming repo's cwd — thin workers pointed at a grading contract that may not exist at that path. Caveat: plugin-skill runtime loading may resolve it; **verify before changing anything** (preserved do-not-do). Absorbed at spec time by INFRA-304 E13/E14 (Phase 114, AG-5.1) — resolution annotation lands when that story completes. **RESOLVED Phase 114 — INFRA-304: rendered absolute, evidence in INFRA-304 § Evidence.** Verified experimentally (bootstrap fixture, see § Evidence) that the bare relative pointer does not exist relative to a genuinely bootstrapped consuming project's own tree — bootstrap.py/pairmode_sync.py never vendor `skills/pairmode/` into the target project, and the only documented plugin-root substitution mechanism (`${CLAUDE_PLUGIN_ROOT}`) applies solely to `hooks.json` command strings, not to prose paths in agent markdown bodies read via the model's own Read tool. All six affected templates (`builder`, `reviewer`, `intent-reviewer`, `loop-breaker`, `security-auditor`, `gate-worker` — `reconstruction-agent.md.j2` has no procedure pointer, confirmed no change needed) now render the pointer absolute via the existing `pairmode_scripts_dir` context variable (already populated identically by both `bootstrap.py` and `pairmode_sync.py`), pinned by a new parametrised test in `tests/pairmode/test_procedure_skills.py`. | Cold-eyes review 2026-07-29 (F7) | 2026-07-29 | 114 |

| CER-123 | MEDIUM: `README.md`'s build-loop description predates CER-074 — it says the resolver emits `spawn-reviewer`, but the shipped loop holds reviewer dispatch and the PASS/FAIL branch as orchestrator-held prose inside the `spawn-builder` iteration (`next_action.py:163` comment); no prior CER row tracked the divergence and INFRA-305's original scope pointed its README edits elsewhere. Absorbed at spec time by INFRA-305 Ensures 19 (Phase 114, AG-5.2) — resolution annotation lands when that story completes. **RESOLVED Phase 114 — INFRA-305: README build-loop description corrected to the post-CER-074 orchestrator-held reviewer dispatch.** | Cold-eyes review 2026-07-29 (F8) | 2026-07-29 | 114 |

| CER-124 | MEDIUM: `check-index` exits 1 with 48 pre-existing violations (2 status-drift, 12 cross-link, 21 orphan-story, 13 deferred-without-section in `phase-97.md`) while INFRA-310 Ensures 14 demands exit 0 — and the story's original `touches:` list excluded every offending file, so `scope_guard` would have blocked its own acceptance criteria. Absorbed at spec time by the widened INFRA-310 (Phase 116, AG-4: violation files enumerated into `touches:`, per-category cleanup Ensures 23, no baselining) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-310: check-index driven to a true exit 0 (48 violations fixed per category, no baselining or check-weakening).** | Cold-eyes review 2026-07-29 (F4) | 2026-07-29 | 116 |

| CER-127 | HIGH: hook command paths are machine-bound absolute paths, and migration leaves them stale — (1) repos that went through the 0.3.0 migration still carry pre-rename `/mnt/work/flex-harness/hooks/*` commands in their `.claude/settings.json` (no migrate/sync rule rewrites hook paths), and (2) even correctly-synced repos embed this dev machine's absolute `/mnt/work/...` iSCSI mount path, which is committed and pushed — so cloning a consuming repo on any other machine hard-blocks every prompt (`UserPromptSubmit ... /usr/bin/python3: can't open file '/mnt/work/flex-harness/hooks/user_prompt_submit.py': [Errno 2] No such file or directory`; hit live 2026-07-29 registering a cloned project with the observability UI). Since flex is consumed as a plugin/skill, hook commands should resolve via the plugin runtime / skill cache (e.g. a `${CLAUDE_PLUGIN_ROOT}`-style root or discovered path), never a hardcoded host path. Fix needs: (a) portable hook-path resolution in the canon hook-registration templates; (b) a migrate/sync rule that rewrites stale `flex-harness` and machine-absolute hook paths in already-migrated repos; (c) an audit check that flags machine-absolute hook paths as findings. Operator-flagged for inclusion in the era-004 closeout (candidate: Phase 114 migrate-rules story or Phase 116 0.3.1). Absorbed at spec time by INFRA-319 (Phase 114) — resolution annotation lands when that story completes. **RESOLVED INFRA-319 (Phase 114)** — the real defect in fix direction (a) was not the absolute path itself but that a machine-bound absolute path was written into a **committed, shared** file; the fix moves plugin-first hook registration into `.claude/settings.local.json` (a pre-existing machine-local surface, with a `.gitignore` guard) rather than emitting `${CLAUDE_PLUGIN_ROOT}` into `.claude/settings.json` — that literal token is expanded by Claude Code only for a plugin's own `hooks/hooks.json`, never for a project's settings file, so writing it there would not have worked; this is delivered as "machine-bound value moved out of the committed file", **not** as "`${CLAUDE_PLUGIN_ROOT}` in settings.json". Fix (b) landed as a `to-030` repair block (no new `MigrationRule` — INFRA-303 pins the 14-entry count) that relocates a stale `flex-harness`/machine-absolute entry out of an already-migrated repo's committed settings.json. Fix (c) landed as `hook_view.machine_absolute_hook_entries()`, wired into both `audit-hooks` and the fleet scan. `PreToolUse` also gained the plugin-already-registered skip the other three settings-level events already had (INFRA-288/CER-104). | Operator report 2026-07-29 (fleet portability) | 2026-07-29 | 114 |

| CER-128 | HIGH: story scope enforcement has no path from a deny back to an allow that does not require a human. `scope_guard.check_path` matches `candidate in allowed_paths` exactly (`scope_guard.py:149-151`), where `allowed_paths` is the literal `primary_files` + `touches` concatenation plus the story's own spec path (`flex_build.py:577-584`) — no globs, no prefixes, and no auto-widen. When a build discovers an undeclared file mid-loop the only remedies are an operator hand-editing `touches:` and re-running `permissions-create`, toggling auto-mode off so the permission prompt surfaces, or falling back to shell writes. Downstream consequence observed on a fresh 0.3.0-bootstrapped repo: teams over-declare `touches:` at spec time to pre-empt later prompts, which turns a scope guard into a scope fiction and produces story churn (CERs filed per build loop, then pulled back into existing specs). Same-session evidence in flex itself: INFRA-297 and INFRA-298 both edited `docs/cer/backlog.md` undeclared (reviewer MEDIUM findings under § 9 RAIL SCOPE, self-disclosed as a spec-preflight gap), and the INFRA-319 spec-writer was scope-blocked writing planning docs and used the CER-087 shell-write workaround. The 0.3.0 spec-elaboration step only helps an under-specified stub *before* the build (`spec_preflight.py` scans routes and constants, never `touches:`); it does nothing for mid-build scope discovery. `spec_exception.py` — the existing override audit trail — is reachable only through an interactive companion-sidebar keypress (`sidebar.py:1283-1320`), so a headless builder subagent cannot use it. Fix needs: (a) a central standing allowance for shared documentation/record surfaces (`docs/cer/backlog.md`, `docs/architecture.md`, the story's own spec, the story's own phase doc) declared once rather than per story; (b) an audited mid-build widening path — the builder names the file and a reason, the declaration lands in the story frontmatter and a `## Scope widenings` row, and the reviewer judges it — instead of a flat deny that stalls the loop; (c) spec-time prediction warning when a story's Ensures name repo paths absent from its declared scope. The hard block must be preserved: protected paths stay unreachable and no code file is ever granted implicitly. Absorbed at spec time by INFRA-320 (Phase 113) — resolution annotation lands when that story completes. **RESOLVED Phase 113 — INFRA-320: standing shared surfaces (`scope_guard.STANDING_SURFACES` + `standing_paths_for()` — `docs/cer/backlog.md`, `docs/architecture.md`, the story's own spec, its one phase doc — never require a `touches:` declaration), an audited `permissions-widen` command (names the path and a reason, writes `touches:` + a `## Scope widenings` row, regenerates the artifact — not an auto-widen), and a `check-story-scope` rule 3 / `spec-preflight` warning when a story's own Ensures/Instructions name a path absent from its declared scope. The hard block was preserved: protected paths remain unreachable by any standing or widening mechanism, and no code path may ever join `STANDING_SURFACES`.** | Operator report 2026-07-29 (scope friction) | 2026-07-29 | 113 |

| CER-129 | HIGH: context accounting attributes story/subagent token spend to the orchestrator's own context window, while the thing that actually fills that window is not tracked as a distinct signal. Three live consumers compare an `effort.db` quantity against an orchestrator-window threshold: (1) `context_health.check_context_health` (`context_health.py:141-227`) computes its `/clear` recommendation from `phase_retry_burden`'s sum of reviewer-FAIL `COALESCE(tokens_out, tokens_total * 0.15)` rows (`:77-111`) and emits "consider/recommend /clear before next phase" (`:211`, `:216`, CLI exit 1 at `:273-275`) with zero reference to `context_current_tokens`; (2) `context_budget_check.py` sums `attempts.tokens_total` for a phase (`:64-77`) against `state["context_budget_threshold"]` (`:46-62`, `:118-123`) — the same key the orchestrator gate uses at `context_budget.py:882` — and prints "CONTEXT BUDGET EXCEEDED ... Orchestrator MUST surface a proceed-vs-pause prompt" (`:140-147`); (3) the observability `/context` route feeds `context_budget_threshold` into `queryWaypoints`/`queryMisses` over `attempts.tokens_total` (`effortDb.ts:123-177`, `:246-303`; `routes/context.ts:280-283`), including the orchestrator overrun formula `threshold * 1.1`, exposing rows as `tokens_at_block` rendered under the SPA heading "Near-miss blocks" (`ContextMetrics.tsx:428-437`) — blocks that never occurred. Those tokens burned in subagent windows; INFRA-251 added the `isSidechain` filter (`context_budget.py:210-231`) precisely to keep them out of the orchestrator count. Meanwhile the orchestrator track is undercounted: `context_current_tokens` has exactly one automatic writer, `hooks/post_tool_use.py`'s Task/Agent branch (`:63`, `:100-135`), so everything entering the window between spawns (next-action poll output, merge/git output, task-completion notifications, spec-writer coordination, the orchestrator's own reasoning) is never observed; `_is_stale` only compares against `context_session_reset_at` (`:641-666`), so a many-turns-behind value is believed rather than flagged, and the only additive path `bump-context-tokens` (`flex_build.py:1929-1990`) is documented as having zero live callers. Consequence: the context-health/budget prompt fires on the wrong quantity in both directions — it can pause a loop whose orchestrator is nearly empty (story budgets summed against it) and fail to pause one whose orchestrator is nearly full (accumulation never counted). This is the DP7 rule (`docs/architecture.md` § effort.db != context-control invariant, `:2005-2029`) and the standing repo lesson that effort.db cost totals must never be used to estimate orchestrator context headroom — reinvented three times in code; the § Codified comingling note (`:2031-2043`) flagged only `CLAUDE.build.md:320-326` (now gone; that file is 52 lines) and missed all three. Fix direction: orchestrator accumulation becomes a named track separate from story/effort budgets, the context-health prompt is driven by orchestrator-track growth only with story budgets reported as informational, per-story gates that legitimately protect subagent windows are distinguished from orchestrator-headroom checks, and the two-track model is documented with operator surfaces labeling the two numbers distinctly. Forcing function: phase 116's INFRA-316 (draft) plans to wire `context_budget_check.py` into `next-action`'s between-story pause, which would promote consumer (2) into the resolver's live cadence decision (`next_action.py:1017` already reserves `_ADVISORY_CONTEXT` with no producer). Absorbed at spec time by INFRA-321 (Phase 114) — resolution annotation lands when that story completes. **RESOLVED INFRA-321 (Phase 114)** — the two-track model and its constants landed in `context_model.py` (`TRACK_ORCHESTRATOR`/`TRACK_STORY_SPEND`, `ORCHESTRATOR_TRACK_KEYS`, `STORY_SPEND_SOURCES`, `track_label()`) (§ A); the health/pause verdict is re-based onto the orchestrator track only — `context_health.orchestrator_headroom()` is the sole source of `check_context_health`'s top-level `recommendation`/`message`, and the retry-burden signal survives as an informational `story_spend` sub-object with no `/clear` language anywhere in its message (§ B); `context_budget_check.py` now resolves a dedicated `story_spend_threshold` (never falling back to `context_budget_threshold`), declares `track=story-spend` in its stdout, and its over-threshold message states plainly it is not a headroom signal (§ B6); orchestrator-track coverage between spawns now has a second measurement writer — `user_turn_seq.record_user_turn()` refreshes `context_current_tokens` from the same `isSidechain`-filtered JSONL measurement `post_tool_use.py` uses, fail-open, without touching the protected `hooks/user_prompt_submit.py` (§ C); per-story numbers are classified rather than invented — `flex_factor` documented/tested as an orchestrator-ceiling multiplier, `story-cost-estimate` captioned as story-spend informational via `track_label`, and the finding that no subagent-window gate exists today is recorded rather than papered over (§ D); the observability `/context` surface labels every threshold with its track, `queryWaypoints`/`queryMisses` take the story-spend threshold and are renamed (`near_miss`→`over_spend_band`, `misses`→`spend_outliers`, `tokens_at_block`→`tokens_total`) so no field claims a block that never happened, and `ContextMetrics.tsx` renders the two tracks as two visually separate, captioned groups (§ E). **No gate was weakened and no new gate was added** — `context_budget.decide()`'s decision logic is untouched (only the § A4 `effective_ceiling()` extraction touched it, and that extraction is arithmetic-preserving, pinned by a parity test). The rejected effort.db-derived-headroom direction (R1, § Out of scope) is named explicitly in `docs/architecture.md` so the record cannot be read as licensing it. **One arm deferred, not silently dropped:** § C6's provenance field (`context_current_tokens_source`) has only two live writers as of this story — `record_user_turn` (`"user-prompt-submit"`) and `flex_build.py set-context-tokens`/`bump-context-tokens` (`"manual"`). The third, `hooks/post_tool_use.py`'s Task/Agent branch stamping `"post-tool-use"`, requires editing a protected path (`hooks/**`) and was correctly left untouched per this story's own § Out of scope instruction; see the CER-135 follow-up row below. | Operator report 2026-07-29 (context accounting) | 2026-07-29 | 114 |

| CER-135 | LOW: `context_current_tokens_source` (INFRA-321 § C6) is stamped by two of its three intended writers — `user_turn_seq.record_user_turn()` (`"user-prompt-submit"`) and `flex_build.py set-context-tokens`/`bump-context-tokens` (`"manual"`). The third, `hooks/post_tool_use.py`'s Task/Agent branch (the writer that runs after every builder/reviewer/auditor spawn — the highest-frequency write path for `context_current_tokens`), does not stamp `"post-tool-use"`, because `hooks/**` is a protected path and INFRA-321's own § Out of scope names this exact edit as the one exception requiring a hook change, instructing the builder to report `BUILDER BLOCKED` rather than touch it (which is what happened). Consequence: today, a `context_current_tokens` value most recently written by PostToolUse carries no source stamp at all (every reader treats an absent/unknown source exactly as it already does — `context_budget.decide()` does not gate on this field either way, so there is no functional gap, only an observability one). Fix direction: a small, hook-scoped story that adds the one-line stamp (`state["context_current_tokens_source"] = "post-tool-use"`) inside `post_tool_use.py`'s existing Task/Agent read-modify-write (mirroring the write § C1 already added to `user_turn_seq.record_user_turn`), with a regression test asserting the stamp appears after a Task/Agent PostToolUse observation. `hooks/post_tool_use.py`. **RESOLVED INFRA-374** — `hooks/post_tool_use.py`'s Task/Agent branch now stamps `state["context_current_tokens_source"] = "post-tool-use"` as a literal inside the existing `_mutate` read-modify-write, guarded by the same `if live_tokens is not None:` condition the other two writers use; no new import, delegated call, or state write was added, and `context_budget.decide()` remains indifferent to the field. | INFRA-321 build (§ C6 BUILDER BLOCKED disposition) | 2026-07-30 | 119 |

| CER-130 | HIGH: the `cer-do-now` checkpoint guard decides whether a project may enter its checkpoint sequence with a bare, case-sensitive substring membership test on the whole Do Now row line — `if "RESOLVED" not in stripped and "SUPERSEDED" not in stripped` (`skills/pairmode/scripts/next_action.py:437`, left verbatim by INFRA-297 when the row splitting moved to `table_utils.split_table_row`) — and that one expression is wrong in both directions. **Direction 1, hit live on a consuming repo's phase-35 checkpoint:** a backlog whose convention is title-case (`Resolved cp-34 — …`) never matches, so every resolved Do Now row reads as unresolved, `check_checkpoint_guards` returns `failed_guard: cer-do-now` at every checkpoint forever (`:541-544`), and the operator's only remedy is to bypass the resolver and hand-run `record-checkpoint-step checkpoint-tag` — the CER-067 failure class (an un-clearable mechanical gate that operators and agents learn to route around, after which the gate protects nothing), and the same shape as CER-072 and CER-094/INFRA-294. **Direction 2, the opposite and more dangerous direction:** with no boundaries, any row containing the letters in any context passes — a row reading `UNRESOLVED naming gap …` is classified resolved because `UNRESOLVED` contains the substring, and aspirational prose (`… this SHOULD BE RESOLVED before the tag`) reads as an accomplished closure, waving a genuinely open Do Now item silently through a checkpoint. Root cause is not one repo's typo: nothing in the codebase writes a resolution marker (`cer.py`'s CLI writes findings and Do Never rejection reasons only, `:314-316`), and the grammar the guard accepts has never been written down — `checkpoint-docs/procedure.md:58,99` says only that items "must be marked `RESOLVED`" without defining what "marked" means, and the backlog template preamble every consuming repo is bootstrapped from says only "resolved findings remain in place with a resolution note". Fix needs: (a) a case-insensitive **and** anchored resolution-marker match — a lowercase-only fix repairs direction 1 and widens direction 2, since `unresolved`, `should be resolved` and `to be resolved` all begin matching once case is dropped; (b) the marker grammar defined once as a shared predicate in `cer.py` beside `is_placeholder_row` (the INFRA-294 two-reader precedent) rather than inline in the guard; (c) the scaffolded empty-state placeholder exemption preserved byte-identically and *ahead* of the resolution test; (d) the grammar published in `docs/architecture.md`, the checkpoint-docs procedure and the backlog template preamble, so a consuming repo can read what the guard accepts instead of guessing. **RESOLVED INFRA-322 (Phase 114)** — landed the shared anchored, case-insensitive resolution-marker grammar as `cer.is_resolution_marked` (a module-level `re.IGNORECASE` pattern requiring the keyword to begin an annotation segment: text/line/cell start, sentence-ending punctuation plus a space, or an emphasis/bracket opener), consumed by `next_action._check_cer_do_now` as a one-expression substitution with the placeholder exemption, fail-open behavior, and `check_checkpoint_guards` contract all preserved byte-identically, and published the grammar in `docs/architecture.md`, `checkpoint-docs/procedure.md` and the backlog template preamble (this file's own preamble too) so a consuming repo never has to guess the convention again. A lowercase-only fix was rejected: it would repair direction 1 but widen direction 2. Backtick, double-quote, single-quote and underscore are deliberately excluded from the accepted opener set so prose and code quoting the literal keyword (including this very row) is never misread as a marker — do not add them back. | Operator report 2026-07-29 (phase-35 checkpoint, consuming repo) | 2026-07-29 | 114 |

| CER-131 | MEDIUM: worker-contract rejections arriving via the INFRA-298 SubagentStop relay are refused correctly but logged nowhere — `parse_worker_outcome` is now called from five sites (`subagent_transcript.py:1472`, `:1548`, `:1905`, `:2295`, `:2488`) and `read_completed_spawn` from two (`:1932`, `:2162`), but INFRA-299's C4 wired the `rejected` out-list and `skip:non-enum-outcome` recording-decision logging into only the two sites its spec enumerated (`reconcile_pending_attempts`, `record_attempt_from_transcript`) because the `:1905`/`:1932` relay sites (`reconcile_one`) were added by INFRA-298 after INFRA-299 was specced. Correctness is uniform — the gate lives in the parser, so a non-enum outcome leaves the row pending on every path — but a rejection on the deterministic relay path (now the PRIMARY reconciliation path post-INFRA-298) emits no `effort_recording.log` entry, recreating for the relay exactly the invisibility CER-091(1) was about. Fix: thread the `rejected` out-list + `log_recording_event` call through `reconcile_one`'s parse/read sites, mirroring the two existing consumers; regression-test that a refused JSON BUILD outcome via the relay writes a `skip:non-enum-outcome` line. Surfaced by INFRA-299 attempt-2 review (non-blocking observation). **RESOLVED Phase 119 — INFRA-373:** the `rejected` out-list and `log_recording_event`/`skip:non-enum-outcome` call are now threaded through `reconcile_one`'s `parse_worker_outcome`/`read_completed_spawn` sites, mirroring the two pre-existing consumers. | INFRA-299 review, session 2026-07-29 | 2026-07-29 | — |

| CER-132 | MEDIUM: `.pairmode-overrides` is seeded by bootstrap (`bootstrap.SCAFFOLD_FILES`, `.pairmode-overrides.j2`) and is the sanctioned keep-my-extension mechanism that both audit's EXTRA severity split and sync's RETIRED_SECTIONS pruning (INFRA-311) defer to — yet no audit surface tracks it: it is in neither `audit.CANONICAL_FILES` nor `audit.SCAFFOLD_FILES` and has no dedicated existence/health check (unlike `docs/ideology.md`/`docs/reconstruction.md`), so a deleted or corrupted overrides file silently strips a project's declared protections on the next sync. Body compare is the wrong tool (content is inherently project-owned); the fix is an existence check plus a parse-health check (unparseable lines reported, not silently dropped by `_load_overrides`). Surfaced by INFRA-311's `bootstrap.SCAFFOLD_FILES ⊆ audit-tracked` parity test, which excepts it via `_KNOWN_GAPS` pending this row. **RESOLVED Phase 119 — INFRA-372:** `audit.py` gained a dedicated `_check_overrides_health` existence/parse-health check for `.pairmode-overrides` (mirroring the ideology/reconstruction staleness-check pattern), wired into `audit_project`; `_KNOWN_GAPS`'s exception for this file was removed. | INFRA-311 parity test, session 2026-07-29 | 2026-07-29 | — |

| CER-133 | MEDIUM: four residual seams from INFRA-311's canon-retirement landing (attempt-3 review, non-blocking observations — MEDIUM = fix before phase end or route): (1) `pairmode_sync.py:1015,1074` still asserts sync.py has no `--dry-run`, now false; (2) `RETIRED_SECTIONS` keys are not file-scoped, so a generic section key could match a genuine extension of the same name in another canonical file — consider `(file, key)` scoping before the fleet campaign runs sync-all; (3) `docs/pairmode/PAIRMODE.md:45,165` still describes sync as non-destructive (the README/architecture claims were fixed in-story; this surface was outside touches); (4) audit's RECOMMENDATION output and SKILL.md's "Already up to date" short-circuit never mention retirement prunes, so an operator can be told up-to-date while registry-matched stale canon sits downstream; (5) `skills/pairmode/SKILL.md:258` still claims sync "Never overwrites project-specific content (EXTRA items)" — stale as of INFRA-311's canon-retirement pruning, which does overwrite/remove registry-matched retired sections; (6) `skills/pairmode/SKILL.md:844` still asserts "This step only runs in `--apply` mode because `sync.py` has no `--dry-run` flag" — same stale claim as item (1), and additionally `pairmode_sync.py`'s `sync_all` (~lines 1073-1076) hard-skips invoking `sync.py` at all when not in `--apply` mode (`skip_in_dry_run=True` for the sync.py entry), so `sync.py`'s real `--dry-run` flag is unreachable from the sync-all wrapper even though the flag exists and works when `sync.py` is invoked directly. Gate: before the post-0.3.1 fleet sync campaign (item 2 especially — key collisions are only dangerous at fleet scale). **RESOLVED Phase 119 — INFRA-371:** all six seams closed — stale `--dry-run` claims removed; `sync_all`'s dry-run mode now actually invokes `sync.py --dry-run` instead of hard-skipping it; `RETIRED_SECTIONS` re-keyed to `(canonical file, section key)` tuples; `PAIRMODE.md`/`SKILL.md`'s non-destructive claims corrected; audit's RECOMMENDATION output and SKILL.md's up-to-date short-circuit now name pending retirement prunes. | INFRA-311 attempt-3 review, session 2026-07-29 | 2026-07-29 | — |

| CER-118 | MEDIUM: hooks write the current flat `tempfile.gettempdir()/companion.pipe` path (INFRA-238), but `skills/companion/scripts/sidebar.py:1677-1710` independently computes `_hash = hashlib.md5(...)` and `PIPE_PATH = f"/tmp/companion-{_hash}.pipe"`, then `mkfifo`s and reads *that* path (`:1709-1710`, `:1753`) and writes it back to `state.json["pipe_path"]` (`:1692`) — so on any project the sidebar reads a pipe no hook writes to. Filed by INFRA-305 while correcting `docs/architecture.md` § Data flow (CER-079): the doc's md5-hash claim was not merely stale prose, it described real (if divergent) sidebar code. `docs/pipe-architecture.md` is stale for the same reason — it documents the project-scoped design the sidebar still implements but no hook feeds. Fix needs: either point `sidebar.py` at the flat `tempfile.gettempdir()/companion.pipe` path (retiring the md5 hash and the `pipe_path` state key it writes) or restore project-scoped pipe writes in the hooks — a decision, not a doc fix. Not resolved by INFRA-305 (doc-currency only, no runtime behaviour change). **BACKLOG-RETAIN — Phase 115 / INFRA-310.** A real, open, code-side defect deliberately carried past the 0.3.1 line: hooks and the sidebar are pointed at different pipes, the fix is a code story with a runtime behaviour change, and era 004 was scoped to doc/record closeout after the sibling code stories. Filed by INFRA-305; CER-079 is the doc-side row that closed around it. | INFRA-305 (Requires 5, re-verification) | 2026-07-30 | — |

| CER-134 | HIGH: Claude Code loads `.claude/agents/*.md` agent definitions — and plugin/skill registrations, and the `hooks` blocks of `.claude/settings*.json` — **at session start only**, but every pairmode tooling path that installs or updates those surfaces writes them **mid-session**: `bootstrap.py` renders all seven agent shells (`bootstrap.py:85-92`, written `:1374-1391`) and registers four hook events (`:1440-1453`); `pairmode_sync.py sync-agents` rewrites shell frontmatter in place (`:795-799`), `sync-all` runs it as chain step 2 (`:1069-1076`), and `audit-hooks --apply` rewrites both settings files' hook blocks (`:1320-1323`); `pairmode_migrate.py` rule 2 delegates to `sync-agents` (`:104-110`, `:458-482`), rule 3 regex-substitutes shell bodies (`:111-115`), and `to-030`'s B7 deletes/flags shells (`:1108-1156`). So in the very session that just bootstrapped or migrated a repo the agents are not registered: spawns fall back to `general-purpose` or fail, and the operator's only available conclusion is that the bootstrap failed. **Nothing anywhere says a restart is needed** — a grep for `restart` / `new session` / `exit the session` across `bootstrap.py`, `pairmode_sync.py`, `sync.py`, `pairmode_migrate.py`, `hooks/session_start.py`, `skills/pairmode/SKILL.md`, `docs/harness-cutover-runbook.md` and `docs/pairmode/PAIRMODE.md` returns one unrelated hit (`PAIRMODE.md:136`, lessons surviving "agent restarts"). All three completion outputs are silent on the session boundary, and `bootstrap._print_next_steps` (`:704-722`) sends the operator into `story_new` → `story_context` → `audit`, none of which exercise the registry, so the scaffold looks healthy up to the first spawn. `/clear` does not fix it and is the trap: it resets the context window inside the same process, the registry is untouched, and `hooks/session_start.py` prints a reassuring `Pairmode v… is active` status block (`:168`) that is true about `state.json` and silent about registration. Consequence: post-bootstrap confusion on every new repo and every fleet migration — imminently RELEASE-068's canon-only Repo-K migration, which creates `gate-worker.md` and rewrites seven agent shells via `sync-all --apply` and then verifies only that the *files* are on disk. Fix direction: every tooling path that writes an agent-registration surface (bootstrap, migrate, `to-030`, `sync`/`sync-all`/`sync-agents`/`audit-hooks` when agent shells or hook registrations changed) ends with an explicit, unmissable operator notice enumerating what changed and stating that the session must be exited and restarted — `/clear` is not sufficient — plus the restart step written into the bootstrap/migration runbooks and SKILL.md flows, and (if cheaply detectable) a SessionStart-time staleness advisory when the registration surfaces on disk postdate the session's registration. **RESOLVED Phase 114 — INFRA-323: added `skills/pairmode/scripts/session_lifecycle.py` (pure, stdlib-only) as the single definition of the `RESTART REQUIRED` notice, the `agent_surfaces_written_at`/`agent_surfaces_written_by` state.json stamp, and a SessionStart-only staleness advisory for `clear`/`compact`. Wired into `bootstrap.py` (final output, after `_print_next_steps`), `pairmode_sync.py` (`sync-agents`, `sync-all` via a read-back stamp rather than child-stdout parsing, `audit-hooks --apply`), and `pairmode_migrate.py` (`migrate --apply`, `to-030 --apply`'s B7 deletions) — each prints the notice only when a registration surface actually changed, never on a no-op run. `hooks/session_start.py` gained one import, one pure call, and one conditional append (no new state write) so a `/clear`/`/compact` session sees an advisory when the stamp postdates the session start. Docs updated in `skills/pairmode/SKILL.md` (bootstrap, sync, sync-agents, sync-all, migrate-from-anchor), `docs/harness-cutover-runbook.md` (new Step 5 in the 6-step Era 3 procedure), and `docs/architecture.md` (§ Session-lifecycle contract).** | Operator report 2026-07-29 (bootstrap session lifecycle) | 2026-07-29 | 114 |

| CER-136 | HIGH: `cmd_merge_story_worktree` (`skills/pairmode/scripts/flex_build.py:4049-`) rebases, fast-forward-merges, and tears down a story's worktree/branch on reviewer PASS, but never flips the landed story's `status:` frontmatter or its Status cell in the phase doc's Stories table — both stay `draft` forever unless someone hand-edits them. Live-hit: Phase 114's checkpoint (`next-action --json`) returned `await-user / checkpoint-guard-failed:phase-incomplete` after all 14 of its stories (INFRA-301..330) were built, reviewed PASS, and merged to `main` — `check-index`'s `status-drift` check confirmed 13 of them (all but INFRA-323, which happened to be hand-corrected already) had a merged `feat(story-INFRA-XXX)` commit but a stale `draft` status in both places. A corrective story (INFRA-330) fixed the 13 stale rows, but needed a second pass to flip its own status too — the same gap recurring on itself, since it merged before it could record its own completion. Fix direction: have `cmd_merge_story_worktree` flip the story's frontmatter `status:` and its phase-doc Status cell to `complete` as part of the same merge operation (mirroring `mark-phase-complete`'s existing phase-index flip), so a merged story is never observably `draft`; alternatively, add an explicit post-merge step to the `CLAUDE.build.md` loop that calls a dedicated status-flip command right after `merge-story-worktree` succeeds. `skills/pairmode/scripts/flex_build.py` `cmd_merge_story_worktree`. **STILL OPEN — re-confirmed at Phase 115 and Phase 116 checkpoints (2026-07-31/2026-08-01)**: both required a manual orchestrator-side status sync across all landed stories before `checkpoint-tag` would clear the phase-incomplete guard, the exact same unfixed gap. The build-loop cold-eyes review (`docs/build-loop-cold-eyes-review-20260801.md`) independently corroborates this via `index_integrity.check_index`'s status-drift check having zero automated callers anywhere in the loop — nothing runs it proactively, so the drift is caught only when a human happens to look. **Absorbed at spec time by INFRA-347 (Phase 117)** — resolution annotation lands when that story completes. **RESOLVED Phase 117 — INFRA-347: `cmd_merge_story_worktree` now calls `story_update.update_story_status`/`update_phase_story_status` after every successful merge, flipping both the story's frontmatter `status:` and its phase-doc Status cell to `complete` in the same command invocation.** | INFRA-330 self-diagnosis (Phase 114 checkpoint blocker), session 2026-07-31 | 2026-07-31 | 117 |

| CER-137 | HIGH: three of `templates/agents/`'s eight agent roles are not registered as dispatchable subagents. `spec-writer` has a fully-specced procedure (`skills/pairmode/skills/spec-writer/procedure.md`, WORKER-013) and a live, non-advisory dispatch action (`next_action.py` `ACTIONS`/`_SPAWN_ACTIONS` include `spawn-spec-writer`, emitted by `resolve_next_action` Row-2 for any stub story) but has no `templates/agents/spec-writer.md.j2` template at all — it was never given a shell. `docs-reviewer` and `gate-worker` do have templates (`docs-reviewer.md.j2`, INFRA-325; `gate-worker.md.j2`, RELEASE-010) and are both scaffolded by `bootstrap.py`'s `AGENT_FILES` list and referenced by dispatch (`CLAUDE.build.md`'s `checkpoint-docs → docs-reviewer` map entry; `next_action.py`'s `spawn-gate-worker` action) — but neither file exists in flex's own `.claude/agents/` (verified live, this session: only `builder.md`, `intent-reviewer.md`, `loop-breaker.md`, `reconstruction-agent.md`, `reviewer.md`, `security-auditor.md` are present; INFRA-325 was marked complete at Phase 114's checkpoint without ever regenerating flex's own registry). Live consequence, same session: dispatching `checkpoint-docs` fell back to a generic `claude` subagent because `docs-reviewer` was not an available Agent-tool type. Absorbed at spec time by INFRA-331 (Phase 116) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-331: `templates/agents/spec-writer.md.j2` created; spec-writer/docs-reviewer/gate-worker registered in `ACTION_SUBAGENT_TYPE`.** | Operator-directed process review, session 2026-07-31 (spec-writer model-escalation discussion) | 2026-07-31 | 116 |

| CER-138 | HIGH: `pairmode_sync.py`'s `sync-agents` (also chained by `sync-all` and delegated to by `pairmode_migrate.py` rule 2) only rewrites agent files that already exist on disk in a target project's `.claude/agents/` — it has no path to add a file that exists only as a template in `templates/agents/` but was never scaffolded into that project. `bootstrap.py` is the only code path that creates new agent files, and it runs once, at fresh-install time. Consequence: a template added after a project's initial bootstrap (docs-reviewer.md.j2/INFRA-325, gate-worker.md.j2 for any project bootstrapped before RELEASE-010) can never reach that project's registry through the ordinary sync/migrate maintenance path — only a from-scratch re-bootstrap or a manual file copy closes the gap, and nothing detects or reports the drift (`audit.py` does track the file per CER-122's fix, but auditing an absence is not the same as a path to remedy it without hand intervention). This is the direct cause of CER-137's live finding: flex's own `.claude/agents/` never received `docs-reviewer.md` despite INFRA-325 landing. Absorbed at spec time by INFRA-332 (Phase 116) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-332: `sync-agents` gained an add-missing-file backfill path for already-bootstrapped projects; flex and flex-harness backfilled.** | Operator-directed process review, session 2026-07-31 (spec-writer model-escalation discussion) | 2026-07-31 | 116 |

| CER-139 | MEDIUM: `model_selector.py` defines a `select_*_model` function for five of the eight agent roles (builder, reviewer, intent-reviewer, security-auditor, loop-breaker) but not for `gate-worker`, `spec-writer`, or `docs-reviewer`. `gate-worker.md.j2` and `docs-reviewer.md.j2` hardcode `model: sonnet` in frontmatter with no attempt-based variation (`docs-reviewer.md.j2` carries its own comment on this: "checkpoint-docs currently resolves with model=None from next_action.py... adding a dedicated model-selection tier is separate follow-on scope"); `next_action.py:1299` hardcodes `model="opus"` inline for `spawn-spec-writer` rather than calling through `model_selector.py` like every other dispatch site. Consequence: these three roles cannot be tuned, tiered, or audited through the same mechanism as the other five — a model change requires an inline code edit at the call site instead of a table entry. Absorbed at spec time by INFRA-333 (Phase 116) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-333: `select_gate_worker_model`, `select_spec_writer_model`, `select_docs_reviewer_model` added to `model_selector.py`; dispatch call sites route through them instead of hardcoded literals.** | Operator-directed process review, session 2026-07-31 (spec-writer model-escalation discussion) | 2026-07-31 | 116 |

| CER-140 | MEDIUM: `select_builder_model`'s `story_class` table gives `doc` and `lesson` classes `haiku` at every attempt number with no upgrade path ("auto-downgrade", never re-evaluated), and gives `methodology` `sonnet` at every attempt with escalation to `opus` only conditional on a same-phase `code`-class story existing (`select_reviewer_model`'s docstring table). Live-hit reported by the operator: era-001 `docs/stories/DESIGN/*.md` frontmatter-elaboration stories were classified `story_class: doc` (no code-file modification, so `doc` looked like the correct fit at spec time) but required heavy citation verification against a live external codebase with zero tolerance for fabrication — exactly the kind of work that benefits from retry-escalation, and `doc`'s ladder structurally cannot provide it: a haiku builder that fails on a hard task is dispatched again at haiku, forever. `code` is the only class with a real retry-upgrade path today; the class taxonomy is being read as "does this touch application code" when the two live consumers (`select_builder_model`, `select_reviewer_model`) actually use it as a proxy for verification rigor. Absorbed at spec time by INFRA-334 (Phase 116) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-334: every `story_class` now has a real retry-upgrade path (doc/lesson haiku→sonnet, methodology sonnet→opus unconditional).** | Operator-directed process review, session 2026-07-31 (spec-writer model-escalation discussion) | 2026-07-31 | 116 |

| CER-141 | LOW: no document states which agent type is the correct dispatch target for a given kind of work, or what a new agent type must have before it is considered wired up (template, `.claude/agents/` registration, `ACTION_SUBAGENT_TYPE`/dispatch-action entry, a `model_selector.py` selection function, a real escalation ladder). CER-137/138/139/140 each independently rediscovered a piece of this — three separate incomplete-registration gaps and two separate escalation gaps existed simultaneously with no single place that would have caught any of them at review time. Absorbed at spec time by INFRA-335 (Phase 116) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-335: work→agent-type classification doc and new-agent-type definition-of-done published to prevent this class of drift recurring.** | Operator-directed process review, session 2026-07-31 (spec-writer model-escalation discussion) | 2026-07-31 | 116 |

| CER-173 | MEDIUM: INFRA-394's scrub_fleet_names.py deliberately excludes lessons/lessons.json (protected_paths, append-only per the Lessons integrity rule — existing entry content must never be modified) and its generated rendering lessons/LESSONS.md from both apply and verify, so verify reports zero hits while several real sibling-repo names (matching CER-172's real-name list) remain literally present in these two committed, public, git-tracked files (confirmed: lessons.json trigger/source_project fields for at least 6 distinct real names as of Phase 125). This is a genuine, currently-unresolved conflict between CER-172's public-leak remediation goal and the append-only lessons.json integrity rule, not an oversight in the scrub logic itself — fixing it needs either an explicit, narrow append-only exception (rewrite existing entry text without touching status/structure) or an operator decision to accept the residual exposure. Fix direction: propose a follow-up story that either (a) adds a scoped exception letting a real-name-only text substitution touch lessons.json's existing entries without otherwise violating append-only intent (no additions/removals/reordering, no status changes), regenerating lessons/LESSONS.md from the result, or (b) has the operator explicitly accept the residual real names in these two files as an out-of-scope carve-out, documented in CER-172's own row. skills/pairmode/scripts/scrub_fleet_names.py, lessons/lessons.json, lessons/LESSONS.md. **RESOLVED Phase 122 — INFRA-395:** `scrub_fleet_names.py` gained a lessons-scoped `apply_lessons()`/`verify_lessons()` mode (`--lessons` flag) that substitutes real names in existing entries' free-text fields only (never structure/status/ordering/count), regenerates `LESSONS.md`, and documents the narrow append-only exception in `lesson_utils.py`, the builder/reviewer procedures, and `docs/architecture.md`'s non-negotiables section. | builder | 2026-08-05 | 125 |

| CER-176 | MEDIUM: skills/pairmode/scripts/reviewer_bash_guard.py's shadow-reviewer allowlist admits 'git diff --no-index <abs-path-A> <abs-path-B>', which reads and prints arbitrary files outside the worktree (verified live: 'git diff --no-index /dev/null <scratch>/fakeauth.json' emitted the file's contents verbatim). The role's stated contract (shadow-reviewer/procedure.md Input contract item 2) says git state is scoped to the worktree at {cwd}; --no-index is the one allowlisted form that escapes that scoping. Not raised to HIGH because cold_read_guard.check_path already returns allow unconditionally for any non-empty agent_type, so the role's pre-existing Read grant already confers the same arbitrary-read capability -- this is a second path to a capability already granted, not a new privilege. Found during the Phase 122 checkpoint security-auditor's third (passing) pass, post-CER-174/CER-175 remediation. Fix direction: add --no-index to _SHADOW_REVIEWER_DENIED_GIT_FLAGS in reviewer_bash_guard.py. | security-auditor | 2026-08-05 | 122 |

| CER-177 | LOW: skills/pairmode/scripts/scope_guard.py's check_path, when the calling agent's cwd is the main checkout rather than a story worktree, has resolve_call_story fall through to lifting the story ID out of the target PATH instead of the cwd -- so a shadow-reviewer write to ANY existing story worktree's .pairmode-suggestions.md (not just its own story's) is allowed when invoked from the main checkout (verified: cwd=<main>, path=<main>/.pairmode-worktrees/INFRA-501/.pairmode-suggestions.md -> ALLOW). From a real worktree cwd (the actual dispatch shape) this is correctly denied. Blast radius is limited to one gitignored, take-it-or-leave-it advisory filename that is never code. Found during the Phase 122 checkpoint security-auditor's third (passing) pass. | security-auditor | 2026-08-05 | 122 |

| CER-178 | LOW: skills/pairmode/scripts/scrub_fleet_names.py's apply_lessons() writes lessons/lessons.json and lessons/LESSONS.md -- both matched by scope_guard's PROTECTED_GLOBS and this project's declared protected_paths -- with no _read_protected_paths/_is_protected consultation, unlike the general apply() loop (lines ~282-286) which does refuse protected paths; because it's invoked via Bash it never passes through the Edit/Write scope_guard hook at all. Bounded: both targets are module-level literals with no input-controlled path, root is not settable from the CLI, it no-ops without the gitignored fleet map, and it raises if entry count/id order would change. This is the documented CER-173 exception (docs/architecture.md, lesson_utils.save_lessons, builder/reviewer procedures) -- flagging for consideration of adding an explicit code-level guard rather than relying on the documented exception alone. Found during the Phase 122 checkpoint security-auditor's third (passing) pass. | security-auditor | 2026-08-05 | 122 |

| CER-179 | LOW: reviewer_bash_guard.py:139 and scope_guard.py:124 both gate their shadow-reviewer-specific confinement on an exact literal string match against agent_type == 'shadow-reviewer' in the tool-call payload. Any casing or naming drift in that harness-supplied field silently fails OPEN to unrestricted Bash and full builder-scope Write for the shadow-reviewer role, with no test able to pin the literal the harness actually sends at the hook layer (only that the guards behave correctly once the string is exactly right). CER-174/CER-175's live-confirmed blocks are the only evidence the literal currently matches production. Consider a periodic live-harness smoke check or a documented assumption in docs/architecture.md. Found during the Phase 122 checkpoint security-auditor's third (passing) pass. | security-auditor | 2026-08-05 | 122 |

| CER-182 | MEDIUM: skills/pairmode/scripts/audit.py's INFRA-398 stale-shape migration diagnostic (~lines 565-568) strips a leading '#' from a legacy override key but never lowercases/collapses whitespace the way _split_sections' real normalised keys do, so for a mixed-case legacy entry (e.g. 'CLAUDE.md:## Review Checklist') it advises a 'corrected form' ('Review Checklist') that still fails to match the actual normalised key ('review checklist') -- an operator following the diagnostic's own advice ends up with a still-broken override. Found during the Phase 123 checkpoint security-auditor's second (still-failing on an unrelated finding) pass. | security-auditor | 2026-08-05 | 123 |

| CER-183 | LOW: skills/pairmode/scripts/sync.py's _append_section_to_file (~lines 414-443) now hardcodes '## ' + header_key when appending a MISSING section, because heading depth is no longer recoverable from the post-CER-170 section key shape. Verified latent only: no current CANONICAL_FILES/SCAFFOLD_FILES template contains an ###+ (H3 or deeper) heading, so this has not fired incorrectly yet, but would silently mis-level a newly-appended H3+ section if one were ever added to a managed template. Found during the Phase 123 checkpoint security-auditor's second pass. | security-auditor | 2026-08-05 | 123 |

| CER-184 | MEDIUM: skills/pairmode/scripts/lesson_review.py:253 _candidate_pattern_id builds f'{file}::{section}' from pairmode_drift_report.py's section key for its .pairmode-drift-rejected persistence. INFRA-399 (CER-181) changed that key shape from '## build loop' to 'build loop' -- previously latent (flagged as out-of-scope by INFRA-398 while the drift-report bug made the key path effectively dead), now live: every previously-persisted operator rejection is stranded (suppressed check now returns False for pre-existing entries), so a rejected convergence candidate is silently re-offered, and re-rejecting it appends a second, differently-shaped line rather than updating the existing one. Not HIGH: promotion is interactive/confirm-gated, no destructive write, no silent corruption -- the failure mode is a visible, harmless re-prompt. Found during the Phase 123 checkpoint security-auditor's third (passing) pass. Fix direction: migrate or dual-accept both key shapes in .pairmode-drift-rejected the same way CER-180/INFRA-398 did for .pairmode-overrides. | security-auditor | 2026-08-05 | 123 |

| CER-185 | MEDIUM: skills/pairmode/scripts/audit.py's _load_overrides_with_diagnostics (~line 565) returns the override key un-lowercased, and audit.py/sync.py compare it against a lowercased _normalise()'d section key (so a mixed-case override key like 'Build Loop' never matches there), while pairmode_drift_report.py's thin wrapper (INFRA-399) explicitly lowercases before comparing -- so the same .pairmode-overrides entry is honoured by drift-report but silently ignored by audit/sync, or vice depending on which direction the case mismatch runs. Verified live: entry 'CLAUDE.build.md:Build Loop' -> audit loader ('CLAUDE.build.md', 'Build Loop') no match; drift loader ('CLAUDE.build.md', 'build loop') match -- same file, two different tools, two different answers. Bounded: .pairmode-overrides.j2 and SKILL.md:220 both document lowercase keys, so a compliant operator is unaffected. Found during the Phase 123 checkpoint security-auditor's third (passing) pass; distinct from CER-182 (which covers only the stale-shape diagnostic's advice text, not this underlying case-sensitivity divergence). Fix direction: lowercase the override key at the same point audit.py's loader currently strips the '#' marker, so all three consumers (audit, sync, drift-report) compare against an identically-cased key. | security-auditor | 2026-08-05 | 123 |

| CER-186 | MEDIUM: docs/exemplars/EXEMPLAR-000.md's YAML frontmatter (id/rail/title/status/story_class/auth_gated/schema_introduces/primary_files/touches) is structurally invisible to both audit.py and sync.py: _split_sections stores the content following a leading '---' line under a separator key, and both the audit comparison loop and sync skip separator keys via _is_separator_key. Proven empirically: editing 'id: EXEMPLAR-000' to 'id: EXEMPLAR-000-LOCAL' in a project copy produced zero audit findings, and a subsequent 'sync --yes' left the corruption in place while correctly repairing an unrelated body-section edit in the same file. INFRA-332's sync-agents step exists precisely because sync.py cannot see agent-file frontmatter, but EXEMPLAR-000.md was deliberately kept out of AGENT_FILES (it's a CANONICAL_FILES entry per INFRA-392/CER-171), so it inherits the same blind spot with no compensating mechanism. Net effect: the file-absent case CER-171 fixed is closed, but a file-present-and-frontmatter-drifted case remains a silent, permanently-undetectable degrade of the same kind. Found during the Phase 124 checkpoint security-auditor's first (passing) pass. Fix direction: extend a frontmatter-comparison mechanism (similar in spirit to sync-agents' approach for AGENT_FILES) to CANONICAL_FILES entries that carry meaningful YAML frontmatter, or explicitly document EXEMPLAR-000.md's frontmatter as an accepted drift-blind exception if a fix is judged not worth the complexity. | security-auditor | 2026-08-05 | 124 |

| CER-187 | LOW/MEDIUM: skills/pairmode/scripts/bootstrap.py:1375's --force-agents help text ('Overwrite existing agent files in .claude/agents/ even if already present.') and skills/pairmode/SKILL.md:140's matching description were not updated when INFRA-392 (CER-171) widened the flag's scope to also unconditionally overwrite docs/exemplars/EXEMPLAR-000.md with no prompt (bootstrap.py:1777-1781). An operator refreshing agent shells via --force-agents can now clobber a locally-edited exemplar with no warning from the docs describing the flag. Related, pre-existing and not newly introduced: SKILL.md's 'Outputs' list (~lines 43-58) is already chronically stale (missing narratives, spec-writer/docs-reviewer/gate-worker/shadow-reviewer, .pairmode-overrides) so this is an aggravation of existing drift, not a new class. Found during the Phase 124 checkpoint security-auditor's first (passing) pass. | security-auditor | 2026-08-05 | 124 |

| CER-189 | LOW: .pairmode-fleet.local.json.example uses 'example-repo-1'..'example-repo-3' as JSON keys, but the real key shape is the anonymized label (Repo-A..Repo-O), not a repo-like name -- an operator copying the template naturally writes {'<their-repo>': '/mnt/work/<their-repo>'}, making label == real name so the scrub becomes a self-substitution no-op that then reports every mention as an unfixable hit forever. Fix: change the template's example keys to Repo-A-style labels. Found during the Phase 125 checkpoint security-auditor's first pass. | security-auditor | 2026-08-05 | 125 |

| CER-190 | LOW: scrub_fleet_names.py's _expand_case_variants (~lines 177-194) covers only as-written/capitalize/upper case forms -- mixed-case, hyphenated, or underscored prose variants of a real name survive unreported. Separately, _is_domain_context (~lines 213-219, _DOMAIN_SUFFIX_RE ~73-75) silently skips, in both apply and verify, any real name immediately followed by a domain suffix (.com/.org/.net/.io/.dev/.app/.co/.help/.ai/.xyz) -- correct for the operator's own contact-domain mentions but unbounded and unlogged, so a coincidental '<realname>.io'-shaped mention would be dropped from the report with no trace. Found during the Phase 125 checkpoint security-auditor's first pass. | security-auditor | 2026-08-05 | 125 |

| CER-191 | LOW (data-flow, half-implementation): externalizing fleet_discovery.py's hardcoded candidate list into .pairmode-fleet.local.json silently dropped its coverage from 16 previously-documented candidates to the 15 the local map carries, so that one sibling is no longer scanned by _default_candidates() at all -- no test or check asserts the local map covers the full previously-documented candidate set, so the loss is silent. Same root cause as the CER-188 CRITICAL finding (a dropped map entry), filed separately since this is about scan coverage rather than committed-doc leakage. Found during the Phase 125 checkpoint security-auditor's first pass. | security-auditor | 2026-08-05 | 125 |

| CER-192 | HIGH, DEFERRED PENDING OPERATOR DECISION (not to be built without explicit direction): every pre-scrub commit in this repo's git history still contains all real sibling-repo names in full -- the CER-172/INFRA-393/394 scrub rewrote only the working tree at HEAD, not history. This repo's origin is the public GitHub repo git@github.com:nullvalues/flex, so all real names remain visible via 'git log -p' or GitHub's history UI for anyone with repo access, despite CER-172 being marked RESOLVED. Remediation (git filter-repo + coordinated force-push to origin/main and the flex-harness fold-prep branch, or a GitHub support purge request) is a destructive, high-blast-radius operation on a shared public repo and was explicitly deferred by the operator on 2026-08-05 pending a separate discussion -- do not action this CER without a fresh, explicit go-ahead. | security-auditor | 2026-08-05 | 125 |

| CER-193 | MEDIUM: tests/pairmode/test_next_action.py::test_live_backlog_do_now_classification_parity fails on main independent of any Phase 130 change -- confirmed failing identically on the pre-INFRA-400 baseline via git stash, and the INFRA-400 diff touches zero lines in test_next_action.py or docs/cer/backlog.md (the files that test depends on). First noticed during INFRA-400's build/review full-suite runs (2026-08-05/06). Needs root-causing -- likely a CER Do Now backlog-row classification/parity check that has drifted out of sync with the current backlog.md content given how many CER rows have been added/resolved across Phases 122-130 in quick succession. | builder | 2026-08-05 | 130 |



---

## Do Much Later

Not urgent, marginal value. Style, cosmetics, speculative improvements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|



| CER-152 | LOW: `cer.py gate`/`cer.py groom`'s own docstrings claim they are wired into the checkpoint sequence ("Wired into the `checkpoint-tag` step of `record-checkpoint-step`"); the live gate (`flex_build._cer_do_now_gate_message`) imports the shared function directly and never shells out to the CLI, and `groom` (era 002's stated "run on every cold-eyes review" policy) has no enforcement or reminder anywhere in the loop. Two implementation surfaces for the same concept that can drift. `skills/pairmode/scripts/cer.py`. **RESOLVED INFRA-338 (Phase 117)** — `cmd_gate`'s and `cmd_groom`'s docstrings were corrected to describe the real wiring (checkpoint-tag imports `find_open_do_now_rows` directly and never shells out to the CLI; `groom` has no automated invocation anywhere in the loop); no new CLI-invocation wiring was added. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, fable finding M7 / opus finding M6) | 2026-08-01 | 117 |

| CER-153 | LOW: several `effort.db` columns have no live writer or reader in the hook-driven recording path — `tool_uses` (hard-coded `None`, zero readers), `duration_ms` (only populated on a file-fallback branch, not the primary reconcile path, zero readers outside tests), and `story_class`/`model_selection_reason` (populated only by the legacy `record_attempt.py` CLI writer that `CLAUDE.build.md` says is not part of the live loop — only 49 of 575 live rows carry them, degrading `pairmode_effort`'s decision-quality report section to a near-empty group on real data). `effort_db.next_attempt_number` has zero callers — 43 lines of maintained dead code whose own docstring warns against the racy pattern its existence invites. `skills/pairmode/scripts/effort_db.py`, `record_attempt.py`. **RESOLVED Phase 117 — INFRA-348:** applied the mechanical keep-and-wire-or-remove rule to all four items. `tool_uses` and `effort_db.next_attempt_number` are deleted (schema column dropped idempotently via `effort_db._drop_columns_if_present`; function body removed, not deprecated). `duration_ms` is kept and wired on the primary live path — `subagent_transcript.py`'s synchronous sidechain path, SubagentStop payload branch, file-fallback branch, and quiescent-retirement sweep all now derive it through one shared `_duration_ms_from_ts` helper — because the observability API's `readers/effortDb.ts` `queryEffortSummary` has a real, live reader (`effort_summary.by_phase[].median_duration_ms`, served by `GET /api/repos/:id/context`); that reader already filtered NULLs correctly and needed no change. `story_class`/`model_selection_reason` gain a live hook-driven writer: `story_class` is read fresh from the story file's frontmatter at record time; `model_selection_reason` is plumbed from `create-story-worktree --model-selection-reason` (the orchestrator's own `next-action` dispatch decision) through a new per-story `state.json["current_stories"][id]["model_selection_reason"]` stamp, never recomputed at record time. `record_attempt.py`'s manual CLI writer for both columns (INFRA-345) is unaffected. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, opus findings M9/M10) | 2026-08-01 | 117 |

| CER-154 | LOW: era-ledger flip failures are silently swallowed — `_flip_era_ledger_row`'s `"not_found"` return value is computed and discarded by its caller, so `checkpoint-tag` can silently leave the era ledger stale while the phase-index flip succeeds; compounded by the search being restricted to `status: active` era docs, so re-tagging a phase from an already-closed era silently skips the ledger entirely. Separately, `era_transition`'s disposition gate fails open on any unparseable `## Phases` ledger table (no cells matching exactly `phase`/`status` → vacuous `[]` → the gate passes and "Era N closed" prints over live, un-dispositioned phases). `skills/pairmode/scripts/flex_build.py` `_flip_era_ledger_row`, `skills/pairmode/scripts/era_transition.py`. **Absorbed at spec time by INFRA-346 (Phase 117)** — folded into the phase-completion/checkpoint-gate unification fix rather than deferred, since it's the same subsystem; resolution annotation lands when that story completes. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, opus findings M4/M5) | 2026-08-01 | 117 |

| CER-155 | LOW: `docs/phases/index.md`'s `Tag` column is never mechanically written by any tool (`phase_new.py` writes only the phase-file link cell; `_mark_phase_complete_in_index` only touches the status cell) — rows 8-105 carry hand-written `· cpNN` suffixes, rows 106-116 carry none despite `cp-106`..`cp-116` all existing. No tool anywhere checks whether the corresponding git tag actually exists before or after `checkpoint-tag`'s step 1 (mark-complete) runs, so if step 2 (`git tag && push`) fails after step 1 succeeds, re-running step 1 is idempotent-and-useless with no detector. `docs/phases/index.md`, `skills/pairmode/scripts/flex_build.py`. **Absorbed at spec time by INFRA-346 (Phase 117)** — folded into the phase-completion/checkpoint-gate unification fix rather than deferred, since it's the same subsystem; resolution annotation lands when that story completes. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, opus finding M7) | 2026-08-01 | 117 |

| CER-156 | LOW: multiple docstrings and comments in the harness actively misdescribe current wiring rather than describing it accurately — `flex_build.cmd_next_action`'s own docstring still says "Advisory only — not wired into the live CLAUDE.build.md loop (DP7)" when it is the live loop driver; `cmd_record_intent_review`'s docstring references a nonexistent `_is_fresh_phase`; a `next_action.py` Row-PBI comment claims checkpoint-intent carries a model override when it does not (see the harness-integrity phase); `.claude/agents/spec-writer.md` claims `select_spec_writer_model` doesn't exist when it's wired. This class of drift (comment describes a prior or aspirational state, not the shipped one) is the same shape CER-078/CER-085 already flagged elsewhere in this file — a documentation-currency sweep across `flex_build.py`/`next_action.py` docstrings specifically would catch a cluster of these at once. `skills/pairmode/scripts/flex_build.py`, `next_action.py`, `.claude/agents/*.md`. **RESOLVED Phase 117 — INFRA-349:** corrected four stale wiring claims in `flex_build.py:cmd_next_action` (docstring), `flex_build.py:cmd_record_intent_review` (docstring), `.claude/agents/spec-writer.md` (frontmatter), and `.claude/agents/docs-reviewer.md` (frontmatter); the bounded sweep also found and fixed a fifth instance in `.claude/agents/docs-reviewer.md` that the original CER-156 filing did not name. CER-156's original `checkpoint-intent` sub-claim (that it carries a model override) was overtaken by INFRA-340's landing (the behaviour was wired post-INFRA-340, so the comment became true) rather than being a live defect at fix time. Guard tests in `tests/pairmode/test_docs.py` prevent recurrence. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, both reviewers, various L-numbered findings) | 2026-08-01 | 117 |

| CER-157 | LOW: 137 pairmode tests fail headless (no interactive gpg agent) because they shell out to real `git commit` and inherit the operator's global gpg-signing config; all 137 pass with `commit.gpgsign=false`. Same class of environment-coupled-test finding as CER-146 (the `flex-harness`-directory-name-substring test), just a different coupling axis (signing config vs. cwd path). A CI or headless gate would read red for a non-reason. `tests/pairmode/` (137 affected tests, no single file). **Absorbed at spec time by INFRA-350 (Phase 117)** — folded in rather than deferred, especially since it hit live during this same phase's own spec-drafting commit; resolution annotation lands when that story completes. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, opus finding M8) | 2026-08-01 | 117 |

| CER-158 | LOW: `record-checkpoint-step` without an explicit `--phase-key` degrades ambiguity (one active phase plus queued `planned` rows — the common case, not a corner case) to a warning and stamps the step under key `""`, which the keyed-shape resolver read never sees — the same gate step can re-emit indefinitely. Mitigated only by the prose mandate in `CLAUDE.build.md` to always pass the flag explicitly; nothing enforces it. `skills/pairmode/scripts/flex_build.py` `_record_checkpoint_step`. **Absorbed at spec time by INFRA-346 (Phase 117)** — folded into the phase-completion/checkpoint-gate unification fix rather than deferred, since it's the same subsystem; resolution annotation lands when that story completes. | Build-loop cold-eyes review, session 2026-08-01 (docs/build-loop-cold-eyes-review-20260801.md, fable finding M1) | 2026-08-01 | 117 |

| CER-050 | `.companion/state.json` writes are non-atomic across all writers — `hooks/session_start.py:73-75` (INFRA-175 reset write), `hooks/pre_tool_use.py` (`acknowledged_at`), and the flex_build.py CLI paths all use direct `write_text()` with no temp-file + `os.replace()`. A process kill mid-write could truncate state.json (recovery: re-bootstrap or manual `set-context-tokens`). Concurrent writers are not expected in normal operation. **AMENDED Phase 109 — INFRA-286: concurrent writers ARE now expected — single-orchestrator parallel story builds (Phase 109's target capability) run more than one builder/reviewer cycle at once. INFRA-285 (CER-097) added the bounded advisory `state_utils.state_lock` and converted every writer CER-097 named to route through it; the lock narrows the read-modify-write window, it does not close it — this doctrine note must not be read as "concurrent writers are now safe," only as "expected and partially mitigated."** Fix: shared `_atomic_write_state()` helper (tempfile in same dir + `os.replace`) adopted by all writers. LOW severity (informational; pre-existing pattern, called out by Phase 68 security audit for parity). **RESOLVED HARNESS015-main (INFRA-202) — remaining writers (`hooks/post_tool_use.py`, `story_context.py`, `bootstrap.py`, `sidebar.py`) now use the shared `state_utils._atomic_write_json` helper.** | Phase 68 security audit | 2026-06-12 | 68 |

| CER-051 | `_derive_transcript_path` constructs `home/".claude"/"projects"/cwd_key/f"{session_id}.jsonl"` without normalising or rejecting traversal sequences in the `session_id` basename. If `session_id` ever contained `../`, the constructed path could resolve outside `~/.claude/projects/`. Impact is low in practice: `session_id` is supplied by Claude Code in the PreToolUse stdin payload (not user input); the function only tail-reads the file; all exceptions are caught and `None` returned (fail-open). Worst case: read of an arbitrary `.jsonl` file, sum of three integer fields written to `state.json["context_current_tokens"]`. Fix: reject any `session_id` containing `/` or `..` before path construction, or replace the existence check with a `Path.resolve()` containment check against `home / ".claude" / "projects" / cwd_key`. LOW severity. `skills/pairmode/scripts/context_budget.py:_derive_transcript_path`. **RESOLVED INFRA-203 — `_derive_transcript_path` rejects `session_id` containing `/` or `..` and applies a `.resolve().is_relative_to()` containment check against `home / ".claude"`; covered by `test_context_budget.py:1384-1413`. Confirmed already fixed in code during HARNESS015-main phase planning — this backlog entry's status was stale.** | Phase 72 security audit | 2026-06-16 | 72 |

| CER-052 | `_derive_transcript_path` in `context_budget.py` constructs a JSONL path from `session_id` (hook event input) without a `Path.resolve().is_relative_to()` containment check. A crafted `session_id` with `..` segments could escape `~/.claude/`. Fix: add containment guard after `candidate.resolve()`. HIGH severity. `skills/pairmode/scripts/context_budget.py` `_derive_transcript_path`. **RESOLVED cp-HARNESS009-main (Phase 75) — _derive_transcript_path() applies is_relative_to((home / ".claude").resolve()) at context_budget.py:116; the containment check is live. Resolved note was not written at checkpoint time.** | Phase 74 security audit | 2026-06-22 | 75 |

| CER-083 | [Renumbered from a duplicate CER-066 at the cp-100 checkpoint-docs review — that ID is already used twice (story_update pipe bug, resolved Phase 94; original checkpoint_step finding, resolved HARNESS015-main).] Stale `checkpoint_step` state recurred at cp99→phase-100 **despite** the RESOLVER-017 fix being live: `cmd_record_checkpoint_step` does reset the list to `[]` when `checkpoint-tag` is *recorded*, but `CLAUDE.build.md`'s checkpoint-tag instruction is a raw `git tag && git push` — the terminal step is never recorded via the CLI, so the reset (and the INFRA-239 index mark-complete) never fires. Evidence: cp99's leftover list held only the three gate steps, no `checkpoint-tag` entry. Result: phase-100's `next-action` resolved straight to `checkpoint-tag`, silently skipping all three gate workers; caught live 2026-07-24 via resolver-state inspection, state manually cleared, gates run explicitly. Fix direction: make `record-checkpoint-step checkpoint-tag` the mandated tagging path (it already marks the phase complete per INFRA-239 — fold the `git tag`/push into it, or update `CLAUDE.build.md`/template to record the step before/with the git tag); add a regression test that a state.json holding a full prior-phase gate list still dispatches checkpoint-security first after a raw-tag-style checkpoint. HIGH severity (checkpoint gates are the phase-quality backstop; skipping them is silent). `CLAUDE.build.md` § Checkpoint, `skills/pairmode/templates/CLAUDE.build.md.j2`, `skills/pairmode/scripts/flex_build.py` `cmd_record_checkpoint_step`. **RESOLVED Phase 102 — INFRA-260:** both halves fixed — (1) `CLAUDE.build.md` § Checkpoint and `skills/pairmode/templates/CLAUDE.build.md.j2` § Checkpoint now mandate `record-checkpoint-step checkpoint-tag` before any raw `git tag`, with the ordering rationale stated inline (a raw tag alone is forbidden); (2) `_record_checkpoint_step` now stamps `state.json["checkpoint_phase"]` with the resolved phase key on every call (reset to `""` alongside `checkpoint_step` on the terminal step), and `next_action.infer_position` clears the exposed `checkpoint_step` to `[]` whenever that stamp names a phase other than the one currently active — a stale gate-step list recorded for a superseded phase can no longer be silently mistaken for the active phase's own progress. Regression tests in `tests/pairmode/test_next_action.py::TestResolveNextActionCheckpoint`. | phase-100 build observation (session 2026-07-24); renumbered + root-caused at cp-100 checkpoint-docs | 2026-07-24 | 102 |

| CER-084 | LOW: `skills/pairmode/skills/security-auditor/procedure.md`'s documented thin-delegation exception list for `hooks/post_tool_use.py` (check 1) enumerates only `context_current_tokens` + timestamp as authorized state.json writes; INFRA-254 (Phase 100) added a second delegated write path (`record_step_growth()` → `context_step_growth_samples`, `expected_step_tokens`), documented in `docs/architecture.md` but not in the procedure's own exception list. The cp-100 security auditor had to re-derive the authorization from architecture.md. Same doc-lag class as CER-078. Fix: name the `record_step_growth` write explicitly in the procedure's exception list. `skills/pairmode/skills/security-auditor/procedure.md`. **RESOLVED Phase 114 — INFRA-305: `hooks/post_tool_use.py`'s exception bullet now names `record_step_growth` (INFRA-254) and its two state.json keys (`context_step_growth_samples`, `expected_step_tokens`), plus `subagent_transcript` (INFRA-236) and `session_state`; pinned by `test_hook_delegations_are_documented_exceptions`.** | Phase 100 checkpoint-security audit (informational note) | 2026-07-24 | — |

| CER-085 | LOW: `docs/architecture.md:583,605` and `README.md:162` still present the spec-derived settings.json deny list as an active protection surface for downstream projects; this sits in unreconciled tension with INFRA-253's "settings.json carries tooling only; scope_guard + permissions artifacts are the single protection surface" doctrine (architecture.md § 9.5). The passages describe the downstream mechanism, not flex's own retired denies, but a reconciliation pass should state which doctrine downstream projects are expected to converge on. **RESOLVED Phase 114 — INFRA-305: each live deny-list passage in `docs/architecture.md` and `README.md` now states `scope_guard` + the per-story permissions artifacts (INFRA-253, § 9.5) are flex's own enforcement surface and the spec-derived `.claude/settings.json` deny list is a bootstrap-era convenience for downstream projects; the INFRA-253 doctrine block itself (§ 9.5) is untouched.** | Phase 100 checkpoint-docs review | 2026-07-24 | — |

| CER-087 | MEDIUM: INFRA-255's fail-closed containment blocks *all* legitimate out-of-repo Edit/Write tool calls from a session anchored in this project, not just traversal attacks — observed live minutes after cp100: a Write to the harness memory directory (`~/.claude/projects/-mnt-work-flex/memory/…`) was denied with `path escapes project root` (no active story; previously this fell through as `no active story — allowing`). The same applies to scratchpad writes (`/tmp/claude-*/…`) and any other user-level path. The spec intended fail-closed on escapes in every guard state, but the blast radius includes benign harness-infrastructure writes that have nothing to do with story scope. Fix direction: an explicit allow-list of harness-owned out-of-root prefixes (e.g. `~/.claude/projects/<cwd-key>/memory/`, the session scratchpad root from env) checked *after* resolution (so traversal into them still can't be forged from a relative path), or scoping the escape-deny to the active-story guard states only while keeping no-story out-of-root writes allowed for non-protected targets. `skills/pairmode/scripts/scope_guard.py` `check_path`/`_normalise`, `hooks/pre_tool_use.py`. **RESOLVED Phase 105 — INFRA-271: `scope_guard.harness_owned_prefixes(project, raw_project_dir, home)` derives a narrow allow-list — `<home>/.claude/projects/<key>/memory/` (allow-listed only at this subdirectory; the sibling `<session>.jsonl` transcripts are excluded because `subagent_transcript.py` derives the effort ledger from them, so an agent that could write those could forge its own effort record), `<tmp>/claude-<uid>/<key>/` (the session scratchpad root), and `<home>/.claude/plans` — keyed on both the resolved main-checkout root and, when different, the resolved raw `project_dir`. `_out_of_root_decision()` consults this allow-list, on the *resolved* path via `Path.is_relative_to` (never a string prefix check), only after `_normalise` has already determined the path escapes the project root, and both `"path escapes project root"` deny sites in `check_path()` now route through it. `_normalise`'s containment itself is explicitly unchanged — this is a decision layered on top of it, not a hole punched in it — and everything else under `~/.claude/` (`settings.json`, `policies/`, `plugins/`, `skills/`) stays denied, since those are harness configuration, not harness scratch state.** | Live-hit post-cp100 (orchestrator memory write denied, session 2026-07-24) | 2026-07-24 | 105 |

| CER-088 | MEDIUM: `effort_db.pending_reconcilable()`'s query (`WHERE tokens_total IS NULL AND output_file IS NOT NULL ORDER BY id DESC LIMIT ?`) has no supporting index (existing indices cover story_id/phase/rail only) — a full table scan on every invocation, reachable from the SessionStart hook path via `reconcile_pending_attempts`. LIMIT bounds result size, not scan cost; small per-project tables make this low-likelihood today. Fix: add an index (or partial index) on `output_file`/`tokens_total` if attempts tables grow. `skills/pairmode/scripts/effort_db.py` `pending_reconcilable`. **RESOLVED Phase 104 — INFRA-266. Added a partial index `idx_attempts_pending` on `attempts(id DESC) WHERE (tokens_total IS NULL OR outcome IS NULL) AND output_file IS NOT NULL` — created after `_MIGRATIONS` so a legacy (pre-INFRA-258) table upgrades cleanly. Also added an opt-in `max_age_days` cutoff (`effort_db.PENDING_MAX_AGE_DAYS = 14`) applied only at the `reconcile_pending_attempts` hook-sweep call site — the shared `pending_reconcilable` query itself still defaults to no cutoff so a diagnostic that needs to see permanently-pending rows is unaffected.** | Phase 101 checkpoint-security delta audit (non-blocking note) | 2026-07-24 | 104 |

| CER-089 | LOW: `subagent_transcript.read_completed_spawn()` opens the persisted `output_file` path directly with no `resolve()`/containment check against the session tasks directory. The value is harness-generated launch metadata (same trust category as hook-payload `cwd`, which the audit procedure excludes from traversal flagging) and no adversarial injection path was found — observational only. Fix if desired: contain resolved path under the session tasks root before opening. `skills/pairmode/scripts/subagent_transcript.py` `read_completed_spawn`/`_extract_spawn_ref`. **RESOLVED Phase 104 — INFRA-266. `read_completed_spawn` now routes `output_file` through a new pure helper, `_contained_spawn_output`, before opening anything: by default the path must resolve under a temp root (`tempfile.gettempdir()`/`$TMPDIR`) and contain a `tasks` path component; an explicit `tasks_root` (used by `reconcile_pending_attempts` and tests) requires strict containment instead. A rejected path returns `None` (row stays pending) rather than being opened.** | Phase 101 checkpoint-security delta audit (observational note) | 2026-07-24 | 104 |

| CER-086 | LOW: `CHANGELOG.md` has no `[Unreleased]` entry for Phase 99 (post-fold self-sync remediation, INFRA-247..252) — the per-phase-entry convention (Phases 95/96/98, Phase 100) was skipped at cp99's checkpoint-docs step. Backfill from `docs/phases/phase-99.md`'s story list. `CHANGELOG.md`. **RESOLVED Phase 114 — INFRA-305: backfilled `### Fixed [pairmode] — Phase 99 (Post-fold self-sync remediation)` between the Phase 100 and Phase 98 headings, naming INFRA-247..252.** | Phase 100 checkpoint-docs review (pre-existing gap) | 2026-07-24 | — |

| CER-090 | HIGH: the vendored `skills/observability/node_modules` tree is only partially tracked — `.gitignore`'s global `build/` and `dist/` patterns (`.gitignore:7`) exclude payload directories inside vendored packages (e.g. `@tanstack/react-query`'s `build/` with all its compiled JS and `.d.ts` files; ~untracked `dist/` dirs across many `.pnpm` packages, visible via `git status --ignored`). The main worktree works only because the files exist untracked on disk; every fresh pairmode story worktree gets just the tracked subset, so `tsc -b` fails with TS2307 module-not-found and `test_observability_ui.py::test_ui_build_emits_dist_index_html` FAILs the build gate for *every* story. Hit live during INFRA-259's review (reviewer FAIL, environmental); orchestrator worked around it by rsyncing the payload from the main worktree into the story worktree. Fix: negate the ignore patterns under the vendored roots (e.g. `!skills/observability/node_modules/**`) and commit the payload, or scope `build/`/`dist/` ignores away from vendored trees; add a guard that the vendored tree builds from a clean checkout. `.gitignore`, `skills/observability/node_modules`, `skills/observability/ui/node_modules`. **RESOLVED cp-103 (INFRA-261) — `.gitignore` scoped to un-ignore vendored payload, 1613-file payload committed, guard test `tests/pairmode/test_vendored_payload_tracked.py` added; fresh-worktree build + full suite proven with no repair.** | INFRA-259 build cycle (reviewer build-gate FAIL, session 2026-07-24) | 2026-07-24 | 102 |

| CER-091 | HIGH: INFRA-259's live smoke test caught three async effort-recording defects (evidence in `docs/stories/INFRA/INFRA-259.md` § Smoke results C–E): (1) a reviewer Agent spawn produced **no attempts row at all** — the PostToolUse Task/Agent recording branch did not fire for the second (re-review) spawn of the same story in the same session, so no attempt_number=2 row exists and no sweep ran; (2) row 344 (`phase:101`, intent-reviewer) sits in a partially-backfilled state — `tokens_total = 6597` set but `outcome` still NULL — and the lifetime rollup happily counts it, meaning reconcile can commit tokens without outcome; (3) row 343 (`phase:101`, security-auditor) never reconciles despite multiple sweep opportunities and an extant transcript — likely its last entry lacks `stop_reason: end_turn`, leaving a permanent-pending leak with no diagnostic surface. (4, added same session: row 352's FAIL reconciled at a later spawn's PostToolUse *after* `merge-story-worktree` had already cleared the attempt counter, resurrecting `.companion/attempt_counter.json` to `{INFRA-259: 1}` for a story already merged — reconciliation-time counter bumps have no story-lifecycle guard, so a stale counter can outlive its story.) Fix directions: instrument/verify the PostToolUse recording branch for repeat spawns of the same story+role (dedup key? stamp state?); make reconcile atomic over tokens+outcome; add a pending-rows age diagnostic (read-only view on `pairmode_effort.py` per INFRA-259's out-of-scope note). `hooks/post_tool_use.py`, `skills/pairmode/scripts/subagent_transcript.py`, `skills/pairmode/scripts/effort_db.py`. **RESOLVED Phase 104 — INFRA-264:** all four defects closed. (1) No code-level dedup ever suppressed a repeat spawn's row (proven by regression test); the real gap was diagnostic — `subagent_transcript.log_recording_event` now appends an append-only, size-capped trace (`.companion/effort_recording.log`) on every `record_attempt_from_transcript` return path, and `hooks/post_tool_use.py`'s spawn branch now also covers `SendMessage` (a continuation, logged as `observed:non-spawn-tool`, never recorded as an attempts row) — the next occurrence is attributable from one `tail`. (2) `parse_worker_outcome` now recognises `ALIGNED` (`RECOGNISED_REVIEW_VERDICTS`, mirroring `worker_result.py`'s `REVIEW_RESULT` enum) and `effort_db.reconcile_attempt` is atomic over `tokens_total`+`outcome` (`_ATOMIC_RECONCILE_FIELDS`) — writing tokens without a resolvable outcome no longer commits. (3) `pending_reconcilable`'s guard widened to `(tokens_total IS NULL OR outcome IS NULL)` so a partial row is reachable again; `classify_pending_reason` names one of nine enumerated reasons (`no-output-file`/`file-missing`/`file-empty`/`in-flight`/`not-terminated`/`no-usage`/`line-cap`/`no-outcome`/`reconcilable`), surfaced read-only via `pairmode_effort.py pending` (age + reason per row, zero writes) and retirable via an explicit `include_quiescent` sweep (age-gated on both the row's `ts` and the output file's mtime, `outcome="UNKNOWN"`, never bumps the counter). (4) `subagent_transcript._story_accepts_late_bump` gates the reconciliation-time FAIL bump — skipped when the story's own frontmatter `status` is complete/merged/deferred/backlog, or when the story is neither counter-recorded nor the loop's `current_story`; the synchronous PostToolUse-time bump remains deliberately ungated. A third reconciliation trigger — `uv run python skills/pairmode/scripts/subagent_transcript.py reconcile` — lets an operator sweep on demand instead of waiting for the next spawn or session start. Regression tests: `tests/pairmode/test_subagent_transcript.py`, `tests/pairmode/test_effort_db.py`, `tests/pairmode/test_pairmode_effort.py`, `tests/pairmode/test_hooks_json.py`, `tests/pairmode/test_post_tool_use_hook.py`. Repairing the live rows 343/344 in `.companion/effort.db` is an explicit post-merge operator action, not part of this fix (INFRA-264 § Instructions 14). **INFRA-298 (Phase 113) closes item (1)'s root-cause disposition:** read `.companion/effort_recording.log` in full (153 lines at build time, 2026-07-26T02:13Z – 2026-07-29T18:26Z) and searched for the CER-091(1) shape — a spawn with no resulting attempts row, or two same-story/same-role spawns in one session with only one `recorded` line. **Disposition (b), explicit closure:** no repeat-spawn drop observed since the INFRA-264 instrumentation landed — every `tool_name: "Agent"` entry in the log carries a decision (81 `recorded`, 62 `skip:not-recordable-role`, 3 `skip:target-unregistered`, plus 5 `skip:late-bump-blocked`/2 `bump:late-fail` reconciliation-time entries), no `recorded:deduped` occurrences, and every story/role pair that spawned a builder or reviewer twice in one session (e.g. INFRA-296's re-build after a FAIL) shows a `recorded` line for each spawn. The original observation's root cause is therefore attributed, retroactively, to the same diagnostic gap INFRA-264's own resolution named — no attempts row plus no trace line, both now impossible since every `record_attempt_from_transcript` return path logs unconditionally. INFRA-298 additionally closes the *other* half of CER-091(1)'s risk surface for the async case specifically: `hooks/subagent_stop.py` -> `subagent_transcript.reconcile_one` gives a second, deterministic completion signal keyed on `agent_id`, independent of whichever PostToolUse invocation an async spawn's launch produced. | INFRA-259 live smoke test (review-close observations, session 2026-07-25) | 2026-07-25 | 104 |

| CER-092 | MEDIUM: `skills/pairmode/scripts/story_new.py:67` emits story-stub frontmatter with a trailing comment on the `touches:` line (`touches:  # If this story changes any documented architecture, ...`). The minimal frontmatter parser (`schema_validator._parse_frontmatter`) reads that as a *string* value, and `flex_build.generate_permissions_artifact` then crashes with `TypeError: can only concatenate list (not "str") to list` inside `create-story-worktree` — blocking the build loop for every freshly-stubbed story until the frontmatter is hand-fixed (hit live on INFRA-259, fixed by two spec commits). Worse, `tests/pairmode/test_story_new.py::test_story_frontmatter_touches_has_architecture_comment` pins the buggy output. Fix: emit `touches: []` and move the architecture guidance to an adjacent HTML/MD comment line (or a `<!-- -->` outside the frontmatter); invert the pinning test; consider hardening `_parse_frontmatter` to strip trailing `#` comments on scalar lines. `skills/pairmode/scripts/story_new.py`, `tests/pairmode/test_story_new.py`, `skills/pairmode/scripts/schema_validator.py`. **RESOLVED cp-103 (INFRA-262) — `story_new.py` emits `touches: []` (guidance moved to story body), pinning test inverted, `_parse_frontmatter` strips scalar trailing comments via shared `_strip_inline_comment`; the 20 on-disk trailing-comment stubs now parse.** | INFRA-260 spec-writer recon (session 2026-07-25); root-caused live during INFRA-259 worktree creation | 2026-07-25 | 102 |

| CER-125 | LOW: `docs/phases/phase-64.md`'s Stories table lists INFRA-164..168 as `backlog` while all five completed under `HARNESS007-main` (Phase G resume) — a stale manifest unreachable by `story_update.py` by design (legacy phase docs are outside its write scope). Fix is a hand edit of the five status cells with a pointer note to HARNESS007-main. gate: next hand edit to `phase-64.md` for any reason, or any `story_update.py` change that adds legacy-phase-doc handling — fold it in then. | Cold-eyes review 2026-07-29 (F12) | 2026-07-29 | — |

| CER-126 | LOW: after the 0.3.1 bump, flex's own `.companion/state.json` `pairmode_version` would still read `0.3.0`, making flex report itself "behind canon" in status surfaces — advisory but noisy for the release that fixes exactly this class of record drift. Absorbed at spec time by the widened INFRA-310 Ensures 27 (Phase 116, AG-5.5: one-key bump, file added to `touches:`) — resolution annotation lands when that story completes. **RESOLVED Phase 116 — INFRA-310: `.companion/state.json`'s `pairmode_version` bumped to 0.3.1 (single-key change).** | Cold-eyes review 2026-07-29 (F11) | 2026-07-29 | 116 |

| CER-169 | HIGH: both `bootstrap.py` hook registrars — `_register_pretooluse_hook` and `_register_context_budget_hooks` — return before ever reaching their own A7 stale-committed-entry eviction call when the plugin-sourced skip fires (INFRA-288/CER-104: an installed plugin's `hooks.json` already provides the event, so no settings-level entry is registered). `_register_pretooluse_hook` (`skills/pairmode/scripts/bootstrap.py`) hits the skip check first (`if ("PreToolUse", pre_tool_use_path.name) in plugin_registered: ... return False`) and its own docstring-cited A7 call (`_evict_stale_committed_hook_entries(settings_path, "PreToolUse", pre_tool_use_path.name)`) sits dozens of lines further down, after the local-settings write — structurally unreachable on the skip path. `_register_context_budget_hooks` has the same shape one level down: its per-event loop `continue`s past the rest of the loop body on a plugin-sourced skip, so the skipped `(event, basename)` pair is never appended to `registered_this_run`, and the function's own A7 eviction loop (`for event, basename in registered_this_run: _evict_stale_committed_hook_entries(...)`) only ever runs over the pairs that *were* registered locally — a plugin-sourced pair is excluded by construction, not just skipped incidentally. `pairmode_migrate.py`'s `to-030 --hooks-only` (INFRA-386, the mechanism `sync-all`'s fifth step invokes) calls exactly these two registrars as its only repair path for a detected offending entry (`_register_pretooluse_hook(settings_json_path, plugin_root)` / `_register_context_budget_hooks(settings_json_path, plugin_root)`, no `home=` override) — it has no other code path that evicts a stale committed-settings.json hook entry. Net effect: on any repo where the plugin already provides these hook events via its own `hooks.json` (the standard shape for every marketplace-installed-plugin repo post-INFRA-383/384, i.e. the entire current fleet target set), `to-030 --hooks-only`'s repair block correctly *detects* the offending entry (`to_repair_events` is populated, `[apply] relocating ...` is echoed) but the eviction call it makes is a guaranteed no-op — the stale/duplicate/machine-absolute entry in the committed `.claude/settings.json` is never actually removed. Confirmed by reading `_register_pretooluse_hook`/`_register_context_budget_hooks` in full (skip-and-return / skip-and-continue precede the A7 call in both) and by the existing regression coverage gap: `tests/pairmode/test_bootstrap.py::test_pretooluse_registration_skipped_when_plugin_provides_it` only asserts that no *new* settings.local.json entry is written on the skip path — no test in either `test_bootstrap.py` or `test_pairmode_migrate.py` asserts that a stale committed-settings.json entry is actually evicted when the plugin-sourced skip also fires, which is exactly the shape that would have caught this. First surfaced live during INFRA-387 (fleet stale-hook remediation, Phase 121): a real `sync-all --apply --yes` run against the fleet found every scanned repo hit this skip branch, so `sync-all --apply` could not clean up stale hook entries anywhere. Fix direction: in `_register_pretooluse_hook`, move (or duplicate) the A7 `_evict_stale_committed_hook_entries` call so it also runs on the plugin-sourced-skip return path (the correct entry's *absence* from settings.local.json is fine here — the plugin itself is the correct entry, so eviction from the committed file is unconditionally safe once a plugin-sourced pair is confirmed); in `_register_context_budget_hooks`, evict per-spec on the skip continue, not only from `registered_this_run` (e.g. a second, always-evicted-on-skip list, or restructure the loop so eviction is decided by "an authoritative source now covers this pair" rather than "this run registered something locally"). Add the missing regression coverage: a stale/machine-absolute committed-settings.json entry present alongside a plugin-sourced `hooks.json` must be gone from the committed file after the registrar runs, for both registrars and through `to-030 --hooks-only`. `skills/pairmode/scripts/bootstrap.py` `_register_pretooluse_hook`, `_register_context_budget_hooks`, `skills/pairmode/scripts/pairmode_migrate.py` `cmd_to_030` (`--hooks-only` repair block), `tests/pairmode/test_bootstrap.py`, `tests/pairmode/test_pairmode_migrate.py`. **RESOLVED Phase 121 — INFRA-389:** both `bootstrap.py` registrars now call `_evict_stale_committed_hook_entries` on their plugin-sourced-skip branches before returning/continuing, closing the eviction bypass; regression tests added in `test_bootstrap.py` and `test_pairmode_migrate.py`. | INFRA-387 fleet stale-hook remediation build (live `sync-all --apply --yes` run against fleet, session 2026-08-04); re-investigated and re-filed after the original filing was lost to a discarded worktree (reviewer FAIL on an unrelated INFRA-387 Ensures item) | 2026-08-04 | 121 |

| CER-142 | MEDIUM: `skills/observability/api/src/readers/resolverState.ts`'s `getFlexBuildPath()` resolves one directory too high before rejoining `pairmode/scripts/flex_build.py` — from `dirname(import.meta.url)` (`api/src/readers` under `tsx`, `api/dist/readers` after `tsc`), four `path.resolve(..., '..','..','..','..')` hops already land on `skills/` (`readers` → `src`/`dist` → `api` → `observability` → `skills`), but the code then does `path.join(obsApiDir, '..', 'pairmode', 'scripts', 'flex_build.py')` — the extra `'..'` steps back out of `skills/` entirely, producing `<repo-root>/pairmode/scripts/flex_build.py` (missing the `skills/` segment) instead of `<repo-root>/skills/pairmode/scripts/flex_build.py`. `spawnSync` therefore always fails (`ENOENT`/non-zero exit) and `readResolverState` silently returns `null` on every call — `/api/repos/:id/system` and `/api/repos/:id/context`'s `resolver_state` field, and `context.ts`'s `current.story_id`/`current.phase` (sourced from `resolverState?.position`), are always null/empty in production, not just in the INFRA-312 test fixture that surfaced it. Found during INFRA-312's route-smoke test authoring (`tests/system.test.ts`): the fixture project is a real, valid flex-shaped tree, and the resolver still returned null — traced to this path bug, not a fixture gap. Fix: `path.join(obsApiDir, 'pairmode', 'scripts', 'flex_build.py')` (drop the extra `'..'`). Out of INFRA-312's declared `touches:` (routes/`package.json`/lockfiles/SKILL.md/this backlog — not `src/readers/`), so filed rather than fixed in-scope per Ensures 5's explicit provision. `skills/observability/api/src/readers/resolverState.ts` `getFlexBuildPath`. **RESOLVED Phase 119 — INFRA-368:** the extra `'..'` hop is removed; `getFlexBuildPath()` now correctly resolves to `<repo-root>/skills/pairmode/scripts/flex_build.py`, verified with an `fs.existsSync` regression test and a sanity revert/restore. | INFRA-312 route-smoke test authoring (dogfood/test defect) | 2026-07-31 | 115 |



---

## Do Never

Rejected findings. Record the rejection reason so it is not re-raised.

| ID | Finding | Source | Date | Phase | Resolution |
|----|---------|--------|------|-------|------------|


| — | *(none)* | — | — | — | — |

