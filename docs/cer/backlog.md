# flex — Cold-Eyes Review (CER) Backlog

*Last updated: 2026-06-11 (CER-046 filed — PAIRMODE_VERSION split)*

This file is the structured triage log for findings from external cold-eyes reviews.
Each finding is assigned to one quadrant. Findings are not deleted — resolved findings
remain in place with a resolution note.

---

## Do Now

Urgent and important. Blocks correctness, security, or the next phase.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|
| CER-046 | `PAIRMODE_VERSION` is defined in three places with diverging values: `audit.py:28` = `"0.1.0"`, `bootstrap.py:33` = `"0.2.0"`, `sync.py` imports from `audit.py` (stale). Every `sync` run stamps `"0.1.0"` into the target project's `state.json["pairmode_version"]`, silently downgrading projects that were bootstrapped at `"0.2.0"`. Confirmed in production: coherra's state.json was downgraded and required manual restoration. Fix: single-source the constant (e.g. `skills/pairmode/scripts/_version.py`) and have `audit.py`, `bootstrap.py`, and `sync.py` import from it. HIGH severity (silent state corruption on every sync run). `audit.py:28`, `bootstrap.py:33`, `sync.py` (import site). | Upstream operator report | 2026-06-11 | — |
| CER-021 | `story_context.py` new CLI (`--set STORY_ID`) constructs a story file path from user-supplied input with no `Path.resolve().relative_to()` containment check before reading the file. A crafted story_id can escape `project_dir/docs/stories/`. Also `--project-dir` has no depth guard, unlike every sibling CLI. Fix: add `relative_to(stories_root)` containment to `_resolve_story_file`; add depth guard to `--project-dir`. HIGH severity. story_context.py:120-135, 173. | Phase 32 security audit | 2026-05-13 | 32 | **RESOLVED Phase 32 INFRA-078**
| CER-027 | Pairmode canon requires `/context` between every story with comparison against `context_budget_threshold` (default 120k tokens) — see CLAUDE.build.md.j2 line 453 area. Enforcement is purely LLM-attention based: the rule lives in a doc the agent reads at session start and is expected to honor each iteration. Operator observation (cross-project session 2026-05-29) confirms the failure mode: LLM moves quickly through stories, skips the check, context bloats past threshold, and the prompt that should have appeared at UI-013/UI-014 never fires. The architect of the rule cannot reliably follow it. Fix: mechanical enforcement via hook (PostToolUse after reviewer commit, or stop-hook that compares token count against threshold and blocks the next tool call until acknowledged). Investigate whether flex/hooks/ already has scaffolding for this and just needs wiring, or whether a new hook needs writing. HIGH severity (methodology safety: missed compactions risk losing in-flight work). CLAUDE.build.md.j2 context-budget section + hooks/. | Operator observation 2026-05-29 | 2026-05-29 | 47 | **RESOLVED Phase 47 — INFRA-127 (context_budget.py module + refresh_effort_baseline.py seed CLI + bootstrap seeding), INFRA-128 (hooks/pre_tool_use.py thin delegate + Task matcher), INFRA-129 (template prose replacement + CLAUDE.md HOOK PERFORMANCE carve-out + architecture.md step 9). Checkpoint cp-47.**
| CER-036 | `flex_build.py` `cmd_permissions_create` takes `STORY_ID` as a positional CLI argument and uses it unsanitised to construct both an input path (`docs/stories/<rail>/<story_id>.md`) and an output path (`docs/phases/permissions/<story_id>.json`). No regex validation on `story_id`, no `Path.resolve().relative_to(project_path)` containment on `out_path`. A crafted `story_id` with `..` segments can write attacker-shaped JSON to any path the invoking process can reach. The permissions file is the source of truth that `scope_guard.py` consults for hook-level write enforcement — write-side traversal weakens scope enforcement integrity. Fix: add `re.fullmatch(r"[A-Z][A-Z0-9_]*-\d{3}", story_id)` guard at function entry; add `resolve().relative_to()` containment on both `story_path` and `out_path` before any I/O. HIGH severity. `flex_build.py` cmd_permissions_create. | Phase 55 security audit | 2026-06-03 | 55 | **RESOLVED Phase 55 INFRA-141** |
| CER-037 | `docs/architecture.md` still describes `hooks/pre_tool_use.py` as "context budget enforcement (CER-027); thin delegate to skills/pairmode/scripts/context_budget.py" (line 26). Phase 55 INFRA-139 added a second dispatch branch (`Edit`/`Write` → `scope_guard.py`) but did not update architecture.md. The "Documented exception" block at line 785 also names only CER-027 and context_budget. A future reviewer reading architecture.md alone sees no authority for the scope_guard dispatch. HIGH severity (doc-currency gap for security-relevant decision-block surface). `docs/architecture.md` lines 26, 169-178, 785-792. | Phase 55 security audit | 2026-06-03 | 55 | **RESOLVED Phase 55 INFRA-142** |
| CER-038 | `phase_new.py` `--phase-id` and `--suffix` CLI arguments are accepted as free-form strings and embedded into filesystem paths (`phases_dir / f"phase-{phase_key}.md"`) without sanitization or containment check. The `relative_to()` calls are display-only and provide no write protection. A crafted argument (e.g. `--suffix "../../../../../../attack"`) can write outside the project directory. Secondary: unsanitised `phase_key` is also written verbatim into `index.md` and era files (markdown injection). Missing tests for traversal vectors in new test classes. Fix: validate `phase_id` and `suffix` against `re.fullmatch(r"[A-Za-z0-9][A-Za-z0-9_-]*", value)` at CLI entry; add traversal-payload test cases. HIGH severity. `phase_new.py` lines 287–294. | Phase 56 security audit | 2026-06-03 | 56 | **RESOLVED Phase 56 INFRA-145** |
| CER-039 | `context_budget.py` secondary hook gate non-functional since Phase 47. `compute_context_tokens(transcript_path)` returns `None` whenever `transcript_path` is absent or incorrect in PreToolUse hook payloads — which is every production session. Confirmed: `context_budget_acknowledged_at` is absent from ALL projects' `state.json` across the entire fleet (flex, forqsite, cora, radar, and 8 others). Root cause: Phase 47 D3 chose transcript JSONL parsing instead of a state.json contract written by the orchestrator's `/context` call. Effect: the secondary gate has never blocked a single Task spawn; the only protection was the LLM-attention primary gate, which also fails on long phases (confirmed forqsite PM023-main: 6-story phase with retries ran to 100% context with no interrupt). Fix: add `flex_build.py set-context-tokens --tokens N` CLI; have orchestrator write token count to `state.json["context_current_tokens"]` after each `/context` call; have `context_budget.py` read from there. HIGH severity. `skills/pairmode/scripts/context_budget.py`, `hooks/pre_tool_use.py`, `CLAUDE.build.md` Context gate. | Phase 57 build post-mortem | 2026-06-03 | — | **RESOLVED Phase 58 — INFRA-148 (`context_budget.py` state.json contract + `flex_build.py set-context-tokens` CLI + test rewrites), INFRA-149 (`CLAUDE.build.md` Context gate records token count; forqsite synced). Checkpoint cp-58.** |
| CER-045 | Three interlocking bugs defeat the context gate on multi-story phases. (1) `story_context.py --clear` erases `context_current_tokens` after every story (Phase 59 CER-041 belt-and-suspenders), forcing a re-estimate at each Context gate. (2) The orchestrator cannot call `/context` programmatically; it estimates (e.g. 25k) and calls `set-context-tokens` with the wrong value every story. So `decide()` always sees 25k + 53k = 78k < 132k → never blocks. (3) `story-cost-estimate` filters by (rail, story_class) with ≥5 sample requirement — new phases always show "insufficient data." Fix (Phase 65): (A) remove token-count pops from `clear_current_story()` (INFRA-170); (B) add `bump-context-tokens --cost N` that accumulates actual story cost (INFRA-169); (C) redesign Context gate to READ from state.json rather than calling /context — absent/stale key emits CONTEXT CHECK REQUIRED once per session (BUILD-027); (D) cross-phase/cross-rail estimation fallback (INFRA-171). HIGH severity. `story_context.py`, `context_budget.py`, `flex_build.py`, `CLAUDE.build.md`. | forqsite operator observation 2026-06-10 | 2026-06-10 | — | **RESOLVED Phase 65 — INFRA-169, INFRA-170, INFRA-171, INFRA-172, BUILD-027, BUILD-028. Checkpoint cp-65.** |


---

## Do Later

Important, not urgent. Quality improvements, architectural refinements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|
| CER-040 | `context_budget.decide()` fails open silently when `state.json` is absent or contains malformed JSON — same failure shape as the old transcript-path bug, just one layer out. `_read_state()` returns `None` on any I/O or parse error; `decide()` returns `None` (pass-through) immediately. A pairmode project with a corrupted or missing state.json gets an invisible dead gate with no operator signal. Fix options: (a) return `_CONTEXT_CHECK_REQUIRED_MSG` block when `_read_state()` returns `None` (strictest — treats missing state as uninitialized); (b) add a `state_missing` sentinel path that emits a softer warning but still passes through (preserves non-pairmode compat). Non-pairmode projects intentionally have no state.json — fix must not break that case. MEDIUM severity. `skills/pairmode/scripts/context_budget.py` `_read_state()` and `decide()`. | Phase 58 back-check (opus) | 2026-06-03 | 58 | **RESOLVED** Phase 59 INFRA-150 — `_read_state()` now returns `{}` (not `None`) on JSON decode error or non-dict root; non-pairmode compat preserved (file-absent still returns `None`). |
| CER-041 | `context_current_tokens` has no session invalidation. After `/clear`, `state.json` retains the previous session's token value until the orchestrator overwrites it at the first Context gate. If anything spawns a Task tool before the Context gate runs (e.g. an early orchestrator Bash call that happens to be a Task), the hook reads a stale — potentially too-high — value and may block incorrectly. In the happy path this is benign (Context gate runs before any builder spawn per CLAUDE.build.md). Fix: add `context_current_tokens_recorded_at` (ISO timestamp) alongside the value; `read_context_tokens_from_state` treats a value older than N minutes as absent. Alternatively: reset the key in `story_context.py --clear` or a `SessionStart` hook. LOW-MEDIUM severity (mitigated by protocol; only dangerous if Context gate is skipped or reordered). `skills/pairmode/scripts/context_budget.py` `read_context_tokens_from_state`, `skills/pairmode/scripts/flex_build.py` `set-context-tokens`. | Phase 58 back-check (opus) | 2026-06-03 | 58 | **RESOLVED** Phase 59 INFRA-151 — `set-context-tokens` writes `context_current_tokens_recorded_at` (UTC ISO-8601); `read_context_tokens_from_state` treats values older than TTL (default 60 min, configurable) as absent; `clear_current_story()` removes both keys. |
| CER-001 | reconstruct.py `parse_ideology()` compat wrapper uses NamedTemporaryFile round-trip; on SIGKILL leaves ideology.md copy in /tmp. Fix: add `parse_ideology_text(text: str)` to ideology_parser.py to eliminate temp file. reconstruct.py:33-50 | Security audit cp12 | 2026-04-24 | 12 | **RESOLVED** Phase 13 |
| CER-002 | bootstrap.py has no `--yes`/`--no-input` flag; non-interactive callers must use `input="y\n" * N` workaround. Limits CI/scripted use. Add `--yes` flag that auto-confirms all prompts. | Intent review cp13 | 2026-04-25 | 13 | **RESOLVED** Phase 18 BOOTSTRAP-001 |
| CER-003 | cer.py and phase_new.py call Path(project_dir).resolve() but do not apply the len(parts) < 3 suspicious-path depth check that all other pairmode scripts apply. Inconsistent guard discipline. | Security audit cp14 | 2026-04-25 | 14 | **RESOLVED** Phase 17 INFRA-009 |
| CER-004 | lesson_review.py uses str.startswith() for path containment instead of Path.relative_to(); vulnerable to prefix collision on unusual paths. lesson_review.py:149 | Security audit cp14 | 2026-04-25 | 14 | **RESOLVED** Phase 28 INFRA-060 |
| CER-005 | phase_new.py missing len(parts) < 3 depth guard; resolve() is called but shallow-path check absent. All other pairmode entry points apply this guard consistently (bootstrap, audit, sync, score, reconstruct, story_new, era_new). phase_new.py:286 | Security audit cp15 | 2026-04-27 | 15 | **RESOLVED** Phase 17 INFRA-009 |
| CER-006 | validate_story_file rejects empty primary_files list, but story_new.py writes primary_files: [] by default. Every new story file fails its own validator until primary_files is filled in. Either allow empty list for draft stories, or omit the field entirely when empty. schema_validator.py, story_new.py | Intent review cp15 | 2026-04-27 | 15 | **RESOLVED** Phase 17 INFRA-008 |
| CER-007 | era_new.py writes unquoted id field (id: 001 — YAML integer) while bootstrap.py writes quoted id (id: "001" — YAML string). Current regex-based parsers tolerate both, but a proper YAML parser (e.g., python-frontmatter, PyYAML) would return integer 1 from era_new output. Fix era_new.py to write id: "{era_id}" (quoted string). era_new.py | Intent review cp15 | 2026-04-27 | 15 | **RESOLVED** Phase 17 INFRA-008 |
| CER-008 | permission_scope.py: if settings.local.json contains valid non-object JSON (e.g., a bare array), _read_json returns it as-is and the subsequent .setdefault() call raises AttributeError. Add a guard: if the parsed value is not a dict, treat it as {}. permission_scope.py:75 | Security audit cp16 | 2026-04-28 | 16 | **RESOLVED** Phase 17 INFRA-009 |
| CER-009 | hooks/stop.py, post_tool_use.py, session_end.py: PIPE_PATH defaults to hardcoded "/tmp/companion.pipe" and is then conditionally overridden by reading pipe_path from .companion/state.json (relative path). A crafted state.json could redirect pipe writes to an arbitrary path. No secrets in payloads; write silently drops on ENXIO if no FIFO reader. LOW severity. exit_plan_mode.py correctly uses tempfile.gettempdir(). | Security audit cp17 | 2026-04-30 | 17 | **RESOLVED** Phase 28 INFRA-062 |
| CER-010 | story_new.py --rail input is .upper()'d but not validated against a regex before being used in path construction. A caller passing --rail "../../../etc" constructs a path that escapes project_dir. No containment check present. MEDIUM severity. story_new.py:183-185 | Security audit cp18 | 2026-04-30 | 18 | **RESOLVED** Phase 25 INFRA-052 |
| CER-011 | era_new.py _slugify() neutralizes "/" and "." in --name before path construction, providing effective (but informal) traversal prevention. No formal resolve().relative_to() containment check present. LOW severity — _slugify() is correct but not a formal guard. era_new.py:25-31, 114-116 | Security audit cp18 | 2026-04-30 | 18 | **PARTIALLY RESOLVED** Phase 25 INFRA-052 — formal resolve().relative_to() containment check added; _slugify() approach retained alongside guard |
| CER-012 | pairmode_status.py: FLEX_ROOT computed as `Path(__file__).resolve().parent.parent.parent` resolves to `<repo>/skills/`, not the flex repo root. The constructed `start_sidebar.sh` path becomes `<repo>/skills/skills/companion/scripts/start_sidebar.sh` — a file that does not exist. Must be `parent.parent.parent.parent` (four levels up). HIGH severity — surfaced to user as broken instructions. pairmode_status.py:33 | Story review cp20 INFRA-019 | 2026-05-01 | 20 | **RESOLVED** Phase 20 INFRA-020 |
| CER-013 | INFRA-033 propagation gap: fallback-policy pointer was added to flex's own CLAUDE.build.md but NOT to the canonical `skills/pairmode/templates/CLAUDE.build.md.j2` template. Future bootstraps will not inherit the orchestrator-level pointer to the fallback policy (the inline `# fallback:` template comments and architecture.md subsection still propagate via separate paths). LOW severity — flex's own dogfood is correct; downstream effect contained. | Phase 21 intent review | 2026-05-04 | 21 | **RESOLVED** Phase 22 INFRA-041 |
| CER-014 | docs/architecture.md "Reviewer-class agent tool restriction (build-loop safety)" subsection asserts the existence of a "pre-reviewer commit discipline (committing story files and running `git checkout -- lessons/` before the reviewer fires)" as one of two layers protecting the working tree, but neither CLAUDE.build.md (project) nor CLAUDE.build.md.j2 (template) encodes this discipline. Either add the discipline to CLAUDE.build.md.j2 (preserving the architecture claim) or trim the claim (preserving truth). MEDIUM severity — defense-in-depth claim rests on aspirational orchestrator behaviour; the load-bearing tool-restriction layer remains in force regardless. | Phase 21 security audit + intent review | 2026-05-04 | 21 | **RESOLVED** Phase 22 INFRA-042 |
| CER-015 | INFRA-030's CLAUDE.build.md examples hardcode `--phase N --rail RAIL` and `--attempt-number 1` placeholder literals; the orchestrator has no plumbed source for phase/rail at record time and no per-story retry counter state. The cp22 cleanup commit clarified that phase and rail are read from the current story file's frontmatter, but a small helper (e.g. `record_attempt.py --story-file <path>` that auto-extracts phase/rail from frontmatter) would close the typo surface. Without it, retry attempts may be miscounted as fresh attempts and rollup reports may see NULL phase/rail rows. MEDIUM severity. CLAUDE.build.md:82-94. | Phase 22 intent review | 2026-05-05 | 22 | **RESOLVED** Phase 25 INFRA-051 |
| CER-016 | effort_db.py `resolve_effort_db_path` accepts an absolute or relative `effort_db_path` from .companion/state.json and applies only `_depth_guard` (rejects paths with fewer than 3 parts after resolution) — it does not assert containment under project_dir. Same shape mirrored in record_attempt.py and pairmode_effort.py. Strictly weaker guard than permission_scope.py's resolve().relative_to() containment check. State.json is treated as project-owned (consistent with existing trust boundary), so this is informational, not exploitable. LOW severity. effort_db.py:115-139. | Phase 22 security audit | 2026-05-05 | 22 | **RESOLVED** Phase 28 INFRA-058 |
| CER-017 | bootstrap.py `_record_state` auto-enables `effort_tracking: true` on every pairmode-bootstrapped project unless the user explicitly sets the key first. No data leaves the host (sqlite is local), but the behaviour is documented in architecture.md only — not surfaced to the user during bootstrap. Consider an interactive prompt or a one-line bootstrap-summary note. LOW severity (transparency, not security). bootstrap.py:267-268. | Phase 22 security audit | 2026-05-05 | 22 | **RESOLVED** Phase 28 INFRA-061 |
| CER-018 | `lesson.py` CLI (`capture_lesson()` and its argparse entry point) does not accept the `value_framing` or `validation_phase` fields introduced by L012 (Phase 24). These fields exist in `lessons/lessons.json` and are documented in `architecture.md`, but can only be written by direct JSON append — not through the canonical CLI writer. Schema drift: data model ahead of the writer. LOW severity (usable via direct edit; append-only policy and lessons integrity check are unaffected). lesson.py: CLI definition. | Phase 24 intent review | 2026-05-07 | 24 | **RESOLVED** Phase 28 INFRA-059 |
| CER-019 | `pairmode_sync.py` `_get_project_name` returns `state["project_name"]` with only `.strip()` applied before rendering into agent-file frontmatter. A `project_name` containing embedded newlines or YAML control characters could inject extra frontmatter keys (e.g. additional `tools:` or `model:` lines) when `sync-agents` writes agent files. A malicious or malformed `.companion/state.json` could silently elevate reviewer-class tool permissions or override model assignments. Realistic threat is low (state.json is local and user-owned, same trust boundary as bootstrap.py:582), but this is the first script to write project_name into security-sensitive frontmatter fields. Fix: strip `\n`/`\r` from project_name or YAML-quote the value before template substitution. LOW severity. pairmode_sync.py:55-63. | Phase 25 security audit | 2026-05-07 | 25 | **RESOLVED** Phase 28 INFRA-057 |
| CER-020 | `hooks/exit_plan_mode.py` accepts any string from `state.json["pipe_path"]` without validating that it resolves under `tempfile.gettempdir()`. INFRA-062 (CER-009) fixed the same pattern in `stop.py`, `post_tool_use.py`, and `session_end.py`, but `exit_plan_mode.py` was not included. A crafted `state.json` with `pipe_path` pointing outside tempdir could redirect pipe writes. No secrets in payloads; write silently drops on ENXIO. LOW severity — same trust boundary as other hooks; state.json is user-owned. Fix: apply the same `_resolve_pipe_path` pattern as INFRA-062. exit_plan_mode.py:15-18. | Phase 28 security audit | 2026-05-08 | 28 | **RESOLVED** Phase 30 INFRA-068
| CER-022 | `pairmode_sync.py` `sync-agents` subcommand lacks the depth guard present in `sync-build`. `sync-build` applies `_depth_guard_sync_build` (rejects paths with fewer than 3 components) at startup. `sync-agents` calls `Path(project_dir).resolve()` but has no equivalent check, so `--project-dir /` or `--project-dir /tmp` would be accepted. Practical exploitability is low (agents_dir would need to exist at a shallow path), but the inconsistency is an architectural gap. Fix: apply `_depth_guard_sync_build` (or a shared helper) at the top of the `sync-agents` handler. LOW severity. pairmode_sync.py:441. | Phase 33 security audit | 2026-05-17 | 33 | **RESOLVED Phase 38 INFRA-100**
| CER-023 | `.claude/settings.json` PostToolUse hook command contains a hardcoded absolute path (`cd /mnt/work/flex`) making it non-portable across developer machines. The path was retargeted (anchor→flex) in INFRA-088 but the underlying portability problem was explicitly deferred per the phase-35 spec. Fix: replace the hardcoded `cd /mnt/work/flex` with a path derived at runtime from the hook input payload's `cwd` field or the plugin's own location. LOW severity (single-developer fork). `.claude/settings.json` PostToolUse hook command. | Phase 35 INFRA-088 (deferred) | 2026-05-22 | 35 | **RESOLVED Phase 38 INFRA-101**
| CER-024 | `record_attempt.py` stores `--outcome` verbatim; two rows in cora have `outcome = 'pass'` (lowercase) instead of `'PASS'`, excluding them from `outcome = 'PASS'` pass-rate aggregations. Fix: normalise to `.upper()` before inserting. LOW severity (data quality, not security). record_attempt.py:237. | Effort DB analysis 2026-05-22 | 2026-05-23 | — | **RESOLVED Phase 38 INFRA-102**
| CER-025 | PEP 723 inline script metadata was added to `pairmode_sync.py` (commit 8d3101a, Phase 40 window) without a story spec, violating the "spec before build" policy in `~/.claude/CLAUDE.md`. The change is functionally correct (resolves `ModuleNotFoundError: jinja2` when `uv run` is called from a sibling project directory) and has no security surface, but it is unrecorded in the effort DB and invisible to future intent reviewers. LOW severity. pairmode_sync.py:1-4. | Phase 40 intent review | 2026-05-23 | 40 | —
| CER-026 | `checklist_items` field in `pairmode_context.json` is captured at bootstrap (bootstrap.py:806 — "spec-derived only; universal items are hardcoded in templates") and persisted to disk but never consumed by `templates/CLAUDE.md.j2`. The intended design was: universal items hardcoded + spec-derived items rendered via iteration. The iteration was never implemented (or was removed). Result: spec-derived checklist items captured from `.companion/product.json` (visible in forqsite's context with name/description/severity) never reach the reviewer. Quiet correctness gap. Fix: either add `{% for item in checklist_items %}` iteration to `CLAUDE.md.j2` (formal extension point) or remove the field from capture (clarify the contract). MEDIUM severity. bootstrap.py:806, templates/CLAUDE.md.j2. | Phase 47 T3 recon | 2026-05-29 | 47 | —
| CER-028 | BUILD GATE check uses different variables in the two canonical templates: `CLAUDE.md.j2:40` reads `Does \`{{ build_command }}\` pass without errors?` while `reviewer.md.j2:60` reads `Does \`{{ test_command }}\` pass cleanly?`. These resolve to different commands in real projects (forqsite: `build_command = "pnpm build && pnpm typecheck && pnpm test"`, `test_command = "pnpm test"`). Semantic question that needs a decision: should the reviewer's BUILD GATE run the full build (compile + typecheck + test) — making the gate stricter and catching type/compile regressions — or just the tests — making the gate faster and matching what the reviewer actually has time to interpret? Fix: pick one variable, use it in both templates, document the choice in `docs/architecture.md`. MEDIUM severity (correctness of gate semantics, not a hard bug — both invocations pass valid commands, just different ones). CLAUDE.md.j2:40, reviewer.md.j2:60. | Phase 47 T4 recon | 2026-05-29 | 47 | **RETRIAGE: moved from Do Now → Do Later 2026-05-31 (cp-47). Requires semantic decision; not a correctness bug. Neither command is wrong — they're different in scope. Pull forward when the reviewer checklist is next revised for a specific project stack.**
| CER-030 | **Root cause (updated 2026-06-01):** The primary driver is **boundary collapse** — orchestrators embed story-level implementation specs (file paths, acceptance criteria, test instructions) and codebase recon prose directly in the phase doc during plan time. Two failure modes: (a) **embedded specs** — full story specs written inline in the phase file, causing it to balloon to 300–2000 lines; (b) **recon accumulation** — codebase recon prose written while drafting stays in the phase doc permanently and pattern-matches into future phases. The template being thin is a secondary contributing factor. — Original finding: Phase-spec drafting has no canonical "source of truth and beauty" — drafters (LLM or human) read recent phase docs to learn conventions, which causes narrative/structure drift across phases. Diagnostic: the canonical Jinja template `skills/pairmode/templates/docs/phases/phase.md.j2` exists and `phase_new.py` scaffolds from it, but the template is a 27-line stub carrying only title, back/forward links, `## Goal`, empty `## Stories` table, and a `### CP-N Cold-eyes checklist` stub. Every other convention that flex phase docs now use — `## Tracks`, `## Decisions recorded` (D1..DN format), `## Working principles`, `## Resume marker`, `## Open questions`, `## Follow-on scope`, story sub-structure (`#### Requires` / `#### Ensures` / `#### Out of scope` / `#### Instructions` / `#### Tests`), the `Tag: cpNN-slug` line at the bottom, the `## Deferred stories` convention from `~/.claude/CLAUDE.md` "Phase continuity," sub-track conventions (e.g. phase-47 CER-027 sub-track) — has evolved inline and lives only in recent phase docs as exemplars. New scaffolds therefore either use the bare template (and lose every convention) or pattern-match the last few phase docs (recency-bias drift). Observed in this session: phase-48 and phase-49 scaffolds were both pattern-matched from phase-46/47, not driven from a canonical structure spec. Fix shape (design at spec time, not here): (i) **enrich the canonical template** with the mandatory and optional sections, gated by phase-shape flags (`--type simple|complex|placeholder|sub-track`); (ii) **maintain one or two annotated exemplars** in `docs/phases/EXEMPLARS/` (e.g. one complex multi-track phase like phase-47, one placeholder phase like phase-49) showing what good looks like with rationale comments for each section — these are read by both drafters and reviewers; (iii) **add a structural lint** (`scripts/phase_check.py` or a reviewer-checklist item) that verifies a phase doc against the structural spec at checkpoint — Tag line present, story table valid, no naked Deferred stories without Parent phase backlink, etc. Without these three, "spec narrative and style drift" compounds across phases and the reviewer's job gets harder over time (no fixed structural shape to anchor against). MEDIUM severity (methodology integrity over time; not a correctness or security bug). Connection to other CERs: CER-006/CER-007 are upstream cousins (story scaffold mismatches that this same template-stub-vs-evolving-practice gap created at the story layer); CER-029 part 2 design depends on `primary_files`/`touches` accuracy in story specs — if phase-spec drift propagates to story-spec drift, allowlist quality suffers. `skills/pairmode/templates/docs/phases/phase.md.j2`, `skills/pairmode/scripts/phase_new.py`, all of `docs/phases/`. | Operator observation 2026-05-29 | 2026-05-29 | — | **PARTIAL-RESOLUTION Phase 50 BUILD-006/007/008** — root-cause diagnosis updated (boundary collapse is primary; template thinness is secondary); policy gap addressed in CLAUDE.build.md.j2 and phase.md.j2. Template structural enrichment (type flags, exemplars, structural lint) remains open.
| CER-031 | NP-6 ("Source of Truth over Recall") pattern doc (`docs/patterns/agentic-architecture/source-of-truth-over-recall.md`) was drafted in Phase 48 and is catalog-ready, but was held from PR #4 submission because open PR #3 in cloudnirvana/open-patterns ("Canonical Source Over Recall," nsmedia-io, 2026-05-29) covers the same principle in a RAG/vector-retrieval context. Our NP-6 targets agentic development workflows (LLM recall vs. canonical files) and is a genuinely distinct scope, but submitting both while PR #3 is open risks editorial conflict for the catalog maintainer. Trigger: submit NP-6 as a follow-on PR after PR #3 merges or closes. Check PR #3 status: `gh pr view 3 --repo cloudnirvana/open-patterns`. MEDIUM severity (PR #4 ships 5/6 intended patterns; NP-6 is complete and tested, just blocked by external PR timing). `docs/patterns/agentic-architecture/source-of-truth-over-recall.md` | Phase 48 intent review | 2026-06-01 | 48 |
| CER-032 | `CLAUDE.build.md.j2` template omits the four extended `<usage>` block fields (`input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_write_tokens`) and their corresponding `--tokens-in`/`--tokens-out`/`--cache-read-tokens`/`--cache-write-tokens` flags in the `record_attempt.py` example, while the live `CLAUDE.build.md` includes them. Any project bootstrapped or `sync-build`'d from the template will silently miss recording these columns in `effort.db`. LOW severity. `skills/pairmode/templates/CLAUDE.build.md.j2` Step 1 / Step 2. | Phase 52 intent review | 2026-06-02 | 52 | — |
| CER-033 | `builder.md`, `reviewer.md` (and their `.j2` templates) retain the old verbose output blocks (`BUILT: Story [N.X]`, `REVIEW PASS/FAIL — Story [N.X]`) alongside the new `## Final output to orchestrator` `BUILD-RESULT`/`REVIEW-RESULT` blocks. The orchestrator correctly ignores non-structured output now, but a future template reader sees two competing exit conventions with no label marking which is authoritative. Cleanup: remove the legacy verbose blocks from both live agent files and both `.j2` templates. LOW severity (no runtime breakage). `.claude/agents/builder.md`, `.claude/agents/reviewer.md`, and their `.j2` counterparts. | Phase 52 intent review | 2026-06-02 | 52 | **RESOLVED Phase 53 BUILD-019** |
| CER-035 | `docs/architecture.md` line 248 states "`security-auditor` has no `Bash` since it never runs commands" — but BUILD-020 (Phase 53) explicitly added `tools: [Read, Bash, Glob, Grep]` to all four reviewer-class agents including security-auditor (needed for running tests and grepping). The claim is now stale and contradicts the live agent definition. Fix: update architecture.md § Agent definitions to reflect that all reviewer-class agents share `[Read, Bash, Glob, Grep]`. LOW severity (doc accuracy). `docs/architecture.md` line 248. | Phase 53 BUILD-020 reviewer | 2026-06-02 | 53 | **RESOLVED** Phase 53 already updated architecture.md (lines 276-277 document all reviewer-class agents share `[Read, Bash, Glob, Grep]`). Verified at cp-61 checkpoint. |
| CER-034 | `flex_build.py` `_story_path()` constructs `docs/stories/<rail>/<story_id>.md` from the `--story-id` CLI argument with no `Path.resolve().relative_to(stories_root)` containment check. A crafted story ID could escape `docs/stories/`. Blast radius is low (read-only, operator-supplied input from the orchestrator), but inconsistent with the containment discipline applied elsewhere. Fix: add `relative_to` guard after resolving the constructed path. LOW severity. `flex_build.py` `_story_path()`. | Phase 52 security audit | 2026-06-02 | 52 | — |
| CER-042 | `skills/observability/api/src/server.ts` registers `@fastify/cors` with `origin: '*'` (line 35). When the server is bound to the default `127.0.0.1` this is acceptable (browser same-origin restrictions do not apply to loopback, and a local dev tool has no secret to protect). Risk activates if an operator sets `FLEX_OBS_HOST=0.0.0.0` to expose the server on a LAN or remote interface — in that case the wildcard origin allows any website to read all registered repos' context data, effort metrics, and file paths via cross-origin requests. Fix: narrow `origin` to explicit allowed-origins list when `FLEX_OBS_HOST != "127.0.0.1"`, or document the loopback-only constraint in the SKILL.md serve notes and warn to stderr when host override is non-loopback. LOW severity (default config is safe; requires explicit operator override to activate). `skills/observability/api/src/server.ts:35`. | Phase 63 security audit | 2026-06-10 | 63 | — |
| CER-043 | `GET /api/user/memories` and `GET /api/user/policies` return an `abs_path` field containing the absolute filesystem path of each memory/policy file (e.g. `/home/username/.claude/projects/-mnt-work-flex/memory/user_role.md`). This discloses the operator's home directory path, username, and directory structure to any client that can reach the API. In the default loopback-only configuration this is low-risk (only the local user can reach the endpoint). Risk activates when the server is exposed beyond loopback. Fix: either omit `abs_path` from the response (the UI can reconstruct relative paths) or gate it behind a separate `?include_path=true` query parameter. LOW severity (loopback default; field is primarily a dev-convenience field). `skills/observability/api/src/routes/user.ts:15,37,126,160`. | Phase 63 security audit | 2026-06-10 | 63 | — |
| CER-044 | `skills/observability/api/src/parsers/phaseIndex.ts:44–46` extracts the `href` from a Markdown link in `index.md` and constructs a filesystem path as `docs/phases/${href}` without any `Path.resolve().relative_to()` containment check (TypeScript equivalent: `path.resolve()` + check that the resolved path starts with the project root). A crafted `index.md` with a link like `[Phase](../../../../etc/passwd)` would cause the system endpoint to attempt to read an arbitrary file outside the project directory. In production the operator controls `index.md` so practical exploitability is low, but the pattern is inconsistent with the containment discipline applied in the Python scripts. Fix: after constructing the candidate path, verify it resolves under `projectDir` before reading. LOW severity (attacker must control `index.md` which is project-owned). `skills/observability/api/src/parsers/phaseIndex.ts:44-46`. | Phase 63 security audit | 2026-06-10 | 63 | — |
| CER-029 | Permission-prompt friction during builder turns is the methodology's "achilles heel" — auto mode gets dropped multiple times per phase to approve file writes that operator-intuition says should already be in-scope. Two-part investigation needed before fix is designed: **(part 1, triage)** instrument or capture-by-hand the actual prompt-fires across the next 2-3 builder turns and classify each: (a) target file IS in the active story's `primary_files`+`touches` but `.claude/settings.json`/`settings.local.json` denylist or static rules still gate it (→ legacy cruft, expunge); (b) target file is NOT in declared scope but is legitimately needed (→ spec quality issue, story scoping is wrong); (c) target file is in CLAUDE.md "Protected files" list (sidebar.py, plugin.json, hooks/, etc.) (→ Layer-2 LLM-attention rule, structural cousin of CER-027 — same mechanical-enforcement template applies); (d) target is a true breakout-containment target (.git/, .claude/settings.json itself) (→ correct firing, keep). **(part 2, design — only if part 1 surfaces real signal in buckets a or b)** spec-derived dynamic permission scoping: pre-build hook at builder-turn start reads the active story's `primary_files`+`touches`, writes them to `.claude/settings.local.json` as a story-scoped allowlist, marks the allowlist immutable for the duration of the builder turn (cannot be expanded mid-build — preserves "scope is the spec" principle), teardown hook at checkpoint revokes. Static `.claude/settings.json` shrinks to the catastrophic denylist only (.git/, settings.json itself, plugin.json, hooks/ when not the active rail) — pure breakout containment. Reviewer's RAIL SCOPE check #9 remains as post-hoc verifier; redundant with the allowlist for the common case but load-bearing if the dynamic allowlist ever fails to apply. MEDIUM severity (methodology friction + load-bearing-rule clarity; not a correctness or security bug). Open design questions for the eventual phase: (i) where the immutability lock lives (file attribute, in-memory pid-locked, separate state); (ii) concurrency with operator manual edits to settings.local.json during build (likely: detect and abort builder turn); (iii) whether CLAUDE.md "Protected files" Layer-2 folds into the same mechanism or stays separate. Cross-reference: catalog pattern `per-agent-data-access-control` (cloudnirvana/open-patterns) describes the same shape applied to DB access — the file-system equivalent is what this CER proposes. Phase 47 D11 also applies: a new write path to `settings.local.json` requires explicit concurrency-model documentation when specced. `.claude/settings.json`, `.claude/settings.local.json`, CLAUDE.md Protected files list, story file `primary_files`+`touches`. | Operator observation 2026-05-29 | 2026-05-29 | — | —


---

## Do Much Later

Not urgent, marginal value. Style, cosmetics, speculative improvements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|


| — | *(none)* | — | — | — |


---

## Do Never

Rejected findings. Record the rejection reason so it is not re-raised.

| ID | Finding | Source | Date | Phase | Resolution |
|----|---------|--------|------|-------|------------|


| — | *(none)* | — | — | — | — |

