---
id: OBS-003
rail: OBS
title: D1 display half — `expected_step_tokens` provenance (CER-053)
status: complete
phase: "HARNESS007-main"
story_class: code
auth_gated: false
schema_introduces: false
primary_files:
  - skills/observability/api/src/routes/context.ts
touches:
  - skills/observability/ui/src/App.tsx
---

## Context

The display half of D1/CER-053 (agreements `HARNESS007-main.md` DP3). HARNESS006 (HARNESS-003)
re-sourced `expected_step_tokens` off effort.db on the **state** side; this story fixes the
**SPA display**: the context-budget panel must read the re-sourced value, label its provenance
("thin-harness return-block growth," not effort-median), and drop any assumption that `53000`
is a fixed per-project constant. The INFRA-165 `render_alert_prompt` ceiling fix (flex_factor)
is folded in here since it shares the surface.

## Requires

- HARNESS-003 complete: `expected_step_tokens` is re-sourced (no effort-derived value in the
  gate/state).

## Ensures

- The context route (`context.ts`) / state reader surface `expected_step_tokens` from the
  re-sourced value with a **provenance label** ("thin-harness return-block growth"); the UI
  shows the label, not a bare 53000 assumption.
- The INFRA-165 `render_alert_prompt` ceiling fix is folded: when `flex_factor != 1.0`, the
  displayed `[R]` remaining figure is factored correctly (per `phase-64.md` D2). (The NaN-clamp
  half stays INFRA-165; this story takes only the shared ceiling-display fix to avoid
  double-editing the panel.)
- No effort-derived value appears in the context-budget panel (asserted).
- The API builds/typechecks; a route test asserts the provenance label + factored remaining
  figure.
- `PATH=$HOME/.local/bin:$PATH uv run pytest tests/pairmode/ -x -q` passes.

## Instructions

- Read the re-sourced constant (HARNESS-003's single source); do not reintroduce an effort read
  in the SPA.
- Coordinate with INFRA-165 to avoid overlap — this story owns the **display** (provenance label
  + the shared ceiling figure); INFRA-165 owns the **NaN clamp** in `decide()`. If sequencing
  makes the ceiling fix land in INFRA-165, reference it instead of duplicating.

## Tests

```bash
PATH=$HOME/.local/bin:$PATH uv run pytest tests/pairmode/ -x -q
```

Acceptance: panel shows the re-sourced value + provenance label; remaining figure factored; no
effort-derived value in the panel; builds + suite green. Closes CER-053 display half.

### Out of scope

- The state/gate re-source — HARNESS-003.
- The `context_budget.py` NaN clamp — INFRA-165.
- D2/D3 — OBS-004/005.
