{% set _phase_class = phase_class | default(None) -%}
{% set _phase_key = phase_key | default(phase_id) -%}
{% if era_id or _phase_class -%}
---
{% if era_id -%}
era: "{{ era_id }}"
{% endif -%}
{% if _phase_class -%}
phase_class: {{ _phase_class }}
{% endif -%}
---

{% endif -%}
# {{ project_name }} — Phase {{ _phase_key }}: {{ phase_title }}

{% if prev_phase or next_phase -%}
{% if prev_phase -%}
← [Phase {{ prev_phase.id }}: {{ prev_phase.title }}](phase-{{ prev_phase.id }}.md)
{%- endif -%}
{% if prev_phase and next_phase %} | {% endif -%}
{% if next_phase -%}
[Phase {{ next_phase.id }}: {{ next_phase.title }}](phase-{{ next_phase.id }}.md) →
{%- endif %}

{% endif -%}
<!-- Phase doc = planning surface only. Story-level detail (acceptance criteria,
     file paths, implementation guidance, test instructions, codebase recon)
     belongs in docs/stories/<RAIL>/<ID>.md — not here. -->
## Goal

{{ goal if goal else "— fill in the phase goal —" }}

## Stories

| ID | Title | Status |
|----|-------|--------|

## Schema delivery

For each new persistent schema object (table, collection, migration) introduced in
this phase, record the management surface before the phase is checkpointed.

| Object | Management surface | Exception |
|---|---|---|
| | | |

---

### CP-{{ _phase_key }} Cold-eyes checklist

— developer fills in after phase completion —
