[project]
name = "anchor"
version = "0.1.0"
description = "Claude Code plugin for persistent architectural memory"
readme = "readme.md"
requires-python = ">=3.11"
dependencies = [
    "jinja2",
    "click",
]

[dependency-groups]
dev = [
    "pytest",
]
