{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://flex.halfhorse.com/schemas/next_action/v2",
  "title": "NextAction",
  "description": "Versioned action grammar emitted by the next-action resolver (Era 003). Schema version 2 (WORKER-004 adds spawn-reviewer, spawn-security-auditor, spawn-intent-reviewer).",
  "type": "object",
  "required": ["action", "scalar", "model", "reason", "meta"],
  "additionalProperties": false,
  "properties": {
    "action": {
      "description": "The action the resolver recommends. Closed set for HARNESS001–HARNESS005; open-ended by design — later phases add values without breaking consumers.",
      "type": "string",
      "enum": [
        "spawn-builder",
        "spawn-loop-breaker",
        "spawn-gate-worker",
        "spawn-reviewer",
        "spawn-security-auditor",
        "spawn-intent-reviewer",
        "checkpoint",
        "await-user",
        "done"
      ]
    },
    "scalar": {
      "description": "Action-specific payload string (e.g. story ID for spawn actions, empty string for done/await-user).",
      "type": "string"
    },
    "model": {
      "description": "Model override for auto-resolved spawn actions only. Null for checkpoint, await-user, done, and spawn-gate-worker.",
      "oneOf": [
        {"type": "string"},
        {"type": "null"}
      ]
    },
    "reason": {
      "description": "Human-readable explanation of why this action was chosen.",
      "type": "string"
    },
    "meta": {
      "description": "Structured metadata. Always carries schema_version. All other fields are optional.",
      "type": "object",
      "required": ["schema_version"],
      "properties": {
        "schema_version": {
          "description": "Integer version of this action grammar. Must equal 2 for Era 003 HARNESS003+.",
          "type": "integer",
          "const": 2
        },
        "attempt": {
          "description": "Builder attempt number (1-based).",
          "type": "integer",
          "minimum": 1
        },
        "gate": {
          "description": "Gate name that caused a block (e.g. 'context-budget', 'auth-gate').",
          "type": "string"
        },
        "fail_rung": {
          "description": "Loop-breaker failure rung label.",
          "type": "string"
        },
        "warnings": {
          "description": "Advisory messages for the orchestrator. Non-blocking.",
          "type": "array",
          "items": {"type": "string"}
        }
      },
      "additionalProperties": true
    }
  }
}
