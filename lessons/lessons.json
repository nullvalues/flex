{
  "version": "1.0.0",
  "lessons": [
    {
      "id": "L001",
      "date": "2026-04-20",
      "source_project": "flex",
      "trigger": "Ran audit against cora \u2014 a project with pairmode scaffold but no pairmode_context.json",
      "problem": "44 INCONSISTENT findings for every section body because fallback context renders all template variables as empty strings. The output is confusing \u2014 it looks like everything needs repair, when really the project just needs to be bootstrapped with pairmode to generate the context file.",
      "learning": "When audit detects no pairmode_context.json, it should emit a prominent warning: 'No pairmode_context.json found \u2014 template body comparison will show false INCONSISTENT for all variable-bearing sections. Bootstrap this project with /flex:pairmode to fix.' MISSING and EXTRA findings remain reliable; INCONSISTENT findings require a context file to be meaningful.",
      "methodology_change": {
        "affects": [
          "audit.py"
        ],
        "description": "Add a warning in audit output when pairmode_context.json is missing. Ideally suppress INCONSISTENT findings entirely when no context file exists, or clearly label them as 'body comparison unreliable \u2014 no context file'."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "lint"
    },
    {
      "id": "L002",
      "date": "2026-04-20",
      "source_project": "flex",
      "trigger": "Ran audit against radar and forqsite \u2014 projects with pairmode scaffold",
      "problem": "INCONSISTENT findings include entries with section keys like '---' and '---__0'. These are separator lines (_split_sections treats '---' as a section delimiter and the text after it as the section body). Users see 'Section --- content differs from canonical template' and cannot tell what content is being compared or where to look in the file.",
      "learning": "The _split_sections output uses '---' as section keys in audit output. These entries are hard to interpret. Either: (a) skip separator-keyed sections from INCONSISTENT comparison since --- separators carry no semantic content, or (b) display the surrounding context (what section comes before/after) when reporting --- inconsistencies.",
      "methodology_change": {
        "affects": [
          "audit.py"
        ],
        "description": "In audit.py _split_sections or the INCONSISTENT loop, skip or relabel entries whose section key starts with '---' to reduce noise in audit output."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "lint"
    },
    {
      "id": "L003",
      "date": "2026-04-21",
      "source_project": "flex",
      "trigger": "Bootstrap dogfood on flex \u2014 re-running after bug fixes",
      "problem": "Bootstrap overwrote hand-authored .claude/agents/ files with template output that was strictly worse: truncated checklist names, empty layer-rule sections, build_command interpolated into prose.",
      "learning": "For mature projects, templates are a starting point, not a replacement. Files that already exist should be skipped by default (same as CLAUDE.md/CLAUDE.build.md), or bootstrap should warn before overwriting hand-authored content.",
      "methodology_change": {
        "affects": [
          "orchestrator"
        ],
        "description": "Bootstrap should skip existing agent files by default. Add a --force-agents flag for intentional overwrites. Document in SKILL.md that agent files are treated as project-owned after first bootstrap."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "lint"
    },
    {
      "id": "L004",
      "date": "2026-04-21",
      "source_project": "flex",
      "trigger": "Bootstrap dogfood \u2014 builder.md Python standards section",
      "problem": "builder.md template uses {{ build_command }} inside the 'All Python execution uses ...' prose, producing 'All Python execution uses PATH=$HOME/.local/bin:$PATH uv run pytest tests/pairmode/'. The build command is not the Python runtime.",
      "learning": "Template variables should be scoped to their semantic slot. build_command belongs in the build-gate section only; the Python standards section should hardcode 'uv run'.",
      "methodology_change": {
        "affects": [
          "builder_agent"
        ],
        "description": "Fix builder.md.j2: replace '{{ build_command }}' in the Python standards prose with the literal string 'uv run'. Use build_command only in the build-gate verification block."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "hook"
    },
    {
      "id": "L005",
      "date": "2026-04-21",
      "source_project": "flex",
      "trigger": "Bootstrap dogfood \u2014 reviewer.md checklist from spec non-negotiables",
      "problem": "checklist_deriver produces items whose 'name' field is the first N chars of the non-negotiable text, truncated mid-sentence. reviewer.md became a 595-line wall of truncated constraint prose instead of a human-readable checklist.",
      "learning": "Non-negotiables are constraints, not checklist item names. The reviewer checklist needs human-authored label + short action question, not raw spec text. checklist_deriver should either produce module-scoped labels (e.g. 'companion-skill: spec write isolation') or not populate the reviewer checklist at all \u2014 leaving that to the human.",
      "methodology_change": {
        "affects": [
          "reviewer_checklist"
        ],
        "description": "Remove spec-derived checklist_items from the reviewer.md template, or change the template to use module+ordinal labels. The reviewer checklist should be maintained by the developer, seeded with universal items only."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "hook"
    },
    {
      "id": "L006",
      "date": "2026-04-21",
      "source_project": "flex",
      "trigger": "Dogfood audit run on flex after clean bootstrap",
      "problem": "Audit reports INCONSISTENT for every hand-authored section that is richer than the template. It cannot distinguish 'unintentional drift' from 'intentional override with better content'. Result: a mature project sees constant audit noise it can never resolve without degrading its docs.",
      "learning": "Audit needs a way to mark sections as intentionally overridden. Without that signal, any project that customises its scaffold will permanently live in a noisy INCONSISTENT state, eroding trust in the tool.",
      "methodology_change": {
        "affects": [
          "orchestrator"
        ],
        "description": "Add a per-section override marker (e.g. a comment tag like '{# PROJECT_OVERRIDE #}') that audit treats as EXTRA rather than INCONSISTENT. Alternatively, add a .pairmode-overrides file listing section keys the project owns. Either approach lets audit surface real drift without nagging about deliberate customisation."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "lint"
    },
    {
      "id": "L007",
      "date": "2026-04-21",
      "source_project": "flex",
      "trigger": "Dogfood pairmode on multiple projects simultaneously \u2014 flex + ud running at same time",
      "problem": "All hook scripts wrote to /tmp/companion.pipe, and pgrep -f sidebar.py checked for any sidebar process. With two projects running, all events from both projects merged into one sidebar window. The wrong project's spec was shown for every event.",
      "learning": "The pipe path must be scoped to the project directory. Each project gets its own pipe derived from an 8-char md5 hash of the project dir. The sidebar reads its own pipe only. Hooks read the pipe_path from .companion/state.json at startup.",
      "methodology_change": {
        "affects": [
          "hooks",
          "sidebar",
          "bootstrap"
        ],
        "description": "Per-project pipe isolation: compute PIPE_PATH = /tmp/companion-<md5hash>.pipe from project dir. Hooks read pipe_path from .companion/state.json (with /tmp/companion.pipe fallback). Sidebar writes pipe_path to state.json at startup. start_sidebar.sh passes --project-dir and uses per-project /tmp/flex_project_dir_<hash> temp file."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "hook"
    },
    {
      "id": "L008",
      "date": "2026-04-24",
      "source_project": "radar",
      "trigger": "Phase 21 spec written, CER agent run on spec before build, found 20 issues including compile-blockers",
      "problem": "Spec had multiple errors that would have caused the builder to produce uncompilable code or silently wrong behaviour: wrong call site argument in updateTask (data vs existing), missing explicit createRail body pass-through, reorderRail cross-context corruption, missing ApiRail type update in adapters.ts, stale contextRailsById race condition.",
      "learning": "Running a cold-eyes review on the phase spec itself (not just on built code) catches architectural and correctness errors before any builder time is spent. The CER agent reading actual source files alongside the spec finds mismatches the spec author missed. This is more valuable than a post-build reviewer alone.",
      "methodology_change": {
        "affects": [
          "orchestrator",
          "reviewer_checklist"
        ],
        "description": "Add a spec-CER step to the build orchestrator sequence: after writing a phase spec and before saying Build Phase N, spawn a security-auditor (or general-purpose) agent to cold-eyes review the spec against the actual codebase, then incorporate findings before building. Document this as a standard checkpoint in CLAUDE.build.md."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "skill"
    },
    {
      "id": "L009",
      "date": "2026-05-04",
      "source_project": "flex",
      "trigger": "Cross-project audit (cora, radar, forqsite) of .claude/agents/ configurations",
      "problem": "Pairmode templates pinned only the builder to sonnet, leaving reviewer-class agents to inherit the orchestrator's model. If the orchestrator ran on sonnet (efficiency reasons), the entire quality gate ran on sonnet, losing the judgment-quality benefit.",
      "learning": "Model selection should be explicit per role, not inherited. Volume work (builder) -> sonnet for compute efficiency. Judgment work (reviewer, intent-reviewer, loop-breaker, security-auditor) -> opus for judgment quality. Inheritance from the orchestrator is a silent capability leak. Add a documented fallback policy: if the preferred model is rate-limited, fall back one tier (Opus -> Sonnet on reviewers; Sonnet -> Haiku on builder), never below Haiku.",
      "methodology_change": {
        "affects": [
          "pairmode-builder-reviewer-loop"
        ],
        "description": "Pairmode templates pin model per agent: builder=sonnet, reviewers=opus, with fallback comments. INFRA-026, INFRA-033 implement this in templates; future bootstraps inherit it. Validation comes from Phase 22's pairmode_effort.py models report -- once token-and-PASS-rate data accrues per (model, role), the methodology is data-defensible rather than aesthetic."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "hook"
    },
    {
      "id": "L010",
      "date": "2026-05-04",
      "source_project": "flex",
      "trigger": "Forqsite restricted reviewer tools to [Read, Grep, Glob, Bash]; cora and radar did not. Cross-project audit surfaced the divergence.",
      "problem": "Reviewers with full tool access can \"fix\" failing code (edit a test until it passes, edit production until the test passes) instead of reverting, hiding the real failure. Concrete example: a reviewer faced with a failing assertion can edit the assertion to match the (wrong) actual output and commit, presenting a green test that no longer tests the invariant. This compromises the commit-or-revert contract.",
      "learning": "Reviewer-class agents should be limited to read-only tools plus Bash. Bash preserves the commit-or-revert capability via git; Edit/Write removal closes the \"reviewer backdoor\" failure mode. This is layered with the orchestrator's pre-reviewer commit discipline (which protects against accidental erasure of uncommitted methodology files) \u2014 neither layer alone is sufficient.",
      "methodology_change": {
        "affects": [
          "pairmode-builder-reviewer-loop"
        ],
        "description": "Pairmode templates restrict reviewer tools. INFRA-027 implements this."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "hook"
    },
    {
      "id": "L011",
      "date": "2026-05-05",
      "source_project": "flex",
      "trigger": "User observed total opus:sonnet usage running at roughly 3:2, exceeding the Opus quota relative to the Sonnet quota. Methodology had Phase 21 baseline of \"reviewer-class agents -> opus, builder -> sonnet\" applied uniformly across all reviews.",
      "problem": "Treating every review as judgment work overcommits opus on routine cases. Most reviewer passes catch nothing (most builders produce correct work). Per-story reviewer work is mechanical (diff matches spec, tests pass, checklist OK, commit) and within Sonnet's capability. Intent-reviewer and security-auditor at routine checkpoints are similarly mechanical. The result: opus consumption disproportionate to the value it adds, leaving Sonnet quota underutilised.",
      "learning": "Model selection should be sonnet baseline, opus on demand, not the inverse. Reserve opus for explicit upgrade triggers where the judgment edge actually matters: story retries (sonnet missed it the first time), pre-PR audits (last cold-eyes before code leaves the repo), mid-phase spec pivots (the spec itself moved), and production-code phases for security-auditor. Loop-breaker stays opus permanently because by the time it fires the case is by definition hard.",
      "methodology_change": {
        "affects": [
          "pairmode-builder-reviewer-loop"
        ],
        "description": "Pairmode templates flip reviewer / intent-reviewer / security-auditor defaults from opus to sonnet, with inline upgrade comments documenting the triggers. INFRA-044 implements this in templates. Phase 24 refines the upgrade triggers into data-defensible per-story-class rules once Phase 22's effort tracking has produced enough data to validate the rebalance with actual token-and-PASS-rate per (model, role) numbers."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "hook"
    },
    {
      "id": "L012",
      "date": "2026-05-07",
      "source_project": "flex",
      "trigger": "Phase 23 INFRA-044 / LESSON-004 documented model upgrade triggers in prose. Phase 24 made them structural and data-defensible, adding per-story model evaluation (INFRA-050) and an efficiency-ratio report (INFRA-049) to close the feedback loop.",
      "problem": "Methodology decisions in prose are unauditable in either direction. \"Most reviews catch nothing\" was either true or false \u2014 no way to know without measurement. Without measurement, the next person to question the methodology has only the same intuitions to argue with. The same applies to cost: \"haiku is fine for doc stories\" is a claim that needs a retry-rate check to validate, not just intuition.",
      "learning": "A methodology lifecycle worth codifying: (1) Ship the change under intuition. (2) Capture the rationale as a lesson. (3) Instrument the relevant signal. (4) Wait for data to accrue (\u2265 2 phases). (5) Validate the methodology against the data. (6) Formalize, refine, or reverse based on findings. The goal is not minimum cost (that sacrifices quality and causes rework) and not maximum intelligence (that wastes budget on trivial work). It is best outcome per token \u2014 optimising the efficiency ratio: PASS rate / cost. This framing is stable even as model prices and capabilities shift; the thresholds in the decision table are the thing that changes, not the objective.",
      "value_framing": "Efficiency ratio: (pass_rate / avg_cost) / (baseline_pass_rate / baseline_avg_cost). A ratio > 1.0 means the path delivers more PASS-rate per token than the baseline. The efficiency ratio is the durable metric: as models evolve and prices change, re-run the report; if the ratio for a decision path shifts, update the decision table thresholds in a new story.",
      "methodology_change": {
        "affects": [
          "pairmode-methodology-evolution"
        ],
        "description": "Pairmode adopts this lifecycle for any future intuition-driven methodology changes. INFRA-045 through INFRA-050 implement the structural changes and instrumentation. The lesson template gets a new optional `validation_phase` field pointing at the phase that confirms or revises the lesson, enabling traceable methodology evolution."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "validation_phase": "24",
      "enforced_by": "none"
    },
    {
      "id": "L013",
      "date": "2026-05-07",
      "source_project": "flex",
      "trigger": "Phase 24 session start revealed that flex's own `.claude/agents/` files had no `model:` frontmatter, and forqsite/radar still carried pre-INFRA-044 opus reviewer assignments \u2014 despite INFRA-044 having updated the templates.",
      "problem": "Pairmode templates and bootstrapped project files diverge silently after any template update. There is no mechanism to propagate changes to existing projects short of manual edits or a full re-bootstrap. The drift is invisible until a session starts and the wrong model is used.",
      "learning": "Two complementary patterns close the gap: (1) a sync command that re-renders template frontmatter into existing agent files on demand; (2) a note in the methodology that any template change affecting agent behaviour should be followed by a `pairmode sync-agents` run on all active projects. The sync command is idempotent \u2014 running it twice produces no further changes.",
      "value_framing": "Template updates that affect build behaviour (model selection, tool restrictions, upgrade triggers) must propagate to existing projects or the update has only partial effect. The effort spent tuning templates is wasted if projects accumulate stale copies.",
      "methodology_change": {
        "affects": [
          "pairmode-template-propagation",
          "cross-project consistency"
        ],
        "description": "After any pairmode template update that changes agent frontmatter (model assignments, tool lists, upgrade triggers), run `pairmode sync-agents --project-dir <each active project>` before starting the next build session on that project."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "validation_phase": "25",
      "enforced_by": "hook"
    },
    {
      "id": "L014",
      "date": "2026-05-13",
      "source_project": "flex",
      "trigger": "INFRA-077 required updating lessons.json status fields and regenerating LESSONS.md. The builder ran lesson_utils.py as a script and assumed it worked because exit code was 0.",
      "problem": "lesson_utils.py has no __main__ block \u2014 running it as a script exits 0 silently and does nothing. The builder used `echo \"LESSONS.md regenerated\"` after `&&`, which printed the message regardless, masking the no-op. Separately, json.dump with ensure_ascii=False expands \u2014 escape sequences to literal UTF-8 bytes, making the round-tripped JSON appear to mutate non-status content fields \u2014 triggering the lessons integrity guard on reviewer #1.",
      "learning": "When a utility module has no __main__ block, running it as a script is a no-op. To call generate_lessons_md and write LESSONS.md: import the function directly and write the output in the same Python invocation. Always use json.dumps(..., ensure_ascii=True) when writing lessons.json to preserve existing \\uXXXX escape sequences byte-for-byte. Never confirm success with a shell echo after &&; verify by reading the output file.",
      "methodology_change": {
        "affects": [
          "lesson_utils.py",
          "CLAUDE.build.md"
        ],
        "description": "When build instructions involve updating lessons.json: do not run lesson_utils.py as a script (it has no entry point). Instead: import generate_lessons_md and write LESSONS.md directly in the same Python invocation. Use ensure_ascii=True when serialising lessons.json to avoid spurious diffs from Unicode normalisation."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "none"
    },
    {
      "id": "L015",
      "date": "2026-05-18",
      "source_project": "forqsite",
      "trigger": "External CER on forqsite 2026-05-18 surfaced multiple doc/code mismatches accumulated over many phases: architecture.md claimed 58 role_permissions rows but actual seed produces 56 (migration 0050 was tightened by 0052 with no doc update); three additional backlog items pointed to docs that no longer matched code. None caught by per-story or checkpoint reviews. Pairmode's existing DOCUMENTATION CURRENCY check in reviewer.md.j2 covers only README.md; checkpoint Documentation review covers only README + brief.",
      "problem": "The per-story doc check has too narrow a scope. Any documentation that describes a code area \u2014 architecture, configuration runbooks, design docs, operator guides, schema docs, anything in docs/ \u2014 should travel with the code that the story touches. Today those docs decay silently until an external review catches them, by which point original intent is lost and the doc actively misleads any future agent reading it cold.",
      "learning": "Documentation reliability across builds is what preserves project context across sessions and compactions. The reviewer's doc check should expand from README-only to any non-history doc in docs/ whose content references code the story touched. The fix is builder-remediable inline: the builder updates the relevant doc as part of the same story commit, not a doc-rebuild phase. The check is approximate but cheap \u2014 grep the doc surface for references to changed files/symbols, flag candidates, let the reviewer judge. Severity HIGH when the doc statement is now factually wrong, MEDIUM for missing README user-facing change.",
      "methodology_change": {
        "affects": [
          "reviewer_checklist",
          "orchestrator",
          "checkpoint_sequence"
        ],
        "description": "Expand the DOCUMENTATION CURRENCY check in agents/reviewer.md.j2 from README-only to the project's documentation surface \u2014 every *.md under docs/ excluding append-only history paths (docs/phases, docs/stories, docs/cer, docs/eras), plus README.md. Reviewer reads each surface doc, identifies references to files in the diff, flags HIGH when a touched code area is described in a doc the diff did not update. Builder remediates inline (no phase rebuild). Mirror the expansion in CLAUDE.build.md.j2 checkpoint Documentation review step. Add a brief mirror to CLAUDE.md.j2 review checklist. Allow projects to override default discovery via optional docs/documentation-surface.md manifest. Standardize the discovery command as a single bash form so projects can pre-authorize it without permission prompts."
      },
      "applies_to": [
        "all"
      ],
      "status": "applied",
      "enforced_by": "skill"
    },
    {
      "id": "L016",
      "date": "2026-05-18",
      "source_project": "flex",
      "trigger": "Reviewing L005 marker hygiene during forqsite session 2026-05-18. Found that L005's marker in CLAUDE.md.j2 points at the wrong file \u2014 the actual implementation correctly landed in agents/reviewer.md.j2. Also realized L015 was implemented directly (template edits written outside the /flex:pairmode review flow), leaving its status as 'captured' despite the change being live in the canonical templates.",
      "problem": "Two related gaps in lesson_review.py: (1) markers are append-only \u2014 they are never removed or transformed when status flips to 'applied', so stale markers accumulate in templates and may trick future maintainers into re-implementing already-applied changes; (2) affects\u2192template file mapping is too coarse \u2014 'reviewer_checklist' maps only to CLAUDE.md.j2, but the actual implementation of review-flow changes often belongs in agents/reviewer.md.j2, so the marker lands in the wrong file. Plus a missing affordance: no flag for 'I implemented this directly, just mark it applied', forcing either redundant markers (via --approve) or manual JSON edits.",
      "learning": "Markers are 'pending work' signals and must be lifecycle-managed. When a lesson flips to 'applied' the marker should either be removed entirely or transformed into a brief breadcrumb ({# LESSON LNNN APPLIED YYYY-MM-DD in <file> #}). Affects keys should be granular enough that markers land near where the actual change is most likely to happen. And there must be a clean path to declare a lesson applied when the change was implemented directly without going through review's annotation step.",
      "methodology_change": {
        "affects": [
          "tooling"
        ],
        "description": "1) Add lesson_review.py --mark-applied LESSON_ID flag that flips status to 'applied' without writing markers, for cases where the change was implemented directly. 2) When --approve flips status to 'applied', extend apply_template_change to handle marker lifecycle \u2014 either remove the marker on a later --mark-applied call, or write the marker in 'APPLIED' form from the start when a date is supplied. 3) Expand the affects\u2192file map: split 'reviewer_checklist' into 'reviewer_checklist' (CLAUDE.md.j2 review-mode section, the brief checklist) and 'reviewer_agent' (agents/reviewer.md.j2, the full review process). Audit builder/orchestrator/checkpoint mappings for similar conflations. 4) Optionally: when --approve is run, prompt for the implementation file path so the marker lands where the change will actually live, rather than always defaulting to the affects-map target."
      },
      "applies_to": [
        "pairmode"
      ],
      "status": "captured",
      "enforced_by": "none"
    },
    {
      "id": "L017",
      "date": "2026-05-26",
      "source_project": "PokusFokus",
      "trigger": "Reviewer FAIL-revert wiped the operator's uncommitted, git-tracked .claude/settings.json permission change mid-build, re-blocking all subsequent stories.",
      "problem": "The orchestrator spawned the reviewer while an operator-authorized deny->allow change sat uncommitted in the working tree. The reviewer's revert-on-FAIL (git reset --hard / checkout) restored tracked .claude/settings.json to HEAD, erasing the authorized relaxation. The reviewer also (correctly, blind to intent) flagged the unexplained protected-file unlock as two HIGH/MEDIUM findings unrelated to the builder's actual code, conflating operator permission bookkeeping with builder output.",
      "learning": "A git-tracked permission-config file is part of the working tree the reviewer reverts. Authorized changes to it must be committed BEFORE the reviewer fires (the same protection Step 1.5 gives methodology files), or they are silently destroyed on any FAIL. Two corollaries: (1) maintain an EXPLICIT allow block for the files in active scope rather than relying on absence-from-deny ('implicit approval'), so authorizations are legible in the diff and reviewable; (2) when the harness self-modification guard bars the agent from editing a permission file, write the proposed valid file to a tmp path for the operator to mv into place instead of abandoning the change.",
      "methodology_change": {
        "affects": [
          "build-loop",
          "reviewer"
        ],
        "description": "Extend CLAUDE.build.md Step 1.5 ('Commit pending methodology files before the reviewer fires') to explicitly add tracked permission config to the pre-reviewer commit set (git add .claude/settings.json) whenever it carries authorized changes. Add reviewer guidance to distinguish operator-made permission-config changes from builder output rather than auto-attributing settings.json deltas to the builder. Recommend an explicit allow block per active story scope, and a tmp-file + operator-mv handoff for permission edits the agent cannot write directly."
      },
      "applies_to": [
        "all"
      ],
      "status": "captured",
      "validation_phase": "2",
      "enforced_by": "none"
    },
    {
      "id": "L018",
      "date": "2026-06-29",
      "source_project": "meander",
      "trigger": "Orchestrator post-build note from meander INFRA-008: the pre-reviewer git add docs/phases/ swept up a story deliverable whose primary_file lived under docs/phases/, committing it unreviewed under the chore(orchestrator) message.",
      "problem": "The blanket 'git add docs/phases/ docs/cer/' in the pre-reviewer step is intentionally broad. But when a story's declared primary_file is also under docs/phases/ \u2014 common for doc and spec stories \u2014 the blanket add silently commits that deliverable before the reviewer fires. The reviewer then diffs an empty working tree, cannot verify the story output, and the commit lands under the wrong message and without review.",
      "learning": "The pre-reviewer git add must exclude files declared as primary_files (and touches) in the active story's spec. Correct pattern: (1) blanket 'git add docs/phases/ docs/cer/', then (2) 'git reset HEAD -- <file>' for each primary_file/touch that falls under a staged directory. This preserves the reviewer's ability to diff the story deliverable while still protecting methodology files. Applies any time a story's deliverable lives under a directory covered by the blanket add.",
      "methodology_change": {
        "affects": [
          "orchestrator",
          "build-loop"
        ],
        "description": "In CLAUDE.build.md Step 1.5: after the blanket git add, add a 'git reset HEAD -- <primary_files>' pass using the active story's declared primary_files and touches. Add a comment explaining why \u2014 the reviewer must see the builder's deliverable in the diff, not find it already committed under the chore message."
      },
      "applies_to": [
        "all"
      ],
      "status": "captured",
      "enforced_by": "none"
    }
  ]
}
